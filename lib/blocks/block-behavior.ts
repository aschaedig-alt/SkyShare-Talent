export const blockUpdateOptions = [
  "Update all jobs using this block",
  "Update selected jobs only",
  "Update only the current job",
  "Save as a new block version"
] as const;

export const safestBlockUpdateDefault = "Save as a new block version";
