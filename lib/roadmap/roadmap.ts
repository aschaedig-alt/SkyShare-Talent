/* ============================================================================
 *  📋  PROJECT ROADMAP  —  EDIT THIS LIST TO UPDATE THE COMMAND CENTER CHECKLIST
 * ----------------------------------------------------------------------------
 *  This is the ONE place to edit. The Command Center checklist reads from here.
 *
 *  HOW TO ADD / CHANGE ITEMS (just edit the text between the backticks below):
 *
 *    ## Section Title            → starts a new section (a phase/group)
 *    A short line under it        → optional one-line description for the section
 *    - [x] Did this thing (Jun 10)   → DONE  (the "(Jun 10)" date is optional)
 *    - [~] Working on this now        → IN PROGRESS
 *    - [ ] Haven't started yet        → TO DO
 *    - [ ] Item text — extra note     → text after " — " shows as a small note
 *
 *  Section status (Complete / In Progress / Upcoming) is figured out
 *  automatically from its items, so you don't have to set it.
 *
 *  Don't worry about making it pretty — just jot items in and ask Claude to
 *  reformat. Only rule: avoid using the backtick character ( ` ) in the text.
 * ========================================================================== */

export const ROADMAP_MARKDOWN = `
## Phase 1: Critical Fixes
Essential bugs and missing features from the initial build.
- [x] Candidate editing (Jun 8)
- [x] CSV import reliability (Jun 8)
- [x] Remove "prisma-backed" UI text (Jun 8)

## Phase 2: High Priority Features
Core recruiting capabilities.
- [x] 2.1 Duplicate Job Detection & Merge (Jun 9) — shipped; quality improvements planned
- [x] 2.2 Candidate Suggestion Engine (Jun 16) — matches candidates to open roles via pilot-requirement gates plus a scoring engine (sub-scores, per-position config, triage + feedback); per-job candidate fit shows on the Recruiting Jobs detail (JobScreeningPanel) and a candidate scan runs on Pilot Requirements
- [x] 2.3 Google Calendar Sync, two-way (Jun 10) — service account, stage color-coding

## Phase 3: UX & Polish
Making the day-to-day experience better.
- [x] Sidebar redesign (Jun 11) — squared icon rail + items panel; domains Home/Recruiting/People/Data/Admin; Publishing folded into Recruiting as a collapsible section (Job Post Builder, Final Review, Content Blocks); collapsible sections remember their state; hover flyouts + mobile drawer
- [x] People workspace — Pre-onboarding, Orientation tracker, and Compliments recognition are all shipped (see People Ops section)
- [x] 3.2 Candidate Profile Editing UX (Jun 10) — split layout, profile tabs, inline PDF preview for resume/pilot app, add/rename/delete docs
- [x] 3.3 Calendar UX Improvements (Jun 10) — month/week/day views, drag-to-reschedule, autofill, stages
- [x] 3.4 Pilot Requirements Layout (Jul 14) — finished two-pane layout (grouped SkyShare/Managed profile list + a rich detail view: fleet/requirement editors, hard gates, candidate triage, extraction confidence, source-evidence panel). Fixed the selection lag: the WHOLE role card is now one click (was a small inner link that was easy to miss), and switching opens the review INSTANTLY — every requirement's review detail is preloaded to the client so there's no server round-trip; only the candidate-fit list lazy-loads (spinner) after the review is already on screen.

## Phase 4: Feature Completeness
Filling out the platform.
- [x] Manual candidate create + job linking (Jun 14) — "New candidate" button on the Candidates page creates a candidate from a form (no CSV); "Add candidate" on a job's Linked-candidates panel links an existing candidate (searchable) or creates a new one and links them in one step. Backed by POST /api/candidates and /api/candidate-applications
- [x] Resume intake (Jun 14) — "Upload resumes" button (Candidates page + each job): drop multiple resumes and each one auto-creates a candidate (name/email/phone parsed from the file via unpdf extraction), attaches the resume, and (from a job) links them all to that job in one step. POST /api/resume-intake (one file per request to stay under the ~4.5MB serverless body cap); dedupes by email/phone; shows a per-file result list. Name is best-guess (filename/header heuristics) — editable on the profile; an LLM extraction upgrade would improve accuracy (tracked under Document Intelligence below)
- [x] Document checklist + type filter (Jun 14) — candidate profile sidebar shows a real document checklist driven by the type tags: each expected document is green (on file) or red (required + missing) / muted (optional + missing), with an X/Y summary; the Documents tab gained type-filter chips to narrow files by tag
- [x] Document currency roll-up + live widget (Jun 14) — Reports gained a "Document currency" panel: workspace-wide counts (expired / due 30 / due 90 / tracked) + a table of the soonest expirations linking to each candidate. New data-bound "Document currency (live)" widget reads the same roll-up and can be dropped on the Jobs/Calendar editable pages (widgets can now receive live data via EditableGrid). lib/data/document-currency.ts is the shared source
- [x] Document expiry + currency panel (Jun 14) — CandidateFile.expiresAt; expirable docs (Medical, Passport, Driver's License, FCC, Pilot's Certificate, Insurance) get a date picker on the Documents tab. A Currency panel on the profile counts down each dated doc and color-codes expired (red) / due within 30 days (amber) / current (green), and lists expirable docs still missing a date
- [x] Document type tags (Jun 14) — CandidateFile.documentType (Resume, Pilot Application, Paycom Application, Medical, Passport, FCC Radio Operator's License, Driver's License, Pilot's Certificate, Insurance, Other). Auto-detected from filename on every upload path (resume/document intake, profile Add, Link); editable via a dropdown on each document in the Documents tab. lib/files/document-types.ts holds the list + detector
- [x] Bulk document intake (Jun 14) — "Upload documents" button (Candidates page + each job): drop many docs and each is matched to an EXISTING candidate (by email in the file, then by the name on the filename) and attached; unmatched files drop into the Documents "Link" queue. POST /api/document-intake (one file per request); optional jobId also links matched candidates to the job
- [~] Resume intake no longer names people after their own filename (Jul 28) — a Paycom export of 14 candidates imported 11 of them called things like (333909) pdf: the Paycom person-id became the FIRST NAME and the file extension became the SURNAME. TWO SEPARATE FAULTS, both in app/api/resume-intake/route.ts. nameFromFilename stripped only the LAST extension, so Paycom's doubled .pdf.pdf left a stray pdf behind, and nothing stripped the leading (NNNNNN) id. That should not have mattered, because the resume text is tried first — but nameFromText required the name to sit ALONE on its own line, and these resumes run straight from the name into prose (Matthew Johnson Active Certified Flight Instructor...), so every one of them fell through to the filename. FIXED BOTH: the filename cleaner now removes the id prefix, repeated extensions, (1) duplicate markers, possessives, month names and stray digits; the text reader now also accepts a name at the HEAD of a running line, trimming trailing prose words and section headings so Yaakov Nissenbaum EDUCATION does not file someone under the surname EDUCATION; and a new looksLikeAName guard means a filename fragment can never become a person at all — the import says Unnamed candidate instead, which is obviously wrong and therefore gets fixed. Checked against all 13 real filenames and resume openings from the actual import: 13 of 13 now produce the right first and last name. The same filename cleaner was applied to document-intake, where the id prefix was silently preventing any name match. DATA REPAIRED TOO: 10 of the 11 broken candidates were renamed from their resumes, each one confirmed by at least two independent sources out of the filename, the resume text and the email address; 12 paycomPersonId values that were sitting unused inside the filenames were backfilled; and all 14 documents were given readable display names (Kyle Morris - Resume.pdf) without touching the stored file. ONE LEFT FOR A HUMAN: candidate (334958), filename Rebmann Resume, is a scanned PDF with no extractable text, no email and no phone, so only a surname is knowable — look up Paycom id 334958. ALSO WORTH KNOWING: those imported files are not on the dev machine, so the scanned one cannot be OCR'd locally.
- [x] 4.1 Jobs Page Integration (Jul 17) — job-to-candidate and job-to-requirement linking + resume/document intake were already live on /recruiting-jobs. Now the candidate-fit / screening panel (JobScreeningPanel) is surfaced on the Jobs page itself — it used to live only on the Matchboard. It loads on demand for whichever job is selected (a candidate scan is heavy, so it is not run for every job on page load), tagged by job id so a stale result is never shown; a job with a linked requirement shows the scored tiers (Strong signal / Worth a look / Needs review) with the scan button, and one without shows the "link a requirement" prompt. Added as a full-width panel below the job detail in the editable grid. Verified in the live UI (a rare success for this app): selecting a job with a requirement renders the tiers + scan, one without shows the no-requirement message. The stale "Suggested candidates will be added after matching logic exists" empty-state text was also replaced with an accurate prompt.
- [x] A re-applicant now comes back out of the archive automatically (Jul 30) — SHIPPED. Both intake paths used to strand people who apply twice. Document intake filtered every match tier on archivedAt: null, so a returning applicant never matched and their document fell into the Link queue; resume intake had the opposite half of the bug — it DID find archived people (no filter) and then left them archived, so the new resume and a fresh application landed on a profile nobody can see. That was not hypothetical: Matthew Higginbotham, a 2024 Jazz record archived Jun 27, re-applied Jul 30 and resume intake filed his new resume and application onto the archived profile silently. Now: when nothing ACTIVE matches, both paths retry the same tiers against the archive, and a single hit is un-archived, set to ACTIVE at stage Applied, and given a candidate note explaining what changed and why. Deliberately a FALLBACK, so an archived namesake can never turn somebody's clean live match into an ambiguous one; MERGED records are excluded, because that person lives under another id now; and a candidate archived at stage Hired keeps their stage rather than being demoted. New shared helper lib/candidates/reactivate.ts. The upload dialog now says "Brought back from the archive" on those rows. USED FOR REAL the same day on the 14-application Paycom batch — Karina Gaynutdinova and Matthew Higginbotham both came back, each with the note. Committed and pushed Jul 30 by a sibling session, so the deployed button behaves this way too
- [x] 14 Paycom applications filed in one pass (Jul 30) — a single Applications(1-14).pdf export holding 14 separate pilot applications was split on the per-application header (they run 4 to 6 pages each, so a fixed page count would have been wrong), renamed to "Name - Paycom Application id.pdf" so detectDocumentType tags them correctly instead of Other, and filed against candidates: 11 matched on the email printed on the application, 1 on phone, and 2 came out of the archive. All 14 verified after the fact — row present, type Paycom Application, text extracted, and the S3 object read back at exactly the recorded byte length. Two reusable scripts came out of it: scripts/paycom-intake-dryrun.ts (read-only, says what an upload WOULD do before it does it) and scripts/paycom-intake-upload.ts (dry run by default, refuses to write unless storage really is S3, and records an undo file). WORTH KNOWING: the candidate-files bucket lives in us-east-2 while the local AWS config says us-east-1, and the wrong region fails as a 301 PermanentRedirect that does not read like a region problem
- [x] 412 candidate documents were in the database with no file in S3 — all repaired (Jul 30) — every one showed on a profile and failed to open, and the document checklist counted them as satisfied, so a recruiter believed a signed application was on file when it could not be produced. All 412 came from one source, front-pilot-application, all written Jul 27 by the historical Adobe Sign backfill, which was run from a laptop: local dev wrote files to LOCAL DISK while the database is shared with production, so it created 412 live rows pointing at S3 keys that were never written. Not a dusty-archive problem — 308 sat on ACTIVE candidates across 365 people, one already Hired. NOTHING WAS LOST: all 412 still had their bytes on the dev machine, each matching the size recorded on its row exactly. Repaired with scripts/candidate-file-audit.ts by uploading each local copy to the key its row already referenced, so no database row was touched. Kevin Wayman's was fixed first while diagnosing (both his applications read back with DIFFERENT hashes, settling that they are two genuine submissions rather than one filed twice) and the other 411 went in one run — which is why the bucket gained 411 objects, 5,859 to 6,270, for 412 documents. Verified after: the audit reports 0 rows pointing at a missing object, and repaired documents read back byte-exact through the app's own storage adapter. PREVENTED AT SOURCE: the S3 block in .env.local was uncommented, so local runs now write to the real bucket instead of the laptop
- [ ] File storage should refuse to disagree with the database (found Jul 30) — the guard that would have stopped this asks isHostedRuntime(), so a laptop is unguarded by design, which is exactly how 412 live rows came to point at files on somebody's disk. The real invariant is that file storage must match database scope: if DATABASE_URL points at the shared Neon database, writing candidate files to local-dev storage should be refused outright. Small change in lib/files/file-security.ts, and it would have blocked the Jul 27 run
- [ ] 43 candidate file rows have no storageKey at all (found Jul 30) — separate from the 412 and much lower impact. All 43 came from the June JazzHR import (33 JAZZ, 10 JAZZ_TEXT_RESUME), all are UNASSIGNED to any candidate, and all have a null size — metadata-only records where the file itself was never obtained. No profile claims to hold them. Decide whether to source the files from Jazz or drop the rows
- [ ] Two Kevin Wayman candidate records (found Jul 30) — cms3p7mh8 (created Jul 27 from the Adobe Sign application, email clavin44@hotmail.com, no phone, holds both pilot applications and the Paycom application) and cms863qd7 (created Jul 30 by resume intake, NO email, phone 8014149748, holds only the resume). Resume intake dedupes on email or phone and had neither in common: it could not read an email off the resume, and the phone it did read does not match the 8016331005 on the application. Same person, two profiles, documents split across them. Needs a human decision on which record survives — do not auto-merge
- [ ] Support-role gates — when a job is Support, disable most pilot gates but keep U.S. work authorization and U.S. driver's license required; allow position-specific gate fields
- [ ] Role identity defaults to job title — pilot requirement role identity should default to the job post title instead of a synthesized "Citation 560 XL Captain"; support roles get a non-pilot seat/identity
- [x] 4.2 User Access Controls / RBAC (Jun 9) — 4 roles, permission matrix
- [x] Access control condense (Jun 11) — merged each role's sidebar checkbox + access dropdown into one Hidden/View/Full control (5 columns, no horizontal scroll), grouped by domain, equal-width controls; still driven by the nav registry so new pages appear automatically
- [x] 4.3 User Statistics Dashboard (Jun 9) — activity logging + team analytics

## Job Builder / Publishing
Restoring and streamlining the publishing toolset.
- [x] Job Builder data restored (Jun 10) — migrated 79 jobs, 16 blocks, 660 instances, templates from local dev.db to Postgres
- [x] Content Blocks + Final Review live (Jun 10)
- [x] Publishing cleanup, step A (Jun 10) — removed Sandbox Lab, empty Changes/Approvals tabs; moved Templates into Settings; deleted dead code. 7 tabs → 3
- [x] Content block library UX (Jun 11) — group blocks by category/scope/placement, one-click category filter chips with counts, sort (name / most used / recently updated), and collapsible groups so it is not one long scroll
- [x] Block editor Save at top (Jun 11) — sticky editor header with a Save button so you do not have to scroll down to save
- [x] Mixed block formatting (Jun 11) — new "Bullets + text" format: start a line with - for a bullet, other lines stay paragraphs, mixable in one block; renders the same in the editor preview, the final job post, and all exports (HTML / limited HTML / plain text)
- [x] Content Blocks page condense (Jun 11) — narrower library list, inline editing (no separate edit screen) with Save at top, Apply-to-jobs + Version history as a side column; moved Archive/Delete + the jobs-using list to a new Settings → Block management tab (admin-only)
- [~] Job Builder layout redesign — Layout Lab shipped (Jun 11): a draggable/resizable widget board (/jobs/layout-lab) with every field box from Job Builder, Final Review, and Content Blocks; widgets show real Gulfstream G450 & GV Captain content; now includes every box from all 3 pages with cross-page duplicates visible (two previews, two exports, both readiness/status) so they can be merged, plus per-widget lock (now truly immovable via collision prevention), hide-a-box to declutter duplicates, and a snap-grid toggle; arrange + Copy layout, then bake the winner into the real top half and retire Final Review
- [x] Layout Lab as a reusable tool (Jun 13) — Settings > Layout Lab: a tab (left rail, grouped by domain) for every one of the 20 real pages, each auto-seeded with its real boxes; drag/resize, lock, hide, adjustable snap grid, density, auto-arrange, and move-a-box-to-another-page; Copy this page exports the arrangement
- [~] Edit-in-place layouts (Jun 14) — pivoted from the static lab to editing the REAL pages: admins get an in-page "Edit layout" mode (drag/resize the real panels with real data) that saves to the database, so whatever is saved becomes the global layout for everyone. Live on Calendar, Recruiting Jobs, and Candidates (list + profile); reusable EditableGrid + page-layout WorkspaceSetting store. Roll out to more pages after sign-off. (/jobs/layout-lab was retired; the Settings > Layout Lab visual overview STAYS per the Jul 9 decision — it complements edit-in-place rather than being replaced.)
- [x] Candidates page visual refresh (Jun 14) — hand-designed: gradient header with inline search, icon stat cards, avatar initials, color-coded stage pills, contact/activity chips (testing the hand-design approach vs the drag tool)
- [~] Widget palette (Jun 14) — Edit-layout mode now has an "Add widget" drawer with a catalog of config-driven blocks (flight hours gauge, type ratings, compliance gates, currency countdown, readiness score, stat tile, pipeline funnel, coverage bars, document checklist, quick actions, aircraft spotlight, note). Each addable, configurable (gear), removable, and saved globally with the layout. Live on Calendar + Recruiting Jobs. Next: bind data-driven widgets to real candidate/requirement data and add the expiry-date fields some widgets need
- [ ] Publishing cleanup, step B — consider merging Final Review into Job Builder (review a sample first)
- [ ] Changes / version history — build out later if needed
- [ ] Approvals workflow — build out later if needed

## Fleet Positions & Roles
Keeping the position list consistent and modeling how SkyShare actually hires.
- [ ] JOBS AND PILOT REQUIREMENTS STORE THE SAME FACTS TWICE AND CAN SILENTLY DRIFT (found Aug30, from feedback cmtg6yl0f)  — her question was whether the jobs and pilot requirements pages should merge; the analysis found a real defect underneath it, worth fixing whether or not anything merges. Job.pilotSeat and aircraftTypesJson are edited through JobClassificationEditor and the recruiting-jobs PATCH route, while PilotRequirement.pilotSeat and aircraftTypesJson are edited through PilotRequirementEditor - two columns holding one fact, written by two editors that do not talk to each other. The PATCH route writes only the Job; the requirement is seeded from it ONCE, at creation time when a support job is flipped to pilot, and never reconciled again. Base location, pay and raw source text duplicate the same way: Job.city, state, rawPayScale and jobDescriptionText against PilotRequirement.baseCity, baseState, baseAirport, payScaleRaw and originalJobDescriptionText. So changing a seat or an aircraft on the Jobs page leaves the requirement holding the old value, and the requirement is what the Matchboard scores against. THIS COMPOUNDS AN ALREADY-RECORDED PROBLEM under Candidates and Matcher: aircraft ORDER is wrong on six requirements and scoring keys off aircraftTypesJson element zero, so a drifted aircraft list is not cosmetic - it silently moves a role onto a different scoring profile. Fix is one shared writer for the fields both tables hold, rather than two editors writing two tables.
- [ ] ANSWERED, should Jobs and Pilot Requirements merge, and should Match Board stay separate (Aug30, from the same feedback; her words: do we need separate jobs and pilot requirements pages, I feel like those two should be merged)  — RECOMMENDATION IS YES TO THE MERGE AND YES TO KEEPING MATCH BOARD SEPARATE, but not built and not to be started: she asked for visual mockups first and he has parked the item. WHAT IS GENUINELY DUPLICATED: the classification and location fields covered by the entry above, and a mirrored cross-reference panel on each page pointing at the other. WHAT IS NOT: the screening UI is already ONE shared component, JobScreeningPanel, rendered by the recruiting-jobs workspace and again by the Matchboard, so it looks duplicated on screen while being a single implementation - only its two data loaders, getJobScreening and getRoleScreening, are near-duplicates keyed on jobId versus requirementId. THE SHAPE THAT MAKES A MERGE SAFE: keep the Job as the outer list because it is the superset including non-pilot roles, and show a Pilot requirement tab only when isPilotRole is true. FOUR THINGS THAT WOULD BREAK A NAIVE ONE-TO-ONE MERGE, all re-read from the schema at commit time: Job.pilotRequirements is PilotRequirement[] and so one-to-many while every call site reads element zero, so a job can carry more than one requirement row; PilotRequirement.sourceJobRecordId is String? with onDelete SetNull, so a requirement can exist with no job at all, which is how managed and tail-driven roles outlive their posting; requirement-only fields have no home on a Job, namely reviewStatus, extractionConfidence and warnings, the PilotRequirementChange audit log and managed variants; and the two lifecycles are deliberately NOT cascaded, Job status OPEN, FILLED, RETIRED against requirement status ACTIVE, INACTIVE, EVERGREEN, HISTORICAL. MATCH BOARD STAYS ITS OWN PAGE because it is the only pool-wide and candidate-first surface - it scores one person against every active requirement, collapses duplicate requirement rows onto canonical fleet positions, and owns the global skip list, none of which the other two pages have.
- [x] CORRECTED, AND THE WRONG CLAIM WAS MINE (Aug 31) — the entry below dated Aug 28, saying the Matchboard applied count is a display cap and not a pool size, is FALSE. I wrote it from one session's reading without measuring it. The applied count is real: 0, 1, 2, 31, 36, 53 and 476 across the nine active roles, against 4,047 CandidateApplication rows. The 12 cap was always on the job Screening panel, a different surface, and it has since been raised to 50. Left the original standing rather than reworded, so the mistake is visible instead of quietly erased.
- [x] The jobs importer put Paycom requisition numbers in the Jazz-code column (Aug 31) — fixed. Requisitions now route by the shape of the value: digits go to paycomReqId, anything else stays a Jazz code on jobReqId, using the same digits-only test the app's own requisition field enforces. A Paycom number is also matched against jobReqId so a row written before the split is updated rather than duplicated. This never corrupted anything: no Paycom CSV has ever been imported, every JOBS_CSV batch is from Jun 8, and exactly one row held a numeric jobReqId, typed into the app's own form rather than imported.
- [x] The importer never revived a requirement, so a role closed and reposted came back as an OPEN job with a HISTORICAL requirement and stayed invisible to the Matchboard (Aug 31) — fixed; reimporting the posting now reactivates it. Shipped REACTIVATE-ONLY on purpose, stopping short of the bidirectional rule the job-status route uses. Re-measured against live data: the importer derives status as filledDate ? FILLED : OPEN, and that disagrees with the stored status on 52 of the 64 live job rows, because dozens carry a Jazz-era filledDate of 2025-02-25 while correctly stored RETIRED. The live Gulfstream G200 First Officer requirement sits on a job stored OPEN carrying that same stale date, so a deactivating branch would have switched a role the Matchboard is currently serving OFF on the next import. Closing stays an explicit human action.
- [x] Guarded requirement revival against merged-away job rows (Aug 31) — 34 of the 57 linked requirements hang off a job that reconciliation merged away, and the importer's identity match has no merged filter, so those rows are reachable from any CSV carrying an old key. Without the guard one re-import would have resurrected them and drifted the board from 9 roles back toward 27. Simulated read-only against live data before and after: worst case fell from 36 requirements moved to 2, both RETIRED SkyShare roles that revive only if a posting for them is actually imported. Both import routes now report the reactivation count so a revived role is never silent. All four figures re-measured at commit time.
- [ ] The importer can still revive a MERGED-AWAY job row itself (found Aug 31, not fixed) — identityMatches in lib/imports/job-import.ts filters on sourceJobId and jobReqId with no mergedIntoJobId filter, and 37 of the 58 merged rows still carry one of those keys, so a re-import finds the losing duplicate and overwrites its status from MERGED to OPEN. Only the title fallback excludes merged rows. The requirement side is now guarded so this no longer reaches the Matchboard, but the job list can still show a revived duplicate. Left alone deliberately: the obvious fix, excluding merged rows from identity matching, would make the importer create a NEW duplicate instead, which is worse. It needs a redirect-to-the-winner.
- [x] Moved the one misfiled Paycom requisition into its own column (Aug 31, confirmed by the user) — the Maintenance Technician job held 2619 in jobReqId, and he confirmed it is a genuine Paycom requisition number. Applied to the live database and read back at commit time: that row now reads jobReqId null and paycomReqId 2619, all-digits jobReqId across the whole table is 0, and paycomReqId populated is 4. The 44 live Jazz codes are untouched. The script moves only human-confirmed numbers, never everything that looks numeric, because a Jazz code could be numeric too. Reversible via scripts/backfill-paycom-req.ts --undo with the record committed in scripts/paycom-req-backfill/.
- [x] Corrected the G450 and GV Captain hour gates on a live role carrying 31 applications (Aug 31) — the two wrong numbers failed in OPPOSITE directions at once: total time 4000 was too loose and passed pilots below the stated minimum, while jet 2000 was too strict and screened out qualified pilots with 1500 to 1999 jet hours. Total time 4000 to 5000, jet 2000 to 1500, time in type 250 to 1000, PIC time in type off to 250. PIC 3000 and multi-engine 2000 were already right and were not touched. HIS FIGURES, given directly: 5,000 total, 3,000 PIC, 2,000 multi-engine, 1,500 jet, 1,000 in type, 250 plus PIC in type. Read back gate by gate at commit time, all six ON and all six matching. Targeting was checked rather than assumed, because ELEVEN requirement rows carry G450 in the title including three separate ones called Gulfstream G450 First Officer; the single ACTIVE G450 and GV Captain is what changed. Reversible via scripts/fix-g450-gates.ts --undo.
- [ ] The G450 and GV Captain posting wording contradicts its gates and one of them should change (Aug 31) — the post says 1000 PIC hours in the G450/550 and/or GV, which reads as PIC-in-type 1000; he set time-in-type 1000 with PIC-in-type 250 or more. His figures are what is stored, since he sets the bar, but Check against posting will report the gap on every run until the posting is reworded or the gates revisited.
- [x] Check against posting shipped on the requirement editor (Aug 31, his choice of shape) — it reads the job post a requirement was built from and lists every disagreement with its gates, each carrying the verbatim line it was read from. He chose a button in the app over a repo script or an on-request run. It NEVER writes: accepting a finding fills the form and the existing Save requirement button still commits it, because the reader is wrong sometimes and every finding is a question. It read you will be part of our exciting Part 135 flight department as a Part 135 requirement, when the sentence only describes the department. Accepting also sets that gate's evidence to the quoted line, which fixes evidence being the whole posting pasted onto every gate: 5 distinct strings across 70 gates, median 4000 characters. A gate holding a number the posting does not mention is surfaced but deliberately NOT one-click, since the reader missing a line looks identical to the posting not having one. Verified end to end over HTTP against the live G450 role. NOT verified: the click path, because the editor subtree does not hydrate in the Browser pane - the pre-existing Save requirement button has no React props there either, so it is the known pane limitation rather than this panel.
- [x] Chose a checker over an import pipeline, on measurement rather than preference (Aug 31) — across all nine open roles the reader agreed with the stored gates on 40 of 43 hour values and 91 boolean gates, so as an importer it would mostly retype work a person already did. Its whole value was the three it disagreed on, and those three were the real errors on the G450 role corrected in the same batch - the checker found them before a human did. Recorded so nobody reopens the auto-import question without new evidence.
- [ ] Four gaps in the requirement catalog, now surfaced automatically by every check rather than only when somebody notices (Aug 31) — no gate can hold a geographic or domicile requirement (Hawaii; the Ogden rule; the G450 Central/Midwest/Western U.S. rule), a conditional minimum (700 hours instead of 800 for a currently active CFI), a recency qualifier carrying no hour figure, or a duty-day limit. DECIDED, from him: do NOT add catalog gate types for these yet. Check against posting will keep reporting them on every run and that is accepted for now, not a bug to fix. This broadens the Aug 28 entry below, which named only the Hawaii and CFI cases; both still live in each requirement's manualOverrideNotes and are checked by hand on every scan.
- [x] RECORDED, the Challenger 350 type rating is CL-30 and it is now confirmed (Aug 28, from the user) — his words: challenger 350 and challenger 300 are both the CL-30 Type Rating. That closes the item below, which flagged the rating as one session's general knowledge rather than anything the job post said - the post never names a rating. The registry note now records the confirmation instead of asking for it, and lib/fleet/positions.ts was regenerated with npm run fleet:sync rather than hand-edited, because the notes column is generated into that file and editing only the markdown would have left the two disagreeing. WHAT THE SECOND HALF OF HIS ANSWER ADDS: the Challenger 300 shares the rating. SkyShare does not operate the 300, so the low-blast-radius reading still holds, and the resolver already anticipated this - its matcher is challenger 3[05]0 or cl-3[05]0, so a job post or a candidate naming either aircraft resolves to the 350 position. Nothing needed changing there.
- [x] The Matchboard picker now shows only the roles we are hiring for (Aug 28) — the picker is driven by the fleet registry, so it listed every aircraft position we operate whether or not a seat was open: 11 unscannable no-profile rows among 9 real ones. They sit behind a toggle reading Show 11 more fleet positions we are not hiring for. A search still looks through them, because being told a role does not exist when it does is worse than a longer list. Closes the item raised the same morning, below.
- [x] Every role on the board carries its operator and tail now (Aug 28) — SkyShare, or Managed with the tail number, so the managed Henderson G450 and the SkyShare SLC G450 and GV are no longer two near-identical titles with nothing to tell them apart while scanning. An operator is claimed only when the whole collapsed group agrees on one: a slug holding both is a data problem, and labelling it either way would hide that.
- [x] Challenger 350 and Praetor 600 added to the fleet registry (Aug 28) — 31 positions in FLEET_POSITIONS.md now, with resolver matchers for both, so the four newest roles group under their aircraft instead of rendering as unmatched. Challenger 350 sits between the Citation 560XLS+ and the Gulfstream G200 in fleet size order and Praetor 600 immediately after it, both Active, which is his ordering rather than a guess.
- [x] Closing a job now deactivates its pilot requirement, for SkyShare roles ONLY (Aug 28) — managed roles are deliberately untouched, because their job rows are usually stale while the role is live and a blind cascade would have switched off most of the managed fleet; they follow their tail instead. Reopening the job reactivates the same requirement rather than creating a second one. The updateMany matches only rows already in the expected prior state, so reopening cannot resurrect a HISTORICAL row - which matters here because three rows share the title Citation 560XL Captain, two of them HISTORICAL. Exercised against the live route in both directions and both test jobs restored; whole-table counts afterwards are identical to before, ACTIVE 9 / HISTORICAL 32 / INACTIVE 25 and jobs OPEN 14 / MERGED 58 / RETIRED 50.
- [x] The Challenger 350 type rating is recorded as CL-30 from general knowledge, not from the job post, which never names one (Aug 28) — flagged in the registry notes and awaiting his confirmation. Low blast radius, since no other aircraft in the fleet shares that rating, but it is a number in the registry that nobody sourced and it should not quietly become fact.
- [x] The undo records for the open-roles pass are committed rather than sitting on one laptop (Aug 28) — they were written to scripts/_reconcile_output/, which .gitignore catches via the scripts/_*_output/ rule, so the only means of reversing an applied live-database change existed in a single working tree. That change was not small: 22 requirements deactivated, 5 requirements and 6 jobs and 2 variants created, 4 job merges moving 326 applications, 53 rotations. Moved to scripts/open-roles-reconcile/ with all four scripts pointed at it, which matches the five earlier live-data scripts that each commit an undo record under scripts/<work-name>/ - this pass broke a five-for-five convention purely through the directory name it happened to pick. Each script's own path construction was replayed against the filesystem to confirm it still finds its record, because a moved undo record the script cannot find looks reversible and is not. The README there records what each step changed, the state read back afterwards, and the fact that undo has to run in REVERSE order because each step was applied on top of the last.
- [x] Reconciled the Matchboard against the real open-roles list (Aug 28) — it listed 27 roles of which only 4 were backed by an open job, and it now lists the 9 pilot roles we are actually hiring for. 22 requirements moved to INACTIVE and nothing was deleted: gates, fields and linked applications are all kept, and applications hang off the Job rather than the requirement so none of them moved. INACTIVE rather than HISTORICAL on purpose, because /api/pilot-requirements treats INACTIVE as a current requirement, so the row still blocks a duplicate being created for the same fleet position. Verified read-only afterwards rather than taken from the scripts own output: ACTIVE 9, HISTORICAL 32, INACTIVE 25, total 66.
- [x] Created the 5 roles that existed only in Paycom (Aug 28) — Praetor 600 Captain and First Officer, Challenger 350 First Officer, Single-Pilot Jet Captain (Georgia), plus the Challenger 350 Captain requirement for its existing job. Two support jobs added as well, Sr. Staff Accountant and VP of Aircraft Sales. Six job rows and two managed variants in total.
- [x] Rebuilt 6 pilot requirements from the real job post PDFs (Aug 28) — the Challenger 350 and Praetor 600 Captain gates had been cloned from the G200 on the stated basis that the minimums matched. The posts differ on 4 of 6 numbers and every one was set too HIGH, which silently removes qualified people from a scan and looks identical to a thin candidate pool. Multi-engine 3000 to 1000, jet 1000 to 800, instrument 800 to 300, time in type 135 to 300 on the Challenger and off entirely on the Praetor. All four rebuilt roles are flagged NEEDS_REVIEW so the ranking gets a human read.
- [x] Set the crew rotation on 53 requirements across every status (Aug 28) — treated as fleet policy rather than per-role data, so a role switched back on returns with its rotation already correct instead of blank.
- [x] Found the Nevada G450 First Officer running on a row tagged SkyShare with no tail, while its correctly-tagged Managed twin sat HISTORICAL (Aug 28) — retagged the live row Managed and attached N787JS.
- [x] Merged 4 duplicate job rows through lib/jobs/merge.ts (Aug 28) — so applications moved with a JobMergeRecord behind them and unmerge stays possible. Evergreen PDP and Pilatus PC-12 First Officer into PC-12 First Officer, AMT Weekend Shift and Senior Gulfstream Technician into Aircraft Maintenance Technician. Verified afterwards: the two merge targets hold 53 and 273 applications, so the applications did move.
- [x] RECORDED, the source of truth for open roles (Aug 28, from the user) — Paycom will not give a useful active-jobs export, so the authoritative list is one he maintains himself and supplies as job post PDFs. He decided against building an upload-and-reconcile importer for now.
- [x] RECORDED, the Henderson NV G450 roles are managed and must not be merged (Aug 28, from the user) — Captain, Lead Captain and First Officer are a managed aircraft, tail N787JS, and are NOT superseded by the SLC G450 and GV roles. The fleet registry marks the G450 airframe Archived because that registry describes the SkyShare-owned fleet, not managed tails.
- [x] RECORDED, the CE-525 family must stay split (Aug 28, from the user) — SkyShare runs fractional CJ2 positions and managed aircraft share the same 525 type rating; they are separate roles.
- [x] RECORDED, the managed aircraft behind the four newest roles (Aug 28, from the user) — Challenger 350 is managed, tail N522AD, Salt Lake City based. Praetor 600 is managed and Ogden-based; both job posts say the Salt Lake City area and he corrected that to Ogden, and the tail is unknown because the aircraft has not been purchased yet. The Georgia Single-Pilot Jet Captain is the managed Part 91 Citation CJ, tail N443BC, the same aircraft as the former SLC CJ Captain role which was filled before the aircraft moved; initially at CVC, moving to CPP.
- [x] RECORDED, SkyShare crew rotations by aircraft and seat (Aug 28, from the user) — G450 and GV PIC and SIC 15/13. G200 PIC and SIC 8/6. 560XL PIC and SIC 8/6. CJ2 PIC 8/6, SIC 10/5. PC-12 PIC 8/6, SIC 10/5. Managed pilots are as-needed with 8 hard days off per month.
- [x] RECORDED, job-row and pay decisions (Aug 28, from the user) — PC-12 First Officer pay is $40,000 annually, confirmed as correct. All Aircraft Maintenance Technician postings go under the one AMT job. The Customer Service Representative Part-Time role has 2 openings but only needs one job row.
- [x] RECORDED, the SLC relocation gate stays ON for both Ogden Praetor roles (Aug 28, from the user) — deliberately, because Ogden is inside the Wasatch Front commute the gate describes, and turning it off would stop flagging candidates who would need to move to Utah.
- [ ] The five support roles still have no job post text (Aug 28, from the user) — Aircraft Maintenance Technician, CSR (Aviation) Part-Time, Ogden Base Support, Sr. Staff Accountant, VP of Aircraft Sales (Brokerage). He will send them later.
- [x] The Matchboard role picker lists every fleet position marked Active in FLEET_POSITIONS.md whether or not we are hiring for it (found Aug 28) — so 11 unscannable NO PROFILE rows sit among the 9 real ones. It conflates operating an aircraft with having an open seat. Default the picker to roles with a profile and put the rest behind a toggle.
- [x] The Matchboard shows neither operatorType nor tail number (found Aug 28) — so a managed Henderson G450 Captain and a SkyShare SLC G450 and GV Captain are two similar titles with nothing to tell them apart while scanning. One field in the query at lib/matching/matchboard.ts and one badge in the picker.
- [x] Retiring a job still does not deactivate its requirement (found Aug 28) — PATCH /api/recruiting-jobs/[id] updates job.status and returns early, re-confirmed at commit time. It cannot be a blind cascade: most managed roles sit on RETIRED job rows while the role is live, so deriving it that way would switch off most of the managed fleet. A SkyShare role should follow its job, a managed role its tail.
- [x] Challenger 350 and Praetor 600 are absent from the fleet registry (found Aug 28) — verified against BOTH FLEET_POSITIONS.md and lib/fleet/positions.ts, against a positive control of 29 table rows reading fine, so the four newest roles resolve to no canonical position and render as unmatched rather than grouped with their aircraft. FLEET_POSITIONS.md is the editable master and npm run fleet:sync regenerates the block in positions.ts.
- [ ] The jobs CSV importer writes a requisition number into jobReqId, the Jazz-era scheme, and never populates paycomReqId (found Aug 28) — nowhere in lib/imports/job-import.ts, although the field and its digits-only validation already exist and are used by app/api/recruiting-jobs. Paycom numbers and Jazz codes land in one column.
- [ ] The jobs CSV importer creates a pilot requirement only when none exists and never updates or reactivates one (found Aug 28, lib/imports/job-import.ts:317) — so a closed-then-reposted role returns as an OPEN job with a HISTORICAL requirement and stays off the Matchboard.
- [ ] lib/imports/job-pdf-parser.ts carries four hand-written layout parsers, each anchored to literal strings, and records its own silent failure (found Aug 28) — the careers site was redesigned, the parser was not, and every printed posting produced zero rows because the parser wanted the exact heading Job Location: and the page said Location:. Any future job-post ingestion should read the text with the LLM path used by lib/extraction/pilot-metrics-llm.ts, which returns every value with the verbatim line it came from, rather than adding a fifth parser.
- [ ] Two gates cannot be expressed in the catalog and must be checked by hand (found Aug 28) — Challenger 350 Captain requires prior Hawaii operating experience, and PC-12 First Officer allows 700 hours for an active CFI. Both are recorded in manualOverrideNotes.
- [ ] The Matchboard applied count is a display cap, not a pool size (found Aug 28) — every role returns exactly All 12 because CURRENT_MATCH_LIMIT is 12 in lib/candidates/scan-pool.ts, with 12 more from the archive. So the count cannot be used to show that the corrected gates widened the pool, and whether the right people now rank is a judgement he has to make.
- [x] A maintenance section can report to someone other than the card's manager, and a person can carry a named role (Aug 6) — the Supervisors card holds two levels, the department lead reporting to the Director and the base leads reporting to him, but a group carries ONE mgr for the whole card so there was no way to express it. Two optional additive fields on MxSection, both verified optional before committing so the 13 existing sections are untouched: reportsTo, blank everywhere today and inheriting the card's mgr, and roles, a name-to-role map for a named role inside a shared section — the case being two dedicated Gulfstream leads at different bases, where the section label describes the seat and not the person. Roles render through the tag pills PersonRow already had for the crew chart, so there is no new row markup; the maintenance chart simply never passed tags. Edit mode gained a reports-to field per section and a role button per person next to move and link. A ROLE TRAVELS WITH THE PERSON on a cross-section move and is DROPPED when they are removed, because leaving it behind would silently re-attach it to the next person who happens to share the name. NO DATA WAS CHANGED — the live roster is the fleet/mx-roster WorkspaceSetting and is edited through the UI. The normalizer round-trips the proposed three-section shape and still coerces hostile input: a numeric reportsTo is dropped, and a blank name or blank value in the roles map is dropped. NOT verified: the reports-to line and role pills as rendered, or the new edit controls being clicked, since the Browser pane cannot read this app's content
- [x] STRUCTURE APPLIED — the user did it himself (decided Aug 6, applied Aug 12) — verified against the live roster rather than taken on trust, and it is saved: the card is now Supervisors and Dedicated Techs with THREE sections, Rich Paden carrying the Dual Base Manager role, Robert Patrick placed as SLC Team Lead with the OGD Team Lead req still open beside him, and JD Bumgarner and Shawn Stafford placed as the Gulfstream lead techs for HND and SLC. The roster row was last written Aug 12 at 19:13 UTC, where the Aug 7 check had found nothing saved at all. FINISHED Aug 12: the two sections that were still labelled New section are now TEAM LEADS and GULFSTREAM LEADS, renamed by the user before the space fix even deployed — using the very workaround he had reported, typing the words together and inserting the space mid-string afterwards. Nothing is left outstanding on this card. (Aug 6) — the maintenance Supervisors card becomes Supervisors and dedicated techs with three sections: Department lead, Rich Paden reporting to Jonathan Schaedig; Base leads, reporting to Rich; and Dedicated techs, also reporting to Rich. TWO NAMES TO GET RIGHT: JD Bumgarner is spelled with ONE d, not Bumgardner — he is an active G450 Maintenance Technician based at HND and the Gulfstream lead there, and Shawn Stafford is the Gulfstream lead at SLC. Both are ON STAFF, not candidates. AND ONE THING THE APP MUST NOT DECIDE: Shawn Stafford's old seat in Salt Lake City / Days is being handled by the user in the edit UI, and whether that becomes an extra open req is his call, not something to derive
- [ ] Org chart: a person cannot be moved from maintenance to crew (Aug 4, reported from a real move) — the cross-LOCATION move exists; a cross-DEPARTMENT move does not, so the only way through it today is to delete the person from maintenance and re-add them to crew, which loses whatever the card carried. WORSE AS OF AUG 6: a cross-section move now carries the person's role note with it, so delete-and-re-add across departments loses the card's links AND their named role
- [ ] Crew status needs tentative leaving and leaving as two separate selectable options in Edit crew (Aug 4) — alongside on line, training, offered, tentative external and tentative internal, carried over from how the team used the spreadsheet, so a pending departure shows as a tentative departure rather than as nothing. NOTE for whoever builds it: the distinction already half-exists but is not selectable — departures live in a separate out array rather than as a seat status, and an UNDATED departure is already treated as tentative and deliberately never ages out (lib/fleet/staffing/labels.ts, isDepartureArchived). Making it explicit on Departure is a smaller change than adding two new seat arrays
- [ ] The maintenance chart cannot show that somebody is ON THEIR WAY OUT (Aug 12, reported by the user) — his words: he did not know how to mark it "because it doesn't really give me an option to do that", and he is right, there is none. Poom Padmanuja leaves in a couple of weeks, and with no way to say so the only available action was to DELETE him, which is what happened. Two costs, both live right now: the chart no longer shows a technician who is still working for two more weeks, and Salt Lake City Days reads 1 on the line with 3 OPEN, when one of those three is not open yet — removing a person increments the open count, so a planned departure inflates the hiring need the moment it is recorded. THE CONCEPT IS ALREADY BUILT ON THE CREW SIDE and just never reached maintenance: types.ts has a Departure interface (name, to, reason, date) and CrewGroup carries out?: Departure[], crew-data.ts documents TRANSITION OUT = out, CrewOrgChart imports Departure with splitDepartures and isDepartureArchived and a Transitions component, and a departure older than a month drops out of the default view automatically — DERIVED from the date, with nothing written. MxSection has no equivalent: its fields are label, line, train, cand, candInt, offered, open, openNamed, parked, reportsTo and roles, and the only matches for out in MaintenanceOrgChart.tsx are local accumulator variables. parked is not a substitute — it is a COUNT of on-hold seats, not a named person. The fix is to give MxSection the same out?: Departure[] and reuse the crew helpers rather than invent a second concept, and to NOT increment open when the person is leaving on a known date. Until then, adding Poom back with a role note is the only workaround, and it will still overstate the open count
- [x] Removing somebody from the maintenance chart no longer leaves their candidate link behind (found and fixed Aug 12) — a link is keyed by NAME and survives a move on purpose, so it cannot be cleaned up inside the remove handler without breaking moves. The prune therefore runs against the FINISHED roster at saveMxRoster, which is the single write path, so the chart, a script and any future caller all get it — and it catches every way a link can be orphaned rather than only one: a person removed, a section deleted, a whole card deleted, or a name edited. It keeps anyone in the five people buckets or named as a card's manager, and deliberately ignores openNamed, which holds requisition labels rather than people. NOT A ONE-OFF, which is the part worth knowing: run read-only against the live roster it drops THREE — Nick Lembo, Poom Padmanuja and Jordan Pashayan — from 27 links down to 24, so the map had already been accumulating. Verified in both directions on live data: zero people still shown on the chart lose a link, and zero absent people keep one. The existing three clear automatically on the next save of the chart; no migration needed
- [x] A space could not be typed into any of the new org-chart fields, and a blank one would have deleted the card (Aug 7) — reported as not seeing a space between words while typing, but being able to add one later by editing. All six inputs (card title, subtitle, manager, section label, reports-to, person role) are CONTROLLED and were trimming on EVERY keystroke, so a trailing space was erased as fast as it was typed; editing an existing label worked because a mid-string space is neither leading nor trailing, which is exactly the asymmetry reported. Raw on change, trimmed on blur and again by normalizeMxRoster at save. THE TRIM WAS ALSO MASKING A DATA-LOSS PATH, so simply removing it would have opened one: normalizeGroup returns null for any group with a blank name, subtitle or manager, normalizeSection returns null for a blank-labelled section, and both are filtered out — so clearing the title and saving would have silently deleted the card and everyone on it, measured as two groups in and one out with Rich Paden gone. Blur now substitutes a recoverable placeholder (Untitled card / No subtitle / Unassigned / Untitled section) instead of allowing empty, while reportsTo and roles stay optional so blank there still clears the field. NOT click-tested — the Browser pane cannot read this app, so type a two-word section name before trusting this entry
- [x] Adding a candidate on the maintenance supervisors page can now FILL one of the open positions already shown (Aug 4, reported while trying to add Robert Patrick as a tentative SLC team lead; shipped Aug 7) — the page displays the reqs still to fill, openNamed carrying labels like Ogden Team Lead, but offers no way to select one, so there is no obvious route to adding somebody as, for example, a tentative external SLC team lead. Wanted behaviour: when adding a person, FIRST offer the existing open positions to fill, and only then allow a free-text position if none of them fit
- [x] The maintenance chart's edit UI can change STRUCTURE, not just people (Aug 7, reported three times, most recently as I don't see the ability to have those separate sections within that card) — Ogden and Salt Lake each have four sections but Supervisors had one and could not be split, so the department lead, the team-lead reqs and the dedicated Gulfstream techs all shared one block. THE CAUSE WAS THAT NO HANDLER EXISTED — not a broken control but no control at all: renaming a card, renaming a section, adding or removing a section and anything to do with a named opening all rendered as dead text. Now the card title, subtitle and manager are editable in the modal header; section labels are editable in place with add-a-section and a confirmed delete, since deleting takes everyone in it with it; and named openings gained fill, add and delete. FILLING one moves the person in at the chosen status AND keeps the req's label as their role, so the card reads Robert Patrick, SLC Team Lead, candidate rather than a candidate floating beside two unexplained reqs. Symmetric on the way out: removing somebody who holds a role hands that role back as a NAMED opening instead of an anonymous open slot, because losing SLC Team Lead the moment its candidate falls through is exactly when you need it most. Reuses the roles field from 96c5b73 rather than adding a second concept. NOT CLICK-TESTED — the edit UI is a client-rendered modal and the Browser pane cannot read this app, so this is a clean tsc and lint plus the page returning 200 with the real roster, and nothing more. The session that wrote it asks that it be tried by hand before the entry is trusted, having been wrong once before about what this area already did
- [ ] Named openings cannot be MOVED between sections (found Aug 7) — add and delete both exist and people move fine, but a req like Ogden Team Lead has to be deleted from one section and re-added in another to relocate it. Found while writing the restructure instructions rather than reported
- [x] A click-path given for the maintenance restructure was WRONG, and the rule from it (Aug 7) — three of its five steps were impossible in the edit UI at the time it was written. Verify a capability EXISTS before writing instructions that depend on it. This is the second time this area has been described from assumption rather than from the code, and both times the instructions looked entirely plausible
- [x] Training is its own view on the Crew page, not a filter on the chart (Aug 2) — reported as being able to still see fleet options while looking at training info, which would confuse others. Counted rather than assumed: SIX things belonging to the chart stayed on screen while Training was selected — the title, the entire description, the Sort group, the Additional info group, Edit all, and a headline 6 open reqs — and three of those control groups did nothing whatsoever on that tab, which is worse than clutter because it implies they filter the training list. Fixed as Option 3 of three rendered alternatives the user picked from. A real tab strip with role=tablist replaces the View pill, and the chart controls are NOT RENDERED on the Training tab rather than hidden, so nothing exists to click that silently does nothing. Training carries its own identity — blue top rule instead of gold, an eyebrow reading Training, the title Crew Training, a matching blue marker on the selected tab, and its own description. The headline number swaps from Open reqs, a staffing figure, to how many pilots have FINISHED training and are waiting on a decision, with active and archived counts underneath; those come from trainingRows, the same function the table uses, so the header and the rows cannot drift apart. Find a person is removed from the Training tab because it searches the CHART and picking somebody opens an aircraft card, throwing you out of the view — and the training toolbar already has a search across names, seats and locations; the panel now also closes on a tab switch instead of hanging over the wrong view. The tab strip is built to hold more than two, so a further view costs no redesign. VERIFIED IN THE SERVED HTML for the CHART side — the title reads Crew Org Chart, Crew Training is correctly absent, the tab strip and role=tablist are present and all four chart control groups still render. NOT verified: the TRAINING side of the header, which needs the tab selected, so the blue rule, the new title, the swapped headline and the absence of the chart controls are correct by construction and unseen
- [ ] SETTLED — Training stays ON the Crew page and gets no sidebar item (Aug 2, from the user) — their words were that it does not need its own side menu page, but maybe tabbed pages on the crew page itself. Given three rendered options they chose the strongest separation, tabs PLUS a distinct identity, accepting that the page title changes to Crew Training when the tab is switched. Do not quietly revert that to a shared title, and do not promote Training to its own route
- [x] Training tab: two-line rows, colour-coded pickers, short dates, editable names, sortable columns (Aug 2) — six pieces of feedback from working in the tab, with the row layout picked from a rendered side-by-side of three options rather than agreed in the abstract. TWO-LINE ROWS: Type and Pool now lead a second line that always shows, then the note, then archive and delete pushed right. The subtle part is the striping — two rows per record meant striping by row shaded one pilot's notes line with the next pilot's data line, so each record became its own tbody and the stripes alternate by record. NO SIDEWAYS SCROLLING, with margin: moving two columns down and shortening the dates took the table minimum from 1240px to 980px, which is what makes it hold on a laptop rather than only a wide monitor. EDITABLE NAMES in active and archived both — the name used to be a button that jumped to the chart, so the spellings the tracking sheet arrived with could not be corrected at all; the jump moved to its own icon. COLOUR on Type, Pool and Status driven by each value, with Internal carrying the colour and External left neutral since colouring the common case only tints the column, all using the chart's existing tokens so dark mode is free. SHORT DATES: a native date box cannot be reformatted and Chrome will not draw one under about 110px, which over five columns is half the table — so each date reads as text at about 58px and becomes the real picker only for the cell clicked, which is how a two-digit year and a working calendar coexist. SORTING on every header with click-to-reverse, subject to two rules the sort cannot override: active always above archived, and blanks last in both directions so reversing never floats empty cells to the top. NOT SEEN: the Training tab only renders once selected and this app's client components do not attach handlers in the preview pane, so structure, types and compiled CSS are verified and appearance and every click are not
- [ ] STANDING CONSTRAINT — the Training tab must never scroll sideways (Aug 2, from the user) — their words were that they like not having to scroll left and right and to always keep it that way. Treat it as a rule on that tab rather than a fix that happened once: check the table minimum before adding any column. It sits at 980px today against a 1240px starting point, so there is roughly 260px of headroom and no more
- [ ] Training row layout is settled, do not redesign it (Aug 2, from the user) — chosen from a rendered side-by-side of three options: the second line ALWAYS shows, and Type and Pool sit at the START of it. The user explicitly rejected showing a second line only when there is a note, and rejected hiding notes behind a chevron. Also settled: date fields show short and edit full, the cell reading 6/28/26 and becoming the native picker on click. The two-digit year was their explicit ask, chosen after being told a native date box cannot be reformatted, over both squeezing the native boxes and rebuilding date entry by hand
- [~] THE TRAINING IMPORT WORKED — three orphaned rows are left from the id change (found Aug 2, checked against the live database) — the user asked whether they were pasting it wrong, having done it several times. They were not: the stored fleet/crew-roster blob holds 107 training records saved Aug 1 at 20:47 Mountain, and 104 of them carry the NEW columns, so the real Training Info sheet imported correctly with 98 archived. The remaining 3 are stale rows from before the per-event rework, orphaned by the id scheme changing from name plus date to name plus date plus POSITION: the new paste could not match them, so it added its rows alongside instead of updating them. brett-moreland_2026-07-13, jonathan-siswick_2026-07-07 and michael-salvagnini_nodate all carry no position, and Jonathan Siswick therefore appears TWICE, once under each scheme — 3 of the 9 active rows are ghosts. Re-pasting does not clear them, because the merge is keyed on id and these ids no longer exist in the sheet. Two things to settle: the user can delete the three by hand from the tab (they are the rows with a blank Position), and the rework should have migrated existing ids rather than leaving pre-rework pastes to fork — worth a one-off cleanup on read so the next id change does not do this again
- [x] Training tab: full width, ruled rows, and two columns that were never editable (Aug 1, from the user) — three things reported from actually using the tab shipped earlier the same day. WIDTH: the chart shell caps at 1320px, sized for the card grid, and a thirteen-column table does not fit in it — five of those columns are native date inputs that Chrome will not render below roughly 110px, so about 650px is committed before a single character of text, and reading a pilot's own training dates meant scrolling sideways. The shell now widens to min(1800px, 96vw) while the Training tab is open and returns to 1320px for the chart, with a new Onboarding dates toggle that drops start / orientation / indoc for a narrower screen. That toggle defaults to ON: widening the tab was about seeing everything, so hiding columns by default would have answered the opposite of what was asked. SEPARATION: zebra striping, vertical rules between every column, a sticky header, and a distinct blue tint on the three onboarding dates so they read as one block instead of five identical date columns; the finished-course gold and archived grey deliberately override the striping so a row state is never washed out by which stripe it lands on. THE LOCKED FIELDS WERE A GENUINE BUG, not a misunderstanding — Type and Pool were displayed but never given an input, so they could not be edited at all. Both are dropdowns now, with a blank option so a wrong value can be cleared and not merely swapped. Separately there was NO way to add a training record by hand, only import or a chart-derived placeholder, so a course booked outside the tracking sheet could not be entered; an Add a record box now creates a blank row. Progress remains derived from the dates and status rather than editable, and the footer now states which fields are editable rather than leaving it to be discovered. NOT SEEN BY ANYONE: the tab only renders once its tab is selected and this app's client components do not attach handlers in the preview pane, so the CSS was confirmed COMPILED in the build output rather than observed — including the zebra rule, which the minifier rewrites from nth-child(even) to nth-child(2n), so grepping for the word even finds nothing and looks like a failure. SUPERSEDED IN PART the next day: the striping added here was per ROW and is now per record, because two-line rows shaded one pilot's notes with the next pilot's data; and the 1320-to-1800px shell widening is no longer what keeps the table on screen, since the table itself now fits in 980px. See the two-line rows entry above
- [x] Training records are per EVENT, and the importer reads the real tracking sheet (Aug 1) — the user supplied the Recruiting Status Tracking Training Info export, and it immediately disproved the model shipped that morning. Records were one-per-person; the sheet has six pilots twice over (Chris Geradine, Fabio Alves, Jaren Smith, Kylee Madsen, Landon Roberts, Jerry Harrington) because a pilot returns to training for a new seat, a new aircraft or recurrent training — so one record per person quietly overwrote their history. Reworked to one record per training EVENT, with deterministic ids from name plus training start plus position so a corrected re-paste updates rows rather than doubling them. COLUMNS NOW MIRROR THE SHEET so the two read the same: name, external/internal, pool, position, start date, orientation, indoc, training start, training END, location, status. The sheet's Training Status is about whether the booking is firm and is stored verbatim, while progress is DERIVED from the dates, so they cannot disagree. All 98 ARCHIVED rows import and are kept, hidden from the active list but searchable — the user asked for the info without it being active. THE IMPORTER HANDLES THE REAL FILE, not a tidy version of it: quoted notes with commas, blank spacer rows, the ARCHIVED divider, two unlabelled columns read by sniffing their values, N/A and TBD and Contract where dates belong, and a date range in one cell. Exactly two cells were unreadable and both are preserved as notes rather than dropped — Patrick McPartland's 01/03/2025 (?) and Chris Johnston's indoc 5/26/0206, a typo for 2026 rejected by a hard floor at 1990 or it would sort above everything forever. WHAT IT FINDS ON THE LIVE CHART, which is the whole point: Michael Salvagnini finished training on 06/28 and Jonathan Siswick on 07/22, and BOTH are still sitting in training seats five and two weeks later — the banner now offers to move each of them onto the line. It also surfaces Matt Dahle, in a training seat with no row in the sheet at all. NOT YET IMPORTED: the data itself is still the user's to paste and save, deliberately, since that review-then-save step is the only gate before a shared live database
- [x] Two stale labels on the fleet charts (Aug 1) — both found while looking for small rock-6 work, and both were actively telling people the wrong thing. The link picker's empty state said no matches, try a last name, which was advice invented BECAUSE the search only matched the whole query as one string; that was fixed and pushed two days ago, so the hint had gone from workaround to a claim that the search is still broken. And the maintenance chart called its own data Seed data although it has been admin-editable and stored in a WorkspaceSetting (fleet/mx-roster) for weeks, exactly like the crew chart, which correctly says Live data — labelling real editable data as a mockup tells the team not to trust it, which is the last thing a chart at 30 percent adoption needs
- [x] Link-picker search finds a person by any word of their name (Aug 1) — reported as search the exact name or nothing. The org-chart link picker matched the whole query as ONE substring, so Nick Lembo found nobody unless a record literally contained that string; a profile reading Nicholas Lembo or Lembo, Nick missed, and deleting the first name was the only way through. The query is now split into words, any word may match so a surname alone works, and how well each word matches decides the ORDER rather than whether somebody appears at all — exact full name, then whole-string containment, then a word-start hit above a mid-word one, with a bonus when every word is accounted for. Ordering also prefers people you can actually staff, so an archived record matching one word cannot outrank a current employee matching the whole name. Known edge case: a query made only of single-character words now matches nothing where it previously did a literal substring match
- [x] Fleet charts: tails on managed positions, find-a-person, departure archiving, training tab (Aug 1) — six issues from real use of the crew chart, applied to crew and maintenance. (1) MANAGED POSITIONS CARRY THEIR TAIL: the move dropdown labelled by aircraft type, so six managed PC-12s were six identical PC-12 First Officer rows; a pilot was moved onto a managed tail when a fractional seat was meant and it was caught only after the save. Now it reads PC-12 First Officer (N418T), split into Fractional and Managed optgroups, with an adds-a-seat marker on seats the aircraft does not have — which is why FO seats showed on single-pilot tails at all. (2) FIND A PERSON on both charts: searchable flat list, sorts by last name or aircraft, any-word matching so a surname alone works, and picking someone opens their card and rings their row. (3) DEPARTURES AGE OUT after 30 days, derived from a new date field and never written, so nothing runs and correcting the date reverses it; existing free-text departed 06/15 rows are parsed so the backlog ages out too, and an undated departure (a pending tentative move) never archives. On the live chart today that archives Chris Johnston and leaves Dayne Shafer and Matt Dahle. (4) TRAINING TAB with start/end/vendor/status per pilot plus a paste import that reads a copied spreadsheet by its headers and merges by name without deleting anyone; pilots the chart shows in training with no record appear as placeholders so the gap reads as missing data. (5) When an end date passes a banner offers to move that pilot to the line, CONFIRMED and never automatic — this settles the automatic-versus-prompt question the training-dates backlog item raised. Training lives in the same WorkspaceSetting blob as the roster, so no migration against the shared live database, and a save that omits training preserves it rather than wiping it. (6) Edit moved to the top of the card, renamed Edit Crew / Edit Team. VERIFIED: tsc, lint and build:check clean; 43 assertions over the label, date and import logic; a read-only round-trip of the LIVE roster confirms groups, links and training all survive a save unchanged. NOT VERIFIED: any click path — this app's client components do not attach handlers in the preview pane (confirmed by checking for React props on the buttons, not assumed), so rendering is proven and interaction is not. STILL OPEN: the training end date does NOT show on a pilot's candidate profile, which the user asked for — that needs either new columns on Candidate/NewHire or a decision about where training lives for everyone, not just crew; and the real training data has not been imported, only the importer built. SUPERSEDED IN PART the same day: the per-pilot record model described here lasted hours — the real sheet showed a pilot can train more than once, so records are now one per training EVENT and the columns mirror the sheet. See the training entry above
- [x] FIXED: 48 of 60 pilot requirements had silently disappeared (Jul 21) — reported as "requirements are not showing up and I cannot add gates any more". Nothing had been deleted. The Pilot Requirements page hid any requirement whose SOURCE JOB had been merged away as a duplicate, so when the job de-duplication merged 54 duplicate job records it took their requirements with it — including every core fleet seat (G200, G450, PC-12, CJ2, CE-525, 560XL, M2). Only 12 leftovers stayed visible, and because the editor only appears for a requirement you can see, gates became uneditable too. Three fixes: (1) visibility now depends on the requirement's OWN status (ACTIVE/INACTIVE/EVERGREEN show, HISTORICAL/RETIRED/ARCHIVED do not) instead of on an unrelated job merge — a person can see and change that in the editor; (2) consolidated 60 requirements down to one per real role using fleetPositionSlug, the app's own canonical role id, keeping the copy with the most gates filled in and retiring the rest as HISTORICAL — nothing deleted, all 2,242 gates intact, full undo file at scripts/requirement-consolidation/UNDO.json; (3) cleared a stale merge pointer on "Citation CE-525 (CJ2) Captain", which was OPEN yet still flagged as merged. 12 visible before, 28 after, one per role. Two records were deliberately LEFT ALONE because the app cannot say which role they are: "Citation CE525 Captain" (bare CE-525 is ambiguous between M2/CJ/CJ2 on purpose) and "G450 Aircraft Maintenance Technician" (a mechanic stored with roleCategory Pilot; the title resolver wrongly maps it to G450 Captain). Both need a human decision. NOTE: an earlier draft of the consolidation grouped by a hand-written title regex and got it wrong twice — it merged CJ Captain into CJ2 Captain (different aircraft, both Active) and folded the archived 560XLS+ and PC-12 NG/NGX positions into their active siblings; grouping by the fleet slug avoids all of that
- [x] The job PDF import says WHY it could not read a file (Jul 21) — it used to report a bare "imported 0, skipped 1", which is true and useless: it does not separate "this PDF has no readable text in it" from "this PDF is fine but is not a layout I know", and those need completely different things from the person holding the file. Now each skipped PDF gets a plain-English reason, worked out in the browser where the extracted text still exists (the server only ever receives parsed rows): no text layer at all (a scan or image export), very little text (image pages), looks like a posting but incomplete, or simply an unrecognised layout — the last one quotes the opening words so it is obvious which document got picked. A run that imports nothing is now amber and leads with "Nothing was imported", instead of a green tick reporting zeroes. Every message points at New requirement as the direct route for adding a pilot role. Reason ordering matters and is deliberate: the posting-shape check runs BEFORE the character-count check, so a short but genuine posting is not misdiagnosed as a scan and someone sent off to re-save a perfectly readable file
- [x] ROOT CAUSE: the job PDF import could not read the CURRENT careers site (Jul 21) — reported as "this is a true PDF printed to PDF, not a scan, it has everything in it, why is this wrong". It was wrong, and the better error message added earlier was still giving stale advice. Running the real file through the actual parser settled it: 2 pages, 5,070 characters extracted perfectly. The parser was written against an OLDER version of skyshare.com/careers and the site has since been redesigned, so it was hunting for headings that no longer exist. It wanted "Job Location:", the page now says "Location: SLC OGD"; it wanted "About the Role:", the page says "Job Summary"; it wanted "Qualifications:", the page says "Qualifications" with no colon. Every posting printed from the live site produced zero rows. Added a parser for the current layout (anchored on the letter-spaced SKYSHARE CAREERS banner and the real section set: Job Summary / Qualifications / Responsibilities & Physical Requirements / Salary, Schedule & Availability / Benefits / Location / Work Authorization), which also strips the browser print header and footer. The old parser is kept and still runs second, so PDFs saved from the previous site design continue to import. Verified on the real G200 First Officer file: title, department Pilot, base SLC, pay line and 2,364 characters of genuine qualifications all extracted. ALSO FIXED while in there: the importer only matched an existing job on sourceJobId or jobReqId, and a printed PDF carries neither (its id is generated from the filename) — so importing a posting for a job that already existed created a SECOND copy. That is precisely how the duplicate pile that hid 48 pilot requirements accumulated. It now falls back to matching on the normalized job title, excluding merged-away duplicates so a loser is never revived; confirmed that importing the G200 posting updates the existing OPEN job in place instead of adding another
- [x] Add a pilot requirement by hand (Jul 21) — "New requirement" on the Pilot Requirements page (admin + recruiter). Until now the ONLY ways to create a requirement were importing a job or flipping a job to pilot-role, which is why people reached for the PDF importer to add a role it was never designed to add. Pick a fleet position from the registry (not free text, so it lands on a real canonical role with the right slug and seat), and the standard gate catalog is attached automatically so there is something to edit immediately. Positions that already have a requirement are filtered out, and asking for a role whose requirement was retired RESTORES that one rather than creating a second — otherwise the de-duplication above would be undone by the first person to click New
- [ ] History of WHY staffing numbers changed — when the staffing target / open seats for a pilot position changes, keep a dated log of the reason, so months later you can see "on this date, this is what happened, and this is why." The user updates the fleet numbers himself; this is purely the audit trail, not automation. Should capture: the date, who changed it, the old and new numbers, and a free-text reason. Decide where it lives (per fleet position / seat, surfaced on the crew org chart and the position editor) and whether the old-to-new numbers are recorded automatically while the person only types the reason. TOUCHES THE CREW ORG CHART = the Pilot recruiting status tracking rock. REAL EXAMPLE to seed it (Jul 21, 2026): a third G200 is arriving, so we move to full staff across three aircraft — the G200 SIC target went from 1 (with a possible 2nd) to 3 firm, and we are actively hiring three G200 SICs; G200 Captain is already overstaffed so no new openings there; of the three SIC seats Hank is still tentative and two are unknown. Added Jul 21.
- [x] Canonical position order site-wide (Jun 19) — every position list now sorts by the FLEET_POSITIONS size order (PC-12 → … → Legacy 650) instead of alphabetically / by usage count; the Pilot Requirements list also groups SkyShare vs Managed. Sorting where useful still allowed. lib/fleet/positions.ts fleetOrderIndex/fleetSeatRank are the shared sort keys
- [x] SkyShare vs Managed role split + de-duplicate (Jun 19) — shipped the ManagedVariant model + a "Managed aircraft" panel on each role: a canonical role holds the shared qualifications/gates, and each managed aircraft attaches as a variant carrying its own tail number, pay, base, and schedule. Consolidated the whole pilot fleet: SkyShare-vs-Managed grouping site-wide, duplicate/mislabeled rows merged (recategorize/merge only, no removals), and every managed tail recorded (Phenom 100 N450JF, Phenom 300 N409KG, M2 N785PD, CJ N443BC, 560XLS+ N6TM, G450-NV N787JS, Legacy 650 N650JF, PC-12 NG N413UU, PC-12 NGX N825NX retired). 26 roles total (10 SkyShare + 16 managed seats). Follow-ups: newly-created managed roles carry cloned/approximate gates to review per airframe; the dual-aircraft job link is tracked separately below
- [x] Dual-aircraft positions (Jun 20) — a requirement can now link additional fleet positions (linkedFleetPositionSlugs) for roles covering two airframes (e.g. PC-12 Captain + M2 First Officer). Set them in the Fleet position editor ("Also covers (dual position)"); the detail shows a "Dual position" badge; and the linked positions' aircraft are folded into candidate aircraft-fit scoring. Deeper per-seat scoring of each linked position is a future enhancement

## Platform & Infrastructure
Behind-the-scenes work that keeps everything running.
- [x] The feedback screenshot tool can see screenshots again (Aug 31) — it filtered and read only the legacy Feedback.imageKey column and never touched the FeedbackImage relation, so every screenshot uploaded since multi-image support shipped reported as no screenshot attached. It reads both now, legacy first then the relation in sort order, with multi-image reports written to separate suffixed files. The failure mattered more than its size: this is the documented tool for looking at a user's own evidence and it was silently reporting ABSENCE rather than erroring, which is the exact failure mode this repo already carries a standing warning about. Live totals confirmed at commit time: 16 rows still on the legacy column, 15 FeedbackImage rows across 11 reports that were entirely unreachable before.
- [x] Three settled feedback reports are closed, and the stale one is recorded as stale rather than as done (Aug 31) — open reports 7 to 4, re-confirmed at commit time as 50 rows with DONE 46, NEW 3 and REVIEWING 1. The interviewer-list question was answered and then overtaken by a commit that removed the app-account requirement outright. The tab badges shipped, with History reading none rather than the N/A that was asked for, which he reviewed on screen and chose to keep - so it is closed to a DECISION and the script says so, because a substitution closed quietly is indistinguishable later from a request that was met. THE JULY 2 EMAIL BUTTON IS CLOSED AS STALE AND NOT AS DELIVERED: only the candidate half was ever built, while the supervisor half, the CPO half and the role-aware recipient selection were never built at all, and role-awareness is the part that made it a feature rather than a button. His instruction after two months was that whatever exists is fine, plus a standing rule to drop anything old and pending we are unsure about. Reopen it if the need returns. Three reports were deliberately left open with the reason recorded - the duplicate-review and orientation ones because their fixes are in this same batch and not yet deployed, and the recruiting-jobs redesign because he parked it pending mockups.
- [x] RECORDED, close anything old and pending we are not sure about (Aug 31, from him) — his standing instruction, given when asked about the July 2 item: if it makes sense to keep, keep it, otherwise drop it rather than letting it age in the queue.
- [x] Three shipped feedback fixes are still sitting in the queue as NEW (found Aug 31) — the duplicate-review, orientation and tab-badge items all have code on main; only the /matching item was actually closed. This repeats a pattern already recorded here from Aug 20, where two travel items were fixed in July and closed three weeks later. The cost is real in both directions: the queue is the only place anyone looks for what is outstanding, so a fixed-but-open item gets re-investigated, and a shipped-but-imperfect one like the History badge gets closed without the substitution being noticed. A habit rather than a feature - close the item in the same push that ships its fix.
- [x] The feedback screenshot tool is still blind to most screenshots (re-confirmed Aug 31) — recorded once already and repeated because it silently reports ABSENCE on the documented tool for reading user evidence. scripts/feedback-image.ts was not touched by any commit in the Aug 31 batch and still filters and reads on the legacy Feedback.imageKey column with no reference to the FeedbackImage relation. It reported no screenshot attached for all three of that day's reports while the database held image rows for each, and fetched the one Aug 18 report correctly because that row still uses the legacy column, which is the positive control.
- [ ] The feedback screenshot tool cannot see most screenshots (found Aug31)  — scripts/feedback-image.ts filters and reads on the legacy Feedback.imageKey column only and never touches the FeedbackImage relation, so every screenshot uploaded since multi-image support shipped reports as no screenshot attached. Confirmed at commit time: 5 references to imageKey in that file and zero to the images relation. Not a guess in the field either - it printed that line for all three of the Aug31 reports while the database held FeedbackImage rows for each, and the one Aug18 report still using the legacy column fetched correctly, which is the positive control. It blocked reading her evidence until a temporary script using the images relation worked around it. Small fix, worth doing because this is the documented tool for reading user-reported evidence and it silently reports absence.
- [x] RECORDED, the two REVIEWING feedback items are deliberately parked (Aug31, from the user)  — his words were to ignore them for now. They are the Jul 2 Email-button idea on a new-hire page and the Aug 30 recruiting-jobs redesign, which he moved from NEW to REVIEWING himself on Aug 30. Whole-table state re-verified at commit time: 49 feedback rows, NEW 5, REVIEWING 2, DONE 42.
- [x] RECORDED, feedback screenshots live in two places now (Aug 28) — they arrive as FeedbackImage rows rather than the legacy image columns on Feedback, and scripts/feedback-image.ts reads only the four legacy columns so it reports the newer ones as absent. Measured while checking: 6 FeedbackImage rows, oldest Aug 21, against 16 Feedback rows still carrying a legacy imageKey. Both stores hold real data, so anything reading feedback screenshots has to read both
- [x] RECORDED, do not build an undo-record path from __dirname in a script run under tsx (Aug 28) — it resolved to prisma/generated/client, so the record landed in prisma/generated/client/travel-feedback-aug27/ which .gitignore catches, and the only means of reversing a live write would have been unstageable. Build the path from process.cwd() and print it in the dry run so it is checkable without writing anything. SECOND TIME this repo has hit this in two days; the open-roles reconcile hit the same trap by a different route, writing to a scripts/_*_output/ directory that the same ignore file catches
- [x] The "route file work through shell commands" message in scheduled-task output is Claude Code itself, not a prompt injection (Aug 27, after the daily work report flagged it) — it arrives as a harness attachment of type auto_mode carrying FLAGS rather than prose (bashFirst true, steerOnly true, bypass true), delivered one millisecond before the total_tokens_reminder attachment in the same transcripts. The English is rendered client-side from the bashFirst flag, so the words never appear in the transcript at all - which is exactly why it reads as text materialising out of nowhere straight after a tool result, the shape of a real injection. Evidence: 17 of 84 session transcripts carry it, only 2 distinct payloads exist across all 84, and it has been present continuously since Jul 30 across Claude Code v2.1.219 to v2.1.241. steerOnly means it is advisory, so ignoring it costs nothing and no scheduled-task output was affected. It contradicts the built-in PowerShell tool description, NOT CLAUDE.md, which says nothing about tool choice - do not send anyone looking there. Confirmed in the same pass: the git deny-list in .claude/settings.json IS still enforced under bypassPermissions, probed with a git clean -n dry run that came back denied, so reset --hard, checkout and push --force stay blocked for scheduled tasks too.
- [x] A feedback report can carry several screenshots (Aug 20, from feedback Aug 17) — she had been splitting one report across two submissions purely to attach a second picture, which broke the connection between them. Up to four images now, by paste or picker, each removable before sending. New FeedbackImage table rather than more columns; the four legacy image columns are KEPT and still served, because thirty-odd existing rows use them and rewriting live storage keys for tidiness is not a trade worth making. Readers prefer the new rows so a report never shows its first picture twice. Validation is all-or-nothing before anything is written, since silently dropping the third of three attachments is worse than refusing, and a partial storage failure reports exactly how many of how many arrived rather than claiming success. Bytes stay behind the same admin-gated route as before, matched on BOTH the report id and the image id so an image id cannot be used to read an attachment from another report. Exercised end to end against the running app and cleaned up after: FeedbackImage is back to 0 rows and Feedback to 40, which also confirms the cascade delete works. One honest leftover - four 70-byte test objects remain in the live S3 bucket under keys whose rows no longer exist
- [x] Four additive schema changes against the shared live database (Aug 20) — NewHire.seniorityNumber, NewHire.orientationNotNeeded, Interview.coInterviewersJson and the FeedbackImage table. This repo has no migrations directory, so schema changes go through prisma db push straight at production; the SQL was previewed with migrate diff FIRST and is purely additive, two nullable or defaulted columns plus one new table with its index and foreign key. No drops, no type changes, no data loss. Re-verified at commit time: migrate diff against the live datasource returns an empty migration, so there is no drift in either direction, and all three columns plus the table were confirmed present from information_schema rather than through the generated client - a distinction that matters, because a regenerated client can describe columns the database does not have
- [x] The Command Center moved to /settings/command-center and is ADMIN-ONLY (Aug 3, at the user's request) — two gates, and the second is not redundant: the settings module is only admin-locked by DEFAULT, since getModuleAccessRule reads a stored policy first, so an explicit role check sits behind it where a Module Visibility edit cannot reach. The old /command-center is kept as a redirect because it was the landing page for months and is sitting in bookmarks and pasted links. THE LANDING PAGE HAD TO CHANGE WITH IT: DEFAULT_HOME pointed at the Command Center, so leaving it would have redirected every non-admin at login into a page they are now forbidden. It is computed per role now, from the same visibility rules the sidebar uses. Verified by running the real functions against the default policy — admin lands on the Command Center, recruiter and hiring manager on candidates, viewer on the handbook, nobody on a wall
- [ ] The roadmap notes are off the team landing page as a consequence (Aug 3) — they render a candidate's personal email and phone, an outside vendor address and a developer filesystem path, confirmed in the live HTML. Only admins see that page now, but the values are still in this file and still render for an admin, so whether to sanitize the notes or stop rendering them is open
- [x] The Settings environment panel stopped lying, in three ways (Aug 3) — its readiness badge went green for any value that was not exactly the string local development, so a machine labelled development showed green READY; only production and staging go green now, via an explicit allowlist. It hardcoded SQLite local dev now and Local storage now, both false since S3 was enabled everywhere on Jul 30 against the shared Neon database; both are derived from the actual env values now so they cannot drift again. And the remediation text said to point DATABASE_URL at RDS when this runs on Neon. This CLOSES the item logged Aug 2, found while that very panel was being used to confirm the live site reported production
- [ ] Still stale on that same page and NOT touched (Aug 3) — SettingsWorkspace line 98 recommends AWS Amplify Hosting SSR and RDS PostgreSQL as the deployment path, while the app has been on Vercel plus Neon plus S3 for some time. Left alone deliberately because it reads as an aspiration rather than a claim about today, and whether AWS is still the intended destination is the user's call
- [ ] LANDMINE, and it cost real time on Aug 6 — a CLIENT component importing a VALUE from lib/data/candidates pulls the Postgres driver into the browser bundle and kills hydration app-wide. The symptom is that clicks do nothing, with NO console error on the affected page; the only place the cause appears is the webpack import trace, as Can't resolve fs from pg-connection-string, naming neither the constant nor the component. Fixed by moving the shared list constants into lib/candidates/list-config.ts, which imports NOTHING by design. THE DISTINCTION THAT MATTERS: an import TYPE from that module is fine, because types are erased at compile time and never reach the bundle — CandidateTagFilter still has one and is safe. A value import is what breaks it. Probably belongs in the CLAUDE.md tooling-gotchas list rather than only here
- [x] Every page stopped paying for the same lookup twice (Aug 3) — AppShell ran three sequential single-row WorkspaceSetting reads and each page's access check then re-ran the first; that check has 113 call sites across 47 of 59 pages and the repo had ZERO uses of React cache(). Proven with a positive control rather than asserted: with the cache active two requests produce two policy reads, with it removed the same two requests produce four. The app's own access-check instrumentation moved from a 94ms median to 64ms. Wall-clock timings were taken while other sessions hammered the same dev server and are explicitly not a claim worth standing behind; the query count is
- [x] Four index declarations added Aug 3, APPLIED TO THE LIVE DATABASE Aug 6 at the user's instruction — schema.prisma and the database now agree, and prisma migrate diff returns an empty migration. HOW IT WAS DONE, because the method mattered more than the change: the SQL was GENERATED by migrate diff rather than hand-written, so the index names match what Prisma expects and it cannot read as drift again; the diff was inspected first and contained EXACTLY four CREATE INDEX statements with no DROP, no ALTER and nothing else pending, which is what made it safe to run; and it was applied with db execute against that one file rather than db push, so nothing else could ride along. Verified after, not assumed: indexes on the three tables went 15 to 19, all four are present by name, and zero indexes anywhere in the database are invalid. Each is reversible with DROP INDEX; no column, table or row was touched. THE REASON THIS WAS WORTH DOING was never performance — at 3,598 Candidate rows, 455 NewHire and 308 Interview, on tables of 2.3 MB, 312 kB and 1.1 MB, a sequential scan is already fast. It was that schema.prisma and the database disagreed, and db push is the normal way to apply a change in this project since there is no migrations directory, so anybody running it would have silently shipped these four alongside their own work. Two sessions had already routed around that. ORIGINAL RATIONALE BELOW.
- [x] Which four indexes, and why each was chosen (Aug 3, applied Aug 6 — see the entry above) — the who-works-here filter used at 10-plus call sites with neither column indexed, an unindexed foreign key hit on every candidate profile render, the candidates list sort, and the calendar interview filter. THEY ARE INERT, and that was verified before committing rather than taken on trust: there is no prisma/migrations directory and the diff is 14 lines of index declarations and comments with no column or table change, so none of them exists in the database until somebody runs db push against the live Neon instance. That is a deliberate human step and was not taken. The generated postgres schema variant is now stale and should be regenerated. RE-CONFIRMED STILL ABSENT Aug 6, by listing every index on Candidate, Interview and NewHire — 15 exist and none of these four is among them. AND THEY ARE NOW A TRAP FOR SOMEBODY ELSE: a later session needing one small ALTER TABLE found that db push would have applied these four as a side effect, so it applied its own column by hand instead. Anyone running db push against the live database ships this un-reviewed work with it. Decide whether they were meant to ship
- [ ] Hard-delete test data — bulk-select checkboxes on the Candidates and Jobs lists to permanently remove test records; irreversible, admin-only, with confirmation (build when closer to going live)
- [x] Renamed the app to SkyShare Journey in code, docs and the Handbook (Aug 3) — THE REPO HALF IS DONE; the external half is the item below, and until that runs the app still lives at the old domain. THE DECISION IS YES: the July survey was explicitly a cost estimate before deciding, and the user has now chosen to go ahead. Every place the name is actually seen reads SkyShare Journey: the browser tab, the sign-in screen, the environment banner, the default page eyebrow, Command Center, Settings, the Powered by line on the public booking page, the Shared from line on report links, the line appended to Google Calendar interview invites, every comment the app posts into Front, and all seven Handbook files. Also CLAUDE.md, CODEBASE_CONTEXT.md, the README headings and the daily work report scheduled task. Replacements ran LONGEST-FIRST so SkyShare Talent-Ops could not become SkyShare Journey-Ops; a tree-wide grep afterwards found zero of either mangled spelling, and the only Journey-Ops string anywhere is this roadmap describing the trap. THE FRONT MARKER TRAP WAS HANDLED: lib/pilotapp/scan.ts keeps the old literal as LEGACY_COMMENT_MARKER permanently beside the new one and the already-commented test matches EITHER, so threads handled under the old name are still recognised and never get a second comment. That is the only marker of its kind — listComments is imported by that one file, and Paycom and travel dedupe by Front message id instead. THREE THINGS THE SURVEY MISSED, all found by sweeping for the BARE name once the prefixed one was gone: the Handbook cover heading read Talent-Ops Handbook with no SkyShare in front of it so it matched nothing, plus two lines in the front-api skill file. Also fixed: lib/notifications/mentions.ts no longer hardcodes a production host as its link base, preferring NEXTAUTH_URL, then the domain Vercel itself reports for the production deployment, which survives a project rename. THE NAME WAS NEVER IN THE DATABASE, verified with a positive control — 36 WorkspaceSetting rows scanned, 0 name matches, 7 control matches — so no part of this touched data. VERIFIED: lint 0 errors, tsc exit 0 unpiped, and over HTTP against a local dev server the tab title, sign-in, Settings, Candidates, the Handbook index, a Handbook chapter and the public booking page all returned 200 with the new name present and zero of the old. TWO HONEST CAVEATS: the shared report footer was NOT exercised at runtime, because it needs a share token and minting one writes to the shared live database, so it is verified by typecheck and diff only; and an earlier Handbook check that reported zero old-name hits turned out to be a 404 on a guessed slug rather than a clean page, caught only by checking the status code. SCOPE RULE FROM THE USER, and what it left alone: the name changes only where it is SEEN, so the S3 bucket, the Neon project, THE LOCAL FOLDER (both scheduled tasks reference that path and renaming it stops the daily report and the Monday check-in running), package.json, the six older planning documents, and the phrase Talent analytics on the reports pages all keep the old name deliberately
- [x] SIGN-IN OUTAGE DURING THE RENAME, diagnosed and fixed the same afternoon (Aug 3) — everyone was locked out with redirect_uri_mismatch. TWO CAUSES, and neither was the code: the repo change touched two display strings on the login page and no auth file. FIRST, NEXTAUTH_URL in Vercel production had been set to the journey domain BEFORE the Vercel project was renamed, so the app asked Google to return people to a domain that did not exist. NEXTAUTH_URL must always name a domain that exists right now — redirect URIs can safely list a future address, that variable cannot. SECOND, and why the obvious fix did not work: a SECOND OAuth client named SkyShare Journey had been created, and the new redirect URI was added to it, but the app authenticates with whichever client matches AUTH_GOOGLE_ID and that was still the original SkyShare Talent Web client. So every edit landed on a client nothing used. Google's own note under the client Name field says it is console-only and never shown to end users — the name people read at sign-in comes from the CONSENT SCREEN, so no new client was ever needed. THE DIAGNOSIS THAT SETTLED IT, worth reusing: /api/auth/providers on the live site returns the callbackUrl the app will send, which reveals NEXTAUTH_URL without opening Vercel; and Google's error-details panel shows both the redirect_uri AND the client_id, whose prefix identifies which client is really in play. Comparing those two values located the fault in about a minute after several wrong guesses without them — including one of mine, that AUTH_GOOGLE_ID had been changed, which the client_id disproved. FIX: set NEXTAUTH_URL back to the live domain and redeploy. Verified afterwards that the app reports callbackUrl skyshare-talent.vercel.app, matching the URI registered on the client it uses. The runbook now carries all of this as a diagnosis section, and the unused SkyShare Journey OAuth client can be deleted
- [x] SkyShare Journey rename: THE EXTERNAL HALF IS DONE — the app is live at skyshare-journey.vercel.app (Aug 3) — the Vercel project is renamed, NEXTAUTH_URL moved with it, and the Front webhook now points at the new address. THE WEBHOOK IS PROVEN, not assumed: the user mentioned himself in the app, replied to the notification from his email, and both messages came back into the app, which is the round trip that is the only real evidence a Front rule is still connected. Verified independently from the app itself: /api/auth/providers on the new domain reports callbackUrl https://skyshare-journey.vercel.app/api/auth/callback/google, matching the URI registered on the SkyShare Talent Web client, so sign-in is consistent rather than merely working by luck. The old address still returns app content, which is what keeps booking links and shared report links already in outsiders' inboxes alive — worth confirming the alias is configured deliberately rather than relying on it lingering. Sign-in passed straight through the rename this time because the redirect URI was added FIRST, which was the whole point of the reordering. THE HANDBOOK REPUBLISH IS ALSO DONE, found by checking rather than assuming: the published artifacts already carry the new name — the Handbook reads SkyShare Journey Handbook in its title, cover and footer, and the Business Cards SOP footer reads SkyShare Journey. Somebody republished them after the rename commit without reporting it in a handoff, which is the second time today work was done and not recorded (the orphaned training rows were the first). VERIFIED ON TWO OF SIX, not all six: those pages are about 3 MB each to fetch, so the remaining four are assumed to have gone in the same pass and can be spot-checked in a browser for nothing. Cosmetic leftover, not a rename problem: the SOP footers still read updated Jul 22 and Jul 28, but those dates are hardcoded in the repo source too, so the published copies and the repo agree. Also done in this pass: the four production addresses in CODEBASE_CONTEXT.md, deliberately left until the new domain existed
- [~] Settings-tab reorganization (from the Jul admin audit) — FLEET POSITIONS IS DONE (Aug 1). It now lives at /fleet/positions under the Fleet module instead of Admin > Settings, because it is the canonical list of pilot positions that Jobs, Pilot Requirements and Matchboard all resolve against. The module-access implication that paused this move turns out to be benign, and is why it could ship in one sitting: FleetPositionsView is READ-ONLY, since the master list is FLEET_POSITIONS.md plus npm run fleet:sync, so gating on fleet rather than settings widens who can READ the registry and grants nobody a way to change it. The old URL 308s to the new one. ONE TRAP RECORDED: redirect() thrown from a Server Component under /settings was swallowed by a parent error boundary and served as an error page — HTTP 200, NEXT_REDIRECT in the payload, no Location header — so the redirect lives in next.config.mjs, which is a real 308 that never reaches React. It was only caught by curling the old URL rather than trusting the page to work. STILL OPEN below. The 8 Settings pages are all fully built but a few sit in the wrong place: relabel/consolidate **Block management** (delete-only) with where blocks are actually edited so it's not confusing; consider moving **Activity** next to Reports in Data. Keep General, Team Members, Templates, Feedback where they are. **Layout Lab stays** (decided Jul 9 — the user values the visual "where does this live / move it there" overview; revisit simplifying it visually later, but do not retire it). (Paused Jul — needs per-move decisions incl. module-access implications of moving Fleet positions.)
- [x] Google OAuth login fixed (Jun 9) — env vars via Vercel CLI
- [x] PostgreSQL (Neon) for local + production (Jun 9)
- [x] Settings pages: Team Members + Activity (Jun 9)
- [x] Editable roadmap powering this checklist (Jun 10)
- [x] Feedback button + admin review page (Jun 10) — any user submits ideas/bugs/questions
- [~] Feedback: attach a screenshot, and annotate it (Jul 16) — AUTO-CONTEXT + IMAGE UPLOAD BOTH SHIPPED Jul 16; only annotation and the element-picker are still open. IMAGE UPLOAD (Jul 16): the feedback box now takes a picture — pick a file, or just paste one straight in with ctrl-V, which is the actual workflow (Win+Shift+S then ctrl-V) and the reason this is worth having; a pasted shot is auto-named after the page it came from. Thumbnail preview with a remove button before sending, and the image shows on Settings > Feedback, click to open full size. Option 1 of the three below, deliberately: it works everywhere, it covers most of the value, and it does not go blank on PDF pages the way in-app DOM rasterizing does. PII ANSWER (was flagged below as needing a decision, now decided and built): screenshots are treated exactly like candidate documents — stored as a private blob through the normal storage adapter (S3 in prod, local disk in dev), never given a public URL, and served only through a route gated on the SAME admin permission as the feedback list itself, since a screenshot of this app routinely contains candidate PII. Only PNG, JPG, GIF and WEBP are accepted and BOTH the extension and the mime type must agree; SVG is refused on purpose (it is a script-carrying document, not a picture) and the serve route pins the content type rather than echoing what was uploaded, so a stored file can never execute in an admin session. Capped at 10 MB. The image is attached as a separate step AFTER the report is saved, so a storage failure costs the picture and never the words. Verified end-to-end against the real route: upload, byte-identical read-back, SVG rejected, a text/html file renamed to .png rejected, and the no-image path unregressed. STILL OPEN: annotation (arrow/box/highlight/blur on the image before sending) versus the cheaper element-picker (click the broken thing, we record it, no drawing tools), and a retention answer for stored images. Also still open from the original ask, and still true: every submission carries the FULL url including its query string (the old capture stored the pathname only, so which record or tab you were on — the ?id= and ?tab= — was thrown away), plus viewport size and pixel ratio, screen size, light-vs-dark theme, browser and OS, and any runtime errors caught while you were on the page. All of it shows as chips on Settings > Feedback with the errors in a tooltip, so a report is actionable without a picture — a cut-off button is nearly always a viewport problem and the viewport is now recorded. Stored in Feedback.contextJson (additive nullable column), size-capped, never trusted for anything but display. Still to decide for the picture itself: capture path, annotation vs element-picker, and storage — plus the PII answer below. Three options, roughly in order of effort: (1) UPLOAD / PASTE — a file picker plus ctrl-V paste straight from the clipboard, which is how most people already take a screenshot. Cheapest, works everywhere, and covers most of the value. (2) CAPTURE THE PAGE IN-APP — a "Take a screenshot now" button that rasterizes the current DOM (html2canvas-style). No permission prompt and it only ever captures our own page, but fidelity is imperfect — cross-origin images, iframes, and the PDF.js viewer tend to come out blank, which matters since document pages are exactly where bugs get reported. (3) REAL SCREEN CAPTURE — the browser's native getDisplayMedia picker gives true pixels including PDFs, but prompts every time and lets the person accidentally share another window. Then ANNOTATION on top of whichever capture path: a small canvas editor over the image with arrow / box / highlight / blur tools before it is sent. Two cheaper alternatives worth weighing first — an element picker (click the broken thing, we record its position and auto-draw the box, no drawing tools needed), and just auto-attaching context we already have (full URL, viewport size, browser, recent console errors), which fixes a lot of "which page / what did you click" without any image at all. Decide: capture path, annotation vs element-picker, and storage. NOTE, needs a decision: screenshots of candidate or employee pages will contain PII, so attachments need the same access rules as documents plus a retention answer (S3 in prod, local disk in dev).
- [x] Workspace logo library (Jun 11) — admins upload multiple named logos in Settings (Branding) and assign one to each placement: sidebar mark, login page, and reports/exports; logo also serves as the Home button
- [x] Sidebar polish (Jun 11) — rail locked to full screen height (only panel/content scroll), Admin pinned to the bottom, thin gold hairline dividers between rail items, wider 70px rail; Settings tabs (General/Team Members/Activity/Feedback/Templates) moved into the Admin panel as nav items
- [x] Project moved off OneDrive (Jun 17) — repo now lives at C:/Users/Recruiter/Projects/skyshare-talent-ops; path references updated; stale OneDrive-pinned sessions archived
- [x] Secured leaked database credential (Jun 17) — .env had been committed to the public GitHub repo; rotated the Neon password, untracked .env, removed redundant plaintext secret copies, verified prod + local still connect
- [ ] Full local dev parity — real Google login + real S3 uploads on localhost (today dev bypasses auth and saves files to local disk, which is fine for everyday work); when wanted, add the localhost OAuth redirect URI in Google Cloud Console and fill AUTH_GOOGLE_ID/SECRET + AWS keys in .env.local (templates already stubbed there)
- [x] Data security audit (Jun 19) — reviewed unauthenticated API routes, S3 presigned-URL usage, and NEXT_PUBLIC env leaks: no live PII exposure (prod enforces auth + internal permission checks). Defense-in-depth hardening recommended; the auth-bypass guard is now done (below), route-prefix checks in middleware are still open
- [x] Auth bypass can no longer happen on a hosted deploy (Jul 21) — isAuthRequired() asked "is NODE_ENV production?", so ANY hosted environment whose NODE_ENV was something else (staging, a misconfigured slot) would have handed full ADMIN access to anyone. Production was only ever safe because Vercel happens to default NODE_ENV to production — a big consequence resting on an incidental default. Now inverted: auth is required everywhere EXCEPT a demonstrably local dev machine (NODE_ENV development AND no VERCEL_ENV/VERCEL), REQUIRE_AUTH=true still forces it on, and local dev logs a loud one-time warning that every request is being treated as ADMIN. Fixed in BOTH copies — middleware.ts keeps its own, since the edge bundle cannot import the lib. Verified with an 8-case matrix (local, hosted-with-dev-NODE_ENV, staging, NODE_ENV unset, preview, production) plus a check that local dev still bypasses as before
- [x] Cron routes fail closed (Jul 21) — both /api/cron/calendar-sync and /api/cron/paycom-scan used to skip their auth check entirely when CRON_SECRET was unset. Production was never exposed (the secret is set there), but PREVIEW deployments have no CRON_SECRET while still sharing DATABASE_URL with the live database, so a preview URL could drive a real sync unauthenticated. Both now return 503 when the secret is missing. Vercel only runs crons on Production, so nothing legitimate is affected
- [~] Access-attempt tracking + failed-login alerts — LOGGING SHIPPED (Jul 19): every sign-in attempt is recorded in the signIn callback via the existing ActivityLog (new AUTH_SIGN_IN_ATTEMPT type; plus USER_LOGOUT on sign-out), with success/denied + reason in the metadata, visible on the admin /settings/activity dashboard by filtering the type. HONEST SCOPE: only what reaches the app is capturable — Google validates passwords upstream, so a genuine bad-credentials attempt never arrives; the recordable reasons are ok / blocked / not_allowed_domain (a blocked/revoked user's rejected attempts DO show up, extending the blocklist offboarding). STILL OPEN: the 3+-failed-attempts ALERT to an admin is not built — only the log. No migration. (added Jul 17)
- [x] Add a sign-out control (Jul 19) — a Sign out action now sits in the sidebar (desktop rail icon + mobile drawer row) beside the theme toggle, shows the signed-in email, and ends the session via next-auth signOut → /login. Only rendered when there is a real session (hidden in local auth-bypass). Also emits a USER_LOGOUT activity event. SignOutButton.tsx, wired through AppShell → Sidebar.
- [x] Per-user home page (Jul 19) — new "My preferences" page (reachable by any signed-in role, unlike admin-only Settings) with a picker built from the pages that user can actually see; the root redirect (app/page.tsx) AND the sidebar Home button both honor the choice, falling back to Command Center when unset or on local auth-bypass. Stored per-user in WorkspaceSetting (scope user-pref, key home:<userId>) so no DB migration. lib/data/user-home.ts, app/account, /api/user-home. FIX Jul 20 (from real use — it landed on Command Center anyway): the save/lookup were fine, but login redirected to a hard-coded /command-center (login page callbackUrl), bypassing the "/" route where the home resolver runs. Login now lands on "/" (a "next" page still wins), so the chosen home applies on sign-in.
- [ ] Employee self-signup link — send a join link so employees can sign up with their company email (settings/users); gate by allowed domain
- [ ] Create company Google Workspace emails from the app (scoped Jul 27) — provision a new hire's @skyshare.com mailbox straight from their record instead of leaving the app to do it, feeding the work-email field that contacts and business cards already read. YES THIS IS POSSIBLE: the Admin SDK Directory API users.insert creates a real Workspace user, and can set the name, the starting password, change-password-at-next-login and the org unit in the same call. DEPENDS ON THE AUDIT ITEM ABOVE — same API, same service account, same domain-wide delegation — so build that first and this becomes a small addition rather than a new integration. THE IMPORTANT DIFFERENCE IS THE SCOPE. Reading logins needs only admin.directory.user.readonly; CREATING an account needs the WRITE scope admin.directory.user, which also carries the power to change any password in the domain. Do not widen the audit credential to cover this: use a SECOND, separately-held credential for provisioning, so the thing that runs nightly and unattended can only read. NAMING IS NOT AUTOMATIC. The convention is first initial plus surname, but it matched only about 90 percent of the existing roster and produced 7 real collisions (two Shermans, and Charles Hicks whose convention address would be chicks@). So the app SUGGESTS an address, CHECKS it against Workspace for a clash, and a person confirms — never silently assigns one. Decide before building: who is allowed to press it, what org unit and licence a new account lands in, whether a password is generated or typed, and how it is delivered to the new hire without emailing it to the mailbox they cannot yet open.
- [ ] Google Workspace account audit: who has logged in, and who should not still be active (scoped Jul 27) — ONE JOB, TWO QUESTIONS, both answered by the same Admin SDK read. QUESTION ONE, NEW HIRES: has this person EVER signed into their @skyshare.com mailbox? The user was explicit that this is BINARY — on or off, logged in or not. He does not care whether the last sign-in was yesterday or six months ago, so do not build a last-seen timeline, a staleness score or an age-of-account column; a person who has signed in once is done and should drop off the list. The Directory API makes this easy: lastLoginTime comes back as 1970-01-01T00:00:00.000Z for an account that has never been used, so never-signed-in is an exact test rather than a judgement call. The point is to catch a new hire who cannot get into their email and reset their password. QUESTION TWO, LEAVERS: is anyone who has left still holding an ACTIVE mailbox? The system must NOT close anything by itself — the user was clear that a departed person's mailbox is sometimes deliberately kept open (mail still arriving, a handover in progress), so the app RAISES IT and he answers yes-this-should-stay-active or no-shut-it-down, per person. Store the answer so a mailbox he has already blessed stops reappearing every week. THE ACCESS PATH IS DECIDED: a Google service account with domain-wide delegation, impersonating an admin, scope admin.directory.user.readonly. The scaffolding is already half-built — googleapis is installed and lib/google/calendar.ts already expects GOOGLE_SERVICE_ACCOUNT_EMAIL and GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY, though nothing is configured today; the app currently reaches Google as the signed-in user via OAuth instead. That file's comment says delegation needs a Workspace admin, which read as a blocker but is not one here: the user IS the Workspace admin. Setup is his side and takes about ten minutes — enable the Admin SDK, create the service account, add its client id under Security, Access and data control, API controls, Domain-wide delegation with that one scope, then hand over the email, the private key and one admin address to impersonate. KEEP THE CREDENTIAL READ-ONLY. Resetting a password through the API needs the write scope admin.directory.user, and a key with that scope could change ANY password in the domain including an admin's. The app should say who to reset; the reset itself stays a human action in the Admin console, which takes seconds. This matters more than usual here — a database credential was committed to the public repo in June. BUILD: cross-reference every Workspace account against NewHire.ssEmail, flag never-signed-in hires with days elapsed since their start date so started-9-days-ago-never-logged-in stands out from starts-Monday, flag terminated people whose mailbox is still active, and surface both on the new-hires page plus a line in the nightly hrotasks@ report. Also cheap to include from the same call: suspended accounts and anyone still carrying change-password-at-next-login.

## Access Control & Permissions
Department- and tag-based scoping for hiring managers, on top of the existing role-only Module Visibility & Access matri
- [x] The repeated-login loop and the near-daily logout had ONE cause, and it is fixed (Aug26) — production NEXTAUTH_URL named a different hostname than the one people browsed. The app answers on both skyshare-journey and skyshare-talent, both serve the same deployment and neither redirected, so which one you used was habit. next-auth builds redirect_uri from NEXTAUTH_URL but sets its state and pkce cookies with no domain attribute, and .vercel.app is on the Public Suffix List, so those two hosts can NEVER share a cookie. Start a sign-in on the wrong one and the callback arrives with no state cookie, throws, and bounces to /login?error=OAuthCallback - so the first attempt always failed, the second always worked, and the session cookie was left on a host the person was not browsing, which is the daily logout. NEXTAUTH_URL now names the journey host, and middleware redirects every production request to whatever host that variable names, exempting /api/cron and /api/front/webhook. THE MIDDLEWARE HALF IS NOT LIVE UNTIL THIS PUSH DEPLOYS - the env half already is, and both hosts report the journey callbackUrl today, which is why a bookmark on the talent host is still split right now, in the opposite direction
- [x] The login page now shows WHY a sign-in failed, which is the only reason this hid for weeks (Aug26) — auth.ts points both pages.signIn and pages.error at that page, and it read only the reason param, never error. So a genuine OAuth failure rendered identically to a plain please-sign-in: the person completed the whole Google consent flow, landed back at the start with no explanation, and did it again. It was reported as it makes me do every step twice, which is exactly what an invisible error looks like. Checked rather than assumed on a public unauthenticated page: there is no dangerouslySetInnerHTML in the file and the unknown-code fallback interpolates through JSX, so a crafted error value cannot inject markup
- [x] Two defects in that change, found by verifying it and fixed BEFORE it deployed (Aug26) — which is the cheapest moment there is. FIRST, a NEXTAUTH_URL carrying a PORT was a total production outage: the comparison read URL.host, which includes the port, while the target set .host then cleared .port, so the request host could never equal the canonical host and every request redirected to itself forever, with no circuit breaker. It could not fire today since production carries no port, and it is fixed anyway because this middleware's entire premise is that changing NEXTAUTH_URL is safe. SECOND, THE LOGIN PAGE COULD BE CRASHED BY A URL and the new error rendering is what armed it: Next types a search param as string or string ARRAY, the page's own annotation claimed string, so /login?error=a&error=b called trim on an array and rendered the generic error card - a page with NO sign-in button, on the one page that gates every other page. The middleware widens it, because it clones the whole query string into its redirect, so a logged-out click on /candidates?error=a&error=b reaches the same crash. Every param now goes through one helper and the annotation tells the truth; email carried the identical latent crash and is fixed beside it. Also tightened the exemption prefixes, which had drifted in opposite directions - webhook over-matched siblings like /api/front/webhook-test, and /api/cron/ did not cover a bare /api/cron. Verified by re-running the real logic across six NEXTAUTH_URL shapes and nine paths, including that the query string survives every redirect, which matters because the /welcome share tokens ride in it
- [ ] PRODUCTION SERVES THE STAGING BANNER, and the interesting part is not the banner (found Aug26) — NEXT_PUBLIC_APP_ENV resolves to staging AT BUILD TIME on the production deployment, so the public login page tells every visitor, before sign-in, that this is a safe validation environment. WHERE IT COMES FROM IS NOT SETTLED and the first answer was too confident: he reports the variable is set to production and that he cannot find it in the project environment variables at all. Both cannot describe the same build. What IS established, against the repo rather than assumed: the rendered value is the literal string staging, and the fallback can only ever produce Production or Local development, so a real value is being supplied; no tracked env file Next would load sets it, since the only two are .env.hosted.example and .env.production.example and examples are never loaded; next.config.mjs has no env block, vercel.json sets no env, build:hosted is byte-identical to build, nothing in the build chain writes an env file, and scripts/push-vercel-env.ps1 reads a source file that does not carry this key. The same deployment is PROVEN to have VERCEL_ENV production, because the canonical-host redirect is guarded on exactly that and it fires. So the value arrives from Vercel and not from the repo, but not from the project variable list he checked - team-level Shared Environment Variables, which projects inherit and which do not appear in a project own list, is the remaining candidate and is where to look next. A STALE BUILD IS RULED OUT: a fresh deploy was forced and the new bundle still renders staging, so the value is reaching the build now rather than being left over from an older one. The project to look in is skyshare-journey, in team skyshare-talent-projects, read off the deployment URL rather than guessed - and worth knowing while looking, the local .vercel/project.json still calls the project skyshare-talent, which is stale since the Aug 3 rename and would send anyone opening it by name to the wrong label. Real email is NOT affected today: the send guard reads VERCEL_ENV first and short-circuits, so it still resolves production. But the guard was deliberately built with TWO layers and the roadmap entry recording that is now false - it says NEXT_PUBLIC_APP_ENV is production in Vercel production, which is exactly the fallback that would keep real sends flowing if system variables were ever disabled. That fallback is not missing, it is REVERSED: it would now report not-production and either divert every real send to the test inbox or block it outright, which is verbatim the failure he said he feared. The instrument that should catch this agrees with the misconfiguration - Settings treats staging as a hosted env and shows a GREEN readiness badge whose own detail text reads staging. GOTCHA for whoever fixes it: NEXT_PUBLIC_ values are inlined at build time, so changing the variable alone does nothing until a redeploy
- [ ] ksherman@skyshare.com cannot use the per-user calendar features, and he is the recruiter they were built for (found Aug26) — his Account row has no refresh token and its scope carries no calendar.events, so orientation invites, the debrief queue and the schedule marker all refuse for him. Positive control from the same query, which is what makes the absence real rather than a bad lookup: all five Account rows were listed, they are 1:1 with the five Users, and the other four each carry a refresh token and the exact calendar.events scope. Cause is checkable and benign - his token predates the Jul 27 commit that added the scope, so he simply has not signed in since; nothing revoked him and the auth blocklist is empty. One sign-out and sign-in fixes it. TWO CORRECTIONS to how this was first put. Any calendar action of his fails is too broad: the interview sync, booking sync and calendar cron run as a SERVICE account and are unaffected, and that path synced successfully this morning. And the four healthy accounts are inferred from two columns rather than demonstrated - every one of the five access tokens is currently expired, so all four depend on a refresh succeeding, and a present refresh token is not a valid one
- [x] CLOSED THE SAME DAY: the handbook raw route served every internal SOP to anyone with the URL (found and fixed Aug 22, from another session's audit, both ends confirmed over HTTP) — FIXED AT BOTH LAYERS: middleware.ts now lists /handbook, and the route calls requireApiUser itself, which also picks up the per-user module gate for free, because checkUserModuleAccess resolves the request path and /handbook was already mapped to the handbook module. So the route now carries the same protection as the page it frames rather than merely authentication. RE-TESTED ON PRODUCTION AFTER THE DEPLOY, with the same request shape that found it: all three raw URLs now 307 to /login at 15 bytes with zero SOP text, where before they returned 200 and 21420 bytes of it. Public surfaces deliberately re-checked in the same pass so the fix cannot have over-reached: /welcome still 200, the vCard route still 404s without its token, /login still 200. THE SIGNED-IN PATH IS CONFIRMED TOO, by him opening the Handbook after the deploy and reporting the SOPs still render. That was the one thing this machine could not test, since local dev bypasses auth and cannot produce a denial, so it rested on reasoning about the same-origin iframe in SopFrame.tsx carrying cookies until he checked it. THE ORIGINAL FINDING, kept because the reasoning is the reusable part — app/handbook/[slug]/raw/route.ts has no guard of any kind, and middleware.ts does not list /handbook in protectedPagePrefixes, so nothing stands in front of it. VERIFIED AGAINST PRODUCTION WITH NO COOKIES, not from reading the code: /handbook/offers/raw returns 200 and 21420 bytes titled SkyShare Offers, a Step-by-Step SOP, with real headings (Work the offer, Move them in early, Signed the handoff) and zero login markup; /handbook/orientation/raw and /handbook/pre-onboarding/raw also return 200. The positive control is the same request shape against /candidates, /people and /settings, all of which 307 to /login, so the auth wall works and these are genuinely outside it. THE PAGES THEMSELVES ARE FINE and the audit was wrong to imply otherwise: /handbook and /handbook/offers return 200 but carry no SOP content, because requireModulePageAccess redirects and a redirect thrown inside a rendered page cannot change a status code once the layout has started streaming, the same behaviour already recorded for notFound on the welcome page. Only the route handler leaks, and only route handlers can, since they do not stream. Exposed content is internal HR and recruiting procedure, not candidate PII, across six guessable slugs. Same shape as the vCard hole closed earlier the same day, where gating only the page was called out as theatre; this is that exact mistake one directory over. Fix is one line: add /handbook to protectedPagePrefixes
- [x] The Aug 22 route sweep could not have seen that handbook route, so the entry recording it should not be read as a clean bill of health — the sweep is described as run across all 150 route.ts files under app/api, and that scope is the blind spot. Measured now: 153 route.ts files exist under app/, 152 of them under app/api, and the ONE outside is app/handbook/[slug]/raw/route.ts, the route that leaks. A sweep is only as good as its scope statement, and this one was accurate about what it did while reading as though it had covered everything. Any future sweep should walk app/ rather than app/api/
- [ ] Three candidate write routes apply no candidate scoping (found Aug 22, latent not live) — app/api/candidates/[id]/files, the same path plus /link, and app/api/candidates/department all call requireApiPermission and then act on the caller-supplied candidate id with no scope check; none of the three even imports the candidate-scope helper. NOT REACHABLE TODAY: they need files:write or candidates:write, only ADMIN and RECRUITER hold those, and viewer-scope.ts line 67 returns the unrestricted scope for both before any allowlist is consulted. It becomes real the moment either permission reaches a role that CAN be allowlisted, which today means a one-line edit to the HIRING_MANAGER or VIEWER array in roles.ts, not a config toggle. TWO CORRECTIONS to how it was first written. It is WRITE-only, not read: all three export only POST or PATCH, so the read exposure is just a candidate-existence oracle from the 404. And the three are not equivalent: candidates/department takes an unbounded caller-supplied id array into a prisma updateMany with no per-id check, so one request could rewrite departmentOverride across the whole candidate table, while the file routes act on one candidate each. THE STRONGEST ARGUMENT IS THE IN-REPO COUNTEREXAMPLE rather than the abstract inconsistency: app/api/candidate-files/[id] gates PATCH and DELETE on the SAME files:write and still calls isCandidateVisible, and candidates/[id]/tags requires candidates:write and still checks it. So files:write implies you see everything is not this codebase convention. Positive control: ten other routes under app/api/candidates DO scope, and these three are the complete set that do not
- [ ] The app sets no security headers at all, CSP included (found Aug 22) — next.config.mjs has no headers() function, its only async config function is redirects(); vercel.json holds only buildCommand and crons; middleware.ts sets only x-pathname; and there is no meta http-equiv anywhere. BROADER THAN FIRST REPORTED: it is not only Content-Security-Policy, there is also no X-Frame-Options, no X-Content-Type-Options and no app-set Strict-Transport-Security (the one on responses today comes from Vercel, not from us). NOT INDEPENDENT of the orientation-preview item below, and doing them in the wrong order wastes the work: a CSP strict enough to matter has to account for the inline theme script in app/layout.tsx (nonce or hash) and for the mermaid loader in lib/handbook/render.ts, which sets securityLevel loose and renders into an iframe carrying no sandbox attribute. Three weakened controls stacked on the handbook path is worth its own look
- [ ] Orientation email previews inject raw third-party HTML into the DOM (found Aug 22) — five dangerouslySetInnerHTML sites with no sanitizing: OrientationEmailPanel lines 662, 857, 1064 and 1184, and OrientationCalendarPanel line 201. All five line numbers re-checked and current, no drift. MATERIALLY DIFFERENT FROM HOW IT WAS FIRST DESCRIBED, and the correction lowers it: this is NOT stored XSS from the database by a candidates:write author. Traced all five. Three of them (662, 857, 1064) render bodies fetched live from the Front SaaS message-templates endpoint, and the other two are assembled in code with every interpolation escaped, so they carry nothing injectable at all. The orientation-templates store that candidates:write actually guards has no consumer anywhere in the repo outside its own route handler, so the privileged-author-to-privileged-viewer chain does not exist. What genuinely remains is narrower: three sites where a script tag placed in a Front message template would execute in the app origin for anyone opening the send dialog. The attacker is whoever can edit a template in Front, a separate SaaS with its own auth, and the same people who already control what the app emails out. Worth fixing as defence in depth, and the two escaped sites should be dropped from the finding. CAVEAT ON THE OBVIOUS FIX: sanitizeRichText drops every attribute outside its allowlist, and the Front bodies lean on inline font-family and font-size styles, so reusing it verbatim would strip the formatting these previews exist to reproduce. It needs an email-preview policy or an iframe sandbox, not a drop-in
- [ ] Fourteen hand-rolled HTML escapers, four of which ignore quotes (found Aug 22, no live hole) — COUNTS RESTATED BECAUSE THE FIRST THREE ANSWERS ALL DIFFERED, and each was right under a different rule, which is exactly why the rule has to be stated: six functions are literally named escapeHtml; seven if a definition-grep also catches escapeAttr, which is where the original seven came from; fourteen if inline and anonymous escapers count, measured across lib, app and components and excluding the vCard and CSV ones. The four that omit the double quote are notifications/mentions.ts:108, onboarding/hire-email-copy.ts:45, richtext/normalize.ts:84 and business-cards/card.ts:204. Every one of the roughly 34 call sites is in element-text position, so there is NO hole today, and that was established by enumerating all of them after a first pass that showed only 15 and would have rested an absence claim on under half the evidence. normalize.ts:84 omits quotes DELIBERATELY and is paired with escapeAttr at line 88 for its two attribute sites, so it is correct design rather than a defect. THE REASON THIS IS WORTH RECORDING is the fix rather than the risk: the original list of three omitters missed business-cards/card.ts, so a consolidation driven by it would have left a quote-blind escaper behind and looked finished
- [ ] The admin Role Permissions screen shows a permission matrix the app does not enforce (found Aug 22) — lib/auth/permissions.ts is a second, diverged copy of the real matrix in lib/auth/roles.ts: it is missing the files, events, requirements, publishing and duplicates:write groups that are actually enforced, and still lists candidates:delete, pilots, users, permissions:admin and duplicates:resolve, which no longer exist. REFRAMED, because the first description undersold it and a second pass overcorrected. The file is NOT unused and must not simply be deleted: the admin users route imports VALID_ROLES from it to validate the role being assigned to a user, which is a live path, and the Settings Users screen imports both exports. But nothing ENFORCES with the stale matrix, since the users route takes only the role-name list. The actual harm is that Settings renders the non-enforced matrix as a reference table, so an admin deciding who to give a role to is reading permissions that do not match reality. The screen already carries an on-screen disclaimer, which means the mismatch is known and shipped rather than merely stale. It also successfully misled a reader during this audit into believing duplicates:write was unassigned to every role
- [x] next-auth bumped 4.24.14 to 4.24.15 and next 15.5.19 to 15.5.23, clearing the one critical advisory in the production dependency tree (Aug 22) — that surface had never been checked, and the critical was the auth layer itself. npm audit on production deps went from 16 vulnerabilities (5 moderate, 10 high, 1 critical) to 15 (5 moderate, 10 high, and critical ZERO, re-measured at commit time). Both were patch bumps inside the existing caret ranges, so no v5 migration and no application code changed. THE CRITICAL RATING OVERSTATED THE REAL EXPOSURE: the homoglyph at-sign bypass targets the Email provider's normalizer and this app is Google OAuth only, so the address comes from Google's verified profile rather than user input, and the unbound OAuth cookies need more than one provider configured. The reachable one was getToken throwing on a malformed Bearer header, since middleware.ts calls getToken on every protected request and that header is attacker-controlled - confirmed at commit time that it really does - and it fails closed with a 500 rather than granting access, so availability rather than a bypass. ONE CORRECTION to the handoff, which said neither package appears in the report any more: next-auth is gone, but next is still listed - NOT for any advisory of its own, whose three are cleared, but as a transitive carrier of postcss and sharp. Clearing that entry needs next 16.3.2, a MAJOR bump. NOT VERIFIED and worth stating: the getToken fix rests on version metadata only. A probe returned clean on 4.24.15 but behaved identically against the vulnerable 4.24.14, so it never reached the vulnerable path and proved nothing; it was deleted rather than left looking like evidence. And no real Google sign-in was exercised, because local dev bypasses auth and holds no Google credentials - so somebody signing in once after the deploy is the check that matters on a patch bump to the auth layer
- [ ] Fifteen advisories remain in the production dependency tree, all transitive and none in a request path (found Aug 22) — Prisma tooling (hono, @hono/node-server, valibot, deepmerge-ts via @prisma/config and @prisma/dev), build tooling (postcss, brace-expansion, nanoid, fast-uri), plus sharp, mermaid and dompurify. next appears in that list too, but only as a carrier of postcss and sharp rather than for anything of its own. Lower priority than the auth-layer one just cleared, but worth a pass - and note the next entry cannot be cleared without a major version bump
- [x] Closed the one unauthenticated data-returning route found in the frontend audit (Aug 22) — /api/contacts/vcard served the full curated staff directory to any unauthenticated request: names, titles, phone numbers and email addresses. It and /welcome now require a share token. The other eight routes outside the auth wall were already deliberate and individually guarded, which is why this is one entry rather than a sweep - the audit found exactly one hole, not a pattern. See the People Ops entry for the token design and the breaking consequence
- [x] An invite link that can actually be sent (Aug 20, from feedback Jun 18) — invites already configured access against an email address, but there was nothing to hand the person; they had to be told to find the site and sign in. Each pending invite now has Copy invite link, producing a login URL carrying their address, which is forwarded to Google as login_hint so somebody sitting in a personal account lands on the right one instead of the chooser and a refusal. The login page names who the invite is for. THE ADDRESS IS A HINT ONLY and grants nothing: auth.ts and the invite record still decide entry, unchanged
- [x] Per-user candidate allowlist (Aug 20) — an admin can create a hiring manager who sees ONLY hand-picked candidates, plus their history, files and interviews, and nothing else candidate-side. Settings, Team Members, Add a team member. Enforced server-side at one chokepoint: getCandidateProfileData now REQUIRES a viewer rather than taking one optionally, and an off-allowlist candidate returns null, which the page and the API already turn into a 404 - so pasting a profile URL cannot even confirm that person exists. Off for every existing account, so nothing changed for anyone on ship. Verified at commit time against the live database rather than from the handoff: allowlist-restricted 0 of 5 accounts, module-overridden 0 of 5, UserCandidateAccess rows 0, with rpaden's restrictCandidatesToDepartment reading true as the positive control that the query returns true where true exists
- [x] Per-user module access (Aug 20) — turn individual areas on and off for ONE account without touching anyone else with the same role. The workspace Module Visibility policy is per-role and could not do this. Enforced on pages AND on API routes and server actions, so a hidden area cannot be reached by a saved link
- [x] Invite a team member before they have ever signed in (Aug 20) — their role, areas and candidate list apply automatically on first Google sign-in. Needed because a User row CANNOT be pre-created: allowDangerousEmailAccountLinking is false, so NextAuth refuses to link a Google account to an existing row and the person is locked out rather than prepared. This is a permanent constraint of the sign-in setup, not a workaround
- [x] Scoped write path (Aug 20) — a restricted hiring manager may add interview notes and scorecards on their own candidates only, and edit only write-ups attributed to them. No candidates:write, so the record, tags, stage and files stay read-only
- [x] The schema was applied to the live database BEFORE the code shipped, deliberately (Aug 20) — the new code reads three new User columns on every non-admin request, so the database had to lead the deploy or the first request after it would fail. Additive only: 3 ADD COLUMN all defaulted or nullable, plus CREATE TABLE UserCandidateAccess and UserInvite with their indexes and foreign keys. No DROP and no data written. Re-verified at commit time from information_schema rather than through the Prisma client, so this is the database and not just a generated type: all 3 columns present with defaults, both tables present
- [x] Offers page was serving every candidate name, email and offer decline reason to anyone with the candidates module (found and fixed Aug 20) — it rides on that module and passed no viewer at all. Now scoped. Found by an adversarial pass AFTER the feature was built and building cleanly, which is the entire argument for running one: a clean build and a green typecheck say nothing about who can see what
- [x] Mention emails leaked candidate names outside the app (found and fixed Aug 20) — notifyMentions sent the candidate name, job title and a deep link through Front to ANY address typed into a write-up, with no check that the recipient could see that candidate and no requirement that the address belonged to a user at all. Now filtered
- [x] The department restriction never applied to Compare or to saved views (found and fixed Aug 20) — a department-scoped hiring manager was narrowed on the candidates list and then handed the whole roster on the Compare tab. Both now share one departmentScopeWhere helper
- [ ] THE RESTRICTION ITSELF HAS NEVER BEEN EXECUTED (Aug 20) — and this is the honest state of the feature rather than a caveat to bury. Local dev bypasses auth to ADMIN with a null user id, and resolveViewerScope short-circuits that to a fully unrestricted scope, so a userId-keyed allowlist is inert on localhost BY CONSTRUCTION. Everything that was exercised - render checks on ten pages, tsc, lint, build - runs the UNRESTRICTED path, which is the regression risk for the five existing accounts and not the new behaviour. Confirming the restriction needs a real second Google account in production. Turn it on for a test account first, check what that account can actually reach, and only then the real person
- [x] The two ungated write routes are closed (found and fixed Aug 20) — both handlers imported no auth helper at all, so the only thing in front of them was middleware proving that SOME valid JWT existed. Any signed-in account, including a VIEWER or a hiring manager scoped to two candidates, could trigger a full-pool duplicate scan that writes review rows into the shared live database, or attach and reorder content blocks on any job post. api/duplicate-review/candidates/scan now requires duplicates:write, matching its sibling resolve route which has always required it - an inconsistency rather than a deliberate exception - and api/jobs/[id]/blocks requires jobs:write, matching every neighbouring publishing route. Both are ADMIN and RECRUITER, so nobody who could legitimately use these screens loses access. The scan also logs WHO ran it now, because it is an expensive pool-wide write reachable from a button and that is what makes a surprise batch of review items answerable afterwards. Neither route ever leaked anything - the scan returns counts and the blocks routes return the job - so the defect was the unauthorized WRITE, not disclosure. Verified at commit time that the gate is the FIRST statement in the scan handler, ahead of the scan call, so an unauthorized caller cannot reach the write. THE DENIAL PATH IS UNPROVEN: local dev bypasses auth to ADMIN, which holds both permissions, so a 403 cannot be produced on localhost and the refusal rests on reading the role table rather than watching it happen. SWEEP as a positive control rather than an assertion that nothing is left, re-run independently across all 150 route.ts files under app/api: the only handlers with no auth helper are NextAuth's own route, the two public booking endpoints and the public vCard route, all deliberate
- [ ] Five server-action files still gate on their own session helpers instead of requireApiPermission, so the per-user module gate does not reach them (found Aug 20) — travel, people, orientation and both compliments files. createRecognition and redeemReward have no authorization check beyond being signed in, and they MUTATE - a points balance and the rewards ledger - so this is the same defect class as the two routes closed above rather than a lesser one. Worth closing before per-user module access is relied on as a boundary rather than a conveniencex.
- [x] Closed two genuinely unauthenticated GET handlers (Aug 3) — onboarding milestones, and orientation email templates, which returned every template subject and body to anyone with no session. Both now require a read permission. Verified with a positive control rather than a bare absence claim: the middleware allowlist was read in full, /api/orientation/templates is covered by the /api/orientation prefix since isProtectedPath matches a prefix followed by a slash, and the same two files already guarded their own mutating handlers
- [x] Added the missing role checks to eleven mutating handlers that had none while their siblings did (Aug 3) — bulk job status, job update, pilot requirement update, interview create, and every block and job-block-instance write. VIEWER is the default role for a new user and is read-only by design, so these defeated it. Each now uses the same permission as its guarded sibling, prepended ahead of the existing try block, with no success path or response shape changed
- [x] The auth blocklist still fails OPEN on corrupt JSON by deliberate choice (Aug 3) — failing closed on the sign-in path would lock the whole company out to keep out a few people. It logs the scope, key and stored value on both branches now instead of silently returning an empty list, so a broken blocklist gets noticed. The behaviour is the same; only its silence changed
- [~] Department & tag-based hiring-manager scoping (Jul 29) — worked out with the user over a design conversation, then built. SHIPPED: a 4th module-access tier, Edit (between View and Full — normal changes allowed, delete/merge/bulk-delete blocked), enforced today on Candidates and Employees write/delete routes only (other modules still all-or-nothing via the static role table). Employees are scoped to a hiring manager's own department (crew/maintenance/fbo/support, matched via the calendar's resolveDepartmentKey — fixed a real bug there where "FlightOps" with no space fell through to Support instead of Crew) minus anyone tagged Management or Executive; an Executive flag overrides to see every employee except other executives. Candidates stay org-wide by name for every hiring manager by default — department restriction is a per-person exception (restrictCandidatesToDepartment on User), not the rule. Interview notes and candidate notes are the one private field: visible to whoever wrote it, plus any hiring manager whose own department matches that candidate's (derived from their job applications), plus executives — masked server-side before the page/API response is built, not just hidden in the UI. Admin/Recruiter are never narrowed by any of this. New User columns: department, isExecutive, restrictCandidatesToDepartment — set per hiring manager on Settings > Team Members. HONEST CAVEATS: no link exists between a User login and a NewHire roster row for the same person, so department/executive status is hand-set on the User record rather than derived from the employee roster's own tags — same "hand-applied" spirit as NewHire.tags already was. Edit-tier delete-blocking is wired only for Candidates/Employees, not the other 18 modules. The PATCH-response path on /api/candidates/[id] returns the just-edited candidate unmasked (narrow: only reachable by someone who already had edit access to that exact record). Not interaction-tested as a real HIRING_MANAGER session — local dev bypasses auth entirely (treats every request as ADMIN), so this needs a real login in a REQUIRE_AUTH=true environment to exercise end-to-end; verified instead by reading the query/masking logic directly plus tsc + lint.

## Document Intelligence
Making candidate documents searchable and structured.
- [x] Bulk metric scan across every active candidate holding document text (Aug26) — candidates carrying metrics went from 78 to 425 and the metric table from 10,550 rows to 14,675, for 1.97 USD of API spend on Haiku 4.5. HIS DECISION, recorded: scan every active candidate rather than the one job, on that model, after seeing the per-model costs. Re-read from the live database at commit time: 449 ACTIVE candidates hold extracted text, 425 of them carry metrics and 24 carry none. THE 24 ARE NOT A BUG and should not be chased - they are mostly AMTs and cabin attendants, non-pilots with no flight hours, for whom zero metrics is the correct answer. The count reads 14,675 here against the 14,673 in the handoff, which is two rows of drift on a live table rather than a discrepancy
- [x] The Gulfstream G450 and GV Captain role went from 7 of 30 active applicants carrying metrics to 27 of 30 (Aug26) — the reason they were missing was never missing documents, it was that nobody had pressed Scan docs on them
- [x] Intake forms are read by LAYOUT now rather than flattened text (Aug26) — the coordinate parser is wired into metric extraction and its values take precedence over model-read ones, recovering 49 candidates whose hours were present but fused into an unreadable run of digits. The parser's field keys were already CandidateMetric keys; only the bridge was missing, so this cost nothing to build and costs nothing to run
- [x] A free layout-only re-pass REPLACED 1,873 stored metric values and added 1,364 more, at zero tokens (Aug26) — and the replacements are the point, because they were not gaps, they were WRONG NUMBERS read off fused text. Verified against the live database rather than taken on trust: Gregory Tinsley was stored as a 1,341 hour pilot and his signed application says 18,718, which the row now carries with the snippet FLIGHT TIME: 18,718 ENGINE FLIGHT TIME: 18,718. The flattened text had run 1,341 together with his medical expiry date, so the model bound it to the nearest label - and 1,341 is really his hours in the aircraft applied for. He had been sitting below the minimums of every role he applied to. STANDING INSTRUCTION FROM HIM: re-run the free forms-only pass whenever a form template improves, because it costs nothing and it corrects silently-wrong numbers already stored
- [x] Fixed a silent corruption path in the metric extractor (Aug26) — Haiku 4.5 rejects the effort parameter with a 400, and because every caller treats a failed extraction as fall back to regex, the 400 never surfaced: choosing that model quietly wrote regex-derived metrics, from the extractor whose own comment records reading PART 91 as 91 instrument hours for a third of the people it touched. A guard omits the parameter on Haiku 4.5 and Sonnet 4.5. This affected the Scan docs BUTTON, not only the new script
- [x] Text extraction strips null bytes before storing (Aug26) — Postgres rejects U+0000 in a text column, so a single one anywhere in a document failed the whole UPDATE and left the file holding no text, which is indistinguishable from an image-only scan. The whitespace collapse never caught them because \s does not match \0. Verified live: Tavin Newell went from 1 of 2 files carrying text to 2 of 2. A small proof of the same point arrived by accident while checking - a query searching for surviving null bytes was itself refused by Postgres with null character not permitted, so the database will not accept one at any layer
- [ ] Jeremy McGraw's Document (2).pdf is corrupt, invalid header in flate stream, and fails both text extraction and form parsing (found Aug26) — needs re-uploading
- [ ] Sixteen of 4,708 files with extracted text have the letter s dropped by the PDF text layer (found Aug26) — Vi ta Jet, Saint Jo eph. Small, but it silently breaks keyword search on those files, which is the kind of gap nobody reports because the search simply returns nothing. THE COUNT IS THE ORIGINATING SESSION'S, NOT MINE, and it is carried rather than confirmed: my own attempt to reproduce it searched for the two quoted samples literally and case-sensitively and returned zero, which is a narrower query missing rather than a refutation. The 4,708 denominator I did verify. That session also recorded correcting itself here, and the correction is the useful part - its first sizing used a Postgres word-boundary anchor before a space, which can never match, and so returned 0 everywhere and looked like a clean bill of health
- [ ] THE PAYCOM APPLICATION IMPORT CREATED DUPLICATE CANDIDATE RECORDS, and they are the ones that look like they need OCR (found Aug26 at commit time) — every candidate flagged as having no readable document turns out to be a PAYCOM-origin row created on Aug 26 sitting beside an older record for the same person that already holds documents, extracted text and metrics. Measured, one line per row: Ronald Williamson PAYCOM created Aug 26, 1 file, 0 with text, 0 metrics, beside Ron Williamson JAZZ from Jun 2024 with 15 metrics; Thomas Pawlik PAYCOM Aug 26 with 0 metrics beside Thomas Pawlik JAZZ from Dec 2024 with 9 files and 8 metrics; Walter Sorenson PAYCOM Aug 26 with 0 metrics beside Walter Sorenson MANUAL from Jul 2026 with 17 metrics; and Christopher Sharpe PAYCOM Aug 26 beside Chris Sharpe MANUAL from Jul 2026, both carrying 11 metrics. THIS CHANGES WHAT SOMEBODY DOES TODAY and is the reason it is written up rather than filed: those people do not need OCR or a vision call, which is what they would otherwise be bought. Their documents already exist and are already read - on the other record. The action is to merge the pairs, and the merge route now carries card-order lines correctly as of the same week. Not merged here, because merging two candidate records is irreversible and is his call
- [x] The 88-page Paycom application bundle is split into 19 per-candidate PDFs (Aug26) — named First Last Paycom Application, with boundaries taken from the Application Information plus Application ID page marker. RE-VERIFIED INDEPENDENTLY at commit time rather than accepted, because a split that loses or duplicates a page is invisible afterwards: all 19 outputs were re-opened and their page counts summed to 88 against a source of 88, running from 4 to 6 pages each. The original bundle is deliberately left in place beside the splits rather than deleted. NOT VERIFIED, and the session that did it said so: whether the new files have finished syncing to Drive server-side
- [x] The Paycom Application ID is the same key as the (NNNNNN) resume prefix (Aug26) — all 19 matched one to one, so a candidate's resume and application now carry the same name on both files. THIS READS LIKE IT CONTRADICTS THE ENTRY BELOW ABOUT THE PREFIX BEING UNRELIABLE AND IT DOES NOT, which is worth stating because the next reader will hit both: the prefix is a genuine Paycom identifier, and our own database simply does not store it. Measured here, on these exact 19 people: all 19 exist as Candidate rows and exactly ONE of them carries a paycomPersonId, Travis May at 361088. So THE NAME IS THE ONLY KEY THAT ACTUALLY LINKS ANYTHING TODAY, and the two rename passes have now put the same name on both sides, which is the practical outcome rather than the ID match
- [ ] Three of the nineteen applicants return TWO candidate rows each (found Aug26 while linking) — Christopher Sharpe, Thomas Pawlik and Walter Sorenson. Not asserted as duplicates: the lookup matched an exact surname against the first four characters of the first name, so two rows can be one person twice or two different people. Worth a look precisely because these three are the ones whose names were also being decided by hand in the rename passes, so a duplicate here is the case most likely to end up with one document filed against each copy
- [x] CONFIRMED Aug26: he is Christopher Sharpe, so both files stay as they are and nothing needs renaming. Paycom lists him as Sharpe, Chris David while his resume says Christopher Sharpe (Aug26) — recorded as pending rather than as a decision, unlike the Sorenson Jr. and Juan Carlos Sierra calls in the entry below, which he confirmed. If he confirms Christopher, this closes with no work; if he wants the Paycom spelling, two files need renaming
- [x] Nineteen Paycom-era resumes in the Drive folder renamed to First Last Resume (Aug26) — taken from the name printed on each document rather than from the filename, and 12 files that were already correct were verified and left alone. NOT VERIFIED, and the session that did it said so: whether the renames have finished syncing to Drive on the server side. RECORDED DECISIONS on three files, so they are not re-raised: (302238)-Resume.pdf.pdf is a signature-only Keynote page rather than a resume and its id matches no candidate - LEAVE IT AS IS, do not delete it; Walter Sorenson is filed without the Jr. suffix he writes on the resume, confirmed correct; and Juan Carlos Sierra keeps the compound first name rather than being filed as Juan Sierra, also confirmed
- [x] Scanned, text-free resume PDFs are readable on this machine without OCR (Aug26) — decode the page image with pdfjs-dist plus sharp, both already installed, and read the PNG. This reaches CCITTFax and Flate scans that raw JPEG carving cannot, which is the class of file that previously had to be opened by hand
- [ ] The (NNNNNN) filename prefix cannot identify a resume, because paycomPersonId is almost never set (Aug26) — 47 of 3638 candidates carry one, which is 1.3 per cent, and all 20 prefixes checked in this batch matched nothing. RE-MEASURED AT COMMIT TIME rather than copied, and one figure had already moved: the handoff counted 3626 candidates and there are now 3638, while the 47 is exact. The positive control is what makes the misses real rather than a bad query - the same lookup returns Connor Morgan 305282, Tara Ward 306750 and Paxton Boyce 303543, and all 47 populated ids were listed, spanning 199941 to 363019. Separately confirmed for the file side: no CandidateFile carries 302238 in either filename column, against a control of 2106 and 2138 rows whose names do contain Resume
- [x] Matchboard: scan the WHOLE pool, and a skip list you control (Jul 27) — THE MATCHER NOW SEES 3,343 PEOPLE INSTEAD OF 200. The pool filter moved from status ACTIVE to everyone except MERGED duplicates, so the 3,143 archived Jazz records are scanned for the first time; the old take-250-by-updatedAt cap is gone too, since with the archive included it would have hidden almost all of it. Measured cost of a whole-pool scan: about 1.4s of query and 1.3s of scoring. TWO THINGS HAD TO CHANGE FOR THIS TO BE USEFUL RATHER THAN NOISY, both confirmed with the user first. (1) SPLIT LANES, not one ranking: archived people took 10 of the top 12 on a real G450 First Officer scan, so a single merged list would have pushed the live pipeline off the board. Current pool keeps the readiness tiers; the archive is its own collapsible lane underneath with its own budget of 12. (2) RESUME TEXT NOW FEEDS SCORING: archived records have no title, no tags and structured hours on only about 1 percent, so they were being scored on a filename like Jane Doe Document.pdf. The extracted document text that full-text search already uses is now part of the scoring haystack (capped at 12k chars per file), which is what lets an archived pilot show meets 1 of 3 hour minimums instead of nothing. ALSO FIXED A RANKING BUG THIS EXPOSED: scores tie in huge blocks at pool scale (107 people shared the 12th-place score on G450 First Officer, 434 on Phenom 300 Captain) and ties fell through to alphabetical, making the cut an arbitrary A-names sample. Ties now break on strength of evidence — hour minimums met, then how many of the role's factors are established either way rather than unknown — which is a property of the match, not of the person. THE FOUR SKIP-LIST GAPS ARE ALL CLOSED: (1) the skip control is on the Matchboard candidate view, where the thought actually happens; (2) a Skipped tab lists everyone held out with reason, note, who and when, and puts anyone back in one click (restore is the same write, cleared — no separate undo path to drift); (3) Not eligible to hire is its own reason for visa / work-authorization / legal bars, flagged as a standing bar rather than a judgement call, and no migration was needed since the column is a plain string; (4) if a skipped person reapplies as a new record, a Seen before banner surfaces the old decision and explicitly does NOT re-exclude them — email match is certain, exact name likely, surname-plus-shortened-first-name possible. HONEST CAVEATS. The nickname tier was tightened after real data caught it out: agreeing on three letters matched Chareese Hicks to Charles Hicks (different people) for every genuine Alex/Alexander it found, so one first name must now be a prefix of the other; across the whole database that leaves exactly 1 re-apply hit, correctly Alex/Alexander Andrade. Archived candidates are still scored largely on text, so treat them as worth a look rather than a like-for-like comparison — every hour gate reads unknown, and unknown is not the same as failed (2,739 of 2,799 kept results come back qualified, so the gates barely discriminate at this scale). The candidate picker now carries 3,407 entries, taking the Matchboard page to about 979 KB raw / 245 KB gzipped and forcing a 200-row render cap with search still running over everyone. Verified over HTTP only: all modes return 200 with the right content and the dev server logs no errors, but per the standing note in CLAUDE.md the Browser pane cannot read this app's rendered DOM, so the click paths (skipping, restoring, expanding the archive lane) are NOT interaction-tested. Nothing was written to the database by this work.
- [x] Completed pilot applications file themselves from Front (Jul 27) — BACKFILL COMPLETE: all 412 historical Adobe Sign notices are now filed, up from 300. The pilot application is signed in Adobe Sign, which cc's pilotapp@skyshare.com on the completion notice with the signed PDF attached; the scanner reads those notices, matches the candidate, downloads the PDF and files it as a Pilot Application document. THE BEST IDENTIFIER TURNED OUT NOT TO BE THE NAME: the signer's own email is a recipient on every notice, so matching goes email first, then normalized full name, then a narrow surname-plus-first-three-letters tier; two candidates at any tier is reported ambiguous and skipped rather than guessed at. FINAL RUN (Jul 27): HARD_MAX_CONVERSATIONS raised 300 -> 800, because the archive holds 412 notices and the old cap stopped partway through leaving the tail permanently unreachable. Run staged rather than all at once — a dry run over all 412 first, then an apply capped at 330 to prove 30 threads, verified, then the remainder. Outcome: 412 of 412 filed, 112 newly attached, 63 candidates created from applications that had no record at all, ZERO ambiguous and ZERO failures, and the numbers matched the dry run's prediction exactly. Idempotency verified after the fact: a fresh scan now reports 412 already-attached and writes nothing, so the nightly cron and any webhook double-fire are safe. 233 candidates now exist that were created from a signed application; every one has its document attached and there are ZERO email collisions with existing records, so nobody was duplicated. ALSO FIXED: the report counted archived and commented straight off the attached count without knowing that backfill mode suppresses both, so a run that archived nothing reported 30 archived. UI BUTTON SHIPPED (Jul 27, commit 36f0de3): the Paycom-mail button on the New hires page became "Check Front mail" and now runs BOTH sweeps — background-check notices AND completed pilot applications — so nobody has to remember which to click; the two scans stay separate underneath and settle independently, so a failure in one still lets the other report. The modal leads with what could NOT be filed (a filed application is self-evident on the candidate; one with no match is sitting in Front waiting for a person) and names any variant it resolved ("signed as Hilary Luke W. Cholhan") so a silent fuzzy match cannot hide. Filed apps are tagged [Automated] + Pilot App + "Manually Added to ATS" (the team's real ATS marker, read from the account not guessed); unfiled get Pilot App + Needs Review. A backfill mode (?backfill=1) reaches threads already handled by hand for the historical pass, staying quiet — no comments, no re-archiving — but still tagging so they stay findable. SUPERSEDED IN PART Jul 30: the scanner no longer archives anything at all — see the next item.
- [x] Pilot-application threads stay OPEN — no auto-archive (Jul 30, requested) — the scanner filed the PDF onto the candidate and then archived the Front thread, which was wrong by half a step: Hannah still has to add each application to Paycom by hand, and an archived thread took it out of sight before she had done it. archiveConversation is gone from lib/pilotapp/scan.ts entirely; every thread is now left open, filed or not. Everything else is unchanged — the tags still go on ([Automated] + Pilot App + Manually Added to ATS on a success, Needs Review on a miss), and the internal note now says the thread is being left open for the Paycom step instead of saying it is being archived. The Manually Added to ATS tag now means only that the PDF is on the candidate here, NOT that it reached Paycom; the open thread is the queue for that. Repeat runs stay silent and safe because the already-filed check keys on the Front message id, not on the thread being archived, so a thread the cron re-sees every night costs one lookup and never a duplicate document or a duplicate comment. The archived count is dropped from the scan report, the cron log and the Check Front mail modal, which now tells the user plainly that the threads are open on purpose and to archive them once Paycom is done. WATCH THIS: the nightly sweep reads is:open and stops at 40 threads, and archiving used to be what kept that set to a handful. If filed threads pile up unarchived past 40 the sweep may stop reaching the tail — raise DEFAULT_MAX_CONVERSATIONS or narrow the query if that starts happening. Verified by typecheck and lint only; not exercised against the live inbox.
- [x] The nightly pilot-app sweep now CREATES the candidate instead of leaving a dead-end note (Aug 7, asked for by the user) — the cron called the scan without createMissing, so an applicant nobody matched got a Front comment asking a human to add the person and re-run, and the signed PDF was never downloaded at all. The user's reason for changing it: that note was worthless in practice, since it made follow-up work for somebody and left the signed application unfiled in the meantime, and he would rather the person be created from their own pilot application. The capability already existed and was deliberately opt-in; this turns it on for the unattended run and NAMES every candidate created in both the log line and the JSON response, because a job that adds people to a shared database with nobody watching has to say who. UNCHANGED AND LOAD-BEARING: creation fires only on a CLEAN no-match and only when the application yields both a name and an email, an ambiguous match still refuses to guess, and nothing is archived
- [x] Front threads are tagged Candidate Created by App when the run creates the person (Aug 7, the user made the tag) — applied ALONGSIDE Manually Added to ATS rather than replacing it, since the document really was filed AND the person really did not exist beforehand, so both are true and both are worth finding on. VERIFIED read-only against the live Front account: 54 tags visible and all five names the scan uses resolve, the new one included. That check mattered more than it looks — an unresolvable tag name is collected into missingTags and the run CONTINUES by design, so a missing label never costs the filing, which means a typo here would have failed silently rather than loudly
- [x] Fixed a false statement in the Front comment (Aug 7) — a created candidate has no matchedBy, so the note fell through to the default branch and would have claimed it had matched on the name somebody who did not exist moments earlier. That is the single line a recruiter reads to decide whether to trust the filing. It now reads that a new candidate was created from the application
- [x] THE PILOT-APP CREATION PATH RAN FOR REAL, AND IT WORKED (Aug 7) — the user pressed Check Front mail at 10:01 Mountain and the run did exactly what the dry run predicted: 46 threads scanned, 20 filed, 14 candidates created, summary emailed, no missing tags and no email error. The run log records it as trigger manual with his address as the actor, which is the whole point of recording the trigger — the same work through two doors, and the log says which one. Verified against the database rather than the response: 14 candidates carry the pilot-application source with creation timestamps inside 71 seconds of each other, and the all-time count moved 232 to 246. The 14 are Robert Topolovec, Logan Andrade, Justin Farrer, Zachary Welborn, Jake Hammond, Gregory Tinsley, Preston Carroll, Felix Flores III, Bruno Viana, Ajuma olonta, Michael Fobert, Hunter Wilson, Joaquin Garcia and Augustus Hickman
- [x] The app stops applying Manually Added to ATS, and 21 of its own were removed (Aug 7, the user's instruction, reversing Jul 27) — that tag means a PERSON put the application into Paycom. The scanner set it on every successful filing, so the one marker telling Hannah what was still outstanding was being ticked for her by a job that cannot do the thing it claims. Removed from both tag calls; the constant stays in the file with a comment saying never to restore it. THE RETRO-FIX ESTABLISHED PROVENANCE RATHER THAN GUESSING, and that is what made it safe: Front's conversation events name the actor behind every tag as api, teammate or rule, so of 443 threads carrying it, 422 were a TEAMMATE's and were left completely alone while 21 were the app's and were removed. The obvious heuristic — it has [Automated], so the app tagged it — would have deleted 422 of Hannah's own markers. Reading the field correctly mattered too: the actor is at source._meta.type, not source.type, and the first pass read the wrong one and reported a confident zero
- [x] Every Jazz-era pilot application the app created is now tagged Candidate Created by App (Aug 7) — 259 threads gained it. The tag did not exist until Aug 7, so the entire Jul 27 backfill was unmarked and there was no way to tell from Front which candidates came from a person and which from a job. Applied at a limit of 5 first and re-measured before the remaining 254. FINAL STATE COUNTED, NOT ASSUMED: all 273 app-created candidates carry the tag, all 443 threads carry [Automated], and exactly 422 still carry the ATS tag. Reversible from the plan file with --undo
- [x] Two Front paths were touching threads without saying so (found and fixed Aug 7) — the Paycom scanner left [Automated] off its needs-review branch, so the threads the automation understood LEAST were the ones carrying no sign it had been involved. Worse, the travel importer never applied it at all: its tag entry listed Travel Imported then Automated, and NEITHER exists in the account, where the real tag is [Automated] with brackets. An unresolvable tag is dropped silently by design, so travel threads the app read and filed carried nothing at all. Its helper now adds [Automated] itself rather than trusting each call site to remember, which is how the needs-review path came to be bare in the first place. STILL OPEN: the mention-reply handler leaves a comment on a thread and applies no tag, and the Paycom scanner can archive a thread on an outcome that adds no tag
- [x] The dry run predicted the real run exactly, and the preview tooling is committed (Aug 7) — before anything was written, a read-only pass over the live Front account said 46 threads, 26 already filed, 6 newly filed and 14 people created. The real run returned 46, 20 filed and 14 created, which is the same numbers with the 6 and the 14 both landing as filings. Reproduce with npx tsx scripts/pilot-app-dry-run.ts. THE SCRIPT EXISTS BECAUSE THE OBVIOUS INSTRUCTION WAS WRONG TWICE: the equivalent HTTP route needs a LOGGED-IN BROWSER SESSION, since requireApiPermission reads the NextAuth session and curl gets a flat 401 that reads like a broken feature — and createMissing is a NO-OP on a dry run, because the scanner returns before its write block, so passing it reports nothing at all. The script needs no browser and rebuilds the would-create list from the same three conditions the real creation gate uses. ALSO WORTH KEEPING: the Aug 7 morning cron did NOT do this work, because it fires at 07:30 Mountain and the code was not deployed until roughly 08:49 — the timing, not a fault, and it was diagnosed against a positive control rather than from the empty log alone
- [x] The nightly sweep sees ARCHIVED threads too, not just open ones (Aug 7, asked for by the user) — the query was is:open, which stopped being right the day the scanner stopped archiving. Hannah archives a thread once the application is in Paycom, which is the correct end of her workflow, and an is:open sweep can NEVER see that thread again — so anything she archived before the scan reached it was missed permanently and silently. Dropping the state filter alone would have been worse: the archive holds 400-odd notices, so an unbounded all-states query re-walks the whole history nightly and hits the conversation cap on the OLDEST threads, never reaching today's. A rolling 30-day window is what makes all-states affordable, bounding the set by TIME rather than by state, so newly archived threads stay visible for a month and old ones fall out on their own. The cap moves 40 to 200 to match what the wider query returns, still far under the hard 800. THIS RETIRES THE WATCH THIS WARNING left on the Jul 30 entry above, which predicted exactly this problem. The same window IS the 30-day catch-up that was asked for separately — there is no second run to schedule, the first firing does it
- [x] The Check Front mail button and the nightly cron now behave identically (Aug 7) — the button called the scan WITHOUT createMissing while the cron passed it, so clicking it and waiting for 7:30am produced different outcomes on the same thread: the button left a could-not-find-the-candidate note that the cron would then resolve by creating them. One flag fixed the divergence. ALSO FIXED IN THE SAME PASS, and it was the more dangerous half: the modal showed created people mixed into Filed with no sign anyone had been ADDED, so a click could put fourteen people into the shared live database and report only that twenty applications were filed. They are now named ahead of the filed list, with what the record actually holds — the name and email off the application and nothing else — and the Front tag to find them by
- [x] Every writing run is recorded and reported, whichever path ran it (Aug 7) — runs go to a pilotapp/scan-runs WorkspaceSetting, mirroring the orientation reminder's log, because the quiet nights are the point: it never fired is otherwise indistinguishable from it fired and found nothing, which is the exact ambiguity that produced the August false alarm about the orientation cron. Crashed runs are recorded too, since from outside that state looks identical to never scheduled. Each record carries WHICH trigger ran it and, for a manual run, WHO clicked. Recording and reporting live in ONE function called by both routes rather than duplicated — the failure mode of that drift is a person in the database with no record of what put them there. Dry runs are deliberately NOT recorded: they change nothing, and a log full of previews would bury the runs that created somebody
- [x] A daily list of who the scan added, emailed to hrotasks@ (Aug 7, the user chose the destination) — it reports everyone created, and STAYS SILENT when there is nothing, because a nightly nobody-today is reassurance mail that stops being read within a week and a mail nobody reads cannot raise an alarm. The send guard already lists hrotasks@ as an automation endpoint rather than a person — its own comment names the daily report as the reason it exists — so this is the one class of mail allowed to leave a non-production run undiverted. IT FOLLOWS THE PEOPLE, NOT THE SCHEDULE: it was cron-only at first, which meant creating candidates from the button produced no list anywhere, which is the very thing that was asked for. It also names any Front tag that could not be resolved, which otherwise fails silently — an unresolved tag does not stop the filing, so the labelling just quietly stops
- [ ] The pilot-app cron has THREE silent exits that leave no run record (found Aug 7) — a missing CRON_SECRET, a wrong Authorization header and a missing FRONT_API_TOKEN all return BEFORE the try block, so none of them writes to the run log. That is the exact ambiguity the log was added to remove: from outside, all three look identical to the cron never having been scheduled. It cost real time on Aug 7, when the absence of a record had to be diagnosed by elimination — checking the other cron, then the Vercel environment list, then the commit timestamps — where one recorded line would have said so outright. Moving the guards inside the recorded path, or writing a refused-run record before returning, would close it
- [ ] Residual duplicate risk in auto-creation (Aug 7) — the ambiguous-match guard only helps when the matcher gets close enough to SEE a conflict. An applicant writing in from an email we do not hold, whose name the matcher does not connect to their existing record, reads as a CLEAN no-match and gets a second person created. Everything created carries source Pilot application (Adobe Sign) and now a Front tag as well, so both sides have a handle for a periodic duplicate sweep — which nobody has written yet
- [ ] Auto-created candidates land as UNASSIGNED in the department filter (found Aug 7 while committing this) — creation writes a Candidate and a contact but no application, and no departmentOverride, so the person derives to unassigned. That matters more from today than it would have yesterday: the 305 live unassigned candidates were all placed this morning and the count is currently zero, so this is the path that refills it, one applicant at a time, starting with the next nightly run. THE ANSWER IS ALREADY IN THE CODEBASE — proposeDepartment returns flight-ops for anyone with a Pilot Application on file, and that is definitionally true of a record created FROM a signed pilot application, so the creation path could set the override itself rather than leaving each one for the review screen. Not changed here, because it is another session's code and this was a commit pass
- [ ] Auto-created candidates carry origin MANUAL (Aug 7) — in the database they are indistinguishable from hand-entered people except by the source field. Worth deciding whether they deserve their own origin, the way JAZZ separates the historical import
- [x] Fix names taken from Adobe Sign signatures (Jul 27) — splitCandidateName kept the FIRST and LAST token of a name, so a generational suffix became the surname and the real one was dropped: Walter Jr, Jeffrey Jr, Mark Jr and Dwayne II. It now peels trailing Jr / Sr / II / III / IV / V / VI off before choosing the surname, keeps the suffix on the DISPLAY name (it is part of how someone is addressed) and never on lastName, which is what search and matching use. Guarded so it can never strip past two remaining tokens, otherwise a genuine one-word surname like V would be eaten. 17 regression cases cover the fix, the ordinary names, the comma form the CSV importers hand in, the guard and empty input. THE 8 EXISTING RECORDS WERE REPAIRED FROM THEIR OWN SIGNED PDFs: the extracted text looked like form labels only, but the filled values are concatenated after them and Adobe's audit trail states the signer verbatim, so the form NAME field gave Walter Sorenson Jr, Jeffrey Bowdidge Jr, Mark Schelm Jr, Dwayne Gill II, and the four handle-style names resolved to Jared Walton (JW), Christopher Behnam (CBB), Matthew Natal (Mnatal) and Christine Marsan (CLMarsan). Each rename had to be corroborated by the surname appearing in the person's own email address or in the handle they typed, so a bad regex capture could not rename someone into a stranger. Verified afterwards by search: gill now returns Dwayne Gill II alongside Gillespie and Gilliland, and zero of the 233 scan-created records still carry a broken name.
- [x] Paycom threads archive once handled (Jul 22, requested) — the background-check notices were already being tagged and commented on but stayed in the inbox forever. A thread is now archived once it has been handled AND nothing on it still needs a person: "ticked" or "already-done" archives, anything unmatched or unreadable deliberately stays open. Decided across the whole conversation rather than per message, so a thread carrying both a ticked notice and an unreadable one stays open. Archiving is a Conversations write, not a delete — the thread stays searchable and can be reopened — and a failed archive never undoes the checklist update it follows.
- [x] Scan the Paycom application for the candidate Paycom ID + missing info (Jul 20) — the candidate Documents tab often holds the Paycom application, which DOES carry the candidate's 6-digit Paycom PERSON id (confirmed: it sits in the app header right after the job title, e.g. "Aircraft Maintenance Apprentice - NNNNNN Application…"; the JOB requisition id is NOT on the application). Extend the existing "Scan docs" flow (POST /api/candidates/[id]/extract-metrics — which already text-extracts every candidate file and suggests values for human confirmation, never overwriting confirmed data) with a Paycom-application extractor: pull the Paycom person id → suggest Candidate.paycomPersonId (the exact key the Paycom→Front automation needs), plus any BLANK identity/contact fields (legal name, email, phone, address). Best via the planned Claude LLM upgrade (see Extraction LLM upgrade) — regex is brittle for free-form app fields. COVERAGE CAVEAT (measured Jul 20): only 1 of 5 candidates who already have a Paycom id had it in extractable text; 6 of 19 Paycom/app PDFs are image-only scans with no text layer, so full coverage also needs OCR. Feeds paycomPersonId population for the parked Paycom→Front automation. SHIPPED Jul 20: lib/extraction/paycom-application.ts (LLM extractor via the app's existing @anthropic-ai/sdk integration, Haiku, guarded on ANTHROPIC_API_KEY) wired into the Scan docs route (extract-metrics) — detects the Paycom app file, pulls paycomPersonId + name/email/phone, and FILLS ONLY BLANK candidate fields (never overwrites; editable on the profile). Confirmed on real files both DL + print-to-PDF Paycom apps are text PDFs (no OCR needed). Parser unit-tested (id validated 4-8 digits, tolerant of code fences), route verified 200 + graceful skip when the key is unset. FIX Jul 20 (from real use — the id wasn't filling reliably via the LLM-only path). CORRECTION: ANTHROPIC_API_KEY IS set — it lives in .env.local, which Next.js loads at runtime (that's why AI summaries work); the standalone tsx diagnostic scripts only load .env (via Prisma), so they couldn't see it and briefly misled the diagnosis. The Paycom template is cleanly labelled ("Application ID ######", "Email Address", "Name Last, First", "Primary Phone"), so the Paycom ID + fields are now extracted PRIMARILY by deterministic regex (extractPaycomRegex) — no key dependency, more reliable than the LLM for this fixed layout; the LLM stays as a fallback for a non-standard layout (and it IS available). Verified on all 6 real Paycom files (id + email + name + phone extracted from every one). The scan result now also says "Paycom application scanned — nothing new to fill" so the behaviour is visible.
- [x] Full-text document search (Jun 10) — extracts text from PDFs; candidates list searches inside resumes/pilot apps and shows a matching snippet
- [x] In-profile document find (Jun 10) — PDF.js viewer highlights matches in-page, jumps between them, with per-document match badges
- [x] Serverless PDF extraction via unpdf (Jun 11) — fixed text extraction failing on Vercel; self-healing scan re-extracts files missing text
- [x] Flight-data extraction v1 (Jun 11) — Scan docs pulls Total Time, PIC/SIC, Turbine, Multi, Jet (incl. PIC sub-values), Night, Instrument, Cross-Country, type ratings, certificates, medical
- [x] Flight Profile review controls (Jun 11) — suggest/confirm, edit label + value, sticky reject, and manually add a field
- [x] Sortable candidates table (Jun 18) — /candidates → Compare tab: one table comparing flight metrics, type ratings, and certificates across everyone; click-to-sort columns (missing values sort last), name/role search, type-rating + certificate filters, min-total-time filter, column show/hide, and CSV export of the current view; unconfirmed extractions flagged
- [ ] OCR for scanned/image PDFs — so photographed/scanned docs become searchable
- [x] Upgrade extraction to Claude LLM (Jul 27) — DONE, AND THE REGEX TURNED OUT TO BE WORSE THAN ANYONE KNEW. Auditing what the old pattern-matcher would have written across the whole corpus: 318 people would have been given an instrument time of exactly 91, read off the form label PREVIOUS PART 91 EXPERIENCE; 357 values equalled an aircraft type designator (CE-525 read as 525 PIC hours, MU-300 as 300, EMB-120 as 120); 36 people would have had more PIC time than total time, which is impossible. Roughly 700 provably wrong values, and that is only the errors a rule can detect. It was never applied. THE REPLACEMENT reads documents with Claude Opus 5 under a schema-constrained response, and every number comes back with the VERBATIM LINE it was read from, so any value can be audited without opening the PDF. THE PROMPT WAS BUILT FROM THE CORPUS, NOT FROM GUESSES: mining every label that sits next to a number found 156 distinct terms in real use and three traps nobody would have predicted — HOURS IN AIRCRAFT LAST 12 (678 people) where the 12 is months not hours, Twin Engine Under 12,500 (129) where 12,500 is POUNDS, and Total Hours in Last 90 (129) where 90 is days. All three now read as zero hours. RESULT ACROSS 2,747 CANDIDATES: structured total time went from 37 people to 1,254, and a 700-1500 hour search returns 277 people where it used to return 2. Instrument-exactly-91 fell from 318 to 3 and impossible PIC from 36 to 1. Cost 48.90 dollars against a 50 dollar ceiling, using the Batch API at half price with prompt caching that served about 60 percent of input tokens from cache. Zero extraction failures in 2,747 requests. Values land as SUGGESTED, never confirmed, so a recruiter still reviews them; a CONFIRMED or DISMISSED value is never overwritten by any future scan. Residual error rate is roughly 1 to 2 percent, mostly one-field shifts on comma-run resume lines, and it is visible because the evidence line disagrees with the stored number.
- [x] Flight-hour metrics expanded from 12 to 19, and aircraft names normalised (Jul 27) — ADDED at the user's direction: Time in Type, PIC Time in Type, Hours in the last 12 months, Actual Instrument, Simulated Instrument, Hours in Aircraft Applied For, and Single Pilot. TIME IN TYPE WAS THE IMPORTANT ONE: it has always been worth 12 percent of every match score with a 500-hour target, and nothing fed it — it was inferred by a regex hunting for numbers near the aircraft name. It now exists as real data for 544 people. Hours-in-last-12-months drives the Recency category, which had exactly ONE value in the entire database and now has 168. Time in type is stored PER AIRCRAFT (time_in_type:Pilatus PC-12 and so on) because scoring looks it up by airframe, and a single number cannot say which aircraft it refers to. THAT EXPOSED A SECOND FRAGMENTATION PROBLEM: candidates write the airframe however they like, producing 592 distinct designators for 14 fleet types — GV and G-V, GIV and G-IV, PC-12 and PC12 and PILATUS PC-12. A mismatch there reads as unknown and silently forfeits that 12 percent. A normaliser now maps them onto the fleet registry; 1,045 rows were re-keyed and 18 duplicates merged. Two rulings from the DoM are encoded in it: GV time counts toward a G450 requirement, and PC-12 NG/NGX is interchangeable with PC-12. Note the registry lists those as SEPARATE positions, so the normaliser deliberately overrides it — letting the registry self-map re-split the very groups the rulings merge. Non-fleet types (A320, B737, C172) are kept rather than discarded, since they are real evidence of jet and turbine background. ALSO: the per-candidate Scan docs button now runs the LLM extractor instead of the regex, which matters because it used to overwrite good values with bad ones. Two hand-made custom_ metrics were folded into their proper keys. STILL OPEN: the Compare tab only sees 327 of 3,470 candidates (it filters archivedAt) and caps at 1,000 rows, so the min/max range filters per metric are not built yet — that was the original request and is now unblocked by having trustworthy numbers.

## Candidate Evaluation
Tools for assessing candidates — must stay transparent and fair.
- [x] Candidate pros & cons (Jun 18) — a Pros & cons card on the candidate profile: add/remove strength and concern tags (green/amber chips), saved per candidate; recruiter observations, not an automated score (compliance note left in code for the later transparency review)
- [ ] Aircraft class wiki + resume-vs-role experience comparison (added Jul 22) — a recruiter tool the user asked for by name: build a living catalogue of every aircraft anyone lists on a resume OR that we hire for, each classified so that when reading a resume you can see at a glance how the candidate's aircraft experience compares to the seat being filled. THE CLASSES the user wants (an aircraft can carry more than one — a CJ2 is a jet AND a light jet AND turbine): light jet, mid, super-mid, heavy, turbine, turboprop, jet, and a catch-all "something else" for anything that does not fit (piston singles, helicopters, warbirds). So the shape is a small set of orthogonal tags per airframe (size class: light/mid/super-mid/heavy; engine class: turbine/turboprop/piston; is-jet yes/no), NOT one flat bucket — that is what lets "how does their time compare to a super-mid seat" actually be answered. WHY IT HELPS, in the user's words: reading a resume, instantly see whether their hours are in the same category as what we are hiring for, rather than eyeballing tail-by-tail. BUILDS DIRECTLY ON EXISTING WORK, do not start from scratch: (1) lib/fleet/aircraft-normalize.ts already collapses the 592 distinct resume spellings into canonical airframe names and marks fleet vs non-fleet — that is the KEY layer; the class tags hang off each canonical name, so the wiki is "normalize, then classify", and every non-fleet type (B737, A320, C172) already survives normalization as real turbine/jet evidence. (2) Flight-data extraction already pulls type ratings + per-aircraft time-in-type (timeInTypeKey files hours by canonical aircraft). (3) The scoring engine already has an aircraft-fit category worth ~12%, but it matches SPECIFIC airframes; this adds the CLASS layer above it so "no exact type match but plenty of super-mid time" is visible even when the exact-type score is zero. THE WIKI ITSELF: one row per canonical aircraft with its class tags, manufacturer, and notes, seeded from the fleet registry (all classes known for our 14 types) plus the normalized non-fleet corpus; EDITABLE by the recruiter (this is the "wiki" ask — new airframes get classified as they appear, and a wrong guess gets corrected once and sticks). Auto-suggest a class for a newly-seen airframe (rules first — anything ending pattern-matching a known family, or an LLM classify call for a genuinely new type — then a human confirms, same suggest-then-confirm discipline as the rest of extraction). DECISIONS TO MAKE WHEN IT IS TIME: where the class definitions live (a lib table vs a DB table so recruiters edit without a deploy — probably DB given "wiki"); the exact hour thresholds, if any, that separate mid from super-mid (class is usually a property of the airframe, not the hours, so likely just tags, but confirm); whether the resume view shows a simple "same class / one class lighter / heavier than the seat" chip or the full breakdown; and whether class time rolls up into scoring or stays a read-only recruiter aid at first (start read-only — it is a decision aid, and rolling it into the score reopens the scoring-transparency/compliance gate). Not now, per the user — captured in full so it is ready to pick up. Pairs with the OCR + Claude-LLM extraction upgrades under Document Intelligence, since the corpus of aircraft-flown is only as complete as extraction is.
- [ ] Hired-candidate evaluation score — score candidates after hire to learn what works
- [x] Set aside a candidate on ONE position (Jul 28, commit df750d9) — the Skip control on a match card used to write scanExcludedReason on the Candidate row, which barred the person from EVERY position at once; there was no way to say "not for this seat". Added a per-requirement skip list (lib/matching/position-skip.ts plus a .server sibling, stored in WorkspaceSetting the same way tier overrides and match feedback already are, so no migration). Reasons: overqualified, underqualified, not a fit, declined, other. The candidate stays in the system and keeps competing everywhere else. The engine now also catches overqualification automatically — total time at or above 2x the seat's minimum — but SIC seats only, because a captain with twice the minimum is simply an experienced captain and setting those aside would bury the best candidate for every PIC opening. Set-aside people are NOT hidden: they come back in their own collapsed group on the board, because the automatic catch fires off self-reported hours that are known to disagree between a candidate's own documents. An explicit KEEP decision is what overrules the engine — clearing a skip is not enough, since the next scan would just re-flag them. Verified on the live G200 First Officer scan: 60 people auto-set-aside at 4,000+ hrs against the 2,000-hr minimum, ranked list down to the people actually competing for the seat
- [x] The set-aside group is capped at 60 (SET_ASIDE_LIMIT) — the live G200 scan already hits that ceiling exactly, so there are more overqualified candidates than the group shows. Either paginate it or make the cap explicit in the UI so it does not read as a complete list. RAISED AND MADE HONEST (Aug 1) — 60 to 600, and the sibling likely-overqualified cap 40 to 400. Measured across all eight live requirements the real groups reach 455 set aside (Citation CJ2 First Officer) and 237 overqualified (Citation CJ2 Captain), so the old caps were showing between a ninth and a quarter of each while looking complete; the new limits clear every group with headroom. The scan also returns true totals now and the header reads 200 of 237 in gold whenever a list is cut, so a future ceiling cannot hide the same way. Slicing more costs payload, not scan time — the pool is already scored in memory by then
- [ ] Unverified beats set-aside in the scan ordering — scanRequirementPool filters unverified records out BEFORE partitioning the set-aside group, so an overqualified candidate whose hours are unverified appears in neither group. Harmless today (they are in the data-cleanup queue either way) but worth deciding deliberately once extraction is fixed
- [x] Likely-overqualified captains (Jul 31) — the 2x total-time catch was SIC-only, so a 5,000-hour pilot sat at the top of the 1,500-hour PC-12 Captain board; that was being cleaned up by hand, ten manual skips deep. The same test now runs on captain seats but never removes anyone: they get their own collapsed group at the bottom plus a badge naming the multiple, because on a high-minimum seat plenty of hours is exactly what you want. Seat fit is untouched on captain seats. Partitioned with its own budget, not sorted last — sorting pushed them past the ranked slice and the group came back empty every time, caught by testing against the live pool rather than by reading the code. Verified on the live pool: PC-12 Captain has 24 who fit the seat and 40 likely overqualified from 3.1x to 8.6x, the top of them previously outranking every candidate who fits; G200 First Officer unchanged
- [x] The likely-overqualified group is capped at 40 and PC-12 Captain hits exactly 40, so there are more than it shows — needs paging or an explicit count, the same gap as the set-aside group above. DONE (Aug 1) by the same change as that item: the cap is now 400, and a truncated group states its true total rather than silently ending
- [ ] A flat 2x may not suit every seat — right for a 1,500-hour PC-12, but on the 4,500-hour G200 Captain seat it means 9,000 hours, which is an ordinary corporate captain. One constant today if it should become per-seat
- [x] Candidate tags: add by hand, colour, and filter (Jul 31) — the POST route never existed, so tags could only be removed and never applied, and one deleted by mistake was gone for good; adding one is now a control on the profile with type-ahead over existing tags so near-duplicates do not accumulate. Tag.color, unused since the Jazz import, now drives a nine-colour palette matching the employee side, recoloured once per tag and applied everywhere it appears; the palette stores its own value rather than a Tailwind class or a hex, since a class would not survive a rebuild and a hex would not adapt to dark mode. Tags carried over from JazzHR are separated from chosen ones on CandidateTag.source: all 1,648 existing links are JAZZ and describe a workflow we no longer run, so they render grey and collapsed behind a count on both the profile and the list column, with a keep action that PROMOTES one to a current tag rather than duplicating it. The candidates page gained a tag filter that narrows in the DATABASE rather than in the component, which matters because the list is capped at 100 and a client-side filter would silently have missed anyone past the cap; multiple tags AND together, matched through the normalized column so casing between hand-typed and imported tags cannot split a filter. Verified against live data including a candidate beyond the 100-row cap, and the database was restored to its exact starting state afterwards — 38 tags, 1,648 links, 0 manual, 0 coloured, no test data left behind
- [x] Scoring engine correctness fix (Jul 28, commit 913b440) — the Match Board was scoring non-pilots as 100% qualified (a cabin attendant and a maintenance record both hit 100 on the G200 First Officer). Five separate defects, all reproduced against live data then fixed: (1) unknown factors were FILTERED OUT of the qualified score, so a candidate with no hours had nothing to fail and the empty-set fallback returned a flat 100 — unknown now scores zero and still counts in the denominator; (2) seat aliases included the two-letter "fo", substring-matched, so it hit inside "for" / "info" / "before" and EVERY candidate passed seat fit — token matching is now whole-word; (3) time-in-type read the aircraft model number as the hour count ("Gulfstream G450" = 450 hrs in type, full credit against a 25-hr target) — an explicit hours unit is now required; (4) the bare manufacturer token was an aircraft alias, so G450/G550 experience credited a G200 seat — model-specific aliases only; (5) qualified/bonus ignored config.weights entirely and used a flat hard=2/soft=1, so setting hour minimums to 80% of the profile changed nothing on screen — qualified is now the category-weighted average. Bonus is now EXACTLY what is marked bonus (time-in-type and recency were hardcoded as bonus regardless of config). Verified: the cabin-attendant record went 100 qualified / +42 bonus to 5 / +0, flagged unverified
- [x] Unverified state + data-gap queue (Jul 28, commit 913b440) — new third state beside qualified/gated: a HARD requirement with no evidence EITHER WAY is a data gap, not a rejection. Unverified candidates are held out of the ranked scan (so the board is not mostly people we cannot assess). ScoreSplit renders the state muted with a "?" instead of a confident score, and readiness can never read "Strong signal" on unverified evidence. They ARE visible on a job's Applicants list, which never drops anyone
- [x] Unverified data-cleanup queue panel (Jul 28, commit 854bbdc) — getUnverifiedForRequirement had no caller, so the only way to see who was held back was to run a script. Now a collapsible "Missing data — cleanup queue" panel on the Match Board, under the tier lists. Loads on demand rather than on page load, because it rescores the whole pool. Shows each person with a count of missing hard requirements, which ones, a red "no document" flag when we hold no readable text for them at all, and an archive badge; ordered fewest-gaps-first so the quickest wins are on top, with headline counts for "one field away" and "no readable document". Two guards learned from the real data: it DEFAULTS TO THE LIVE PIPELINE, because a real G200 scan puts 2,587 people in the queue and roughly 2,300 are archived Jazz records that would bury the live ones (archive is one click away, with its count shown); and it renders 75 at a time with an explicit "Showing 75 of 2,587" so a truncated list can never read as a complete one. Also fixed on the way past: runRequirementScan did not pass the position skips, so the on-demand Scan button would have dragged every set-aside candidate back onto the board until the next full page load
- [ ] Cleanup-queue ordering ties break alphabetically — within the same gap count the queue is A-to-Z, so the top of the list is an arbitrary alphabetical sample rather than the most promising people. The ranked scan already solved this for matches (compareMatches breaks ties on strength of evidence); the queue should break ties on something similar, most likely how many minimums the person already clears
- [x] PDF form parser — reads the intake forms by COORDINATE, free and local (Jul 28, commit 2b8f9e0) — lib/files/pdf-form.ts. pdf.js (via unpdf, already a dependency) hands back every text item with its x/y position, so a label ending in colon or question-mark pairs with the value immediately to its right. No model call, no per-candidate cost, and MORE accurate than the LLM because nothing is inferred. Three templates: the signed Pilot Application (11 fields incl. medical class and expiry, the most common unverified gap), the Paycom Job Level block (both live wordings), and a conservative resume flight-time table. Wrapped labels are rejoined from the row above at the same x; page furniture is stripped; a Format-hint preamble is skipped; slash-separated answers take the total. Precedence on disagreement is Pilot Application then resume then Paycom, with every losing value kept as a recorded conflict. VERIFIED two ways: all 26 fields across one candidate's three documents match the raw PDF layout, and for a second candidate the Pilot Application and the Paycom application independently agree on all 8 overlapping metrics — including the total time of 1,566 whose flattened text was the unsplittable run 852551566138095488451000. Dry-run reviewer at scripts/review-pdf-form-extract.ts (local paths or --s3) writes every value beside the verbatim line it came from. NOT yet wired into the upload path — nothing writes to the database
- [x] Resume names read by TYPOGRAPHY, not prose (Jul 28, commit 35df929) — names were arriving fused to whatever followed them (one real case parsed as "Aadan Kenck PO Box"). Root cause was not the heuristics: extractFileText collapses ALL whitespace, so text.split(newline) returns ONE line holding the whole document, and every line-based rule in resume-intake was scanning a single enormous string. readHeaderName in lib/files/pdf-form.ts instead takes the largest text in the top third of page 1, which is where a resume always puts the name; ALL CAPS headers are title-cased. Measured over 45 real resumes from S3: agreed with the stored name 24 times, IMPROVED on it 6 (middle names the filename cannot know, e.g. Adam Kavis Rolph vs Adam Rolph), returned nothing 14, wrong once — a resume that sets a job title larger than the person's name. So it is wired to CONFIRM or ENRICH, never to overrule alone: it only wins when it shares a word with the filename, which keeps that one bad case out. The reader shipped first on its own; the resume-intake wiring followed once the sibling reactivate work it shares that file with was ready, since committing the route earlier would have carried a half-finished change and broken the build on an untracked import
- [x] Detail pane opens beside the card you clicked (Jul 28, commit 35df929) — the candidate list scrolls inside its own column while the detail pane is a sibling whose content is top-aligned, so scrolling down and clicking someone opened their details at the top of the screen, away from where you were looking. The pane now measures the selected card and offsets to match, re-measures while the list scrolls, and clamps so it can never be pushed off the bottom. NOT VERIFIED IN A BROWSER — client-side interaction is not testable from here (see the Browser pane note in CLAUDE.md); typecheck, lint and an HTTP load of the page all pass
- [x] Total time pulled out of the flight profile grid (Jul 28, commit 35df929) — it carries most of the scoring weight but sat as one cell among a dozen identical-looking rows. Now its own full-width row above the grid, larger and bold, so it reads at a glance while skimming candidates
- [ ] Wire the form parser into upload + backfill — the parser is proven but inert. Needs: call it in app/api/candidates/[id]/extract-metrics (and at upload) ahead of the LLM, land values as SUGGESTED with the source document and evidence line recorded, surface conflicts on the profile, and demote the LLM to a fallback for documents no template matches. Then a backfill for the live pipeline
- [ ] Form parser gaps worth closing — the resume-table detector is loose, so it claims some resumes it cannot actually parse (they come back "template matched but no fields paired", harmless but mislabelled); the Pilot Application's aircraft type-rating table and its certificate checkboxes are both present and parseable but not extracted yet; and non-pilot Paycom applications correctly pair nothing, which should read as "not applicable" rather than a miss
- [ ] Read the application forms as FORMS, not as flattened text — the single highest-value extraction fix. Both intake forms already carry every gate metric as a labelled field: the Pilot Application (Adobe Sign, 281 files across the 270 unscanned candidates) has TOTAL FLIGHT TIME / TOTAL PIC / TOTAL MULTI ENGINE / TOTAL JET / TOTAL TURBINE / TOTAL INSTRUMENT / HRS FLOWN LAST 12 MOS, and the Paycom application (4 files) has the same set under its Job Level question block. They do not scan today because lib/files/pdf-text.ts calls unpdf extractText, which flattens the page: every label prints first, then every value prints in a separate positional run, so the label-to-value pairing is destroyed. On some files the values fuse with no separator at all (one candidate's run reads 852551566138095488451000+129130 — his 1,566 total time is in there and no regex can split it). Fix is to stop discarding the PDF structure: getDocumentProxy already exposes getFieldObjects() for live AcroForm values and getTextContent() with per-item x/y coordinates for pairing each value to the label on its row. Both ship with unpdf, already a dependency. VERIFIED Jul 28 against real PDFs: getFieldObjects() returns nothing (all three documents are flattened, so the AcroForm route is dead), but the COORDINATE route works on every layout — Pilot Application, Paycom application, and even a resume hours table. The rule is simply that a label ending in colon or question-mark pairs with the next text item to its right on the same visual row, with wrapped labels rejoined from the row above at the same x. It also recovers medical class and expiry (the single most common unverified gap), certificates via checkmark glyphs, and per-aircraft time in type. Free, exact, no model call
- [ ] The PilotApplication PDFs are MISSING FROM S3 — 48 of 50 sampled return NoSuchKey, while resumes and other documents are 10 of 10 present. All the missing ones were uploaded in Jul 2026 with structurally identical keys, so this is not a prefix problem: a bulk import created CandidateFile rows and stored the extracted text but never persisted the bytes. Two consequences. (1) A user clicking through to one of those applications gets a broken download — worth fixing on its own. (2) The coordinate parser CANNOT be applied retroactively to those candidates, because only the lossy flattened text survives; the historical backfill therefore still needs the one-time LLM pass, while everything uploaded from here on can be parsed free. Source PDFs presumably still exist in Adobe Sign or the recruiter mailbox, so a re-upload would make the free path work retroactively too
- [ ] Self-reported hours DISAGREE across a candidate's own documents — for one G200 applicant the Pilot Application, Paycom application and resume give total time 4,200 / 4,300 / 4,300, PIC 2,675 / 1,250 / 2,700, and turbine 556 / (blank) / 2,700. Instrument was the only figure that matched everywhere. PIC differs by more than 2x and turbine by nearly 5x, which is decisive for any PIC-minimum seat. Extraction must therefore record WHICH document each value came from and surface conflicts for a human rather than silently picking one; PRECEDENCE DECIDED BY THE USER Jul 28: Pilot Application first (it is signed and carries the certification clause), then the RESUME, then Paycom last — many candidates have no Pilot Application, and a resume is more trustworthy than the Paycom form. Every number is self-reported by the candidate on all three, so none of them is ground truth; the conflict must stay visible on the profile rather than being silently resolved
- [ ] Bulk metric extraction for the ACTIVE pipeline — the LLM resume scan has only ever run over the Jazz archive: 1,221 of 3,143 ARCHIVED candidates carry extracted hours vs 32 of 341 ACTIVE. Extraction today is a per-candidate POST route (app/api/candidates/[id]/extract-metrics) with no bulk runner. Cost is NOT the blocker it appeared to be — the whole 270-person backfill prices at roughly 5 dollars on Opus 5 or 1 dollar on Haiku 4.5, and 20 new candidates is about 7 cents; the 50-dollar figure was the 8x-larger archive run. Do the forms-not-text fix above first, since it may remove the need to call the model at all for standard-form applicants
- [ ] Extraction model is hardcoded to Opus 5 — lib/extraction/pilot-metrics-llm.ts pins claude-opus-5, the priciest option, while the sibling Paycom extractor correctly defaults to Haiku with an env override. Make it configurable the same way, and key extraction on a document content hash so the same file is never paid for twice
- [ ] Candidates submit more than one application over time, with different numbers — one candidate carries three Paycom application PDFs whose total-time answers disagree (2,200 vs 2,259). Any extractor needs an explicit most-recent-document-wins rule rather than concatenating every file and taking the first match
- [ ] Medical certificate is hard but almost never extracted — it is the single most common unverified gap on the G200 FO applicant list (11 of 31). Either downgrade it from hard, or capture it during intake; as configured it holds back candidates who are otherwise fully qualified
- [ ] Job applicants are not linked to their pilot requirement — the OPEN "Gulfstream G200 First Officer" job has 35 applications but the matching PilotRequirement has 0, so the Match Board Applicants panel reads empty and only the whole-pool scan shows. CandidateApplication carries jobId and pilotRequirementId separately and only jobId is being set
- [ ] Scoring transparency & compliance — clear documented explanation of how a candidate is scored; review CA + NY law (NYC Local Law 144 bias audit, CA FEHA / automated-decision rules) to prevent discrimination — gates the scoring features; needs legal review
- [x] Overqualified lowers seat fit (Jun 19) — an SIC/first-officer seat where the candidate already has >= 2x the role's total-time minimum now reduces the seat-fit sub-score (retention risk: likely wants a PIC seat). PIC seats are never penalized for more hours
- [x] Overqualified as a recruiter fit-tag (Jul 27, commit 8758247) — the matchboard / pilot-requirement "tag the fit" had Underqualified but no counterpart, so passing on someone too senior for the seat had nowhere to go. Added Overqualified as its own verdict rather than folding it into "Not a fit": when this feedback eventually feeds scoring, an overqualified pilot is a retention risk on an SIC seat and is often right for a DIFFERENT opening, not a weak candidate. Distinct from the engine's automatic overqualified badge above (the system's guess) — this is the recruiter's call. lib/matching/match-feedback.ts + both match cards
- [x] Commercial-or-ATP either/or cert (Jun 19) — the ATP cert requirement is now "Commercial pilot certificate or ATP" and awards credit if the candidate holds either (a pilot needs one or the other)
- [x] Multi-select positions in scoring setup (Jun 19) — tick several positions in the scoring sidebar and copy the settings on screen to all of them at once (still saved on "Save scoring")
- [x] Recency weight zeroed pending real data (Jun 19) — recency/currency defaulted to 0% because nothing structured feeds it (the old scan just looked for the word "current" in a resume)
- [x] Real recency / currency signal (Jun 20) — added a structured "Hours (last 12 mo)" metric (recency_12mo) entered on the Flight Profile panel; the Recency category now scores off it vs a configurable currency threshold (Scoring setup > Hours logic, default 100 hrs): at/above = current (full), half-to-threshold = partial, below = not current. Recency weight re-enabled (default 8%); falls back to a weak resume-keyword hint when no hours are on file
- [x] Custom + either/or cert items (Jun 20) — Scoring setup > Custom certs & ratings: add your own cert/rating rows (label + comma-separated match terms + hard/soft/bonus/none). Each row is its own either/or group — credited if the candidate has ANY listed term in their certificates/profile text. Stored in the scoring config; the engine scores them in the Certs category and honors bonus vs gate status
- [x] Matcher bulk export (Jun 20) — multi-select candidates on the candidate-fit panel (per pilot requirement) with select-all, then export the selected contacts (name, email, phone, current title, matched job) to CSV. Recruiter/admin only (behind the scoring edit guard)
- [x] Category-weight explanations (Jun 19) — the scoring setup Category weights section now shows a short plain-English description under each name (what aircraft fit / seat fit / hour mins / time in type / recency / certs actually measure) so the weight can be set with confidence
- [x] Scoring requirement row hover highlight (Jun 19) — requirement rows in the scoring setup highlight (gold border + tint + soft glow) on hover so it's clear which row you're on
- [x] Review cloned gates on new managed roles (Jun 20) — verified the cloned hour minimums against peer airframes: 560XLS+ matches 560XL, Phenom 300 matches the light-jet-captain template (Phenom 100/M2/CJ2 = 1,500/1,500/1,500), Legacy 650 matches the G450 large-jet tier. All consistent with the existing fleet; editable per role from the front end
- [x] Verify scoring-setup positions completeness (Jun 20) — confirmed all 26 roles map to 24 clean (aircraft, seat) profiles with zero missing aircraft tags (no "Any aircraft" collapse); the earlier "missing positions" worry is resolved

## People Ops / Onboarding
Supporting the team beyond recruiting.
- [x] Offer not sent - the ending that was ours, not the candidate's (Sep04) — a verbal offer was given, the President did not approve it, and the letter never went out. Closing that as Offer declined was the only option before, which records a rejection the candidate never made. NOT_SENT is now its own status with its own columns (offerNotSentAt, offerNotSentReason) and never touches the decline pair. Setting it asks why, with one-click reasons (President did not approve, Supervisor did not approve, Position was pulled, Put on hold) so one cause is not spelled four ways. Deliberately NOT derivable from the offer steps: not sent yet and never going to be sent are identical in the ticks, so a person has to say which. Surfaces as grey rather than red on the chip and the timeline dot, a callout reading Never sent - the candidate did not decline plus the last step it reached, a Not sent filter on the offers board, and a timeline row headed Offer stopped rather than Offer. A closed offer is now sticky against step-ticking exactly as DECLINED already was, because people do go back and tick the verbal for the record and that must not resurrect a dead offer. RE-VERIFIED AT COMMIT TIME against the live database, not taken from the handoff: exactly 10 offer columns on CandidateApplication with the two new ones nullable and both empty across all 4,048 applications, the 2 existing declined rows untouched, and migrate diff --from-config-datasource returning an empty migration - zero drift before or after the push. The committed schema diff is those two columns and a comment and nothing else, which matters because db push applies the WHOLE shared file and a sibling's stray edit would have ridden along.
- [x] A hire whose offer fell through can finally be marked as such (Sep04) — NewHire.canceled was READ by four separate surfaces (employees directory, business-card queue, supervisor picker, dateless-hires callout), each carrying a comment describing exactly this case, and the PATCH route accepted it, but NOTHING in the app ever sent it - so the flag was unreachable and a fallen-through hire stayed employmentStatus ACTIVE forever. The absence claim was checked rather than believed: every reference in the tree before this is a read filter, with the single exception of the PATCH route that accepts the value. Found on Matt Smith, who had every date cleared by hand and his orientation seat removed and was STILL in the business-card queue and had become the ONLY entry on the dateless-active-hires callout - a surface that exists precisely so people are not lost. There is now an Offer fell through button on the new-hire page with a confirm listing exactly what the person comes off, a grey chip in the header, and a one-click They are coming after all undo. Kept deliberately apart from Mark as former employee, which is for somebody who actually worked here - filing a non-starter as a leaver writes a job they never had into their history. Sets ONE boolean and nothing else: the dates are the record of what was planned, and every list that matters already filters on the flag. Verified through the real PATCH route - setting it moved the live business-card queue from 4 to 3 and the dateless callout from 1 to 0, clearing it put both back, and Matt was left at canceled=false for him to click himself.
- [ ] FIVE stage and employment buttons on the new-hire page are not canEdit-gated, not the three first reported (found Sep04) — Reactivate, Mark onboarded, Archive, Mark as former employee and Mark as active all render for any viewer, and only the API permission check stops them actually doing anything. The handoff named three; reading the block at commit time found five. Pre-existing and noticed while adding the Cancel control beside them - that one IS gated, so the row is now inconsistent on purpose rather than by accident, and a comment in the file says so, so the next person does not copy the older pattern.
- [x] RECORDED, what actually happened to Matt Smith's offer (Sep04, from him) — he was given a VERBAL offer and moved to onboarding, then the President did not approve it, so the offer letter was NEVER SENT. He did not decline. The Supervisor DID approve: supervisor_signs is ticked Sep 4, alongside verbal_offer and draft_offer from Sep 2, so the offer stopped between the supervisor and the President - confirmed in his stored offer steps at commit time. His dates, supervisors and Sep 29 orientation seat were all cleared by hand on Sep 4. And his SkyShare address msmith@skyshare.com was NEVER PROVISIONED, because IT does not provision until the offer letter is signed - so an unsigned offer leaves no account to clean up, which is worth knowing before anybody builds an offboarding step that assumes there is one.
- [x] RECORDED, the three places orientation actually happens (Sep02, from him) — the addresses were the missing half of the location override and nothing in the repo held them. SKYSHARE HQ, 180 2400 W, Salt Lake City, UT 84116 - the normal one, and already the Location line in Front templates rsp_qnije and rsp_qnioq. ATLANTIC AVIATION FBO, 369 N 2370 W, Salt Lake City, UT 84116 - sometimes. OGDEN, 3715 Airport Road, Ogden, UT 84405 - a chance, not yet used. Only HQ is written anywhere; the other two are per-session facts to set on the session page when a session is actually held there, not defaults to backfill.
- [x] The September 29 orientation has an address, so the location override can finally fire (Sep02) — the capability shipped Aug 31 and had never once been able to run, because the column it reads was empty on every session that mattered. One row written, dry run first, undo record committed at scripts/orientation-address-fill/UNDO.json, and the value read back from the database rather than trusted from an update count. IT IS AN IDENTITY CHANGE FOR THE EMAIL and that is deliberate: the Front templates already say Location: 180 2400 W, so the override finds the same string and rewrites nothing, and what goes out is byte-identical to today. What it buys is that the mechanism is live for the first time - the day a session moves to Atlantic Aviation or Ogden, setting the address on that session now actually changes the mail. Scope was one session, not six: the five past ones were skipped because no mail goes out for them, and the script prints its verdict on all six so what it declined to touch is visible. Note for anyone reading the older entries: there is now ONE future session, not two - Sep 1 has happened.
- [ ] Changing the address does NOT change the parking directions, and on a session away from HQ that is wrong (found Sep02) — the override rewrites the Location: line only. The Front templates follow that line with a paragraph of HQ-specific directions - surrounded by a few hangars, an airport security gate leading to the ramp area, park anywhere - which stays exactly as it is. So a session at Atlantic Aviation or Ogden would carry the right address above directions to a different building. Not urgent, since no session has been held away from HQ yet, but it must be handled BEFORE the first one is: either extend the override to the directions block, or have the send flag loudly that the directions do not match the address and let a person fix the wording in the send window, which is now editable. His standing rule already says anything off-normal gets flagged, and this is the case it was written for.
- [x] A checklist step can email addresses she types in, not only the hire's own two (Sep02, from him) — his words: give me the option to manually put in the email addresses this needs to send to. It closes the gap found the same morning while checking the two PRD templates - 2. PRD Request to ITS is addressed to IT rather than the pilot, and the picker offered only the hire's personal or SkyShare address, so wiring it up would have emailed an internal request ABOUT somebody TO that same somebody. A third option, Addresses I type in below, with a To box beside the existing Cc box and the same parser behind both; the recipient is a list all the way through, since Front already took an array and the two hire-address cases simply resolve to a list of one. IT DELIBERATELY DOES NOT FALL BACK, which is the whole point - personal and SkyShare fall back to each other so a hire missing one address is still reachable, but a custom list has no sensible second guess and falling back would put an internal request in a new hire's inbox. An empty list is REFUSED AT SETUP with a sentence saying what to do, rather than saved into a Send button that could only ever fail in front of whoever pressed it. The greeting switches itself OFF when custom is chosen and says why in the panel: the name in Hi <first name> is the NEW HIRE's and not the recipient's, so on a step that emails somebody else it greets the wrong person. Off rather than removed, so it can be turned back on. Lint 0 errors, tsc exit 0, both pages and the client bundle serve 200 with the new option present. Still unverified and unchanged: the Front send call itself, and the panel's own controls, which open on click and cannot be driven from here.  CONFIRMED BY HIM Sep02: this is done, it all works.
- [x] RECORDED, what a task email actually sends to (Sep02, answering him) — he asked because he could not tell, and the honest answer is worth keeping rather than re-derived. THE DEFAULT IS THEIR PERSONAL EMAIL, and it falls back to the SkyShare address only when no personal one is on file, saying so in an amber line in the send window when it does. Measured against live data rather than asserted: of the 5 active and pre-onboarding hires, ALL 5 have both addresses on file, so with the default setting every one of them goes to the personal address and the fallback fires for nobody today. The send window now also names WHICH field the address came from in words - their personal email, their SkyShare email, or set for this step - so the question does not have to be asked again.
- [x] The checklist can be put in the order the work is actually done, and a step can send a Front template she picks herself (Sep02) — Manage tasks gained a drag handle and up/down arrows on every section AND every task, and a task can move between sections, which was the actual ask: her two new steps (Request PRD Access, Request PRD from ITS) could be renamed but not moved, so they sat at the bottom of Custom however early they have to happen. Order is stored beside the existing renames in the same WorkspaceSetting and staged behind one Save order press, because saving RE-STAMPS group and order on every hire's task rows - four places sort by those columns, so the setting alone would have left the grid, the hire checklist, the dashboard next-task and the completion count all showing the old order. It also fixed something nobody had noticed: attended_orientation, drug_screen and travel_complete each held TWO different order values across hires (older rows numbered one way, newer another, from business_card being added to the code list later), so the checklist ran in a slightly different order for older hires than for newer ones. Second half: any task can now be pointed at a Front message template from the same panel - 74 templates read live from Front, personal or SkyShare address, cc, and whether to prepend Hi <first name> - and a Send button appears on that step of every hire's checklist, with the body EDITABLE in the send window for that one send, exactly as the orientation dialog does it and per the standing rule below. The onboarding welcome and the contacts link are excluded and labelled built in, because each does more than fill a template. VERIFIED AT COMMIT TIME against the live database and the running dev server rather than taken from the handoff: onboarding-task-emails is an empty object and task-email-sends HAS NO ROW AT ALL, against 44 WorkspaceSetting rows listed as the positive control with the other three send-record rows all present - so nothing has been sent through the new path. The two PRD tasks sit at order 21 and 22, and the three keys above each now hold ONE order value across 205 rows. The grid, the hire page and the parked classic layout all return 200 and all three now print the STORED labels (documents, Onboarding and systems) with zero occurrences of the built-in Pilot documents, which was a real divergence - the grid said documents and the hire page said Pilot documents, because the detail page took its section names from code. CAVEAT, stated plainly: no email was actually sent. The Front send call is a near-verbatim copy of the live contacts send and is untested, because FRONT_TEST_INBOX is set and a local send would tick the step DONE on a real person's record even though the message went to the test inbox. Client-side drag is likewise unverified; the arrows are the reliable path and exist for that reason.  CONFIRMED BY HIM Sep02: this is done, it all works. That covers the reordering, the panel and the checklist as he uses them. It does NOT retire the caveat above, which is specifically about the Front send call - he has not said an email went out, and until somebody says so that line stays as written rather than being quietly upgraded.
- [ ] Of the two PRD templates, only ONE can be wired to its step as built - the other cannot go to the person it is addressed to (Sep02) — the handoff offered both as ready to switch on; checking them against Front says otherwise, and wiring the second would send a wrong email to the wrong person. 1. PRD Request to Pilot (rsp_r7mze) FITS: it is addressed to the pilot, it is fully self-contained, and it needs only the greeting toggle turned OFF because the body already opens with Hello. 2. PRD Request to ITS (rsp_rcggq) DOES NOT: it is addressed to ITS, and the audience picker offers only the hire's personal or SkyShare address, so the mail would go to the PILOT. It also has a blank line where the pilot's name belongs and says Their pilot app is attached when this path attaches nothing. Making it work needs a third audience - a fixed internal address - plus at least the hire's name filled into the body; the attachment is a bigger question. Verified read-only against Front: both ids exist under those exact names, both bodies open with Hello, and a search of all 74 templates for PRD returns exactly these two.  *** HALF OF THIS IS ANSWERED, Sep02: the recipient half is built. *** He asked for the option to type the addresses in, so a third choice now sits beside personal and SkyShare, with a To box and no fallback. rsp_rcggq can therefore be pointed at IT. What is still true and still needs a person: the body leaves a blank line where the pilot's name belongs, and it says the pilot app is attached when this path attaches nothing - both are fine to handle in the send window for one send, since it is editable, but neither happens by itself. He also said the step does not have to send through the app at all, so wiring it up is his call and not a to-do.
- [x] Four MAINTENANCE checklist keys still hold two different order values each, and the new panel cannot reach them (found Sep02) — the reorder work collapsed the split on attended_orientation, drug_screen and travel_complete, but checkin_30, checkin_60, checkin_90 and benefits_enrolled each still carry one order for 20 hires and another for 177. Same off-by-one and same cause. It is not an oversight in the work above: the reorder write deliberately excludes the MAINTENANCE group, so the panel cannot fix these and neither can she. Small, and worth doing so the 30 / 60 / 90 day check-ins read in the same order for everybody.  *** CLOSED Sep02, AND THE FINDING ABOVE IS WRONG. *** He asked for help fixing it, so it got measured instead of assumed, and neither half survives. ONE: the off-by-one shifts the WHOLE block, so the relative order is identical in both cohorts - all 197 post-onboard hires read 30-day, 60-day, 90-day, benefits, in that order. Nobody has ever seen a different sequence, which is the thing the entry claimed. TWO, and it makes the first half moot: NOTHING SORTS BY THAT COLUMN. getPostOnboardHires builds the check-in list by mapping over MAINTENANCE_TASKS in lib/onboarding/tasks.ts, and PostOnboardTab renders that array as it stands, taking even the column headings from it - so the stored order on a MAINTENANCE row is dead data. Every other reader excludes the group outright. The one real thing measurement did turn up is latent rather than visible: 17 hires carry Benefits enrolled and Social media announcement BOTH on order 104, a genuine tie, harmless only for as long as no view starts sorting by it. Left alone deliberately - a write against 811 live rows to tidy a column nothing reads is the wrong trade. Worth knowing if that view ever changes. HOW THE WRONG ENTRY HAPPENED, since it is the reusable part: three sibling keys HAD just been fixed for a real split, these four showed the same shape, and the shape was written up as the same bug without checking whether the sequence or any reader actually differed. Same-shape is not same-bug.
- [x] RECORDED, three things she said about the checklist and its emails (Sep02, from her) — ONE: she added the two custom steps herself, Request PRD Access and Request PRD from ITS, and needs them done EARLIER than the bottom of the list, which is what drove the reorder work. TWO: an email built from a Front template must always be editable in the send window before it goes, for that send only - confirmed again and now the rule for EVERY template send in the app, not just orientation. THREE: she wants to choose the template herself from the checklist, rather than an agent configuring it in the background as was done for the first two sends.
- [x] An email built from a template now gives him a box to change what it says before it goes (Aug 31, from him) — his words, and he expected it to exist already because it is obvious. It did not exist anywhere: the contacts email explicitly told you to go edit the wording in Front, which would change it for every future send. The box is pre-filled with the fully resolved template, so approving it sends exactly what it sent before - an untouched body is never sent back at all, so the common case is byte-identical. An edit applies to THAT SEND ONLY: nothing is written back to Front, no template is created, and the next send re-reads the live one. Edited versus untouched is shown on the box, repeated in the confirm, and kept afterwards in a new Wording column with a third state for sends predating this. Live in all three dialogs. THE OVERRIDE REPLACES THE BODY AND NEVER THE GREETING, which is what makes a batch safe - one edit reaches the cohort and each person still gets their own greeting, so nobody is emailed under somebody else's name. THE AUTOMATIC REMINDER CRON DOES NOT GET THIS AND MUST NOT, since nobody is there to approve wording; stated in a comment at all three places somebody would look and verified statically at both call sites. Edited in a plain contenteditable rather than either existing editor, because both are lossy here and silently: one normalizes on load and would restyle the template just by opening the dialog, the other is not an HTML editor at all.
- [x] A rescheduled orientation no longer leaves its Google invite stale, and this time it is actually reachable (Aug 31) — updateOrientationCalendarEvent existed since the reschedule work and had NO CALLER, which is why the entry claiming this yesterday had to be walked back - the capability was correct and simply unreachable. Now wired, with staleness MEASURED rather than guessed: a fingerprint of what was last pushed rides in the WorkspaceSetting the calendar record already occupies, so no migration. Google cannot answer it alone, since events.get returns the summary but not the start, end or location. An event created before this has no baseline and reads as unknown, never as in step. PROPAGATION IS AN EXPLICIT BUTTON RATHER THAN AUTOMATIC ON SAVE, because the obvious choice is wrong both ways: pushing silently with notify emails every guest over an address typo, and pushing silently without it leaves everyone who already accepted holding the old time and never told. The PATCH reports which calendar-relevant fields actually moved, comparing values rather than payload presence so re-saving an unchanged form does not cry wolf.
- [x] The supervisors email was the one being silently rewritten, and now says so (Aug 31) — the digest builder applied the session overrides and discarded the changes list, so the attendee preview announced that the time had been adjusted while the supervisors preview, rewritten the same way from the same session, said nothing at all. Surfaced exactly as the attendee path does.
- [x] A session with no end time no longer degrades in silence (Aug 31) — the override had moved the whole comparison inside its endsAt branch, so a null end time produced no rewrite AND no warning - on screen indistinguishable from the template and the session agreeing. It now names the hours about to go out and says they were not checked.
- [ ] The internal session summary still has no editable body (Aug 31) — it is built by orientation-summary.ts rather than the shared builder, so it was left out rather than bolted on badly. Worth doing for consistency, since his expectation was explicitly about ANY template-driven send.
- [x] RECORDED, two standing rules about template emails (Aug 31, from him) — ONE: an email sent from a template should ALWAYS give him a box to change what it says before it goes. He expected this to already exist because it is obvious, and it is the standing expectation for any template-driven send, not just orientation. TWO: the wording change is PER-SEND and must not become a new template or be written back to Front, because most orientations are identical and only the occasional one differs.
- [x] The History tab says none, not N/A as she asked (found Aug 31) — the Travel half shipped exactly to spec and reads not needed. Her words for the other half were history say N/A if theres no history, and it now reads none - chosen on separate reasoning that did not reference the request. One word, and hers to pick, but it should go back to her rather than be closed as done. The parked classic layout at /people/<id>/classic got neither badge change.
- [x] The oldest open feedback is partly overtaken and partly untouched (Jul 2, reviewed Aug 31) — her ask was an Email button to send to the CPO, candidate and maybe supervisor, depending on the role. THE CANDIDATE HALF IS DONE, though not as a general button: two sends hang off specific checklist tasks, SendOnboardingEmailButton and SendContactsEmailButton, both to the hire's own address cc hrotasks@. THE SUPERVISOR HALF IS NOT - the only supervisor send is the orientation digest, batched per session, even though NewHire.supervisorEmail is on the row. THE CPO HALF IS NOT - the only configurable internal recipient list is orientation-scoped and nothing on a hire's page reaches it. AND NOTHING IS ROLE-AWARE, which is the part that makes it a feature rather than a button. Two months on, the candidate half may be all she needed - worth asking before building.
- [x] The Travel tab says not needed, the same word Business cards uses (Aug 31, from him) — and the useful part is that nothing new was required. travel_complete is already a per-hire OnboardingTask, its status is already TODO / DONE / NA, and the three-state control is already on the Checklist tab - marking it N/A always WAS the does-not-need-a-trip decision. It simply was not visible from the Travel tab, so a hire with no trips looked identical whether travel was unnecessary or merely unbooked. No new field, no migration. Reads live from task state so ticking N/A updates the chip without a reload. History's chip now says none rather than a bare 0, which read like a broken value beside not needed and ordered. Confirmed working by him.
- [~] An orientation can now say its own time and place, and its invite is kept in step (Aug 31) — her ask was to change the time or location when a session is not the standard one, and the app already DETECTED the mismatch - it printed a warning saying the template says 9:30am to 3:00pm while the session is 11:00 AM to 4:00 PM - then sent the template's time anyway. The data was there too, since the session row already stores endsAt and a street address. Both rewrites are ANCHORED rather than guessed: the time reuses the very regex that produces the warning, now one shared constant so the two cannot drift, and only replaces ranges that DISAGREE; the location anchors on the Location: LABEL the bodies use, capturing only to the end of that text node. Nothing is silent - every substitution is reported and shows in the preview before anybody sends. Applied in BOTH builders, which feed all three templates and the reminder cron. Exercised against tomorrow's real Sep 1 session: 9:30am to 3:00pm became 11:00 AM to 4:00 PM, the address was replaced, both changes reported, and a template that already agrees came back byte-identical with zero changes. CHECKED FIRST, because there is an orientation tomorrow with 4 attendees: no automated send is due - reminder-armed names a different session and every recent cron run including today's reports nothing-due with sessionsChecked 0. So this changes what a MANUAL send produces and makes it correct.  *** CORRECTED Aug 31: the time half fires, the PLACE half cannot fire on any session that exists. *** The location rewrite needs OrientationSession.address and the live data has none where it counts. Full scope: 6 sessions, 3 with a null endsAt, 4 with a null or empty address, and BOTH future sessions null on address. Populating it is a data task rather than a build. Two follow-ons: with endsAt null there is now no rewrite AND no warning, because the override replaced the drift warning that used to catch that case, so a session missing its end time degrades silently; and buildSupervisorDigestEmail applies the overrides but discards the changes list, so the supervisor mail is rewritten with nothing surfaced - which contradicts the commit's own claim that nothing is silent.
- [x] Rescheduling an orientation no longer leaves the Google invite stale (Aug 31) — the route offered only create, add-attendees and add-guests - there was no update path at all, so a moved session kept its old TITLE, DESCRIPTION, LOCATION and times and every guest held an invite for the old time. Shipped WITH the email override above rather than after it, because shipping that alone would have made the email right and the invite wrong, which is precisely what the standing propagation rule exists to prevent. updateGoogleEvent could not be reused: it goes through the SERVICE-ACCOUNT client and the shared calendar id, while orientation events live on the clicking user's own calendar under per-user OAuth. Two safety properties worth knowing: it does NOT send attendees, because events.patch REPLACES that array and including them would silently uninvite everybody already on the event; and it is SILENT unless notify is passed, matching how creating and inviting are kept apart everywhere else here. NOT VERIFIED: no calendar event was touched and no email was sent.  *** CORRECTED Aug 31, THE HEADLINE ABOVE IS FALSE AS WRITTEN. *** The capability is real and the identity trap was genuinely avoided, but NOTHING CALLS IT, so a reschedule still leaves the event's title, description and location stale exactly as before. Verified: updateOrientationCalendarEvent appears only in its own definition and its own route with zero callers elsewhere; OrientationCalendarPanel types its action as create, add-attendees or add-guests with no update and no button; and the reschedule PATCH at app/api/orientation/sessions/[id]/route.ts contains zero references to the calendar. Built, not wired. The wiring is small and until it happens the standing propagation rule is still being broken.
- [x] Orientation emails cannot say a different time or place for one session (found Aug31, from feedback cmthlyx3z)  — her words: most of our orientations are at the same time, but every once in a while the time is different, so we need to be able to change the body of the emails that go out and the time or location when needed. THE DETECTION HALF ALREADY EXISTS AND SO DOES THE DATA, which makes this smaller than it looks: her session is 11:00 AM to 4:00 PM and the app printed its own warning saying the template says 9:30am to 3:00pm, and the OrientationSession row already stores endsAt and a street address. lib/front/orientation-email.ts applies only four transformations to the Front template - subject dates and location, the delete-this-part strip, date placeholders and a greeting - and deliberately never rewrites time or location prose in the body, with a comment saying it is HR's copy. So the body ships the standard time even when the session is not standard, and the only remedy offered is editing the Front template, which would change every future session too. Fix is one applySessionOverrides step after fillDatePlaceholders in both builders, reusing the SAME regex that already produces the warning to rewrite the matched time strings, plus the address line from session.address; both builders feed all three templates and the reminder cron, so one insertion covers everything.
- [x] THERE IS NO CALENDAR UPDATE PATH, and rescheduling already leaves the invite stale (found Aug31)  — found while scoping the item above and worth its own line because it is a defect today, not a consequence of that work. app/api/orientation/sessions/[id]/calendar/route.ts exposes only create, add-attendees and add-guests - confirmed at commit time, the route rejects anything else by name. Rescheduling goes through a PATCH that writes the database and nothing else, so the Google event keeps its old TITLE, DESCRIPTION and LOCATION. updateGoogleEvent exists in lib/google/calendar.ts but uses the SERVICE-ACCOUNT client and the shared calendar id, which is the wrong identity - orientation events live on the clicking user's own calendar. This matters for sequencing: shipping the email override alone would make the email right and the invite wrong, which is precisely the inconsistency the standing propagation rule exists to prevent, so the two ship together.  *** REOPENED Aug 31: this was ticked on the strength of the entry above, which is itself corrected. A path now EXISTS but nothing reaches it, so from a user's seat nothing has changed. ***
- [ ] Travel and History tabs should say not needed and N/A, like Business cards does (found Aug31, from feedback cmthixqwj, her words)  — two one-line display changes with NO schema migration, which is the fact worth recording since a new column on a shared live database would have been a much bigger call. Business cards already renders a word by lowercasing NewHire.businessCardStatus. For TRAVEL the signal already exists and is simply not surfaced: travel_complete is a per-hire OnboardingTask and OnboardingTask.status is TODO, DONE or NA, with the three-state control already rendered on the Checklist tab, so marking it N/A IS the travel-not-needed decision. For HISTORY it is purely cosmetic - the count is archived onboarding rounds and the panel already renders nothing at zero.
- [x] Hannah's Aug 27 travel feedback, answered (Aug 28) — three reports, nine asks, eight shipped and one dropped by him. Item times name Mountain (Utah) time and say Arrives SLC or Departs SLC, read from the route against SLC and left unsaid when the route does not name it, because a wrong Arrives is worse than no word at all. Crew travel is a purpose. Clicking a trip anywhere on the hub opens THAT trip. Trips that have passed arrive folded with a Past chip, in the rail and on a person's page. The rail band, which used to be nothing but the focus filter, folds on the name now with focus and open as separate controls. A traveler tab strip sits above the calendar in the new hire page's shape
- [x] A trip can be told what it does not have (Aug 28) — the Orientation date, the Indoc dates and the preference fields each fold to a chip on one line, remembered per trip in the existing travel checklist WorkspaceSetting so there is no migration. His reason: not everyone we fly in is a pilot, a support-role hire has orientation and a start date and no indoc and no training, and those boxes were scrolling space that meant nothing. Deliberately NOT derived from the trip purpose, and the live data says why: the Aug 31 orientation trip runs six days and carries indoc dates, so a purpose rule would hide the fields on exactly the trips using them. Nothing is cleared and restoring brings the values back
- [x] The travel hub links out instead of rendering a profile at its own foot (Aug 28) — clicking a person used to load a second copy of their profile into a pane below the calendar and the roll-up, which is the thing the team asked to be rid of. Calendar chips, rail trip names and table rows now link to that trip on that person's Travel tab; the rail's open button, the traveler tabs and the New trip picker link to the tab. All real links, so they ctrl-click into a new tab, which the in-page pane never could. The roll-up checklist and the trips table stay at the bottom, which he confirmed he was happy to keep
- [x] Deep links into a person's travel (Aug 28) — ?tab= on the new hire page (the candidate page already had it) and ?trip= in the travel panel, both read ONCE as initial state so a hand-switched tab or a hand-collapsed trip is not fought on the next render. A trip named by ?trip= is expanded from the first render including on the server, so there is no flash and the behaviour is checkable over HTTP rather than only by clicking
- [x] Archiving a traveler tab is per browser and says so (Aug 28) — a localStorage list, so it tidies one person's strip and comes back empty in a fresh browser. It hides nothing that matters, since the trip, calendar, rail and table are untouched, which is what makes a view-only preference the right size for it. A shared archive needs a column on TravelTrip and a migration against the live database and is worth deciding on purpose. Archive is offered ONLY once every one of a traveler's trips has ended: offering it on a live trip is how somebody hides the person they are about to need
- [x] Dead code removed with the travel pane (Aug 28) — components/travel/TravelerInline.tsx, loadTravelerDetail in the travel actions, and getTravelerDetail plus the TravelerDetail type in the data layer. Worth naming rather than just deleting: that server action returned a traveller's email and phone and no longer had any caller. Re-checked at commit time, all four symbols return zero references across app, components, lib and scripts against a positive control of TravelPanel at seven
- [x] The feedback page no longer shows answered travel work as pending (Aug 28) — four of the nine open reports closed, all Hannah's: the three Aug 27 travel reports and the Aug 21 date bug. Done through scripts/close-travel-feedback.ts rather than the page, so there is a dry run, an undo record under scripts/travel-feedback-aug27/UNDO.json and a read-back from the database. Five left open on purpose and named in the script's own output: the two REVIEWING ones from June and July, the /people double-scrollbar bug, the new hire layout widgets, and the where-do-interviewers-come-from question. Verified read-only afterwards, whole table: DONE 40, NEW 3, REVIEWING 2, and the five still open are all Aimee's. Reversible with --undo
- [ ] A CAVEAT ON THAT, worth knowing rather than fixing (Aug 28) — the four were marked DONE before the code shipped, on his instruction. If the push does not happen the feedback page is ahead of reality, and one command puts it back: npx tsx scripts/close-travel-feedback.ts --undo
- [x] RECORDED, three answers he gave while this was built (Aug 28, from the user) — ONE, check something as not needed means the trip's DATE FIELDS: his words were that we have pilots or support roles who are not pilots, so they do not have indoc, they have orientation and start date but no indoc and no training, and he does not want fields showing that are not necessary. TWO, he does not want a person's profile rendered at the bottom of /travel, and his reason was that was the feedback I received; a checklist at the bottom is fine and stays. THREE, open the candidate's travel profile means NAVIGATE to their own profile on its Travel tab, not scroll to a pane on the travel page, and it was built as an in-page scroll first and was wrong
- [x] New hires checklist grid no longer has its own vertical scrollbar (Aug 27) — the table runs to full height and the page is the only thing that scrolls. Trade-off taken deliberately: the header row of names and progress bars no longer stays pinned while you scroll down the tasks, because sticky resolves against the nearest scrollport and this box no longer scrolls vertically. The Task column still pins as you scroll sideways, which is why the sticky classes stay. Verified in the live DOM on the running dev server at /people?tab=grid: clientHeight 1624 equals scrollHeight 1624, computed max-height none, and no ancestor between the box and the document is a vertical scroller. The useFillViewportHeight hook existed only to compute the old measured cap and was deleted with it - re-checked at commit time that no reference remains anywhere in the tree, against a positive control of useDialogClose in the same directory returning 16 importers.
- [x] The bottom half of the new hire page is four tabs (Aug24) — Checklist, Travel, Business cards and History, and the checklist is all of it on ONE tab, group by group, rather than one stage per tab. Travel, business cards and onboarding history are not restyled, only moved behind a tab, which was her standing condition: the mock must not restyle anything that already exists on the live site
- [x] The offer is asked for once (Aug24) — the offer stepper and the six OFFER checklist rows were always the SAME six keys, which is why the page appeared to ask for the offer twice. The stepper stayed because it is the richer of the two, carrying the timestamps and the start date. Nicholas Lembo went from 21 tri-state rows to 15, and the arithmetic was re-checked here rather than taken on trust: he has 21 tasks of which 6 are OFFER, so 15 is exactly right
- [x] Adding a milestone to everyone no longer means every former employee (found and fixed Aug24, from his question) — addMilestone backfilled with an unfiltered findMany, so Everyone meant every NewHire row that has ever existed. Measured read-only before changing it: 457 rows would each get a live TODO, of which 428 were ARCHIVED, 23 POST_ONBOARD and only 6 ACTIVE, 264 belonged to people whose employmentStatus is TERMINATED, and 241 carry no checklist at all so they gained a lone orphan row. One milestone created 457 rows of which 451 were noise. Now scoped to stage ACTIVE: the same query run again returns 6, with 0 terminated people selected where there were 264. NOBODY HIRED LATER IS MISSED, and that was worth checking rather than assuming - ensureCustomMilestoneTasks already exists and is already called from both hire-creation routes, so a new hire receives every custom milestone at creation. I nearly got this wrong the other way: defaultTaskCreateData does not read the catalog, and stopping there suggests future hires never inherit one at all, which is false because a separate call two lines down the same route does it. The UI changed with the behaviour rather than being left to contradict it - the button reads Everyone onboarding, and the note no longer claims the task lands on people who already onboarded. NOT VERIFIED by pressing the button, deliberately: the write path is unchanged code, the only edit is the where clause, and adding a real milestone would put a row on all six active hires to prove a filter
- [ ] The milestone catalog has drifted from the task list (found Aug24) — the catalog holds 18 entries while ONBOARDING_TASKS has 21, so bg_check_info, business_card and contacts_link_sent have no short label in the grid. Noticed while scoping the milestone write; not touched
- [x] A checklist item can be added to just one person (Aug24) — adding to EVERYONE already existed and is the workspace milestone catalog, which writes the task onto all 457 hires; adding to one person did not. The new route writes a single row under a one_ key deliberately kept OUT of the catalog, so it never reaches anybody else or the next person onboarded. Verified against live data after the test wrote and removed a real row: task rows back to 5058, group CUSTOM back to 0, keys starting one_ back to 0, and the catalog still 18 entries with none of them a one_ key. NOT VERIFIED and deliberately so - the Everyone scope was not exercised, because proving it means writing a task onto all 457 hires in the shared live database. That route is unchanged and already in use from Settings
- [x] Travel opens on the newest trip instead of shutting the moment something is booked (Aug24) — the old default expanded a trip only while items.length was 0, so the one trip worth reading was the one that closed as soon as anything was actually booked on it
- [x] Business cards shows whether this person has been ordered for before (Aug24) — read from the imported history, and only their own: Jared Esselman reads three orders, a new hire reads never ordered. A roster-wide table on somebody's profile is clutter, which is her decision recorded when the import landed
- [ ] The roster-wide card-order view still has no home (Aug24) — nothing reads the card-order tables anywhere except the per-person panel, so the 9 batches, who was on each and the 4-to-14-day turnarounds are imported and unreachable. The Business cards page is where that belongs
- [x] RECORDED, and it cost a round trip (Aug24, from him) — the four-tab bottom half was approved from a mock and had to be asked for a SECOND time before it was built, because the mock was mistaken for the build. STANDING RULE from that: say SHIPPED, HANDED OFF or MOCK-ONLY as the first thing about any deliverable. He had no way to tell the four-tab design was never built, and the roadmap read as though it were
- [x] FIXED SAME DAY: merging two employee records silently threw away their card-order history (found and fixed Aug24 while verifying the import, and introduced by it) — the merge now re-points businessCardOrderLine alongside the other relations, one updateMany inside the same transaction as the delete, placed next to businessCardVariant with a comment saying why it is easy to miss. Ten relations move now where nine did. HONEST LIMIT ON THE VERIFICATION: nobody ran a merge. Doing so means merging two real employee records in the shared live database, which is irreversible and not something to do to prove a point, so this rests on tsc, on the fix being the identical one-line shape as the nine relations around it, and on it sitting inside the existing transaction rather than beside it. THE ORIGINAL FINDING — app/api/new-hires/merge/route.ts hand-lists nine child relations to re-point onto the surviving record and then deletes the secondary at line 99. BusinessCardOrderLine is not in that list, and its foreign key is onDelete SetNull, so the loser's orders detach instead of moving: the row survives holding only the typed personName and the link to the person is gone. THE POSITIVE CONTROL IS THE LINE ABOVE IT — businessCardVariant, the sibling business-card table, IS re-pointed at line 40, so this is an omission specific to the newly added relation rather than a pre-existing gap. The route's own header promises that all of the secondary's history moves to the primary, which is now untrue. NOBODY WOULD NOTICE, which is what makes it worth fixing before it fires rather than after: nothing reads these tables yet, so the loss would accrue invisibly and surface later as wrong reorder counts, the exact question the tables were built to answer. Fix is one updateMany alongside the other nine. Two other delete paths leave orphaned lines the same way, and the comments on bulk-delete and the TEST-data teardown both still say permanently delete, which no longer describes what happens - a deleted person's full name persists in personName
- [x] Business-card orders have somewhere to live (Aug24) — two new tables, an order per batch sent to the printer and a line per person on it, and the workbook is imported. RE-MEASURED AGAINST THE LIVE DATABASE at commit time rather than copied from the handoff: 9 orders, 71 lines, all source SHEET_IMPORT, 70 matched to a person and 1 deliberately not - South Valley Regional is an FBO, not an employee. 65 distinct people have an order and 4 have more than one: Jared Esselman three times, Case May, Jordan Wayment and Chantil Hughes twice each. That is the question the single status field could never answer. A COUNT THAT LOOKED WRONG AND IS NOT, recorded so it is not re-investigated: this project previously recorded eight batches and the import produced nine orders. Both are right - there are eight DATED batches plus one undated group of seven lines. Reversible with scripts/card-order-import/import.ts --undo; UNDO.json is committed and holds 9 order ids and 71 line ids. HER CONDITION, recorded: importing the history was approved on the condition it disturbs nothing already there
- [x] businessCardStatus deliberately left alone by the card-order import (Aug24) — it answers does this person have cards, which one field can do; the new tables answer a different question. Verified by quoting the whole distribution rather than the one number that would prove nothing: NEEDED 277, RECEIVED 118, NOT_NEEDED 62, totalling 457, identical before and after, with NewHire still at 457 rows and BusinessCardVariant still at 1. The import script contains no update or upsert call at all, only creates and the two deletes in its undo path
- [ ] The card-order importer carries three latent name-matching traps, none firing today (found Aug24) — worth knowing before anyone re-runs it or extends it, and each checked against live data rather than assumed. The alias map rewrites the key BEFORE the lookup rather than falling back to it, so if a real Rich Vance is ever hired his orders would be attributed to Richard Vance and his own exact match never consulted; the roster contains exactly one name matching Vance today, so it is latent. The roster is keyed into a Map with last-write-wins and no orderBy, so two employees with the same name would collapse into a coin-flip attribution with no warning; measured now at 457 rows, 457 distinct normalised names, 0 duplicates. And NOT_A_PERSON is inert - it gates an unmatched array that is declared, pushed to and never read, so adding a name to it changes nothing while reading as though it controls behaviour. South Valley Regional got its null hire from the ordinary unmatched path, not from that set
- [ ] Card tier is not captured yet (found Aug24) — the order sheets distinguish Normal Inexpensive from Executive Expensive and carry the printer routing, art to ogdenlitho with the employee copied and Michael and the department leader always copied, but the DataStatus tab does not, so every imported order has a null cardTier. Fill it going forward rather than guessing backwards
- [ ] The after-order steps stay in the SOP for now, by her decision (Aug24) — and it is not merely deferral: Order Received By and Date Distributed are columns in the workbook and are empty on all 71 rows, so there is nothing to import and nothing to migrate when they are built
- [x] RECORDED on where card orders belong (Aug24, from her) — a person's page shows ONLY that person's card orders. A roster-wide table of everyone's orders on somebody's profile is clutter; that view belongs on the Business cards page. And the card section rolls over to EMPLOYEES, which is where reorders actually happen - a new hire will nearly always read never ordered, so the tracker earns its place later, when someone changes title or fleet
- [x] RECORDED on the new hire page mock (Aug24, from her) — STANDING: the mock must not restyle anything that already exists on the live site. The Journey, Travel and Business card panels are to be reproduced exactly, and only what she has asked to change may change. The bottom half is FOUR TABS - Checklist, Travel, Business cards, History - and the checklist is everything you tick off, on one tab. The offer belongs on the checklist ONCE, as the stepper with the tick boxes and the start date, not as a second set of plain rows. Travel must open on the newest trip expanded and older ones collapsed. Adding a checklist item must offer both just this person and everyone
- [x] The onboarding page rebuilt to the layout Aimee approved (Aug24) — journey pinned to the top, three collapsible detail sections on Sweet Blue at 20% with the standard gold hover, and the checklist behind one tab per stage with the next outstanding item called out above it. Same fields, same tasks, same two Front send buttons; the page just stopped being one long scroll. HER DECISIONS, recorded so they are not relitigated: the sections are Sweet Blue #a6c9e7 at 20% with the standard gold hover outline, which is her pick after rejecting a gold tint; the Journey band must stay EXACTLY as it is live, the one part she did not want redesigned; and the bottom half of the page, meaning the next-action bar, the stage tabs and the checklist, may still change, while the top is settled. VERIFIED AT COMMIT TIME that the rewrite did not silently revert the contacts-link send button committed into this same file two days earlier: both Front buttons survive and are rewired into the new checklist. CONFIRMED ON SCREEN BY HIM (Aug24), after the bottom half was rebuilt: he reloaded a hire and reports the page is correct. Worth recording rather than leaving as a caveat, because the caveat was load-bearing twice in one day - this entry originally read as though the whole approved design had shipped when only the top half had, and that is what sent him looking for a bug that did not exist. The four arbitrary Tailwind classes did compile, but nobody has watched a section open or a hover glow
- [x] Indoc and training dates are enterable on the hire (Aug24) — four nullable columns added to NewHire. Indoc still defaults to the travel booking and the new columns only override it, labelled from travel or typed in so the two are never confused. Training is fully manual, and the reason is measured rather than assumed: there is exactly ONE TRAINING-purpose trip in the whole database. SMALL CORRECTION to how that was first put, from checking it here: that trip is not dateless, it carries a requestedReturn; what it carries no value for is any indoc or training date, which is the part that matters. The positive control is the three ORIENTATION trips, which DO carry indocStart, indocEnd and orientationDate, so the nulls on the training trip are a real absence rather than a query that asked the wrong thing. RECORDED FROM HER: dates fed from travel have to be manually fillable too, and she is not worried about the hires currently missing them, she will fill them in later
- [x] Birthday is editable at last (Aug24) — the column existed and 246 of 457 hires already had one, but it was missing from the PATCH allowlist, so nothing could ever write it and no page showed it. Re-checked against the live database at commit time, including that the round-trip test the work used was properly restored: the four new columns are populated on 0 of 457 rows and birthday is still on 246, unchanged
- [~] The previous layout is parked at /people/<id>/classic, working rather than a screenshot, for Hannah to review before it is deleted (Aug24) — RECORDED FROM HER: it is kept only until Hannah confirms she does not want anything off it, then deleted. Delete the route, NewHireDetailWorkspaceClassic.tsx and the link at the foot of the new page together; nothing else depends on them
- [ ] prisma/schema.postgres.prisma is stale and should not be left half-true (found Aug24) — verified here rather than taken on trust: seniorityNumber, orientationNotNeeded, coInterviewersJson and FeedbackImage are all present in schema.prisma and all absent from schema.postgres.prisma. Its generator still exists at scripts/create-postgres-schema.ts but nothing in package.json references it any more, so it cannot be regenerated by any documented command. Either wire the script back up or delete the file
- [x] The contacts link sends itself from the checklist now (Aug 22, asked for) — a Send contacts button on the Send new hire contacts link row of a new hire's checklist, next to the one that already sends the onboarding email. Preview first, then confirm; the send is what ticks the task, so the tick always reflects something that happened. The body still comes from the Front template SkyShare New Hire - Contacts Link at send time, so HR keeps owning the wording. WHAT THE APP ADDS IS THE LINK: the current share token is read from the database and written over whatever link the template carries, which retires the whole rotation problem two entries below - a rotated token can no longer strand the copy pasted into the template, because that copy is replaced on every send. Goes to the SkyShare address first and personal only as a fallback, the reverse of the onboarding email, which goes to the personal one because the company mailbox does not exist yet; by orientation day it does. REFUSES TO SEND FROM A DEV MACHINE: .env.local points the site at localhost and also holds a real Front token, so it would otherwise email a real person a link only that laptop can open. Three separate failure paths rather than one, because a bookkeeping failure after the message has left must never be reported as Send failed - that reads as nothing went out and invites a second real send; once the email is gone every outcome is a success carrying a warning. The send-guard mode is asked for and recorded, so a redirected test send is not later mistaken for a real delivery. CONFIRMED WORKING ON THE LIVE SITE (Aug 22) - he clicked it for one hire after the deploy and every branch the code could only assert before is now evidenced in the database: the send record holds mode production, so the guard correctly identified prod and did not redirect; it went to rpatrick@skyshare.com, the COMPANY address, which is the deliberate reverse of the onboarding email and the branch most likely to have been wrong; Front returned both a conversation id and a message id; sentBy resolved to a real signed-in actor rather than null; and the task ticked to DONE 214ms after the send, moving the counts from TODO=6/DONE=0 to TODO=5/DONE=1 with NA untouched at 210. No warnings fired. THAT WAS A REAL SEND to a real new hire, not a dry run - he has the contacts link now, ahead of his orientation day, which was a deliberate test rather than a change to the day-of rule
- [x] The contacts task is on all 216 existing checklists, not just new hires (Aug 22) — CORRECTS the entry below it, which said new hires only and reasoned that hires already in the system could just be emailed by hand. That was right about the risk and wrong about the consequence: the hire detail page renders the STORED task rows, so with no row there is no button, and the feature was unreachable for every one of the six people currently onboarding. Backfilled with the dry-run then small-batch then rest pattern - 6 ACTIVE first, verified, then the remaining 210. The earlier reasoning survives intact in the status rule: archived and post-onboard hires get NA, not TODO, so a brand-new task never appears as falsely outstanding work on somebody who finished months ago. NOBODY IS MARKED DONE - unlike the background-check backfill there is no other field implying this step happened, since the link was only ever sent by hand with no record, so a DONE would be a guess presented as a fact. Evidence, with the positive control first: 25 distinct task keys exist of which 19 hold 202 rows each, contacts_link_sent held 0 before the run and 216 after (NA=210, TODO=6), and orders 18/19/100 hold 202/137/20 rows both before and after, which is what shows nothing was renumbered. Order 20 was empty across all 216 hires, so nothing had to shift; the script re-checks that every run and stops rather than creating a tie. UNDO.json carries all 216 ids and accumulates across batches
- [x] A send in flight no longer traps you in the send dialog (Aug 22) — Modal.tsx had already fixed this once and says so: busy used to disable the backdrop, and with no X and no Escape either, a dialog mid-save could not be closed by any means at all. The onboarding send button then re-introduced it one level up with an early return in its own close(), which is exactly the hung-request case it matters for. Removed from both send buttons, with a comment saying why so it does not come back a third time. The double submit is still guarded where it belongs, on the Send button
- [x] Rotating the contacts link now tells you the Front template needs updating (Aug 22) — SUPERSEDED THE SAME DAY by the checklist send above, which injects the current link on every send; the confirm and the reminder no longer send anybody off to update the template, since leaving that instruction would have taught them the app does not handle it. What rotating still breaks, and what the message now says instead, is real: new hires already holding the old link have lost it, and the pasted copy is still dead for anyone who composes that template straight from Front rather than from the checklist. Original entry follows. — the link is pasted into the SkyShare New Hire - Contacts Link template in Front, so rotating kills that copy too, and NOTHING noticed: the template would keep sending a dead link and the first sign would be a new hire saying it did not work. The two-step confirm now names the template, and a reminder stays on screen after rotating with the new link beside it. ASKED FOR BY THE RIGHT QUESTION - he asked whether I would tell him when the token rotates, and the honest answer was no, not reliably: I only exist inside a session, nothing watches the token, and another admin could rotate it without me ever knowing. A promise that depends on me being present and remembering is not a mechanism. This puts the reminder where the action happens, so it fires for whoever rotates whether or not I am in the room
- [x] Sending the new hire contacts link is a checklist item now (Aug 22, from the user) — a new Send new hire contacts link (day of orientation) task in the ORIENTATION group, so the day-of hand-off is prompted rather than remembered. Added in all three places a task has to exist to behave: the task list, the milestone short labels, and the CSV label-to-key aliases. NEW HIRES ONLY, BY HIS OWN REASONING AND IT IS THE RIGHT CALL: onboarding tasks have no backfill - rows are created when a checklist is STARTED, unlike the post-onboard check-ins which do backfill on every page load - so a new item reaches only hires whose onboarding begins after it. He pointed out he can simply send the email by hand to the six already in the queue, which is true and avoids writing a new TODO onto twenty-nine existing checklists. Backfilling would also have been wrong for the twenty-three post-onboard hires, who are past orientation and would have shown it as falsely outstanding
- [x] A Front template for the contacts hand-off (Aug 22, from the user) — written for him to paste into Front rather than added to the repo, because orientation templates live in Front and the app fetches the current body at send time specifically so a stored copy cannot drift. It uses the same delete this part instruction convention the existing orientation templates use, and its standing instruction is to COPY THE LINK FRESH from Settings then New hire contacts on each send rather than baking it into the template - a stored link silently dies the moment anybody rotates it, and the first sign would be a new hire saying it did not work
- [x] The new hire contacts link is no longer public (Aug 22) — /welcome and /api/contacts/vcard both now require an unguessable share token, generated on first visit to Settings then New hire contacts and rotatable from there behind a two-step confirm. Anyone you send the link to can still open and forward it with no login, which is the point; what changed is that typing the bare URL no longer returns the curated staff directory of names, titles, phone numbers and email addresses. BOTH ENDS, not just the page - the vCard route is separately reachable and is the one that actually returns the data, so gating only /welcome would have been theatre. Rotating invalidates every link already sent and writes an audit entry, since a new hire reporting a dead link turns into who rotated it and when. BREAKING: any /welcome link sent before this deploy is dead and must be re-sent. Verified over HTTP against both a dev server and a real production build - no token returns zero contact strings and a 404 from the vCard route, a good token returns the page plus 29 vCards, a keyed request returns exactly 1, and after rotating the old token 404s while the new one succeeds. The gate was re-read at commit time and fails CLOSED in both directions that matter: nothing presented AND nothing stored both serve nothing, so a missing row cannot open the door. MEASURED AND WORTH KNOWING: notFound() thrown from inside a rendered page does NOT produce a 404 status here, in the production build either - the root layout starts streaming before the page throws, so the response is 200 carrying Next's NEXT_HTTP_ERROR_FALLBACK marker. A route that does not exist at all still 404s properly. No data leaks either way and a robots noindex tag carries the indexing protection instead. NOT VERIFIED: the rotate button was never clicked, only its endpoint POSTed directly
- [x] RECORDED on the public contacts link (Aug 22, from the user) — he chose ONE standing rotatable token over per-orientation-session tokens or a stateless signed link, because the link goes into the orientation email once and must not expire and strand a new hire mid-onboarding; rotation is the escape hatch if it leaks. He also accepts that the link stays FORWARDABLE by design - anyone he sends it to can pass it on. The property he wanted is unguessable and revocable, not restricted to one recipient. TIMING, decided Aug 22 and it overrides the assumption above that the link goes into the orientation email: THE LINK IS NOT TO BE SENT UNTIL THE DAY OF ORIENTATION, and it must NOT go in the welcome email, because a hire who has been offered and welcomed may still never start and he does not want staff phone numbers and addresses in their hands before then. Checked at the time of the decision and the link is in NO template - not the welcome email, not the orientation invite, not the SOPs - so nothing had to be removed. DO NOT add it to one. Handing it over is a day-of, by-hand step
- [x] Post-onboard filters, closing the other half of a Jul 22 request (Aug 20, from feedback) — the sort by newest start date shipped Jul 28; the filters asked for in the same breath did not. Search across name, position and department, plus department, employment status, and an outstanding-check-ins-only toggle, with a live count and a Clear. Filtered in the browser because the cohort is tens of rows and already loaded, and a round trip per keystroke would lose optimistically toggled check-in state. Select-all is scoped to VISIBLE rows on purpose: a bulk archive reaching rows a filter is hiding cannot be undone
- [x] Social media announcement is a check-off column (Aug 20, from feedback Jul 22) — no migration needed; the post-onboard columns are driven by MAINTENANCE_TASKS and createMany with skipDuplicates backfills the rest, so adding the entry gave all 23 current post-onboard hires a row on the next page load. Appended rather than slotted in, since stored rows carry an order of 100 plus index. ONE CONSEQUENCE WORTH KNOWING: auto-archive fires when every check-in is done, so it now needs five rather than four  ASKED AND ANSWERED Sep02: it sits LAST, after the 90-day check-in, and he wants it left there - his words, we can leave it as is, it doesnt matter really. Worth recording because the position looks like an accident and is the obvious thing for somebody to want to tidy: it was appended rather than slotted in date-order to avoid renumbering existing rows, and that reasoning turned out to be moot anyway, since nothing sorts by the stored order column and the display order is simply MAINTENANCE_TASKS in code. So moving it IS a one-line change with no migration - it is just not wanted. Do not re-raise it.
- [x] Travel reimbursements owed now alert on the new-hires page (Aug 20, from feedback Jul 28) — the ask was to be told, so nobody is left unpaid. TravelItem already carried selfBooked and a reimbursement state, so no schema change was needed. THE TRAP, and it is the useful part: every outstanding reimbursement in the live database is on a CANDIDATE recruiting-visit trip, not a new hire, so the obvious query scoped to new hires reported nothing while money was owed. Alerts now cover both and link to the right page for each. Deliberately not scoped to active or recently-onboarded hires either, since reimbursement falls due after a trip and would age off the board unpaid. Measured whole-scope: 15 TravelItem rows, 4 owed totalling 871.85 dollars, all four on candidate trips
- [x] A pilot seniority NUMBER, entered by hand (Aug 20, from feedback Jul 28, scoped by the user) — the number lives in Paycom and there is no integration, so this is a field to type into rather than anything derived. New nullable NewHire.seniorityNumber, kept distinct from the existing seniorityDate: the date is the anchor HR seniority is computed from, the number is the position on the pilot list. Editable on the hire profile as Seniority # (Paycom), and an optional Seniority # column on the Employees list that sorts ascending with blanks LAST, since 1 is the most senior and a pilot with no number recorded must not lead the column. An empty box clears to null rather than storing zero, because not recorded and number zero are different answers
- [x] A hire can be marked as not needing an orientation (Aug 20, from feedback Aug 19) — a current employee moving roles has already attended one and is going to indoc instead, so they sat on the orientation page forever looking like an oversight. Two changes, because the user had already recorded it one way: a checklist item marked N/A now counts as handled rather than outstanding, which was her own suggestion, AND a new NewHire.orientationNotNeeded flag set by a Not needed button on the orientation page and undone by a checkbox on the hire profile. Exempt hires drop out of the cohort grouping too, or the same confusion returns one screen over. Verified whole-scope by listing every ACTIVE hire and why each does or does not show: all six are correctly hidden, including the exact person reported, whose attended_orientation was N/A and whom the old filter counted as outstanding because it excluded DONE only
- [x] Seating somebody on an orientation now writes the date onto their own record (Aug 20, from feedback Aug 19) — moveAttendee already did this, but creating a session with attendees and adding one to an existing session both did not, so a hire could be on a session with a blank Orientation date on their profile and on the new-hires grid. All three paths now call one shared function, so the next path added cannot forget it
- [x] RECORDED from the feedback round (Aug 20, from the user) — SkyShare keeps a pilot seniority NUMBER in Paycom; he wants a field for it and will type the numbers in by hand, and no Paycom integration is being asked for. The Email button asked for on a person's page (feedback Jul 2) is PARKED at his request: he wanted to send something he was looking at to the hiring manager but does not fully remember what, so do not build or guess it and leave that feedback item open until he says
- [ ] Group and Drive access packet - DESIGNED, APPROVED and PARKED BY THE USER (Aug 18) — turns the single Add to groups and drive checklist item (key groups_drive, which exists today in lib/onboarding/tasks.ts) into a role-driven list the app resolves from the hire's own record. His words: he has the design and the worksheet and will return to it when able. NOT cancelled and not blocked on us - DO NOT START BUILDING IT UNPROMPTED. DECIDED: the UI is the inline expanding sub-checklist (option A) - the existing task row gains a disclosure triangle listing the resolved groups and drives, each individually tickable, and the parent task completes when the last required line is ticked, so groups_drive becomes a count rather than a promise. Options B (printable packet sheet) and C (cohort matrix) were mocked and DEFERRED rather than rejected - do not re-propose either as a new idea. Rules match on ATTRIBUTES (department, position text, location, tags), NEVER on job title, and the roster is the reason: 76 distinct position strings across 176 active people, including a real typo pair that is live in the data right now - Maintence Control and Maintenance Control, one person each, both confirmed present - which a title lookup would silently miss. Conditions within one rule are AND; two rules on one group are OR. THE APP GETS NO GOOGLE ADMIN API ACCESS - every line is suggest-and-tick, never write. Design spec at https://claude.ai/code/artifact/031f3177-a48f-40fb-b018-ade09643f34b and fill-in worksheet at https://claude.ai/code/artifact/9682f2a5-fdba-4fa2-9c41-802c39f4cc91
- [ ] Blocked on the user returning the filled group worksheet (Aug 18) — 32 of the 87 groups need his answer; the other 55 are pre-filled and only need confirming. A grep of the published worksheet returns 87 addresses, 87 unique, so every group in his pasted list is present exactly once. Until it comes back there is nothing to build against and no other blocker
- [ ] THREE ROSTER FIGURES IN THE INCOMING DESIGN WERE WRONG and two of them would have produced rules that miss people (found Aug 18, re-measured read-only) — the design cited ACTIVE 176 and TERMINATED 264 against 457 total rows, which leaves 17 unexplained: there is a THIRD status, CONTRACT 17, and 176 plus 264 plus 17 is 457. It also listed 14 departments summing to 174 rather than 176 - Sales and Brokerage (2 people) is missing from the list, so a rule set built from it silently under-assigns those two. And distinct position strings are 76, not the roughly 90 quoted. The attribute-matching ARGUMENT survives all three, since 76 is still far too many to enumerate by hand, but the figures are corrected here because they are the numbers any rule set gets built from. The tag counts and the typo pair were both exactly right
- [x] ANSWERED - contractors get the SAME groups as a staff hire, with the row flagged (Aug 18, from the user) — his choice of four options. The rules resolve identically, because they match on department and position text and employmentStatus is not one of the attributes they look at, and the resolved line is labelled contractor so whoever ticks it in Google Admin sees it and can skip anything they judge inappropriate. That keeps the judgment at the point of action, where it already lives, rather than encoding a policy the app cannot know. AND I OVERSTATED THE URGENCY WHEN I RAISED THIS, which is worth correcting rather than quietly dropping: I said it needed an answer before any rule runs and that the default was wrong in both directions. It is not that sharp. All 17 CONTRACT people are stage ARCHIVED and all 17 already have groups_drive marked DONE, so nothing is at risk today and this only decides the NEXT contract hire. WHO THEY ARE: 15 of the 17 are pilots (11 FlightOps, 4 Managed Client), plus one IT Specialist and one in Accounting - so the population is overwhelmingly flight crew, which is why excluding them would have meant a contract PC-12 captain getting no pc12@ suggestion despite flying the aircraft. ONE EVIDENCE CAVEAT that nearly misled me the same way the calendar markers did: their groups_drive completion dates run 2017 to 2026, and the pre-2025 ones are almost certainly the employment-history import defaulting the task rather than anybody ticking it, so they are NOT evidence of a past policy. The single real data point is a contract GV PIC who went through the live checklist and completed groups_drive on 2026-06-12
- [ ] Two group-hygiene observations from the list he pasted, for when he picks this back up (Aug 18) — managersanddirectors@ and managers@ both hold exactly 22 members and may be duplicates, and migration@ and survey@ hold zero members and look like retirement candidates. NEITHER IS VERIFIED against membership: we have the sizes only, never who is in them
- [ ] Journey's own person tags are too sparse to drive group rules yet (Aug 18) — standardscaptains@ has 26 members while only 2 people carry the Standards Captain tag, against a population of 50 captains, so the TAG is almost certainly the incomplete side rather than the group; checkpilots@ is 11 against 6 tagged Check Airman. Every tag in use on the active roster, listed in full as the positive control rather than looked up one at a time: Argus 16, Management 12, Check Airman 6, PDP 6, Executive 4, Standards Captain 2. Tag-driven rules should wait until the tags are filled in, or they will under-assign
- [ ] Terminated employees may hold live Google Group and Drive access - NOT VERIFIED (found Aug 18) — Journey holds 264 terminated records against 176 active. A roster-versus-membership comparison would settle it and comes free with the packet's audit, but it needs a membership export nobody has to hand, and there is no Admin API access and none planned. Flagged because the security value is independent of onboarding. STATED CAREFULLY ON PURPOSE: the recurring pattern of a group being larger than the matching active roster is a SIZE comparison only and is NOT evidence of stale membership - departed crew is one hypothesis, and chief pilots, check airmen, schedulers or maintenance controllers needing type bulletins fit the same numbers equally well
- [x] RECORDED from the access-packet design work (Aug 18, all from the user) — SkyShare has 87 Google Groups, many of them OLD, and a large share exist only so somebody could have an address without paying for a Google account, so much of the list is aliases and shared inboxes rather than real people groups; he expects outdated members and is not troubled by that. THERE IS NO all@skyshare.com - the all-company list is company@skyshare.com (184). THERE ARE NO DEPARTMENT-WIDE GROUPS: flightopsteam@ has 4 against 77 people in FlightOps, accounting@ 1 against 10, fbo@ 3 against 22, hr@ and safety@ 1 each. An early mock-up invented flightops@, maintenance@ and fbo-svr@ - THEY DO NOT EXIST, do not code against them. The seven live aircraft-type pilot groups are pc12@ 38, cj2@ 27, g200@ 22, g450@ 21, xl@ 20, p100@ 9, m2@ 4, plus legacy@ 2, and the all-pilot group is pilot@ 86. fraxowners@ 120 and openjet@ 159 are CUSTOMERS, not staff, and must never be suggested to an employee by any rule. ANSWERED: dual-typed crew DO belong to both aircraft-type groups, so M2 Captain and PC-12 Captain goes in both m2@ and pc12@, which a position-contains rule already handles. ANSWERED WITH A CHANGE COMING: FBO Maintenance staff (3) belong in the maintenance group alongside the 27 in Maintenance today, but he expects to split them out soon - write it as TWO rules, department is Maintenance and department is FBO Maintenance, never one broad contains, so the future change is deleting a rule rather than rewriting a matcher, and the audit then flags those 3 as unaccounted-for, which is the removal list. A G200 First Officer gets exactly three groups today - company@, pilot@ and g200@ - confirmed as the canonical example. Group and Drive membership is done entirely BY HAND in Google Admin and he will keep doing it manually; he does NOT want Admin API write access set up at this time, possibly later, explicitly not now. He considers group membership part of the already-completed pre-onboarding rock, EXTENDING it rather than reopening it - that rock stays complete and must NOT be flipped back
- [ ] Journey visual: real airframe silhouette per aircraft type — the moving aircraft on the journey should be a recognisable TOP-DOWN silhouette of the type that pilot actually flies, keeping the flat icon look (emoji-simple, not a detailed illustration) in the navy + gold system. Reference artifact: https://claude.ai/code/artifact/d83990cf-b221-4938-a94b-50c33298aba5 — it defaults to "match what they fly" (silhouette comes from the pilot's CURRENT role) with an option to force one type. GAP: that study only draws Pilatus PC-12, Gulfstream G450, and a generic bizjet, so the rest of the fleet needs drawing. FULL SET WE HIRE FOR (active in the fleet registry): Pilatus PC-12, Phenom 100, Citation M2, Citation CJ, Citation CJ2, Citation 560XL, Gulfstream G200, Gulfstream G450 & GV. ALSO NEEDED for history, because a journey shows past roles on retired types: PC-12 NG / NGX, Phenom 300, Citation 560XLS+, plain G450, Legacy 650. Sensible shortcut — draw one silhouette per FAMILY and map the variants to it: PC-12 (incl. NG/NGX), CE-525 family (M2 / CJ / CJ2 are near-identical), 560XL family (incl. XLS+), Phenom (100/300), G200, G450-GV, Legacy 650 — roughly seven shapes covers everything, with a generic jet as the fallback for anything unmatched. Keep the non-pilot treatment from the study (eden-blue briefcase dot, no airframe) and the single-posting case (a small holding-pattern loop rather than a route). Pairs with the Great-circle route item below. Added Jul 21.
- [ ] Employee journey visual: adopt the "Great-circle route" treatment (option 3 of the motion study) — rebuild the Role journey timeline on the employee profile as sample 3 from the study. Artifact: https://claude.ai/code/artifact/5338e264-8dee-47f9-b7cd-a5fcbfe766f7 (the user picked option 3). WHAT IT IS: a flight-tracker map — each posting is an airport (a circular node) on a faint dotted-graticule background, curved great-circle legs connect them, and a dashed "planned route" fills in solid gold behind a jet that banks along each leg. Node states: the CURRENT role is a larger navy circle with a gold ring and a soft gold glow; an FO-to-Captain UPGRADE node is gold-tinted; earlier postings stay quiet grey. Under each node: role title, aircraft, a status pill (Hired / Upgrade / Promoted / Active), and the date. ALSO WORTH KEEPING from the study: a jet only makes sense for flight crew — for support staff use the same treatment with a quieter eden-blue travelling dot (or briefcase glyph) instead of the gold jet, so nobody is pretending they fly. It already follows the design system (4px chips, navy + gold) so it drops into the existing Journey at SkyShare panel, and the study honours prefers-reduced-motion by freezing at the end state. Added Jul 21.
- [ ] Pilot flight-activity tracker — where they fly, how often, hours per aircraft (needs the JetInsight link FIRST, added Jul 28) — the user's idea: once the app is connected to JetInsight (the flight-ops / scheduling platform that holds the actual trip and leg data), turn each pilot's real flight history into something the pilot and the team enjoy looking at. WHAT IT SHOWS, from the user: where each pilot goes (the airports and routes they fly most), how often they fly, and hours on each aircraft they are current on — plus room for the "anything else fun" cut he asked for: most-visited airport, longest leg, furthest destination, new airports this year, a route map / heat-map of everywhere they have been, night vs day, busiest month, a per-pilot yearly recap. THE JETINSIGHT INTEGRATION IS THE PREREQUISITE and does not exist yet — that connection is the gating first step, and the first real question: does JetInsight expose an API or export for COMPLETED trips and leg times, and can each leg be keyed to a PERSON (the crew assignment) so hours land on the right pilot and not just the tail. Decide the connection the same way as the other external feeds (an official API preferred over an export or scrape); note it shares nothing with the Front inbound-email pipe — this is operational flight data from a different system, so it is its own slice. BUILDS ON WHAT EXISTS: the employee record + role journey already know which aircraft a pilot flies and their seat, and the fleet registry has the airframes; this adds the ACTUAL FLOWN hours and destinations on top, so it pairs with the Role-journey / Great-circle route visuals above (a pilot's career timeline gains a "where they actually flew" layer) and could feed the flight-hour metrics that today come only from resumes. SCOPE NOTE, flagged not blocking: ongoing operational tracking of hired crew has been treated as other departments' territory before (see the crew-compliance-out-of-scope decision) — this is the user's explicit request and is analytics / fun rather than compliance, but confirm the boundary before building (is this people-ops celebrating flight activity, or does it stray into what Ops owns). DECISIONS FOR LATER: whose data is visible to whom (a pilot seeing their own vs a team-wide leaderboard — a privacy call), how far back to import, refresh cadence, and which "fun" stats earn their space. Not now — captured so it is ready the moment the JetInsight link is on the table.
- [ ] I-9 reminder, due within 3 days of start — the system should automatically raise a reminder to complete the new hire's I-9 by 3 days after their start date. This is a legal deadline (federal rule is Section 2 within 3 business days of the first day of work), so decide business days vs calendar days, whether it becomes a real checklist step (there is no I-9 item on the 19-step list today), and who gets nagged when it is close or blown. Added Jul 21.
- [ ] Push anniversaries AND birthdays to the shared celebrations calendar — create recurring ANNUAL calendar invites on the birthdays/anniversaries calendar for (a) each employee's start-date work anniversary and (b) each employee's birthday. Today Celebrations only surfaces these inside Compliments; this puts them on the actual calendar so they show up without opening the app. Decide: which calendar/account owns the events, all-day + recurring-yearly, who is invited vs just visible, how a date change or a leaver is synced, and birthday privacy (we hold dates-only). Added Jul 21.
- [x] Onboard someone a SECOND time — rehire, or a department move (Jul 30) — until now a person had exactly ONE onboarding checklist, so there was no way to run a former employee back through it, or to give someone who changes departments the fresh checklist that move deserves. Now: a "Start new onboarding" button on the profile of anyone past onboarding (including a former employee, where it reads "Rehire") FREEZES their current checklist into a read-only archived round on the same profile and builds a clean one, then puts them back on the New-hires board. Pick a reason — Department change / Role change / Rehire / Other — and it sets sensible carry-over defaults for what does NOT need doing again; every one of the 21 items is a checkbox you can override. Defaults lean cautious on purpose: a background check and drug screen only carry over for a move INSIDE the same department, never for a rehire. It also records the move on the role journey (skipped automatically if someone already recorded it by hand), and a rehire closes the old employment period and opens a new one from the return date so TENURE still counts the original service. An Onboarding history panel shows every previous round in full — each item, its status and the date it was actually completed — and the most recent one has an Undo that restores the checklist, the profile fields and the role/employment rows exactly. Verified with 42 checks across department-change, rehire and guard cases on throwaway records, torn down clean. KNOWN GAP, deliberate: the offer stepper on the linked CANDIDATE record still shows the previous offer and is not reset, so a second offer is tracked on the checklist only. First real use is Nick Lembo (OGD Base Support in maintenance to a PC-12 First Officer PDP seat in flight ops). FOLLOW-UP, same day: an archived round is now EDITABLE, because the first real use exposed that a frozen copy can be honestly wrong. An archive captures whatever the checklist said the moment the new round started, so anything ticked that day to catch the record up got frozen with today's date — and worse, the round labelled itself with the job it was replaced by, since the new title was already typed onto the profile before anyone pressed the button. Edit dates on a round now opens the whole thing: the role and department they actually held during it, the start / offer sent / offer signed / orientation / onboarded dates, and per item its completion date and status, plus a fill-every-blank-date helper for reconstructing a round by best guess. Un-ticking an item drops its date with it, and a done item is allowed to have no date at all (nobody remembers). What the system did — which items the round contained, its sequence, who archived it and when — stays put. If the round is still undoable, the corrected values are what an Undo restores. 18 checks on throwaway records.
- [x] Marking a hire's business card N/A sets the card status to "not needed" (Jul 21) — setting the "Order business card" checklist item to N/A now moves that person's Business cards status to "not needed", so they stop sitting in the outstanding list forever. The open question from the original note is decided: un-setting N/A DOES put them back to "needed". Deliberately narrow — it only ever moves between NEEDED and NOT_NEEDED, and never touches ORDERED or RECEIVED in either direction, because a card that was ordered or has physically arrived is a fact N/A on a checklist should not erase (and those people are not in the outstanding bucket anyway, so there is nothing to fix there). Applies to both places a checklist gets edited (the person's detail page and the grid — they share one endpoint) plus the CSV hire import, which can carry N/A too. Still one-way from the checklist: changing the card status on the Business cards page does NOT write back to the checklist. Verified on throwaway fixtures, 7 cases including all four no-op ones, torn down clean. ON THE BUSINESS CARD ROCK (finish by Aug 22).
- [x] Pre-onboarding tracker (Jun 11) — replaced the Google Sheet; imported the 34 current hires from CSV; per-hire detail with the universal checklist; lifecycle moves hires Active → Post-onboard → Archived
- [x] Self-service hire import (Jun 23) — "Import hires" button on Pre-onboarding: paste rows straight from the roster spreadsheet (or upload a CSV), it maps the columns (Name, Offer Sent/Signed, Start, Orientation, Phone, SS Email, Personal Email, Position, Department) onto each profile, previews who will be added. Each imported hire gets the standard checklist.
- [x] Hire import: real-sheet layout + upsert (Jun 30) — the importer now auto-detects the actual SkyShare "Pre-Onboarding Status" layout where field names run DOWN the first column and each person is a COLUMN (it was reading field labels as names). Handles the Archived sheet's extra Role Tag/Owner columns and blank gaps. And it now UPSERTS instead of skip-only: matches a person by name/email, updates just the dates/info that changed (never wipes a value the sheet leaves blank), leaves unchanged people alone, and adds anyone new. Verified against both real CSVs (Main 16 + Archived 72, zero mis-parses).
- [x] Hire import: sync the checklist too (Jun 30) — the import now also reads the sheet's task rows (Verbal offer, Background check, Attended orientation, etc.) and sets each hire's onboarding checklist: TRUE -> done, FALSE -> to-do, N/A -> n/a, blanks left untouched; any other note counts as done. Idempotent (only flips tasks that differ) and folded into the add/update/unchanged result with a "N checklist items updated" count. Handles the Main sheet's renamed "Titan" row (= EBCO).
- [x] Bulk actions on the people tabs (Jun 23) — multi-select on Grid, Post-onboard, and Archived with a contextual bulk-action bar: select people (per-row checkbox + select-all; per-column on the Grid) and Mark onboarded / Archive (with confirm) / Restore, plus bulk Set orientation date and Set department. Backed by POST /api/new-hires/bulk (mirrors the single-hire stage logic).
- [x] Copy hire info for an email (Jul 27) — the Checklist grid gained a "Copy for email" button beside Manage tasks: it puts the ticked people's details on the clipboard (or everyone's, if none are ticked) as one block per person read TOP TO BOTTOM (Name, Start Date, Phone, SkyShare Email, Personal Email, Position, Department), which is the shape that actually goes in the message. Selecting the grid by hand copies the other way (a task row across every column) because each person is a column, so this was the missing piece. Copies rich HTML with bold labels plus a plain-text fallback; order follows the grid, not tick order. Shipped in commit c3a76c0. Caveats: the formatter's output was checked directly against real grid data, but clipboard behaviour is not verifiable from the Browser pane here, so the click-to-paste path itself is untested in-app. First cut hid the button behind a selection and nobody could find it — it is always visible now. Autofill-and-send the whole email was the other option on the table and was NOT built; still open below.
- [ ] Autofill + send the new-hire info email — instead of copy/paste, have the app compose and send the message itself (Front, like the onboarding-journey send). Needs the recipient list, subject, and body wording from Aimee before it can be built. Added Jul 27.
- [x] People status labels + delete + restore polish (Jun 23) — renamed the confusing onboarding statuses to plain language: "In process" to In progress, "Urgent" to Due soon (starts within 7 days), "Blocked" to Overdue (start date passed). Archived now shows a Restore button on every row (was only terminated). Added permanent Delete to the bulk-action bar on all three tabs (select then Delete, with an explicit "this cannot be undone" confirm) via POST /api/new-hires/bulk-delete — so test/junk entries can be removed for good.
- [x] Merge Grid + Milestones into one tab (Jun 30) — the Pre-onboarding "Grid" and "Milestones" tabs (mostly the same info) were combined; old ?tab=milestones links redirect to it. [Superseded Jul 18: Milestones fully retired — task management (rename/hide/add/remove) now lives in the Checklist grid's Manage mode.]
- [x] Terminated-roster import (Jun 30) — the existing Import hires flow now also accepts a former-employee CSV (First Name / Last Name / Position / Status / Hire Date / Termination Date / Department). Split names combine, Hire Date maps to Start date, and a new Termination date field is stored. Anyone marked Terminated is saved as a former employee (employmentStatus TERMINATED) and auto-archived; existing people are matched and updated in place (never wiped). The preview shows a Terminated column + a count before you commit, and the Archived tab now has a Termination date column.
- [x] Pre-onboarding tabs (Jun 11) — Dashboard (charts: status donut, by department, starts-by-week, progress funnel + needs-attention/upcoming), Grid (frozen-column matrix, click-to-cycle cells), Milestones (10 key milestones + progress), Post-onboard (auto-receives onboarded hires; 30/60/90-day + benefits check-ins with due reminders), Archived
- [x] New hire contacts, picker + master-list pass (Aug 1) — the admin picker filtered on onboarding STAGE rather than employment status, so it held only the ~28 people mid-onboarding: 19 of the 20 curated contacts showed as Unknown employee and none of them could be re-picked. A hire auto-archives to ARCHIVED when onboarding ends while staying employmentStatus ACTIVE, which is the whole bug. The public /welcome page has no such filter and was correct the entire time, which is exactly why this went unnoticed. The pool is now all non-terminated staff (190), and curated ids are pinned into it regardless of status so a contact who leaves is badged Former employee rather than collapsing to Unknown. Adds a Shown/Hidden switch per contact, making the curated set a master list with a per-intake selection on top; a group with no shown contacts drops off /welcome instead of rendering an empty heading, and an absent flag reads as shown so existing configs are unaffected. The single per-group shared slot becomes a manual list holding both department lines and off-directory people, migrating the legacy blob on read. Search over name, title and department replaces a select that was unusable at 190 entries, with an always-present path to add somebody not in the app yet. Verified read-only against live data — 21 visible contacts before and after the migration, nothing written until an admin saves. NOT verified: clicking the toggles or the search box, since the Browser pane cannot read this app's DOM
- [x] The reminder column now shows that it is queued (Aug 1, from feedback) — an armed session drew six empty grey circles in the 3. Reminder column, indistinguishable from a session with nothing scheduled, and the only contradiction was a box two sections below with the internal summary wedged in between. The column header now carries a gold auto-send chip with the date, each pending cell is a dashed gold clock reading queued, the legend explains it, and the scheduling control moved directly under the grid so it sits with the column it governs. Armed state moved up into the panel and is passed to both the grid and the scheduler, which previously fetched it independently and could have disagreed the moment somebody toggled it. The chip formats its date in UTC because sendOnKey is a calendar day — read in Mountain it lands at 6pm the previous day and would have labelled the Aug 3 send as Aug 2, the same class of bug fixed in the travel calendar the day before. Verified against both live sessions: Aug 4 reads Auto Mon Aug 3 with six queued, Sep 1 reads Auto Mon Aug 31 with two
- [x] The orientation reminder can be checked instead of trusted (Jul 31) — the day-before reminder is the only email the app sends unattended, and every way it fails looks identical from outside: a stopped cron, a missing CRON_SECRET, a session moved out of UPCOMING, or a genuinely quiet day all produce no email and no error. Every run now writes a record INCLUDING one that found nothing due, so the ABSENCE of a record for a day is itself the signal that the cron never fired; 20 runs are kept in one WorkspaceSetting row, and recording never throws, so instrumentation cannot break a send. A read-only health endpoint reports problems worst-first — due today with no run recorded, a missing token, an armed session no longer UPCOMING so it can never send, a send day that passed with people unsent. A dry run builds the REAL emails through the same builder the send path uses and shows exactly who would be written to, sending nothing, so Aug 3 is not the first time anyone sees that list. All three surface on the orientation panel, and the problem box survives the session status failing to load. CAVEAT: none of it has been exercised against a real run — it compiles and lints, and the behaviour is unverified until Aug 3
- [x] Travel checklist, per purpose (Jul 31) — each trip now carries what is left to do and who is left to tell: a shared spine (dates, ask the supervisor what to schedule, booked, itinerary sent), a purpose-specific middle, and a shared tail (day-after check-in, reimbursement). The gap list already answered whether a trip was booked; nothing answered whether anyone had told the supervisor, which is where steps were actually going missing. Everything is booked is DERIVED from the existing gap rules rather than ticked, so it cannot claim a trip is booked while the gaps disagree, and reimbursement is one row advancing through five stages rather than four checkboxes saying one thing. The recruiting-visit pack captures who they will meet, locations, what needs a calendar block, extra people, welcome gift and guest — the answers the calendar blocks will come from. Ticks record who and when, since a bare checkmark cannot answer whether anyone actually told the supervisor. An earlier draft ran to 28 items and was heavier than the five trips it tracks. State lives in a WorkspaceSetting rather than a new table, following the orientation precedent — one shared live database makes a migration the most expensive thing here to get wrong. Indoc and Training render LOCKED pending content from the CPO and from Hannah, deliberately visible so the gap is not mistaken for nothing needed. Verified end to end in the browser including a tick persisting and being cleared again; the Indoc and Training locked states are NOT exercised, because no trip uses those purposes
- [x] Travel calendar on the main travel page, per-person colours (Jul 31, from feedback) — a six-week window above the trips table: the Sunday on or before the 1st plus 42 days, so a trip crossing the month line stays whole. Brandon's Jul 31 to Aug 1 visit reads as one run in BOTH the July and the August view, which no calendar month can do. A trip draws as a connected run: dashed capsule on each flying day, 6px rule across the days between, squared off at a week edge so a break reads as continuing. On a chip the time sits on whichever side SLC is on, so DEN to SLC trails its time and SLC to DEN leads with it, and a time in the other city is never shown. The rail gives each traveller a colour band matching the connector's own hue and lists flights, hotel and rental car with amounts, and follows the window. Opens on the current month, not the oldest trip. Travellers with no readable dates are named rather than dropped. LAYOUT IS PINNED AND MEASURED: every day is the same width in every week (minmax(0,1fr), so a long chip clips rather than widening its column; all six weeks share identical column edges and the header matches them), week rows grow with the number of stacked lanes so overlapping travellers cannot spill into the week below, and a 700px grid floor keeps a 100px cell against an 89px worst-case chip with the calendar scrolling in its own container instead of the page. CAVEAT: the multi-lane case has no real data behind it — no week currently holds two travellers — so it was checked by simulating extra lanes at the height the formula allocates, not observed
- [x] Travel dates were drawing a day early (Jul 31) — a date recovered from booking text, and orientationDate, are calendar days stored at midnight UTC, but schedule.ts bucketed every value in Mountain time, where midnight UTC is 6pm the previous day. Auggie's trip drew Jul 15-18 instead of Jul 16-19, Tara's orientation drew Aug 3 instead of Aug 4, and text-dated flights showed a 6:00 PM departure that is nowhere in the data. dayKeyOf and clockTimeOf in lib/dates/display now choose per value; both the hub calendar and the per-person calendar in the trip panel are corrected. This was live and wrong before today, not a bug in new code. Known limit, stated in the source: a genuine departure at exactly midnight UTC would still read as a calendar day, which is judged far cheaper than a schema column to disambiguate
- [ ] Record a flight's ARRIVAL time, not just one timestamp (Jul 31) — the calendar shows when somebody lands here, but only Tara's record actually holds an arrival, from confirmation text saying arrives 08:22 local. Brandon's DEN to SLC is a single bare timestamp with nothing saying which end it is, so the time shown may be the Denver departure rather than the Salt Lake arrival. Parse arrival from confirmation text into its own field
- [ ] SETTLED — the email safety net stays exactly as it is (Aug 2, from the user) — sends from a development machine keep going to the user's own inbox as tests. They were offered a separate dedicated test mailbox, and the alternative of turning non-production sends into unsent Front drafts instead of redirected emails, and declined both, saying to leave it as it is. Do not re-propose either without being asked. Recorded because it is the kind of decision a later session re-opens in good faith, having no way to know it was already considered
- [ ] Email guard configuration, as confirmed by the user (Aug 2) — FRONT_TEST_INBOX is aschaedig@skyshare.com, set in .env.local and in the Vercel PREVIEW environment only. NEXT_PUBLIC_APP_ENV is already set to production in the Vercel PRODUCTION environment, which is the fallback that keeps real sends passing through the guard untouched if Vercel system variables were ever disabled. NO LONGER TRUE as of Aug26 and this sentence is the reason it matters: production now serves NEXT_PUBLIC_APP_ENV staging, proven by fetching the public login page and reading the banner out of the HTML. Email is unaffected TODAY because isProductionEmailEnv reads VERCEL_ENV first and short-circuits on it - but the second layer this sentence describes is not merely gone, it is REVERSED, so if VERCEL_ENV ever stopped being exposed the fallback would now report not-production and either divert every real send to the test inbox or block it outright. Neither value is in the repo, since both env files are gitignored, so this line is the only durable record of them
- [ ] Why the test-email alert exists, in the user's words (Aug 2) — they wanted to be sure that two months from now something meant for the actual user is not going out as a test without anyone realising. The consequence for the daily report is deliberate and should not be softened: a clean day stays SILENT. They did not ask for daily reassurance, and a message that appears every day stops being read, which would fail on the one day it mattered
- [x] The send guard was about to silence the daily report (Aug 2) — the non-production guard shipped Aug 1 does not know the difference between mail to a person and mail to a mailbox, and the daily what-changed report is sent by a scheduled task running on the laptop. So from Aug 2 that report resolved to FRONT_TEST_INBOX with a TEST-tagged subject: it would have reached one tester and the team would have quietly stopped getting it, with nothing failing. Caught before any send, by running the real guard under the real env rather than reading the code. New INTERNAL_AUTOMATION_RECIPIENTS allowlist in send-guard.ts, currently holding only hrotasks@, and a message passes through untouched only when EVERY recipient across to, cc and bcc is on it. DELIBERATELY NOT A DOMAIN CHECK: new hires are issued @skyshare.com addresses and the orientation template mails them there, so treating the internal domain as safe would have disarmed the guard for the exact flow it exists to protect — an orientation email that merely cc's hrotasks@ still gets redirected, because one real person anywhere in the recipients guards the whole message. Matching is on the bare mailbox so Name <addr> and stray casing still match while a lookalike such as hrotasks@skyshare.com.evil.com does not, and an empty recipient list is refused rather than read as all-internal. Production output is byte for byte identical. VERIFIED: 15 assertions over production, preview, local and the unset-test-inbox case, lint clean, tsc clean, and an end-to-end check against the real .env.local resolving to hrotasks@ with an untagged subject. NOT verified: no mail has actually been sent through the new path yet
- [x] Non-production email sends are blocked or redirected (Aug 1) — decided Jul 20 and never built, and it was worse than the note implied: there was NO environment check anywhere in lib/front, and messages.ts said so in its own header comment — there is no test inbox and recipients are real new hires. Local dev and Vercel Preview both carry a real FRONT_API_TOKEN pointed at the live hrotasks@ mailbox, so running an orientation send from a laptop put a genuine message in a genuine new hire's inbox with nothing to mark it as a test. Fixed at the one chokepoint every draft and every send already passes through (payload in messages.ts), so a future caller cannot forget it: outside production each recipient is replaced by FRONT_TEST_INBOX and the subject is tagged with who it would have reached, and if that variable is unset the send is REFUSED rather than dropped — a silently discarded message looks like a successful send, which is how somebody concludes the real flow works. Production is untouched byte for byte. The environment test is VERCEL_ENV rather than NODE_ENV, because a Preview build has NODE_ENV production too and the existing isHostedRuntime() therefore cannot tell preview from production. PRODUCTION DETECTION CONFIRMED WITH THE USER, since a guard that treats unknown as non-production could otherwise refuse a real send: the live deployment sets NEXT_PUBLIC_APP_ENV to production, which the fallback accepts even if Vercel system variables were ever disabled. FRONT_TEST_INBOX is set locally and in Vercel Preview. Verified with 12 assertions across production, preview, local, staging and the unset case, then end to end against the real .env.local — a local send now resolves to the tester's own address with the true recipients in the subject. WATCH THE FIRST LIVE RUN: the orientation reminder cron sends through this same path, so the Aug 3 reminder for the Aug 4 session is the first real send through new code, and it is rock 2's only remaining gate
- [x] A checklist overview on the Travel page (asked Aug 1, shipped Aug 1) — the roll-up the New hires page already had, now for travel: every trip's outstanding items in one panel above the table, soonest first, each expandable in place. COMPLETED TRIPS ARE KEPT rather than filtered out, because the after-the-trip half of the checklist (the day-after check-in and reimbursement) only becomes due once a trip is over, and dropping finished trips would hide precisely the work most likely to be forgotten. Waiting-on items are listed separately and never counted as outstanding, so a trip blocked on the CPO's indoc content does not read as neglected. Run against live data it reports 5 trips and 25 outstanding items, with Tara Ward's Aug 4 orientation at the top still missing five things including the itinerary send
- [x] A Checklists tab on the candidate profile (asked Aug 1, shipped Aug 1) — the eleventh tab, next to Travel because that is what it summarises. Renders the same roll-up component the Travel page uses, so the two can never drift apart, and the tab's count is what is OUTSTANDING rather than how many trips exist — a badge reading 3 when everything is done would be noise, and this one is meant to be a nudge. Loads nothing extra for a candidate with no travel
- [x] Interview retired as a travel purpose (Jul 31) — same visit and same logistics as a recruiting visit, so keeping both meant choosing between two identical options. Zero trips used it so no data moved; an INTERVIEW value from the email parser or an old row normalizes to RECRUITING_VISIT on the way in, and a retired label is kept so an old row still reads as words. The parser keeps INTERVIEW in its own enum on purpose, since an email really can say interview and forcing the model off the word would cost accuracy
- [x] Orientation SOP written (Jul 28) — a step-by-step for running an orientation, docs/sops/05-orientation.html, published at https://claude.ai/code/artifact/51f060eb-2ee2-448f-b745-9a27a87325f5 and added as Chapter 5 of the Handbook. Built by reading the Orientation page code directly and verified label-by-label against it (one read-only verify agent, PASS). Documents the real order: create session -> add attendees -> Create calendar invite (makes the Meet link) -> invite attendees -> send 1. Invitation then 2. Supervisors -> send internal summary -> arm the day-before reminder -> confirm replies / travel -> Mark complete. Two steps are flagged as manual by design: settling the orientation date + exec time slot, and creating the execs' own calendar invite (copying in the same Meet link). CORRECTED A MISCONCEPTION: managers are NOT cc'd on the invitation — the invitation goes to the new hire alone, and supervisors are reached only by the separate "2. Supervisors" send; the SOP says so. Exec-invite roster supplied by the user and verified against the roster: the Executives group (address unconfirmed, likely level10@skyshare.com) covers the five execs — Cory Bengtzen (CEO), Tommy Aoki, Keith Rabin, Harry Mitchel, Jonathan Schaedig — plus standing guests Mike Newton, Ricky Lee, Morgan Langholf, and HR (Aimee Schaedig, Hannah Byers, Kevin Sherman). Executives group address confirmed by the user (level10@skyshare.com) — ZERO placeholders remain in any SOP. Summary recipients confirmed against lib/orientation/email-cc.ts SEED_CC (hrotasks@, Morgan, Ricky, Hannah, Kevin, Aimee). Jul 28 follow-ups: the diagram now carries a colour KEY (blue = you-in-app, green = system-does-it as dotted branches, gold = you-outside-app) per a standing user preference for all SOP diagrams; and step 8 corrected — sending 1. Invitation clears the selection, so you re-select all before 2. Supervisors (verified: OrientationEmailPanel clears selected on every batch send).
- [x] Orientation module (Jun 12) — People > Orientation: create in-person SLC sessions; per-session prep checklist (default template with owners + due-X-days-before, editable); attendees suggested from pre-onboarding orientation dates (add/remove for last-minute changes); per-attendee confirm / travel / iPad (pilots) / credit card / swag; auto headcounts (out-of-town, pilots); editable email-template library with a who's-been-emailed send tracker (manual mark-sent for now, live send later); Mark complete ticks each attendee's Attended orientation
- [x] Orientation cohorts & calendar (Jun 12) — Cohorts tab on Orientation: a mini month calendar marking orientation dates (hire count) + sessions, and cohort cards grouping active pre-onboarding hires by their orientation date with one-click "Create session + add all" or "Add N to existing session" (auto-links hires to the matching session)
- [x] Orientation attendees: suggestions, tentative, move-to-next (Jun 30) — the "Add attendee" picker now suggests active pre-onboarding people who haven't attended orientation yet (so nobody is missed) above the full employee roster. Added a Tentative confirm status (Pending / Tentative / Confirmed / Declined). Each attendee row gets a one-click "Move to next" (bumps to the soonest upcoming orientation) plus a "Move to…" picker for a specific one; if there's no next session, they drop to the existing "still needs an orientation" waiting list on the Orientation overview.
- [x] Reschedule orientation + Mountain Time (Jun 23) — a Reschedule button on the orientation session page changes the date/time (attendees keep their spots). All orientation times now render and are entered in Mountain Time (labeled "MT") regardless of the viewer's or server's zone: a 9:30 AM session is 9:30 AM Mountain, stored as the correct UTC instant (DST-aware via lib/calendar/format mountainWallClockToIso); cohort-created sessions default to 9:00 AM MT.
- [x] Pre-onboarding dashboard drill-downs (Jun 23) — the headline metric cards (Starting in 7 days, Hires with missing items, Needs attention) are now clickable and expand an inline list of exactly those people, each linking to their record.
- [x] Pre-onboarding dashboard widgets (Jun 23) — added four views (existing widgets kept): "Ready for their start?" strip (everyone starting in 3 weeks with % complete + status), "Where it's jamming" (onboarding tasks most often still to-do across active hires), Orientation timing (% scheduled within a month of start, with breakdown), and Travel readiness (booked vs needs-booking vs none among upcoming starters — ties in the Travel module). Longer-term idea on the list: turnover/retention correlated with orientation timeliness (needs cohorts to accumulate).
- [x] Orientation form + grid polish (Jun 23) — widened the New-session Time field so the full "9:30 AM" shows (was clipped); added a Google Maps link next to the session address (alongside the Meet link). The Pre-onboarding Grid now shows the full roster fields like the source spreadsheet — Offer sent, Offer signed, Phone, SkyShare email, Personal email (plus the existing Start/Orientation/Position/Department and the status badge).
- [x] Pre-onboarding flow de-confusion (Jul 18) — a four-reviewer pass on the runbook found the guard rails were backwards and fixed the worst friction before the Aug 1 rock: (1) the two orphan-hire buttons (+ Add new hire and Import hires, which both create a hire with NO candidate link) are now ADMIN-only and demoted to secondary — the New hires page leads with a real "Start from a candidate" link instead. (2) The silent candidate dedup now shows itself: New candidate and Add-candidate-to-job surface a "matched an existing candidate" notice (plus reactivated / already-linked) instead of quietly reusing a record. (3) Start date is an explicit choice — the Move-to-onboarding form and the quick-add modal both require an acknowledged "no start date yet", and the Dashboard has a new "No start date yet" bucket so dateless hires stop vanishing from the board. (4) A "Link to a job" control now lives on the candidate profile's Applied-to tab (new GET /api/recruiting-jobs?q= + a job picker) so an offer can be attached without the detour out to the Jobs page and back. (5) Wording unified toward "onboarding" / "New hires" (dropped stray "pre-onboarding" copy). Runbook Google Doc corrected to match the live screens. Untested end-to-end in prod (role-gating only shows for a real non-admin; local dev is always admin). ENHANCED Jul 20 (from real use — a coordinator hit a job that did not exist yet): when the "Link to a job" search finds nothing, an admin can now Create the typed title as a new bare job AND link it in one step, right in the modal — no trip out to Jobs and back. The confirmation offers an "Open to add details" link to flesh the job out later, and a same-title collision links to the existing job (409) instead of duplicating. Create rides POST /api/recruiting-jobs (jobs:write). CORRECTION Jul 22: this entry used to say jobs:write was ADMIN-only and the create button was hidden for recruiters — that is wrong. lib/auth/permissions.ts:33-38 grants jobs:write to RECRUITER as well as ADMIN, so recruiters CAN create a stub job inline. The open call it described is therefore already settled in the code.
- [x] Test-data teardown for end-to-end testing (Jul 18) — safe delete of test candidates and test login users so a full onboarding run can be reset. Because dev and prod share one live DB, deletion is fenced so real data cannot be removed: a candidate must carry the TEST tag AND an admin must type its exact name (Delete test button on the profile, admin-only) — it cascades applications/offers/notes/etc. and can also remove the onboarding record it produced. Auth users are backend-only: GET /api/users lists accounts with an isTest flag, DELETE /api/users/[id] requires the email to be a +test sub-address or a reserved test domain, blocks deleting yourself or the last admin, and needs the exact email typed. Verified against the live DB (real candidate + real user both correctly refused; a throwaway TEST candidate cascaded cleanly; counts returned to baseline). Markers live in lib/testdata/markers.ts.
- [x] Remove user access / offboarding (Jul 18) — Settings, Team Members now has Remove access per user (admin only, hidden for yourself). Because sessions are JWT and sign-in is domain-gated, a plain delete would not hold (token stays valid; an allowed domain lets them back in as a viewer), so Remove access BLOCKS the email (WorkspaceSetting auth-blocklist, no migration) AND deletes the account: sign-in refuses blocked emails and every access gate (requireModulePageAccess + requireApiPermission/requireApiUser) rejects a blocked live session on its next request. Fenced: admin only, cannot remove yourself or the last admin, exact email typed to confirm. Reversible: a Revoked access section lists blocked emails with Restore access. Verified live (wrong confirm 400, last-admin protected + not blocked, throwaway @skyshare.com user removed + blocked, restore clears it, zero residue). Separate from the TEST-only candidate/user teardown delete.
- [x] People-page cleanup pass (Jul 18) — five improvements to the New hires pages: (1) Dashboard gained an "In onboarding" worklist (every active hire + anyone onboarded in the last ~3 weeks) with a check-off to hide people; the hide is shared across the team and reversible via a "show checked-off" control (stored in a WorkspaceSetting, no migration). (2) Retired the Milestones tab and folded task management into the Checklist (grid): a "Manage tasks" mode renames any task (applies to the grid and every hire's checklist), hides a built-in you don't use, and adds/renames/removes custom tasks; hidden tasks no longer drag the completion percentage; custom adds live in a Custom group and don't count. (3) Post-onboard: the "due" tag now flashes red once a 30/60/90 date has passed, and when all check-ins are done the employee auto-archives (kept as a CURRENT employee, not terminated). (4) Bulk "Mark check-in done" on Post-onboard — select several people, pick a check-in, done. (5) Archived tab gained an All / Current employees / Former (terminated) filter so current vs past stays clear. Verified live end-to-end (auto-archive fired on the final check-in; a throwaway TEST hire archived as a current employee then was deleted; all overrides/hidden/custom test rows removed with zero residue).
- [x] New hire to candidate navigation + supervisor picker fixes (Jul 27, commit fa6daca) — two fixes from real use. (1) A new hire's profile now LINKS BACK to their candidate profile: the resume, pilot application and interview history all live on the candidate record, and the candidate page already linked forward — this is the way back, so nobody re-searches Candidates for their own hire. It shows the candidate's name when it differs from the hire's (those two records disagree more often than you would think) and appears only where a link exists (159 of 453 hires linked; NewHire.candidateId is a plain column, so it is a second lookup, no schema change against the shared live DB). (2) The supervisor picker was offering 27 of 189 current employees — it excluded stage ARCHIVED, which in this schema means "finished onboarding" (a normal tenured employee), not someone who left, so David Ricks, Kevin Sherman and Hannah Byers were invisible. Employment status is now the only filter; all current employees appear. Same stage-filter bug the event attendee picker had (see Events from the inbox above). Verified against the live roster.
- [~] Orientation: live email send — IN PROGRESS (Jul 20): Front API token created and verified live (Enterprise tier, 200 rpm; scopes include Read + Send Messages, R/W Drafts and Conversations, no Delete anywhere). Built lib/front (client with 429 backoff, channel resolution cached in a WorkspaceSetting, draft-first send helpers, inbound ingestion) plus a front-api skill with the endpoint reference and a read-only CLI. BOTH open questions below are now ANSWERED: it sends FROM hrotasks@skyshare.com (the HR automation mailbox, so automated sends stay out of the human hr@ inbox), and the email body lives in FRONT, not the app — the app fetches the template at send time so HR keeps editing where they already work and the two never drift. First template ("Start Your Onboarding Journey", Front template rsp_qmmai) tested end-to-end: draft created from the live template with a personalized "Hi Name," greeting matched to the template's Verdana 9pt markup, cc to hrotasks@, real send confirmed received with NO duplicate conversation. GOTCHA worth remembering: a Front channel's address field is the internal in.frontapp.com routing address — the real sending address is send_as. SHIPPED (Jul 20): the profile button. A "Send email" control sits on the "Send Start Your Onboarding Journey email via Front" checklist row on a person's profile (admin/recruiter only). It opens a preview showing the resolved recipient (personal email first, since the company Gmail does not exist yet at that point), the cc, the From identity, and the live Front template rendered in an isolated frame — then you confirm. The send TICKS the checklist item (forward-only, never un-ticks); the checkbox deliberately does NOT trigger the send, because a checkbox means "this is done", gets mis-clicked, and would risk emailing a real new hire twice. Warns if it was already sent, and records the Front conversation id per hire in a WorkspaceSetting (no schema migration against the shared live DB). VERIFIED IN PRODUCTION (Jul 20): first real send went to a new hire who had just signed her offer letter, from the live site by a real signed-in user. That closes the two gaps this entry used to carry — the not-yet-seen "Send email" state (every hire sampled during the build was already marked done, so only "Resend email" had ever rendered) and whether sentBy captures a real name rather than the null that local auth-bypass produces. Both behaved. Still to do: decide which further templates send, and consider promoting the send record to a real column if it outgrows JSON. Sends stay draft-first (prepare it, leave it one click away) until a flow is explicitly promoted. Original context (Jul 16): connect Front rather than raw Gmail — Front is where this correspondence already lives, and some mail only ever exists in Front groups, so there is no mailbox to forward from. Front is confirmed as the inbound pipe for FIVE waiting items, not just this one (Paycom offer-accepted, Paycom new-applicant, PAF completion, FlightBridge confirmations, job-board alerts) — worth doing once, properly, rather than five half-integrations. It is an API + webhook integration with a token, not a mail rule, so it deserves its own slice. ALSO WANTED (Jul 16): a button on the person's profile that sends the onboarding email through Front, so the manual send goes away. Feasible — Front has a send API — and the templates already exist here; the two open questions are which inbox it sends FROM and where the email body lives. Deliberately NOT automatic: flipping internal state off a parsed email is safe, emailing a real candidate off one is not, so the automation should prepare the send and leave it one click away. WHICH-EMAIL-AM-I-SENDING MADE VISIBLE (Jul 27) — the user came back to the orientation panel after a gap and could not tell the three templates apart before clicking, which is the exact moment a misfire happens. The recipient was only ever in a hover tooltip and inside the preview dialog, and two of the three go to the new hire while the middle one goes to their supervisor, so "2. Supervisors" in a column header was carrying too much weight. Added, all always-on and no hover required: a three-card legend above the grid (what each email is FOR, its To line, its Cc line, and the real Front template name), an audience chip on every column header and legend card colour-coded by WHO RECEIVES IT (navy for the new hire, gold for the supervisor, amber left alone so it keeps meaning "warning"), a plain line saying nothing sends on the first click, and a banner at the top of the send dialog that renders BEFORE the Front fetch resolves — previously the dialog sat there saying "Building the email from Front" with a Send button and no indication of who was on the other end. The supervisors one says outright that the named attendee does not receive it, because the dialog title reads "2. Supervisors - Jane Doe" and looks like it goes to Jane. Recipient wording now lives in lib/orientation/email-templates-meta.ts so the legend, the header and the dialog cannot describe the same template differently. Typecheck and lint clean; verified over HTTP against the real Aug 4 session page that the legend, both chip variants and all three Front template names render.
- [ ] Redirect non-production email sends to a test inbox — decided Jul 20. Local dev and Vercel Preview now carry a real working Front token and point at the real hrotasks@ mailbox, so a send from a test copy of the site reaches a real new hire. There is no sandbox mailbox and no test database. The fix: outside production, still send the email for real, but swap the recipient for a fixed test address and mark the subject with who it would have gone to, e.g. "[PREVIEW - would have gone to jsmith@example.com]". That keeps the whole flow testable end to end (template fetch, greeting, cc, the confirm dialog) with no way to reach an actual hire. Deliberately chosen over two alternatives: blocking sends outside production was rejected because it would also block the kind of real verification that caught two genuine bugs on Jul 20, and a warning banner alone was rejected as a sign rather than a wall. Notes for whoever builds it: detect the environment from VERCEL_ENV (falling back to NODE_ENV), keep the test address in config rather than hardcoded, apply it to EVERY email type rather than just the onboarding one, and decide deliberately whether a redirected send should still tick the checklist item — it probably should not, since marking a real hire as emailed when they were not is exactly the kind of false record this whole feature is meant to avoid.
- [ ] Email history on a person's page — asked for Jul 20: open Tara Ward and see every email the app has sent her and when. This is the requirement that decides the storage question left open by the send button: per-person history has to be queryable, so the current WorkspaceSetting JSON (one blob keyed by hire id, fine as a single sent-flag) must become a real table — something like SentEmail with person id, template id, subject, recipient, Front conversation id, sent at, sent by. Two things to work out when it is built: it should key to the PERSON across both Candidate and NewHire (the ask says "candidate page" but sends today are keyed to NewHire, and the same human is often both), and it should cover every email type rather than just the onboarding one, since a history panel showing only one template would be misleading. Front holds the real conversation, so the panel can link out to the thread rather than duplicating the body. Build this as part of wiring the SECOND email template, not as a standalone migration.
- [x] Compliments by SkyShare recognition program (Jun 12) — peer-to-peer recognition on the NewHire roster: give to feed to points to redeem rewards to manager analytics, plus an ADMIN Budget tab (cost report, reward catalog CRUD, program settings). Nav item under People; verified end-to-end
- [x] Update Values in Action options (Jun 20) — recognition values now match SkyShare's core values from the job posts. This entry was corrected Jul 16: it used to claim five values including Safety First and Own the Outcome, which is not what is live and not what the job posts say. The live four, verified against the database, are: Fueled by Passion, Team Alignment, Deliver the Wow, Solutions Focused — word for word the list 11 job postings carry under "You must possess SkyShare's core values", each mapped to a distinct color slot. Kevin's Jun 18 feedback asking for exactly this was already satisfied by this change two days later; it had simply never been closed out, so the feedback queue still showed it as open.
- [x] Integrate the orientation tracker into the site (Jul 14 — already covered) — verified stale: People > Orientation is the single, fully in-app, DB-backed tracker (sessions / attendees / prep-tasks, cohorts, mini-calendar) already woven into the pre-onboarding checklist + travel; no separate standalone tracker remained to fold in. Redundant with the shipped Orientation module (Jun 12).
- [x] Travel & logistics tracker (onboarding + candidate fly-outs) (Jun 22) — Travel panel on the new-hire detail page and a Travel tab on the candidate profile: add trips (purpose + status Needed/Booked/Completed/Canceled), capture the request details (airports, orientation/indoc dates, requested arrival/return, preferred airline, frequent-flyer + hotel + rental loyalty numbers, preferences, special requests — mirrors Hannah's Google form), then log booked items (flight/car/hotel/transport with vendor, confirmation #, cost, dates), attach receipts, and see per-trip + grand totals. Attaches to the correct new hire OR candidate (pre-hire fly-outs). Auto-saving fields, on-brand, dark-mode.
- [x] Travel loyalty numbers on the profile (Jun 22) — frequent-flyer, hotel, and rental-car loyalty numbers are saved on the new hire / candidate profile (edited once in the Travel panel) and auto-pulled onto every new trip, so they never have to be looked up or re-entered. Removed the per-trip loyalty fields in favor of the profile-level ones.
- [~] Travel auto-fill from confirmations — the PASTE half shipped Jun 22; the TAG-AN-EMAIL half works as of Jul 28 and shipped. HOW IT WORKS NOW: tag a thread Travel in the Front HR Onboarding inbox and the app reads the email, pulls out the itinerary, works out who is travelling, and files a trip on that person's record. Proven end to end on the first real one — Gabby Lorenzi's Delta booking for Tara Ward, filed as INDOC, LAS to SLC on Aug 4, arriving 8:22am, with the airport pickup and the hangar-badge to-do captured as notes. WHY AN LLM AND NOT THE REGEX: measured against that same email, the regex parser in lib/extraction/travel-confirmation.ts got the route and the airline and nothing else — the flight DATE came back empty because the sentence reads Tuesday, August 4 with no year and every date pattern there needs a four-digit year, and it INVENTED a hotel booking because the phrase access to the stock room contains the word room. So lib/extraction/travel-email-llm.ts now reads prose written by a colleague rather than a machine-generated confirmation, and every field carries the verbatim sentence it was read from. TWO RULES BAKED IN, both from earlier bugs: the model never returns an instant, only a local wall-clock time plus the airport, and this code does the timezone conversion (that is what stops the every-flight-is-six-hours-off bug); and the subject line is fed to the model as well as the body, because the body greeted her as Tara and only the subject carried Tara Ward, which matches nobody in the roster. WHO IT IS is scored, not guessed: the first real email had five SkyShare addresses on it and only one was the traveller, so a name match plus being an active new hire plus an orientation date on the travel day decide it, the sender is excluded outright, and anything short of a clear winner is filed for a human instead of picked. Duplicate-safe via TravelTrip.sourceConversationId being unique, so a re-tag or a webhook retry updates the trip rather than making a second one. STILL OPEN: (a) a review screen for imported trips — needsReview is stored and set, but nothing surfaces it yet; (b) the Front rule that fires the webhook was created Jul 30 (see the Jul 30 entry below for exactly how) and FRONT_WEBHOOK_SECRET was already confirmed correct in Vercel — but neither has been PROVEN for travel specifically yet, since no real travel email has come through it since; (c) hotel, rental-car and multi-leg emails are unproven, since the only real sample so far is a single one-way flight; (d) FlightBridge samples are still wanted for the paste path.
- [x] Trip purpose is editable after a trip is booked (Jul 28, from feedback) — the purpose was rendered as plain text in the trip header, so a trip created as the wrong type could never be corrected. Nothing was missing on the server: updateTrip in app/travel/actions.ts already accepted and validated a purpose patch, only the control was absent. Now a select in place, styled to read as the heading it is until hovered. HONEST CAVEAT: typechecked but NOT visually verified — the trip panel only mounts when a trip is opened, and client-side interaction cannot be driven in this environment, so the select does not appear in the page HTML to check against.
- [x] Remove an irrelevant tag from a candidate (Jul 28, from feedback) — candidates imported from JazzHR carry that system's workflow-stage labels (2.2 Pilot App Complete, 1.3 H Manager Interview Scheduled, 2.3 EBCO Complete) which describe a process we no longer run, and on a historical profile they sit as noise directly under the person's name. Tags were render-only with no way to take one off. New DELETE /api/candidates/[id]/tags plus a small x on each pill. THE IMPORTANT DETAIL: it deletes the CandidateTag JOIN ROW, never the Tag itself — a Tag is shared by every candidate carrying it, so deleting the Tag would silently strip it from all of them. Removal is per person, which is what as-needed means here. Handles both tag stores (the normalized join and the legacy tagsJson array) so a visible label always disappears; checked the data first and every Jazz stage tag lives in the join, with zero candidates using tagsJson for them. TWO-CLICK CONFIRM rather than a modal, matching the trip delete button, because there is NO add-tag control on this screen — removal is currently a one-way door, and adding a tag back is the obvious follow-up. Every removal is written to the activity log. Verified: the endpoint 404s an unknown label and 400s a missing one, and the control renders four times on the profile that prompted this.
- [~] Post-onboarding list sorts by newest start date (Jul 28, from feedback) — the list came back alphabetically, which put whoever happens to be called Aaron at the top permanently; it is worked from the most recent arrivals backwards. Now newest start date first with the name breaking ties. Sorting nulls LAST is deliberate: on a descending sort a hire with no start date would otherwise lead the page. Verified against the real 17 rows (Scott Strahan Jul 6 down to Riley Wooden May 13). The FILTERS also asked for on this tab are still open, as is the Social media announcement column, which needs a new field rather than a sort change.
- [x] Orientation attendee picker: people already booked elsewhere are separated out (Jul 28, from feedback) — the Suggested group listed anyone active who had started, whether or not they already had a seat, so the Sep 1 session suggested NINE people of whom five were already on Aug 4. Suggested now means not yet scheduled ANYWHERE, and a new group, Already scheduled, holds the rest with the date they are on so the row is actionable rather than just a warning. Nobody is hidden — moving a person between sessions is a real thing to want — and Scheduled is tested before Later so no one can fall into two groups. A COMPLETE session does not count as being scheduled, since that person still needs a seat. Measured on the real Sep 1 session: suggested fell from 9 to 1 (Grace Ward), with Axel Leimer, Bryan Weber, Luke Webb, Serene Cummings and Tara Ward moving to Already scheduled. Verified over HTTP.
- [x] Lunch details on an orientation session (Jul 28, from feedback) — Order lunch (use headcount) has always been a prep task, but ticking it recorded NOTHING, so on the day nobody could say where lunch was coming from, when it would land, or who to ring. The session now holds: who it is ordered from, expected arrival time, contact name, contact phone, free-text notes, and the order confirmation file. Nine additive nullable columns on OrientationSession applied straight to the live database (all five existing sessions untouched), following the Event and Feedback precedent of inline file columns rather than a table for a single attachment. The arrival time is stored as a real instant anchored to the SESSION'S OWN DAY in Mountain via mountainWallClockToIso, so there is no second date field to keep in step and a time typed here means what it says on the day rather than in whatever zone the server runs in. Deliberately NOT validated against the session start: lunch can legitimately arrive before doors open. Upload/download/remove live at /api/orientation/sessions/[id]/lunch-file, and the download is permission-gated like everything else because a catering confirmation carries a staff name, a phone number and a headcount. Replacing or removing a confirmation leaves the stored blob in place and only repoints the row — the storage adapter has no delete, exactly as travel receipts already work; adding one is worth doing but not as a side effect of this. VERIFIED: fields write and read back correctly in a fresh process (18:15Z reads as 12:15 PM Mountain on Sep 1), the panel renders, and an invalid time is rejected with a 400. The placeholder vendor and contact used while testing were cleared off the real Sep 1 session afterwards. ONE ENVIRONMENT NOTE, not a code fault: the running dev server keeps the OLD generated Prisma client until it is restarted, so saving through the UI returns a 500 and the values read back blank until someone restarts it.
- [x] Candidate file upload: honest size limit and one file per request (Jul 29, from feedback) — reported as "unable to upload candidate file... i cant add a resume at all". TWO FAULTS, both invisible from the outside. The upload button appended EVERY selected file into a SINGLE request, so their sizes added up against one body; and every upload route advertised a 25 MB limit that is fiction in production, because a hosted serverless function rejects a request body over roughly 4.5 MB BEFORE the handler runs. So our own size check never fired, the rejection came back as non-JSON, the client's response.json() threw, and the catch reported a flat "Unable to upload file." with no reason. Now: sizes are checked in the browser before anything is sent, the limit is a single shared constant (lib/files/upload-limits.ts, 4 MB to leave room for multipart overhead), files upload ONE PER REQUEST the way ResumeIntake already did, a non-JSON response is read defensively so a 413 still explains itself, and one bad file no longer fails the whole batch — the message says what uploaded and what did not. The message names the file and its actual size rather than saying "too large".
- [x] Round trip versus two one-ways on a trip (Jul 29, from feedback) — asked as "give me ideas"; the answer was already in the data. Both real round trips store the SAME confirmation number on both legs (Brandon Edwards GQHGHP, Auggie Quintero GLVG7W) with the fare on the first leg and an empty Cost box on the second — so the rule is simply that flight rows sharing a confirmation are one booking, and nobody has to type anything new. Legs now carry a Round trip · leg 1 of 2 badge, and the second leg's empty cost field reads "Included in leg 1" instead of looking like missing data. Deliberately narrow: only FLIGHT rows group (a package can share a reference with a hotel), a blank confirmation never groups (several blanks are not evidence), and three or more legs are labelled Multi-leg rather than forced into a round-trip shape. Totals were already correct and are untouched. Verified against all five real trips: both round trips detected, and Caleb Green's single confirmed flight, Tara Ward's confirmation-less flight, and the hotels and cars carrying their own references were all correctly left alone.
- [x] Travel spend reporting (Jun 22) — Reports now has a Travel spend section: total spend, hired-traveler vs. not-hired spend, cost per hire, and a by-purpose breakdown (excludes canceled trips), so we can answer "how much should we spend".
- [x] Central Travel page (Jun 23) — a Travel hub under People (/travel) listing every trip across new hires AND candidates, not just orientation: filter by status / purpose / hire-vs-candidate / search, top-line totals (trips, needs-booking, booked, total spend), and each row crisscrosses to that person's record (where trips are added/edited). A "New trip" button on the hub lets you pick any traveler (searchable across the full roster) + purpose and creates the trip, then opens that person's record to fill it in. Travel is its own thing now, with the per-person panels + Orientation chips linking around it.
- [x] Booking travel ticks the onboarding checklist (Jul 17) — marking a new hire's trip Booked (or Completed) now auto-ticks their "Travel accommodations complete" checklist step; before, nothing outside the spreadsheet importer ever set it, so a fully-handled hire sat at "N-1/N" forever and looked unfinished — a real reason to distrust the site and keep the Google Sheet. Forward-only (canceling a trip does not untick, since a hire can have several trips), idempotent, and skips candidate pre-hire trips. app/travel/actions.ts updateTrip. Verified end-to-end on a throwaway hire (TODO -> book -> DONE, cancel leaves it DONE, candidate trip no-op). Pairs with the pre-onboarding runbook (Google Doc) for Hannah's move off the Sheet. NOTE: "Attended orientation" was already handled — marking the orientation session Complete ticks it.
- [x] Travel tied into Orientation (Jun 23) — the orientation session page now derives each attendee's travel status + cost from their real trips (Booked/Needed chip linking to the hire, superseding the manual flag where trips exist; the manual Local/Needed flag stays as a fallback for hires with no trips yet), plus session-level "Travel booked X/Y" and "Travel spend" roll-ups.
- [x] New trip: stay on the Travel page and confirm the traveler (Jul 16) — "New trip" now only picks the person; the hub loads them inline into a confirm card (name, hire or candidate, position, department, contact, loyalty) with a "Yes — start a trip" button, and the whole trip fills out right there. No more bounce to the candidate page. Table rows open the same inline panel (a button, since it swaps a pane) with the real link to the full record kept beside it. Caveat: candidates have no title in the real data, so their confirm card shows only email and phone — a weaker "is this the right person" check than for hires.
- [x] Self-booked trip items + reimbursement (Jul 16) — each line on a trip now carries a "Traveler booked this" tick, and ticking it reveals where the money stands: no reimbursement, reimbursement owed, or reimbursed. Owed lines show their amount inline and the trip totals up what we still owe the traveller, separately from what the trip cost — so spend reporting stays honest about what we paid directly versus what we owe back. Un-ticking self-booked clears the reimbursement, since a trip we booked cannot be owed back. Per item, as asked (TravelItem.selfBooked + .reimbursement, both additive with safe defaults).
- [x] Guests on a trip (Jul 16) — a trip now records anyone travelling with the traveller by name, added and removed as pills on the trip (TravelTrip.guests, trimmed, de-duped, capped at 20). Names only and nothing else about a guest, deliberately: it is all a booking or a cost split needs, and it keeps guest PII out of the system.
- [x] Per-person travel calendar (Jul 16, from feedback) — a month grid beside the trip panel: days away shaded, chips for flights, hotels, cars, orientation, indoc, arrival and return; opens on the first month that has anything and updates live as you edit. Dates recovered from booking text (see the date-parser item below) are drawn with a dashed gold outline and labelled, so a guess never looks like a confirmed booking.
- [x] Fixed the date parser that made travel dates vanish (Jul 16) — found while building the calendar: the confirmation importer only understood "Feb 23, 2026", "02/23/2026" and "2026-02-23", but Delta and FlightBridge send "16Jul2026". So every real flight imported with an empty date column and the actual date stranded in the description text — the calendar would have rendered EMPTY for the only real multi-item trip we have, and looked finished. The importer now reads the compact form (10 parser cases pass, including refusing to mistake a flight number for a date). It deliberately does not guess a clock time, since a trailing number on those lines is as often the flight number. NOTE: this fixes new imports only — rows already in the database still have empty dates, so the calendar reads those out of the text as a labelled fallback.
- [x] Prompt for the travel we have not booked (Jul 16, from feedback) — a trip now says what is obviously missing: nothing booked at all, a flight out but none back, no hotel across a multi-day stay, no ground transport. Derived only from what the trip already knows; no flight-data source needed. Caveats, honestly: the return-flight rule counts flights, so a multi-leg one-way entered as two legs reads as a round trip and stays quiet (chosen deliberately — a false alarm is worse than a miss here); the suppression checks are keyword guesses against the notes (it correctly stayed silent about a rental car because Auggie's trip says he is booking his own), so unusual phrasing can silence a real gap; and prompts cannot be dismissed, they just go quiet once the booking is recorded.
- [ ] Auto-detect local flight times (Jul 16, from feedback) — asked for alongside the item above, but it needs a source of real flight schedules, which we do not have. Parked with the FlightBridge item below: whatever we connect for booking is the same thing that would answer this, so decide them together rather than solving it twice.
- [ ] FlightBridge integration for booking travel (Jul 16) — look into what flightbridge.com offers for scheduling/booking travel from the app (API, portal, or email-only), and whether it can replace or feed the manual trip entry. Related: the travel auto-fill parser above is already waiting on real FlightBridge confirmation samples.
- [x] Employee journey visual + upgrade analytics (Jul 1) — new RoleAssignment table (role history per employee) + backfill of everyone's current role. Person profile now shows a "Role journey" — a vertical timeline of roles held (hire → promotions/upgrades), with a glowing gold node + Upgrade badge for each First Officer→Captain step, the current role highlighted, and tenure/role/upgrade counts. "Record role change" auto-closes the current role, opens the new one (seat detected from the title via the fleet registry), and syncs the headline position. Reports gained a "Pilot upgrades" section: avg/median time to upgrade (SIC→PIC), % upgraded within 1yr/2yr, and once/2×/3× distribution. Analytics populate as promotions are recorded (or via a future promotion-history import).
- [ ] Add non-role events to the pilot journey — Check Airman, Argus, etc. (added Jul 30) — the user's ask: the Role journey timeline today shows only ROLE changes (hire, FO→Captain upgrades); add other career milestones alongside them so a pilot's journey shows the whole arc, not just seat moves. Start with the pilot-qualification milestones the app already knows as tags — Check Airman, Argus, Standards Captain, ATP, PDP (the Jul 11 pilot-qual palette, which already carries its own colours: Argus indigo, Check Airman rose, Standards Captain orange, ATP teal, PDP emerald) — rendered as distinct nodes on the same timeline, interleaved chronologically with the role nodes. THE PREREQUISITE, and the real work: these are held today as UNDATED tags, so the timeline cannot place them yet. Each event needs a WHEN — the date a pilot became a Check Airman, earned Argus, and so on — captured per person. DECIDE the model: a new dated career-event record (event type + date + optional note) vs bolting an earned-on date onto each qual tag; a career-event record is the cleaner home and leaves room for other milestones later (a type rating earned, a base change, a service-award anniversary, the "anything else fun" from the flight-activity idea). Style them clearly apart from role changes (the qual palette gives each its own colour) so an upgrade still reads as an upgrade. DECISIONS FOR LATER: which events earn a place (start with the five quals named + the upgrades already shown), who can add or edit an event's date, and whether any can be auto-derived (an ATP could be inferred from the certificates already extracted). Pairs with the journey-visual redesigns above (Great-circle route / airframe silhouettes), which change how the timeline LOOKS — this adds what it CONTAINS. Not now — captured per the user.
- [x] Navigation review + IA cleanup (Jul 3) — Command Center is now a first-class "Home" nav item (and a row in Settings → Module Access; it was only reachable via the / redirect before). Recruiting split into Sourcing & Matching / Interviews & Scheduling so interview tools aren't mixed with sourcing; the People access row is labeled "People" (was mislabeled "Pre-onboarding" while governing 5 pages); /scheduling gained its missing module guard; dead module IDs (changes/approvals/jobs-sandbox/templates) and the duplicate Layout Lab nav entry removed.
- [x] Fleet progression: "Stayed put" view (Jul 9) — the inverse of the upgrade stat. A clickable "Stayed put" bucket on Reports > Fleet progression lists the active pilots who have NOT upgraded or transitioned (e.g. the 8 behind "19 of 27 with 2+ yr tenure"), each showing their current position, tenure, and a "possible next step". Refinements (Jul 9): suggestions follow the SHARED-fleet career ladder only (PC-12 -> CJ2 -> 560XL -> G200 -> G450/GV) and EXCLUDE managed aircraft (Legacy 650, Phenom, M2, 560XLS+, the HND managed G450) since a managed seat isn't a shared-pool move; a pilot who also holds a management title is measured from their real FLYING role (e.g. Alex Andrade = PC-12 Captain, full ladder available); managed-seat pilots show a "no shared-fleet path" note. Plus a Download CSV button on the roster (name, position, tenure, START DATE for validating timeframe, moves, suggested next steps). Respects the existing scope/tenure/year filters.
- [x] Fleet progression: fractional vs managed filter + pilot-detection fix (Jul 10) — Fleet progression now DEFAULTS to the SkyShare / fractional shared pool (managed-account pilots hidden), with a "Fractional / All fleets" toggle, since a managed-aircraft pilot isn't on a promote-by-date path. Added a NewHire.managedPilot marker (editable via a checkbox on the pilot's profile; seeded from managed department / tail / contract status / managed-only aircraft) so the SkyShare↔managed G450 overlap can be corrected per pilot. Also fixed a pilot-detection bug: ground/support roles mis-tagged with a "*-captain" fleet slug (e.g. JD Bumgarner, "G450 Maintenance Technician") were wrongly counted as pilots — seatOf now excludes maintenance/technician/support titles.
- [x] Reports made interactive + focused (Jul 3) — dropped the low-signal top half (candidates-by-stage/source/job + requirement counts) so Reports leads with the two views that matter, both clickable to the person. Fleet progression: an animated cumulative-advancements-over-time "climb" chart (with a Made-Captain milestone line) + clickable buckets (Advanced / Once / 2×+ / 3×+ / Made Captain) that filter a roster showing each pilot's visual FO→Captain journey (upgrade steps glow gold) linking to their profile. Travel spend: click a purpose to expand the trips behind it (traveler linked, route, status, amount) + a hired-vs-not-hired split bar. getUpgradeAnalytics / getTravelSpendSummary now return the per-pilot journeys and per-trip rows. Document currency kept as a slim secondary section; retired /jobs/layout-lab page deleted.
- [x] Employees directory + full-history import (Jul 1) — new People → Employees section: everyone who is or was staff (416 people — 148 current, 268 past), searchable, filter Current/Past, each linking to their role journey. Imported the full history: the Employee Roster MASTER (year-end snapshots 2010→2022, CB Aviation→SkyShare — ~210 people with stints + role timelines inferred from yearly positions) plus all 112 pilots from the Upgrade Tracker, deduped across sources and merged with the existing roster. New EmploymentStint model captures rehires ("left & came back" — reconstructed from snapshot gaps); tenure sums stint time; birthdays captured (dates only) for future recognition. Importers: prisma/import-employee-roster.ts + import-pilot-upgrades.ts. (Later: training-date importer filled/added dated position changes for 77 pilots + created 7 new-hire pilots → 460 employees.)
- [x] Business-card setup automation for new hires (Jul 7) — new People → Business cards page builds every active employee's card from their record in the printer's exact format, with the automatic rule from the order spreadsheet: pilots & cabin attendants get the shared SkyOps line (801.516.9189), everyone else gets SkyLove (855.SKY.LOVE); mobile = their cell, email = @skyshare, web = skyshare.com. Gallery of brand-styled card previews (each linked to its profile), select any/all → "Copy for printer" drops the formatted block on the clipboard to paste into the order email (no more re-typing). Per-profile "Business card" panel + copy button, an amber flag when a card is missing a mobile/email, and an "Order business card" onboarding checklist item. lib/business-cards/card.ts holds the format + role logic. FIX Jul 17: the first phone line's LABEL was hardcoded to "skyops" for everyone — only the number switched by role. Now the label switches too: crew stay "skyops", everyone else reads "phone" (lowercase, matching the mobile/email/web labels). Applied in all three renderers (the on-screen card, the plain-text export, the HTML export) and the variant-card editor. Verified against the two real people who surfaced it (Bailey Barcelon/Pilot keeps skyops+801; Ben Ducharme/Line Service Supervisor now phone #+855) plus captain, first officer, cabin attendant, mechanic and untitled. CHANGE Jul 19: reversed that split per the user — the first line now reads "skyops" for EVERYONE (pilots, cabin attendants AND support staff); only the NUMBER still switches by role (crew 801, support the 855 SkyLove line). Also reworked the printer copy to a stacked label-above-value layout with a half-line between groups (Name / TITLE / skyops+# / mobile / email / web), and split each phone/email/web value across two spans in the HTML export so it never pastes into the order email as a blue hyperlink (no hidden characters — copies back out clean). On-screen card already used this stacked layout. Also Jul 19: added per-status tabs to the Business cards page (All staff / New hires / Needs cards / Ordered / Received / Not needed / Missing email) — changing a card's status moves that person to the matching tab, with live counts; Needs cards + Missing email glow amber when non-empty. FIX Jul 19: the page was only listing people still in onboarding (stage ACTIVE/POST_ONBOARD), which hid the 147 established employees who auto-archive to stage ARCHIVED while staying employmentStatus ACTIVE — so reorders for tenured staff were impossible. Now gated on employment status only (current staff; former + contractors excluded), matching the Employees directory. All staff jumped 21 → 168.
- [ ] Business cards: send the printer order email straight from the app — today the card page builds the printer-format block and you Copy it to paste into the order email you send yourself from Front. Automating the send would ride on the SAME parked Front email integration as orientation (see People Ops). DEFERRED on purpose until (a) that Front integration is live and its other uses have proven out, and (b) the manual copy-paste flow has been run for real a few times first — the user sends the printer email personally from Front (not Hannah's team), so he will validate the manual path before automating. Target: next quarter, contingent on the other Front items going smoothly. (added Jul 19)
- [ ] Business cards: track the after-order fulfillment steps in the app — today the app tracks per-person status only (Needed / Ordered / Received / Not needed). The after-order steps — approve the proof via the printer's email link, pay via Ramp, confirm the SLC office received the box, arrange orientation-day availability (Morgan), mark the employee received their cards, ship to remote employees — are DOCUMENTED in the Business Cards SOP (artifact b52e49c2) and done manually for now. User's call Jul 19 was document-first; build tracking after the flow settles over a few real orders. Design note for later: the steps split across two levels — order/batch-level (approve, pay, SLC receipt, orientation hand-off) vs per-person (received, shipped-if-remote) — so a batch Order record is the natural model; Ramp/SLC/Morgan/shipping are real-world handoffs the app tracks the STATE of, not integrations. (added Jul 19)
- [x] SOP accuracy audit + rewrite, all five documents (Jul 22) — the SOPs in docs/sops had not been touched in 32 commits and had drifted badly. A multi-agent pass (three review lenses per chapter — factual accuracy against the code, first-day actionability, concision — then an adversarial verify pass on every accuracy claim) found 243 issues; 53 accuracy findings survived verification and 5 were refuted. The worst was structural: the sidebar reorg moved New hires / Orientation / Travel / Business cards under an Onboarding group, so EVERY "People to New hires" instruction in all five documents pointed at a page that is no longer there — a new person could not find the first screen. Also wrong everywhere: the checklist is 20 steps not 19 (the background check became three separate tasks), FIVE steps now tick themselves not two (the onboarding email plus both Paycom background-check steps joined orientation and travel), the org-chart SOP was built around an "Edit roster" button that does not exist (it is Edit all / Edit this aircraft / Edit this location), and the pre-onboarding prerequisite told a non-Admin to fix their own role on an Admin-only Settings page. All five rewritten and re-verified twice against the code. Two live-database behaviours that were undocumented are now called out: picking an employee in the org-chart link search writes a candidate record at click time and survives Cancel, and Remove this aircraft saves immediately in per-card mode. All five were REPUBLISHED to their existing artifact URLs the same day, so the links the team already holds now serve the corrected text. PAYCOM: confirmed by the user that Paycom is the source of truth for the offer — all six offer steps happen in Paycom first and the app only mirrors them; the SOPs now say so, and the old "this app owns the workflow" line was wrong and is gone. CAVEATS: the 14 open questions were answered by the user the same day and ALL placeholders are now filled (the last one, Hannah Byers / hbyers@skyshare.com, closed the same evening) — printer is Sun Print Solutions (mark.bengtzen@sunlitho.net), orders go from recruiting@skyshare.com, 100 cards per person, Aimee Schaedig (Recruiting Manager) sends and pays, Brett Nielson in Marketing (brett@skyshare.com) receives and inspects the box and hands it to Morgan Langholf, EA to the CEO (mlangholf@skyshare.com), and the offer letter template plus both signature routings live in PAYCOM (the app's six offer steps only record that it happened). Two steps had to be rewritten rather than filled: the SLC step was never a front desk, and remote shipping needs no mailing address because Morgan or Hannah ships. EXACTLY ONE placeholder is left — Hannah's surname and email, in the business-cards SOP. Also corrected: this SOP set had been telling people to ask an Admin for role and access changes; the answer is the Recruiting Manager; and none of this was verified by clicking through the running app, only read against the source.
- [x] Purge seeded/demo recognition data from Compliments (Jul 6) — cleared the fake peer-to-peer recognitions so the program starts empty for real use: deleted 41 recognitions (+323 likes, 8 comments via cascade) + 1 demo redemption, reset 70 seeded points balances to 0. Kept the reward catalog (8), the 4 core values, and Celebrations (birthdays/anniversaries, computed from real dates). Verified the dashboard shows a clean zero state and Celebrations still populate. Reusable prisma/purge-seeded-recognitions.ts (dry-run default).
- [x] Recognition tie-in — birthdays & work anniversaries (Jul 5) — new **Celebrations** tab in Compliments surfaces upcoming birthdays + work anniversaries for current staff (next 45 days, grouped Today / This week / Coming up), each with a "Recognize" button that deep-links to Give with the recipient pre-selected. Anniversary years count from the first employment stint (rehires keep real service length). The dashboard hub gained a compact "Celebrate this week" card that links through. getUpcomingCelebrations() in lib/data/compliments.ts. The payoff for capturing every employee's dates.
- [x] New hire contacts hand-off, v1 (Jul 8) — a public, no-login page (/welcome) a new hire opens from a texted/emailed link to load key SkyShare contacts onto their phone in a few taps (works on iPhone + Android via on-the-fly vCard/.vcf — the universal format the built-in Contacts app reads). Grouped by department (Recruiting/HR/Operations/Chief Pilot/Accounting), each with an optional synthetic "SkyShare [Dept]" shared contact + curated individuals; Add all / per-department / per-person / multi-select checkbox download. Admin curates in Settings → New hire contacts (pick who from the full roster is exposed — never the whole directory; per-person title/phone/email overrides with a "don't share a personal cell" control since only personal cells are on file). Config in a WorkspaceSetting (no migration). lib/new-hire-contacts/* (vcard.ts + config), lib/data/new-hire-contacts.ts, public /api/contacts/vcard. Verified end-to-end. (Later: QR code + NFC/dot-style card pointed at the same URL; per-role contact sets.)
- [x] Employee-history data cleanup + inline role editor (Jul 3) — audited the imported history and fixed the one systemic issue: 26 people were marked Terminated yet held an open/current role. 4 who kept working after their "termination" (Patrick Mcpartland, Robert Bell, Joshua Thompson, Andrew Steward) were reactivated (stale term cleared, stint reopened, stage → POST_ONBOARD); ~21 genuinely-departed pilots had their dangling open role closed at the termination date; Craig Rich clipped to his stint boundary, Zach Davis's wrongly-closed stint reopened. Legit "Captain → bigger-jet First Officer" moves and approximate snapshot dates left alone. Reusable scripts: prisma/cleanup-employee-history.ts (+ analyze/dump helpers). Shipped an inline Role-journey editor (pencil on each step → edit title/dates/type; PATCH /api/new-hires/roles/[roleId]) so the ~9 flagged junk-date records — and any approximate date — get corrected per person. (Reconciled Jul 9: the ACTIVE+ARCHIVED contradictions are all resolved — 0 remain. Two junk-date leftovers flagged to the user: Matthew Biddulph looks accidentally terminated (term date before start, still in the current roster); Alex Ortiz has a cosmetic term-before-start from a 2021 year-end snapshot.)
- [x] Employees list: custom columns + filters + back-link fix (Jul 10) — the Employees list is now self-serve. A "Columns" button toggles which fields show (Managed/Fractional, Aircraft seat-agnostic, Seat, Phone, Work/Personal email, Birthday, plus the originals); the layout is a shared WorkspaceSetting so it is the same for the whole team (admin-gated save, POST /api/workspace-settings/employee-columns). A single "Filter" button opens a compact popover (Pilot pool: Fractional/Managed · Aircraft · Department · Location · Started after/before) with active filters shown as removable chips — clean on desktop and mobile. Aircraft is derived seat-agnostically so PC-12 matches captains and first officers. Also fixed the profile back-link: employees (post-onboard/archived) now go back to Employees, pre-onboarding people to Pre-onboarding (was always Pre-onboarding).
- [x] Pilot qualification tags (Jul 11) — renamed the (empty) "Check Pilot" tag to Check Airman to match how the pilot titles read, added Standards Captain (orange), and tagged the current flight deck from the ARGUS sheet: 17 Argus, 6 Check Airman, 2 Standards Captain across 20 pilots (existing tags preserved — e.g. David Ricks keeps Management + gains Check Airman). Pilot-qual palette is now Argus (indigo) / Check Airman (rose) / Standards Captain (orange) / ATP (teal).
- [x] Pilot HR fields + date-data import (Jul 11) — added Seniority date, Birth country, Citizenship (editable on the profile + optional Employees columns), plus Check Pilot (rose) and ATP (teal) joining the pilot-qual tag palette. Also surfaced a derived "Last role change" column (from the role journey) so position-change dates show without a redundant field. Imported an HR "date data" sheet against 148 of 149 people (matched through nicknames + compound surnames): legal names, birth/citizenship country, seniority + aircraft-service dates; reconciled Hire dates into the existing Start date and folded 4 real rehires (Allen, Barcelon, Lee, Smith) into proper employment stints so tenure counts the gap. Per the "reuse what exists" decision, no redundant flat HR fields were added — Hire→Start date, Prev-term/Rehire→stints, Last-position-change→role journey. (RESOLVED Jul 22: the one unmatched row was a surname spelling split. The correct spelling is BRETT NIELSON, confirmed by the user, and the live database already holds it correctly — name "Brett Nielson", legal name "Brett Gregory Nielson", brett@skyshare.com. No data fix was needed; only this note was stale. Two standing exceptions for him: the surname ends -son, and his email is a deliberate one-off that does NOT follow the first-initial + last-name convention. Do not "correct" either.)
- [x] Tag palette + directory follow-ups (Jul 11) — added PDP (emerald) and Argus (indigo) to the hand-applied tag palette (each becomes an add/removable toggle on every profile). Retired the one-off gold-star "PDP graduate" badge in favor of the PDP pill and seeded the tag onto the current graduates; the profile header now shows all tag pills. Tags column is sortable and the Tags filter gained an "Untagged" option to find people still needing labels. Added an Aircraft Service Date to the employee record (pilots' pay-step anchor) — editable on the profile + an optional Employees column; the anniversary PAF reminders remain the separate HRIS item below. Also re-ran the duplicate scan (name-fuzzy + shared email/phone) across all 446 people: zero remain.
- [x] Employee tags + department cleanup (Jul 11) — people can now be labeled with cross-cutting tags (Executive, Management) that are independent of their department, shown as small 4px pills in a new Tags column and editable on the profile; Tags joined the Filter popover. Contract is no longer a hand tag OR an amber status badge — it renders as a pill automatically from employment status, and the Status column simplifies to Current/Past (headcount unchanged). Backed by a NewHire.tags string array (allowlisted + de-duped server-side). Also applied the finalized department-cleanup spreadsheet: 220 moves consolidating legacy values (Pilot/Sky Ops Pilots/MX/Cabin Attendant into FlightOps/Maintenance, the OGD FBO variants unified, Sales & Marketing split into Sales / Sales | Marketing / Sales | Brokerage, the | Management / | Executive sub-departments flattened into the base dept + a tag) plus 22 Executive/Management tags. Verified end-to-end.
- [x] Employee duplicate merge, self-serve (Jul 10) — on the Employees list (current or past), tick two records and Merge: a dialog shows both, you pick which one to KEEP, and the other merges into it and is deleted, leaving one person. The survivor keeps its own identity + fills any blank fields from the other; all history moves over (roles, stints, travel, business cards, recognition, redemptions, points summed, onboarding tasks + orientation attendance deduped against the unique keys). POST /api/new-hires/merge (candidates:write), irreversible with an explicit confirm. Verified end-to-end. Complements the automated employee-duplicate merge scripts with a manual path for ones the scan misses.

## Employee Lifecycle & HRIS Sync
Keep each employee's record in step with the HRIS (Paycom) and the compensation/PAF process — reminders out, changes back in.
- [ ] Aircraft Service Date tracking + pay-increase reminders — pilots have an "Aircraft Service Date" (usually their start date, but reset to a new-aircraft date when they change airframes). They get an automatic pay increase on each Aircraft Service Date anniversary. (Date-capture SHIPPED Jul 11 — the field is on the employee record, editable on the profile + an optional Employees column.) Still to build: ahead of each anniversary, remind the supervisor to create a Personnel Action Form (PAF) in the HRIS for the comp increase.
- [ ] Arrivals and departures list / daily sync — bring the shared "arrivals and departures" document into the app: first as a viewable, maintainable list, then auto-update each employee's status from it once a day (new arrivals activated, departures marked former) so the roster stays current without manual edits. (Merged with the Jul 14 backlog "Arrivals and departures list" item.)
- [ ] PAF-completion email updates the employee's role — when a PAF is completed, HR forwards a confirmation email to a dedicated address and the app parses it to auto-update that employee's role (position + a role-journey entry). Shares the inbound-email pipeline with travel + sourcing + Paycom intake.

## Bugs & UX Fixes
Smaller fixes and polish.
- [x] EIGHT REAL TAGS WERE DELETED FROM THE LIVE DATABASE, taking 915 candidate links with them (found Sep05 by the commit agent, undisclosed in the handoff) — 2.2 Pilot App Complete (650 links), 2.1 Requested Pilot Docs/Webforms (128), 2.3 EBCO Complete (117), 3.2 PRD Access Granted (14), 3.1 Sent PRD Access Request (1), 2nd Resume Review (Mike) (1), 3.6 and 3.7 Pre-Employment Drug Test Scheduled/Passed (2 each), all between 05:10 and 05:11 UTC on Sep 6, from local dev so with no author recorded. The handoff's verification section describes only the ZZ throwaway tag, so none of this was mentioned; it surfaced because the tag count read 45 where the handoff said 53. NO HARM DONE, and that was established rather than assumed: these are JazzHR-era workflow labels for a process no longer run - the app says so itself in lib/tags/colors.ts and app/api/candidates/[id]/tags/route.ts - and the surviving members of the same numbered family sit 98 to 100 per cent on ARCHIVED candidates. The Front automation is untouched, since lib/front/tags.ts uses entirely different names in a different case. So this reads as deliberate cleanup of dead labels on historical records. THEY CANNOT BE RESTORED from the app: the activity log recorded each name and count with a NULL metadata, so the labels are known and the people are not. A Neon point-in-time restore is the only route back and is his call.
- [x] Deleting a tag now records WHO lost it, not just how many (Sep05) — the label, the colour and every candidate id go into the activity metadata, so a delete can be replayed from the log alone. Ids are read BEFORE the delete because the cascade is what removes them, and written AFTER it succeeds so a failed delete leaves no record claiming otherwise. No behaviour change and no new confirm; the control works as before, it is just no longer a one-way door. Prompted directly by the eight above - the same handler on a LIVE tag would have been unrecoverable, and this repo's own rule is that a destructive path is reversible or it does not ship.
- [x] The three height caps the last sweep missed are gone, and the worst was the busiest page (Aug 31) — CANDIDATES had not been touched at all: its table sat in a react-grid-layout slot fixed at about 1028px while a hundred rows need roughly six thousand, so it scrolled inside a box on the page recruiters use most. A grid item cannot grow to content, so the table is out of the grid and in ordinary page flow; the stats strip stays behind so Edit layout still works. THE LIVE STATE WAS WORSE THAN THE DEFAULT AND THAT IS THE FINDING: the saved candidates layout held the records panel at h 230, about 9188px - somebody had already dragged the slot enormous by hand to escape the scrollbar. Confirmed at commit time, with all four page-layout rows listed as the control. A stale saved id is ignored on load, so no migration. TimeGridCalendar lost a 600px cap its own default 7am-to-8pm window already exceeded at 784px. The calendar and schedule form left the grid too, their five surviving ids renamed to -v2 so the saved layout falls back once rather than leaving about 1500px of blank grid - which DOES discard an admin's saved calendar arrangement, knowingly. JobBlockAssembly keeps independent pane scrolling at xl only, the rule's genuine fixed-chrome case, and grows normally once the columns stack.
- [x] Both nav scrollbars are gone by making the menus FIT, which is step one of the rule rather than step three (Aug 31) — she reported the rail AGAIN from her laptop after the previous fix, and neither bar was actually gone: the rail had been given overflow and then no-scrollbar, which hides the bar while leaving roughly 269px of navigation below the fold with no affordance, and the items panel overflowed by about 14px and drew a full-height bar for it. Nothing was removed from either menu, only tightened - padding, hairline spacing, row height. Rail 635 to 581px; the tallest group, Recruiting at 14 items, 764 to 658px. Both now fit a 695px laptop with headroom and no-scrollbar is gone, so a bar can never again be hidden on real content. ONE HONEST CORRECTION: the 964px figure in the old comment could not be reproduced - there are exactly 6 nav groups, which is 635px - and both numbers are left in the file rather than one being quietly overwritten. All heights are arithmetic from the classes, not measured in a browser.
- [x] The pop-out menu was being CLIPPED, not stacked behind anything (Aug 31) — she described it as showing up behind the second menu, which points straight at z-index, and z-index was not the cause - nudging it would never have worked. overflow-x-hidden on the rail was clipping the absolutely-positioned flyout, because an overflow ancestor clips a descendant whose containing block sits inside it. Containment is now gated to viewports under 620px tall, so at her size there is no overflow to clip and the flyout paints; below that height containment wins and the flyout is the acceptable loss. The mechanism is written into the file so the next session does not rediscover it by trial.
- [x] The hydration sweep is finished, and the scope claim in my earlier entry was wrong (Aug 31) — a full balanced scan finds 27 timezone-less call sites across 19 files, not the 22 I described as mostly correct calendar days, and 18 sit in client components where the mismatch actually throws. 25 fixed, 2 deliberately left. THE WORST WERE NOT ON MY LIST: FeedbackWorkspace is the very page the two original reports were captured on and formatted a created timestamp with no zone, and MatchCard called toLocaleTimeString on an instant, wrong by about seven hours rather than by a day boundary. THE EVENTS FILES CORRECTED MY OWN ADVICE, which had them down as moments: Event.startsAt is written from a plain date input and the live column holds BOTH shapes - Girls in Aviation day at exact UTC midnight as a calendar day, Utah State University at 06:00Z as a real moment - so pinning the office timezone across them would have moved the September event a day earlier. Verified with a control from the same query: createdAt, a known moment, is 0 of 2 at UTC midnight, so the test discriminates. TravelTrip splits the same way. Rather than guess per file, the missing pattern was added to the shared helper as a per-VALUE choice. Two were left because their parse and their format cancel in any zone, with the reasoning recorded in each.
- [ ] The compliments budget buckets by the SERVER's month, a different bug from the formatter beside it (found Aug 31) — the month boundary is computed from a locally-built date, so on UTC a recognition made at 7pm Mountain on the 31st counts toward the following month. Found while sweeping formatters and deliberately not fixed there, because it is arithmetic rather than display and deserves its own change with its own check of what is already recorded against which month.
- [x] 42 of the 44 Reopen buttons on the duplicate page are dead (found Aug 31) — the closed-pairs panel shipped and lists every closed pair, but the scan auto-resolves any OPEN item whose side is MERGED and the merge helper throws already merged, so reopening one of those puts it back only until the next scan. Measured against the full closed set rather than a sample: 44 closed CANDIDATE items, 42 auto-reclose, 2 genuinely actionable and both DISMISSED same-name pairs, Brandon Edwards and Matthew Johnson. The pair he originally reported is now permanently unreachable, because repairing the resurrection set the shell to MERGED - arguably correct, but nothing on the page says so. Hide or disable a Reopen whose pair can never merge, rather than offering an action that silently undoes itself.
- [x] The resurrection guard covers one route of three (found Aug 31) — app/api/candidate-applications/route.ts is correctly guarded and the live repair is complete - all 32 rows carrying mergeHistoryJson are MERGED with a non-null archivedAt and none is live, with the inverse check also zero. BUT app/api/candidates/route.ts is an unguarded sibling that reproduces the original bug exactly: it tests employedHire and not mergeHistoryJson, then writes archivedAt null and status ACTIVE, acting on a findFirst carrying no status or archived filter - so posting a candidate with the same email plus a jobId would match a tombstone and resurrect it. lib/candidates/reactivate.ts guards on status MERGED rather than mergeHistoryJson, equivalent on today's data but a different column that can diverge. Close the sibling before this recurs through the other door.
- [x] The nav rail is contained, but by the rule's last resort with the affordance hidden (found Aug 31) — the original defect is genuinely fixed - the rail no longer pushes its overflow into the document. What did not happen is step one of the rule written the same day: diffing the fix shows the ONLY non-comment change is the class list, so no tile was resized or removed and the rail still holds about 964px against a 695px viewport. It then hides the scrollbar, so roughly 269px of navigation sits below the fold with no visual cue, discoverable only by wheeling over a 70px column. The rule says make it fit, then let the page scroll, then only scroll the container - and never hide a scrollbar on real content. This took step three and hid the evidence, exempting itself as fixed-width chrome, which is asserted rather than earned since nobody tried making it fit.
- [x] The nav rail regression, and the scrollbar rule that came out of it (Aug 31, reported by him) — containing the rail's overflow that morning put TWO scrollbars on it, vertical and horizontal, which is worse than the spill it replaced. The horizontal one is a CSS spec trap rather than a styling slip and nothing in the markup hints at it: setting ONE overflow axis to something other than visible makes the OTHER compute to auto, so overflow-y-auto silently enabled horizontal scrolling; the vertical bar then ate 12px of a 70px rail, pushing the dividers wide enough to trigger the bar it had just switched on. Fixed by pinning overflow-x-hidden and hiding the bar chrome with a new no-scrollbar utility, which is for fixed-width chrome only and says so - a hidden bar on real content hides that there is more to see. HIS RULE is now in CLAUDE.md: make it fit, else let the PAGE scroll, and only then scroll the container. About 40 other components use overflow-y-auto without pinning the other axis and were deliberately NOT swept, because auto only shows a bar when content actually overflows and none of them is showing one. Confirmed fixed by him.
- [x] The hydration error on /candidates is fixed, as a pattern rather than one file (Aug 31) — eight components built their own Intl.DateTimeFormat with NO timeZone and fed it a real moment, so Vercel resolved it in UTC and a Mountain browser seven hours earlier - every row touched after about 18:00 Mountain rendered a different DAY on each side, which is React error 418. They now delegate to lib/dates/display.ts, which already pins America/Denver: formatMomentDate for five, a new formatMomentDateTimeLong for the two that show a year, and formatMomentDateTime for DuplicateReviewQueue - a ninth file the report did not name. formatMomentDateTimeLong exists so those two could move WITHOUT changing what they display; the bug was the missing timeZone, not the format. THE SWEEP WAS DELIBERATELY NOT BLANKET: 22 further timezone-less formatters remain under components/ and most are CORRECT, because they format CALENDAR DAYS stored at midnight UTC, and pinning a timezone to those shifts them to the previous day. Two the report named were checked and left alone for that reason - CelebrationsWorkspace formats birthdays and anniversaries, and CurrencyPanel already sets timeZone UTC explicitly. NOT VERIFIED: that the 418 is gone in a real browser, which needs somebody loading the page after 18:00 Mountain; the cause is removed at every site on that page that could produce it.  *** CORRECTED Aug 31: the /candidates half holds, the AS A PATTERN half does not. *** Zero timezone-less formatters remain under components/candidates and that is verified. But the commit's reasoning that the rest are mostly correct calendar days is wrong for at least seven, and the two worst were read in full rather than grepped: components/settings/FeedbackWorkspace.tsx formats an ISO moment with hour and minute and no timeZone, and that is THE PAGE THE TWO ORIGINAL 418 REPORTS WERE CAPTURED ON; components/pilot-requirements/MatchCard.tsx calls toLocaleTimeString with no zone, so it is wrong by hours rather than at a day boundary. Also MatchboardWorkspace on scanExcludedAt, JobMergedHistory, and the two new-hire send buttons on sentAt. A line-based grep for timeZone gives a false pass on any multi-line formatter, which is how the original claim was reached - read the block, not the line.
- [x] The sitewide scroll audit is applied, six changes (Aug 31) — the nav rail was the real regression and it contradicted its own comment: Sidebar says the rail is locked to full screen height with only the panel scrolling, the items panel honoured that, and the icon rail beside it had no overflow handling at all - so its tiles escaped into the DOCUMENT and the whole page grew a scrollbar for a nav that is meant to be fixed. Now h-full, min-h-0 and overflow-y-auto, and min-h-0 is load-bearing because a flex child will not shrink below its content without it. Four height caps removed, each a box shorter than what goes in it: Matchboard positions (78vh gave about 460px to 9 roles needing 576px), Pilot Requirements, Reports (whose own print CSS already overrode the cap, so it was known wrong) and Imports. ONE CAP KEPT ON PURPOSE - the raw job-description reader at 320px, which is a long text block that does not drive page scroll; the rule is about content that grows with the data, not about every max-h. THE CALENDAR IS A COMPROMISE and the file says so: MonthCalendar has no height and no overflow inside an EditableGrid slot fixed at about 708px, so a busy month painted OVER what sat below it, silently. It now scrolls rather than overlaps. Raising the slot height would not fix it, because saved arrangements live in page-layout/calendar and keep their old heights. NOT VERIFIED VISUALLY: all of it is layout and this app's rendered content is unreadable in the Browser pane, so the two worth a second look are the Matchboard panel, whose search box no longer stays pinned, and Reports, whose list is now unbounded on screen.  *** CORRECTED Aug 31: four of seven catalogued offenders, not all of them. *** Confirmed against the fix commit itself: components/candidates/SelectableCandidateTable.tsx does not appear in it at all and still carries overflow-auto inside an unchanged 1028px EditableGrid slot - the busiest page in the app - and components/calendar/TimeGridCalendar.tsx still has max-h-[600px] and was touched by no commit in the batch. The Calendar change is also an inner tweak over an unchanged outer slot: it stops MonthCalendar overlapping what sits below, at the cost of the scroll-inside-scroll the new rule forbids, which the file comment concedes.  *** STALE ON THE CANDIDATES PAGE, corrected Sep05: SelectableCandidateTable is no longer an offender and there is no EditableGrid on that page at all. *** The file now carries overflow-x-auto only, on a container with no height cap, and the min-w-[1000px] that forced the sideways bar is gone; the records panel sits in ordinary page flow. Verified by reading the file at commit time, not from the rebuild handoff that reported it. The OTHER pages named on this line - Calendar, Job builder, Pilot Requirements, Reports, Imports - were not re-checked and are not claimed either way.
- [ ] The Recruiting Jobs page is overstuffed rather than broken, and EditableGrid is the wrong tool for it (found Aug30, from feedback cmtg6yl0f)  — her words: fix this jobs page and make it look better, there is too much vertical scrolling, it seems kind of messy and there is extra stuff. NOT rebuilt, and parked by him pending mockups; recorded so the diagnosis is not re-derived. The page renders 8 separately chromed EditableGrid panels by default, each repeating its own card, uppercase eyebrow and heading, and in detail view it shows workspace-wide stat tiles beside job-specific stat tiles in two different treatments - two scopes of the same idea side by side, which is the extra stuff she is reacting to. THE SCROLL IS THE LAYOUT MATH RATHER THAN LONG CONTENT, which is the part worth knowing: the default layout stacks panels down to row 35 at a rowHeight of 28 plus 12px margins, roughly 1388px of panel height, rendered into an unconstrained div - about twice her 695px viewport before any data loads, so even a job with almost no linked candidates scrolls the same. Two panels are also mis-sized against their content: the screening panel capped near 548px while it can render a scan banner, tabs and several accordion groups, giving scroll inside scroll, and the source panel given roughly 108px for a full imported job description. NOT the known EditableGrid re-init landmine - the panel set is stable behind placeholders and the panels array is memoized, so this is configuration rather than the documented collapse bug. THE COMPARABLE THAT SETTLES IT: the Matchboard renders the very same JobScreeningPanel without EditableGrid, using one viewport-relative pane, and does not have this problem at all - so the honest verdict is that a fixed master-detail workflow was built on a rearrangeable-dashboard component. Belongs with the sitewide scroll audit above; same root shape.
- [x] Sitewide unnecessary-scroll audit, from Aimee (found Aug30, from feedback cmtg6wens)  — RE-RECORDED: this was written into a handoff on Aug30 and never applied, confirmed by grep returning zero matches. Her words: we have to scroll in the left menu bar and then we also have to scroll in the different positions, plus a request for multiple agents to sweep every page. NOT FIXED, catalogued only. The nav rail is the genuine regression and it contradicts its own comment - components/layout/Sidebar.tsx says the rail is locked to full screen height with only panel or content scrolling, but the icon rail carries no overflow-y-auto, so its own 269px of overflow escapes into the document and forces the whole page to scroll. Measured live at her viewport: scrollHeight 964 against clientHeight 695, and documentElement scrollHeight also exactly 964. The positions panel is a separate and older cause, capped at max-h 78vh in components/matchboard/MatchboardWorkspace.tsx since Jun 17, giving roughly 460px to 9 positions needing roughly 576px, so the Aug31 picker work did not cause it. Same pattern sitewide, ranked by traffic: Candidates (SelectableCandidateTable, a 100-row table in a fixed roughly 1028px EditableGrid slot, the busiest page in the app), Calendar (CalendarWorkspace, every view in a roughly 708px slot, and MonthCalendar has NO overflow handling at all so it silently spills past its box and can overlap what sits below, which is worse than a scrollbar), Job builder (JobBlockAssembly), Pilot Requirements (PilotRequirementsWorkspace, the same cap already removed from the New hires grid on request), Reports (ReportsWorkspace, whose own print CSS already overrides the cap), Imports (ImportsWorkspace). CLEAN, checked: Command Center, Handbook, most of Settings, Events, Interviews, Offers, Orientation, Duplicate review, Fleet charts, Employees, Compliments, Business Cards, Travel, People and the new-hire tabs. SHE ASKED FOR A DURABLE RULE, in her words to note somewhere to stop making the vertical distance shorter than it needs to be - that belongs in CLAUDE.md as a standing constraint, not only in a fix.  *** REOPENED Aug 31: this is the entry that LISTS the offenders, so it cannot be closed while three of them are open. *** See the correction above.  *** STALE ON THE CANDIDATES PAGE, corrected Sep05: SelectableCandidateTable is no longer an offender and there is no EditableGrid on that page at all. *** The file now carries overflow-x-auto only, on a container with no height cap, and the min-w-[1000px] that forced the sideways bar is gone; the records panel sits in ordinary page flow. Verified by reading the file at commit time, not from the rebuild handoff that reported it. The OTHER pages named on this line - Calendar, Job builder, Pilot Requirements, Reports, Imports - were not re-checked and are not claimed either way.
- [x] A React hydration error is live in production on the candidates page (found Aug31)  — minified React error 418, a server-versus-client TEXT mismatch, auto-captured in the browser context of two feedback reports. components/candidates/SelectableCandidateTable.tsx builds an Intl.DateTimeFormat with NO timeZone and formats candidate.updatedAt, a real moment. Vercel resolves it in UTC and her browser in Mountain, so every row touched after roughly 18:00 Mountain renders a different day on each side, across dozens of rows. Confirmed at commit time at line 14 of that file, against a positive control: lib/dates/display.ts sets timeZone OFFICE_TZ explicitly, so the check finds the option when it is there. SEVEN SIBLING FILES under components/candidates share the identical timezone-less moment formatting and none mentions timeZone at all - AiSummaryCard, CandidateActivityTimeline, CandidateCommunications, CandidateNotes, CandidateTimeline, SavedViewWorkspace and the table itself - plus DuplicateReviewQueue and CelebrationsWorkspace elsewhere. Fix as a pattern, not one at a time. CurrencyPanel was named in the report but already sets timeZone, so it is not affected. RULED OUT, checked and explicit: lib/dates/display.ts, lib/calendar/format.ts, every orientation formatter and all localStorage reads. ONE HONEST LIMIT ON THE DIAGNOSIS - the error appeared in reports filed from two different pages, but recentErrors in components/feedback/FeedbackButton.tsx is a module-scope array never cleared on route change, so an error thrown on the first page loaded rides along into a report filed later anywhere else. Treat two unrelated pages as unproven; the candidates cause is the confident half.  *** STALE ON THE CANDIDATES PAGE, corrected Sep05: SelectableCandidateTable is no longer an offender and there is no EditableGrid on that page at all. *** The file now carries overflow-x-auto only, on a container with no height cap, and the min-w-[1000px] that forced the sideways bar is gone; the records panel sits in ordinary page flow. Verified by reading the file at commit time, not from the rebuild handoff that reported it. The OTHER pages named on this line - Calendar, Job builder, Pilot Requirements, Reports, Imports - were not re-checked and are not claimed either way.
- [x] The travel date box stopped saving half-typed years (Hannah, reported Aug 21, fixed Aug 28) — a native date input fires change per keystroke, so typing 2026 walked the value through 0002, 0020, 0202 and 2026 and every one was written, which is the glitching through other dates and landing on 1906 she reported. Writes now wait for a year in 2000-2100, deliberately narrower than sane: 1906 is a valid four-digit year, so a 1900 floor would have let the exact reported value through. Typing feels unchanged because the box still updates; only the write waits, and an out-of-range year is called out rather than failing silently
- [x] THE LATENT travel_complete BUG IS CLOSED, and adding Crew travel is what forced it (Aug 28) — this section carried it as open from Aug 4 and the entry below is now ticked. It fired on ANY of a hire's trips reaching BOOKED with no filter on purpose, and was safe only by accident: the non-onboarding purposes attach to the CANDIDATE so they carried no newHireId. Crew travel is booked for people who already ARE hires, so booking a pilot's line trip would have marked their orientation travel arranged. Only ORIENTATION, INDOC and TRAINING tick it now. Zero existing rows change behaviour, checked against the full scope rather than a lookup: every TravelTrip row was listed, 8 of them, and the 4 carrying a newHireId are ORIENTATION x3 and TRAINING. HONEST LIMIT: the new filter has no live exercise, because closing it would need a CREW or RECRUITING_VISIT trip carrying a newHireId and none exists, so that path is reasoned about rather than run
- [ ] DROPPED, do not re-raise (Aug 28, from the user) — dragging the little time boxes on the hub calendar to change a trip's start and end times. Hannah asked for it Aug 27 and he killed it outright Aug 28, his words: lets remove the drag the timeboxes altogether, we will not be doing that, because that is going to mess up times. Dead rather than deferred, so it should not come back as a deferred item
- [x] Fleet maintenance now reads Quality Control, not Aircraft Compliance (Aug 20, from feedback asked Aug 7) — the card is Quality Control / Remote then QCM, with Quality Control Manager as the role line. THE SEED WAS NOT ENOUGH AND THIS IS THE REUSABLE LESSON: a fleet/mx-roster WorkspaceSetting override existed, so maintenance-data.ts had been dead text since the chart was first edited. Both were changed, the live blob backed up before writing, and no name-to-candidate link was touched since only group and section labels moved. Re-verified at commit time against the live setting: it contains Quality Control and no longer contains Compliance, so the rename landed where the chart actually reads from
- [x] The message editor caret jump is fixed, in the editor she actually uses (Aug 20, from feedback reported Aug 16 and AGAIN Aug 20) — clicking mid-sentence threw the cursor to the start and forced bold. THE FIRST ATTEMPT FIXED THE WRONG COMPONENT: there are two rich text editors, and components/shared is only used by the job editor while the candidate page uses components/richtext. The real fault was worse than the one first diagnosed - that editor reassigned innerHTML on EVERY render with no focus guard at all, so typing faster than React re-rendered handed the element its own stale content back, losing characters and dropping the caret at offset 0, which sits inside the leading bold run these write-ups open with. A focus guard now skips the write entirely while the caret is in the box; blur still normalises and re-emits, so an external change reconciles the moment focus leaves. The components/shared guard was tightened as well and kept, since the same class of bug was latent there
- [x] The New orientation session modal no longer clips its End field (Aug 20, from feedback Jul 28) — three native date and time controls in a flex row could not shrink, because a flex child defaults to min-width auto and Chrome gives those inputs a large intrinsic minimum, so End was pushed outside the panel. Now a grid with min-w-0 children in a wider panel, stacking on narrow widths so no time picker drops under the ~110px Chrome refuses to draw
- [x] The new-hires grid has one scrollbar again (Aug 20, from feedback Aug 17) — the box was capped at 75vh, which left the page itself taller than the screen and put two vertical scrollbars side by side. It now measures the space remaining below it, so the page ends at the fold. Sticky headers, the pinned task column and the on-screen horizontal bar all still work, and the old fixed cap remains as the pre-hydration fallback. Measured rather than done in CSS because the bulk-action bar above it appears and disappears
- [x] Two travel feedback items were already fixed and never closed (Aug 20) — the calendar on the main travel page with a colour per person, and clicking a traveller to expand them, both shipped Jul 31 in 1511c78, three days after they were asked for. Closed in the feedback queue rather than rebuilt
- [x] Ticking an orientation email box by hand now clears the warnings that contradict it (Aug 4) — the internal summary and the reminder health box are both derived from the sent-template marks but loaded once on mount, so marking a late-added attendee as contacted left the page still saying she had no invitation and that the send day had passed with one unsent, with a page reload the only cure and nothing on screen suggesting one was needed. Both re-read the server after the mark is saved. THE ORDERING IS THE FIX, not the refetch: onToggle returns its promise so the refresh waits for the PATCH to commit, because refetching first reads the pre-tick row back and re-renders the stale warning as though it were fresh. Nothing changed about what the health check or the summary judge; both were already correct, only stale. Verified against the live health endpoint, which returned problems 0 for the Aug 4 session while the page was still showing the red box, with the Sep 1 session's pending count of 1 as the positive control proving the number can come back non-zero
- [x] Travel checklist items are now To do / Done / N/A (Aug 4) — a checkbox could only say done or not yet, so an item that did not apply to a trip stayed unticked forever and made a finished trip look incomplete. Same three states and the same words as the onboarding checklist (OnboardingTask.status), deliberately, because both appear on the same profiles and two vocabularies for one set of states is how people stop trusting either. N/A comes OUT of the denominator rather than counting as done, so a trip with two N/A items reads 4 of 4 instead of 6 of 6, and it records who marked it and when, exactly like a completion — deciding an item does not apply is a call somebody made. The cross-trip roll-up no longer lists an N/A item as outstanding. Derived items and waiting-on items keep no picker: one is read from the trip, the other is not ours to finish. THE LIVE STATE WAS DELIBERATELY NOT MIGRATED: it is one shared WorkspaceSetting row holding every trip against the database dev and prod share, so the old done-boolean shape is read in place and a row is rewritten only when somebody next touches that item. Verified read-only first that all three trips with stored state are on that old shape, with the full 36-row scope as the positive control. Also fixed by the per-tick validation: a malformed date was reaching the UI and rendering the literal words Invalid Date. NOT verified: the tri-state picker rendering or responding to a click, since the Browser pane cannot hydrate this app
- [x] LATENT BUG, and the visit gate would activate it (found Aug 4) — travel_complete is ticked by ANY of a hire's trips reaching BOOKED, with no filter on trip purpose (app/travel/actions.ts, the updateTrip status branch). NOT firing wrongly today, verified read-only with the full scope rather than a single lookup: all 6 TravelTrip rows were listed, all 4 RECRUITING_VISIT trips carry newHireId null because they attach to the CANDIDATE, and the positive control is the two trips that DO carry a newHireId — both non-visit purposes. So only ORIENTATION and TRAINING trips can reach it. IT BECOMES REAL THE MOMENT VISIT TRIPS CARRY A newHireId, which is exactly what the pre-offer visit gate requires: booking somebody's pre-offer fly-out would mark their ORIENTATION travel complete. Fix is one clause on the updateMany, and it should be done BEFORE the gate rather than after
- [ ] DECIDED, not built — the pre-offer visit gate (Aug 4, from the user) — non-local candidates, most often maintenance techs, are NOT hired without first being flown to Utah for an in-person visit, and the verbal offer happens only AFTER they have come out. Two decisions were made about how to model that. FIRST: the gate lives on BOTH the candidate application and the onboarding checklist under one shared step key, the same pattern the six offer steps already use, so a visit can be tracked without moving anyone to onboarding first. SECOND: on /offers and the New hires list, somebody parked at the visit gate gets their own status chip reading from the trip date, for example Visiting Aug 9, rather than showing as a stalled 0 of 20. The live worked example is Robert Patrick — candidate stage Interviewing, application offerStatus NONE, already moved to onboarding as NewHire cmsgavnqe000004l2n19p1ukj, with a RECRUITING_VISIT trip BOOKED arriving 2026-08-09
- [ ] Uniforms are not accounted for anywhere (Aug 4, added from the user) — nobody tracks who needs one, what they need, or whether it arrived. THE NOTHING-EXISTS-YET CLAIM WAS CHECKED, not assumed: a tree-wide grep for uniform, apparel, polo and sizing returns only false positives on unrelated features, and the one that looks like a hit — a line in schema.prisma — is the events module's item categories, SWAG PRINT BOOTH APPAREL OTHER, not a field. No NewHire column, no onboarding task, no roadmap line. THE REQUIREMENT IS PER ROLE, not per person: maintenance techs need shirts and other roles will differ. PILOTS ARE OUT OF SCOPE — they do get uniforms, but those are issued at indoc and do not come from us. THE SHAPE ALREADY EXISTS AND SHOULD BE COPIED RATHER THAN INVENTED: business cards are a NewHire.businessCardStatus column (NEEDED / ORDERED / RECEIVED / NOT_NEEDED) plus a business_card task in the SYSTEMS checklist group, and the piece most worth copying is lib/business-cards/checklist-sync.ts, which keeps the column and the checklist task in step so ticking either side updates the other. That is exactly the ordered-for-you, arrived-before-day-one problem. NOT ordinary paperwork: the user's point is that this is a day-one EXPERIENCE item and should be planned for, not just ticked, so arriving late or wrong is the failure mode to design against. OPEN QUESTIONS, none answered and all needed before building: which roles need what garment, who places the order and from which vendor, how sizes are collected and whether they can be asked for without storing anything sensitive, how far ahead of the start date it has to be ordered, and whether it should be a checklist task, a status column, or both the way business cards are
- [ ] STANDING BEHAVIOUR — a last-minute move between orientation sessions is handled by hand, and the app must accept it (Aug 4, from the user) — moving a new hire between sessions at the last minute happens from time to time. The person is contacted outside the app, their three email boxes are ticked by hand, and that must count as fully satisfying the email requirement. A move creates a FRESH attendee row with no marks carried over, so it is three ticks per moved person. The worked example: Grace Ward was moved from Sep 1 to Aug 4 on the morning of Aug 4 because she became able to attend, and her invitation, supervisor and reminder emails were all handled outside the app and marked manually. That is why a false unsent warning matters here — it is the one case where it would cost a real duplicate email to a new hire
- [ ] INTERVIEW TIMES MAY BE STORED SIX TO SEVEN HOURS OFF IN PRODUCTION (found Aug 3, flagged not fixed) — this would change what somebody does, so it is recorded with evidence rather than acted on. THE MECHANISM IS CONFIRMED IN THE CODE: the schedule form uses a datetime-local input, which yields a naive string with no offset, and the server parses it with new Date, which resolves in the SERVER's zone. Vercel runs UTC. The identical string 2026-08-05T14:30 parses to 14:30Z under TZ=UTC and 20:30Z under TZ=America/Denver. It works perfectly in local dev because the developer machine IS Mountain, which is exactly why it was never caught, and it is pre-existing rather than introduced by this work. THE LIVE DATA WAS THEN CHECKED READ-ONLY, 40 most recent interviews: most are correctly offset (15:00Z to 21:00Z, reading as 09:00 to 15:00 Mountain), BUT six recent records sit at exactly 12:00:00.000Z, which displays as 06:00 Mountain — the precise fingerprint of a noon entry parsed as UTC. So the damage looks PARTIAL rather than universal, which is the fact that matters for deciding between fix-forward and migrate. Caveat on that read: the plausibility window used was 07:00-18:00 Mountain, which over-flags genuine evening interviews in the 2025 historical import, so treat the exact-12:00:00Z cluster as the signal and not the raw suspect count. NOT FIXED deliberately: making new writes correct while old rows stay wrong could be worse than consistent. The correct helper already exists as zonedWallClockToUtc in lib/dates/display.ts. Needs a decision on the existing rows first
- [x] Cross-group navigation is one click again (Aug 3) — the sidebar flyout existed but was gated to the collapsed rail only, which made collapsed faster to navigate than expanded; it now shows for any group that is not already open. Measured before the fix by extracting nav hrefs from rendered HTML, with positive controls on two other groups: only 7 of 36 destinations were one click from the landing page
- [x] The last real-link-rule violation is gone (Aug 3) — NewJobButton used router.push for pure content navigation to an existing job and is now a Link, so it is ctrl-clickable into a new tab, which is exactly the case where you want to read the clashing requisition beside the one you are creating. The other twelve router.push sites were checked and are legitimate post-write redirects
- [ ] Still open from the navigation review, none of it done (Aug 3) — no detail page anywhere has prev or next, so working a queue of five candidates costs ten navigations; the candidate back link discards a search that is already in the URL as a query parameter; and half the app's tabs live in useState rather than the URL, so they cannot be linked or survive a refresh
- [x] Active candidates in system is no longer wrong (Aug 3) — it read 361 before a scan and 3,432 after, because the page counted status ACTIVE while the matcher used the canonical scan pool. Both paths call the same helper now rather than two predicates that were supposed to agree, since duplicating it is what caused this. Evidence, read-only with the full status scope as a positive control: ACTIVE 361, MERGED 27, ARCHIVED 3,140, 70 rows carrying a scan-exclusion reason, canonical total 3,432. The page renders 3,432 on first load with no scan
- [x] Six fake progress bars removed (Aug 3) across the Command Center, Calendar, Duplicate Review, Imports, Pilot Requirements and Recruiting Jobs. A hardcoded two-thirds bar sat inside each stat-tile map, so every tile showed the same meter regardless of its number. The whole meter went rather than just the fill, since an empty track reading zero percent is equally false. Zero occurrences remain sitewide, confirmed
- [x] Modal no longer traps the user (Aug 3) — one 29-line component backed 22 call sites with backdrop-click as its only close, and gated even that on busy, so a modal mid-save could not be left by any means. It now has Escape, a visible close control, role dialog with an accessible name, a focus trap, scroll lock and focus restore, and saving no longer means you cannot leave — the double-submit guard stays on the submit button where it belongs
- [x] The interview write-up can no longer strand a recruiter's typed notes (Aug 3) — a rejected fetch skipped the line clearing the busy flag, so the button read Saving forever, stayed disabled and showed no error; long-form notes were recoverable only by not touching the page. Notes are cleared only after the server confirms
- [x] The orientation cc list can no longer silently wipe itself (Aug 3) — a failed load read as empty, the panel said nobody is cc'd, and adding one address then POSTed a one-element replacement list over everyone configured. Load failure is a distinct state now and refuses to save. No write is computed from a failed read
- [x] Four empty states stopped lying while the request was in flight (Aug 3) — including the PUBLIC booking page, which told external invitees there were no open times before it had asked, and the add-job search, which offered a create-new-job button during the debounce and so produced near-duplicate jobs in the live database. That last one is a real mis-write caused by a missing loading state rather than a missing guard. Each is modelled as null-until-loaded so empty and not-yet-asked cannot be confused
- [x] The supervisor picker no longer lets a slow response for a shorter query overwrite the results for a longer one (Aug 3) — it had no in-flight guard while its siblings did
- [x] EditableGrid no longer collapses the user's layout on an unrelated re-render (Aug 3) — it keyed off the panels array identity rather than the panel id set, the landmine CLAUDE.md documents, so any parent state change reset the grid; on the candidate profile that meant a reset on a timer after every save. It compares by value now, protecting every future caller, and the two inline call sites were memoized
- [x] Drag-to-reschedule reports failure instead of swallowing it (Aug 3) — it had no else branch and a catch commented silently fail, so a rejected move looked exactly like a missed drop and got retried. The card moves optimistically now and rolls back with the reason shown
- [x] A month grid firing roughly sixty prefetches on one screen now matches the prefetch-false pattern already used elsewhere (Aug 3)
- [x] Date fields accept a pasted date (Jul 21) — you can now paste 10/02/2026 (or 6/1/26, 2026-10-02, 10-02-2026, 10.02.2026, "Oct 2, 2026", "October 2 2026", "2 October 2026", "02-Oct-2026") into any date field in the app and it fills correctly. Built as ONE document-level paste listener mounted in the root layout rather than a wrapper swapped into each of the ~23 date inputs: nothing at the call sites changed, so nothing could regress, and every date field added from here on is covered without anyone opting in. US month-first wins on ambiguous input like 12/11/2026 since that is the house format; day-first is only used when month-first is impossible (22/07). Anything it cannot read confidently — "next tuesday", a name, a bare year — is left alone and the browser handles the paste exactly as before, so it can only ever add behaviour. Parser is lib/dates/parse-pasted-date.ts, 25 cases covered including Feb 30 and other impossible dates. Verified in a real browser: a genuine paste event on a live date field filled it and the value survived React reconcile (so the controlled onChange really fired), unparseable text was NOT intercepted, and text inputs are untouched.
- [x] Front threads get TAGGED, not just commented (Jul 21) — a comment is only visible on the one conversation; a tag is searchable, so the automation's work can be found in Front. Threads the scan acts on now get [Automated] plus a tag naming which step it was; anything it saw but could NOT action (unknown name, ambiguous match, wording it could not read) gets a needs-review tag, which turns Front itself into the follow-up list. Matched by NAME, case-insensitively, so creating a tag in Front is all it takes to switch one on — no tag ids in code. A name with no matching tag is REPORTED rather than thrown: the checklist update is the work, a label must never cost us it. Wired to the four tags the user created under an "App Tags" parent: automated, submitted background check, background check complete, needs review. Each slot accepts SEVERAL spellings and takes the first that exists, because the user said these names may change — a rename then cannot silently switch tagging off. Nesting is irrelevant to matching; a nested tag keeps its own plain name. VERIFIED against Front, not just by return value: ran a real scan and read four conversations back through the API, all four carrying Needs Review. The Check Paycom mail panel lists any tag name it could not find. Only newly-handled threads are tagged — the 40 already-recorded ones are left alone rather than bulk-writing to Front; say so if you want history backfilled
- [x] Dashboard says when someone is cleared to hire (Jul 21) — closes the loop on the background-check automation, which until now ticked a box and told nobody. When bg_check_complete is DONE but Hire in Paycom is still outstanding, the person shows on the dashboard alerts strip as a GREEN row: "background check cleared — hire in Paycom". Framed as an action rather than a status, so it names what to do and where; it clears itself the moment Hire in Paycom is ticked, so there is no second list to maintain and nothing to dismiss; and it never appears at all if the check clears and they are hired the same day. Escalates to red after 3 days sitting unactioned (READY_TO_HIRE_ESCALATE_DAYS in lib/data/onboarding.ts is the dial) — good news that nobody acts on becomes a problem. "ready" is the first POSITIVE alert level; the other three all mark something wrong. Verified on a throwaway fixture: green on day 0, red at 4 days with the day count in the text, and gone once hired. Currently matches nobody, because no background check has completed yet — the first real completion email is what lights it up
- [x] Rename a checklist GROUP heading (Jul 21) — Offer / Pilot documents / Onboarding & systems / Orientation are now editable in the same Manage tasks panel as the task names, rather than being fixed in code. Only the DISPLAY name changes: tasks stay filed against the same underlying group key, because the Paycom and offer handlers look tasks up by key and a rename must never re-file anything. Clearing the box restores the built-in name instead of leaving a blank heading. Stored beside the existing per-task renames in the same WorkspaceSetting, so there is one place to look. The Custom group heading stays fixed, since it is generated rather than configured
- [x] Checklist grid scrolls in its own box (Jul 21) — with enough active hires the grid runs off the right of the screen, and the horizontal scrollbar sat at the bottom of the PAGE, so seeing the last few people meant scrolling all the way down just to scroll across. Rather than bolt a second scrollbar on top, the table now lives in a bounded box (max-h 75vh, overflow auto): the horizontal bar is at the bottom of that box, which is on screen. Two things the markup was already written for start working as a side effect — the header row had sticky top-0 that had never done anything without a bounded container, so names and progress now stay put while scrolling down, and the corner cell was pinned only left so it now pins both ways. Re-stacked the sticky z-indexes for the new vertical scrolling: corner 30 above header row 20 above task column 10, since the row labels previously sat ABOVE the header and would have covered it. max-h only caps, so a short list is unchanged. NOT VERIFIED INTERACTIVELY: the Browser pane renders this app inside a hidden container, so scroll and sticky behaviour could not be exercised here — the classes, computed max-height (540px at a 720px viewport) and stacking order were checked instead
- [x] Checklist grid: a line between each person's column (Jul 21) — from Hannah's feedback, that the grid "all blends together since there aren't defined lines separating each checklist", unlike the Excel sheet it replaced. Every column IS one person's checklist, and the table had horizontal rules but none vertical, so the ticks read as one wall rather than a column per hire. Added a faint vertical rule to every cell in a person's column — header, the facts rows, the group bands and the task rows — so the line runs unbroken top to bottom. Kept faint on purpose: the job is to separate columns, not to draw a cage around every tick. One shared COLUMN_RULE constant rather than five copies of the class
- [x] Candidates page said 3,213 above a list of 45 (Jul 21) — reported as "I can only see 44 candidates but the system says 3213, can you check into this". No data was missing and the list was right: of 3,214 candidates, 3,169 are ARCHIVED historical records (3,159 from the JazzHR import plus 10 Paycom) and only 45 are live. The list correctly showed the live ones while the stat tile counted every candidate ever imported, so the page contradicted itself and read as data loss. With-files and with-applications were inflated the same way. Every tile now counts the SAME population the list shows, "Total candidates" is relabelled "Candidates here", and a new "In historical archive" tile plus a sentence linking to the archive answers "where did the rest go" on screen instead of by asking. Also replaced "Showing up to 100 records", which left you to guess whether 45 meant all of them or a truncated view, with either "Showing all 45" or "Showing the first 100 of N" — the list is capped at 100 and has no paging, so it must say when it is holding back (see the archive paging item for the same problem there)
- [x] Travel: the times were stored wrong, not just shown wrong (Jul 21) — chasing "I fixed the timezone but Brandon Edwards travel is still off" found the real cause. The confirmation extractor called new Date() on a bare wall-clock string from a booking email, and a string with no zone is read in whatever zone the code runs in — UTC on the server. So a 9:37am departure was written as 09:37Z, which IS 3:37am Mountain. The display fix earlier was correct and fixed a different bug; this data was wrong underneath it. The parser now reads date/time into components and anchors them to Mountain explicitly, and the three existing rows were corrected (Brandon 3:37am/4:39am to 9:37am/10:39am, Caleb Green 1:45am to 7:45am) with an undo at scripts/travel-time-fix/fix.ts --undo
- [x] Travel: a date no longer needs a time to count (Jul 21) — a travel item used one datetime-local input, which yields NOTHING until both halves are filled, so recording a flight whose departure time is not known yet saved no date at all — and the item then never appeared on the travel calendar, which is the "the little calendar disappeared" report. Now a required date plus an OPTIONAL time. A day with no time is stored as midnight Mountain and treated everywhere as day-known/time-unknown, so the calendar draws it and shows no invented 12:00 AM
- [x] Travel: autosave says so (Jul 21) — the panel saves as you type (on blur, or on change for pickers) and said nothing at all, so "did that save?" had no answer and a failure looked identical to success. Every item edit now flashes Saving / Saved, and shows Not saved in red if the write fails
- [x] Travel calendar bucketed days in UTC (Jul 21) — dayKey used toISOString().slice(0,10), so an 8pm Mountain departure (02:00Z the next day) was drawn on the wrong square. Now bucketed by the Mountain day
- [x] Times and dates show in Mountain, not UTC (Jul 21) — two bug reports, one root cause. Travel showed every flight six hours out (a 9:37am departure read as 3:37), and the offer stepper dated an offer signed at 6:10pm on the 20th as the 21st — an evening action in Mountain time is already tomorrow in UTC, so the date silently jumped a day every evening. Confirmed against the real record: Tara Ward's offer is stored at 00:09:12Z, which is Jul 20 6:09pm Mountain, and the app was displaying Jul 21. THE TRAP, and why this had been "fixed" before and came back: this database holds two different kinds of date and they must be rendered differently. A MOMENT (offerSignedAt, offerSentAt, a flight's startsAt, any completedAt) is a real instant and belongs in the office timezone. A CALENDAR DAY (startDate, orientationDate, birthday, offerStartDate) is a day someone picked, stored at midnight UTC, and MUST stay in UTC — render it in Mountain and it reads back as the previous day, so blanket-converting everything would have broken every start date in the app. New helpers in lib/dates/display.ts are named after what the value MEANS (formatMomentDate/Time vs formatCalendarDay) so the choice is explicit at each call site instead of a bare timeZone option nobody questions. Fixed in the offer stepper, the Offers list, and all three travel views — each of which had been using ONE formatter for both kinds. Verified with the reported values, both DST halves of the year (it follows MST/MDT rather than a hardcoded -6), and a check that chosen days still render unchanged. The remaining ~20 UTC formatters in the app were reviewed and left alone; they are all genuine calendar days
- [ ] Move Historical Archive into Recruiting/Candidates (out of Data) — the historical archive is old candidate records, so it arguably belongs with Candidates under Recruiting rather than tucked under Data where it is easy to miss. Reconsider its nav home (and whether /archive + /archive/reports move together). Judgment call — Data may have been chosen because it is a heavy search/reporting surface; decide intentionally. Added Jul 20.
- [x] Popups close by X, Escape, AND clicking off — site-wide sweep (Jul 19) — root cause of the reported dead-X: a global CSS rule set pointer-events:none on EVERY button under a view-only module, which killed the close X (backdrop-click + Escape survived because they aren't buttons, which is why it looked intermittent — admins saw a working X, view-only users a dead one). Fixed by excluding data-dialog-close controls from that rule (globals.css) and tagging close buttons. Added a shared useDialogClose hook (Escape) and swept it across the two org charts + ~13 one-off modals (candidates, calendar, jobs, travel, employees, settings, feedback) so X + Escape + click-off all work. Menu/flyout popovers (already close on outside-click, no X) left as-is. (added Jul 17)
- [~] Everything that navigates should be a real link (ctrl/right-click -> new tab) — site-wide expectation: any click that changes the page must be openable in a new tab. Audit (Jul 9): most content navigation already uses next/link (candidate/employee/pilot rows, nav, Reports rosters). Fixed the Matchboard role + candidate list cards (were button + router.push -> now Link href=/matching?mode=..&id=..). REMAINING: the Matchboard in-panel cross-links (JobScreeningPanel "view candidate" / CandidateRoles "view role" callbacks — shared component, deeper change) and the mode tabs; and consider an eslint guard so onClick-navigation never creeps back in. (Legit non-links left alone: post-submit redirects, filter/query updates, file downloads.)
- [x] Calendar weekly view errors (Jun 10) — dynamic hour range so interviews are never clipped; no column crushing
- [x] Click outside a window/panel to close it (Jun 10) — interview editor + feedback panel close on outside click
- [x] Movable feedback button (Jun 20) — shift-click and drag the floating Feedback button to reposition it (so it no longer covers resize handles); position persists across reloads via localStorage
- [x] Feedback & suggestions inbox (Jun 10) — covered by the Feedback button + Settings inbox
- [x] Done feedback sinks to the bottom and fades (Jul 22, requested) — the inbox listed everything newest-first, so items already marked DONE sat between the ones still needing attention. Anything DONE now renders below the active items behind a "Done · n" divider at 55% opacity (full opacity on hover, so it stays readable when you go back to one); active items keep newest-first order, and the type/status tags are unchanged. Purely a reordering of what was already fetched — no query, filter, or status-editing change, so the Status filter still works and picking "Done" simply shows that group on its own. Verified over HTTP against the real inbox: 7 active items render first, all 9 DONE contiguous at the end, faded
- [x] Candidate duplicate merge & dismiss (Jun 11) — review queue now lets you pick which record to keep, merge, or mark not-a-duplicate
- [x] Duplicate review: clear resolved items from the scan (Jul 7) — the queue now shows OPEN items only (resolved/dismissed pairs drop off; the Resolved stat still counts them). The scan de-dupes against EVERY prior review item (open, resolved, or dismissed) and skips already-merged candidates, so a pair you merged or marked "not a duplicate" never comes back on a re-scan.
- [x] Duplicate review: bulk merge (Jul 7) — each open pair has a checkbox + keep-record selection; "Select all", "Merge selected", and "Dismiss selected" run them one pair at a time (each merges its own two records — not one global merge). Individual Merge / Not-a-duplicate buttons remain. New DuplicateReviewQueue client component.
- [x] Candidate document upload fixes (Jun 11) — fixed upload error, removed the redundant upload button, and added "Link" to attach an Imports-uploaded file to a candidate
- [x] PDF viewer polish (Jun 11) — 100% zoom, whole-page fit, and search match navigation that scrolls inside the pane
- [x] Job role classification fix (Jun 11) — imported jobs are Pilot only when the TITLE says Captain / First Officer / PIC / SIC / Pilot (aircraft names no longer imply pilot, so "Senior Gulfstream Technician" is support); added a Pilot/Support toggle + seat/aircraft editor on the Jobs detail that clears pilot tags everywhere and removes the role from Pilot Requirements when set to Support; corrected the existing mis-flagged job

## Scheduling / Booking Links (Calendly replacement)
Public "schedule with me" links so candidates and guests can self-book onto the shared SkyShare calendar.
- [x] Calendar Timeline view (Jun 15) — per-person schedule timeline (rows = recruiting team with avatars, bars colored by interview stage) added as a 4th calendar view alongside Month / Week / Day
- [x] Booking data model (Jun 15) — BookingHost per employee (slug + settings), recurring weekly availability, per-date overrides (vacation / holiday / custom hours), meeting types (30 / 45 / 60 min, 0 or 10-min buffer, Interview or Meeting), and Booking records
- [x] Slot engine (Jun 15) — recurring weekly windows minus busy time, honoring minimum notice (default 6h), bookable window (default 90 days), max bookings per day, buffers, and the invitee's timezone
- [x] Per-host availability on one shared calendar (Jun 15) — every booking event is tagged with the host id so each person's free/busy is independent even though all events live on the single shared Google Calendar; also reads the host's optional personal @skyshare calendar free/busy
- [x] Public booking pages (Jun 15) — /book/(slug): pick a meeting type, see open times in your own timezone, and book; Interview-type bookings auto-create or match a candidate + interview; no login required
- [x] Admin scheduling page (Jun 15) — /scheduling: manage team members, weekly availability, date overrides, meeting types, settings (min notice / max per day / buffer / window), and copyable share links
- [ ] Read @skyshare calendars without each manager opting in — enable service-account domain-wide delegation (one-time Workspace-admin step) so each host's real Google calendar free/busy is honored automatically
- [ ] Email confirmations — confirmation + reschedule/cancel emails to invitee and host (needs a transactional email service or Gmail send)
- [ ] Text / SMS reminders — optional SMS reminders before the meeting
- [ ] Reschedule / cancel links — self-serve tokenized links for invitees
- [ ] Round-robin pool — one link that assigns whichever team member is free
- [ ] General meetings on the in-app calendar — show MEETING-type bookings (no candidate) on the calendar / timeline, not just on Google
- [x] Scheduling nav entry + anti-spam (Jul 9) — Scheduling was already in the sidebar (Interviews & Scheduling); added basic bot protection to the public booking endpoint: a hidden honeypot field (bots that fill it get a fake "booked" response but nothing is created — verified) plus a per-IP rate limit (8 / 10 min, best-effort per serverless instance). lib/rate-limit.ts

## Candidate Intake Automation
Auto-ingest applicants from Paycom into the system, hands-off.

- [ ] Batch add MATCHES, it does not CREATE - raised by him the moment it shipped (Sep04, from him) — his words: if I am just adding lines of contact info then I need to get that info elsewhere, and there is no guarantee these candidates are in the system already with their resume. He is right about the mechanism, and it was checked rather than argued: the batch route reads candidates twice and creates none, so a line that matches nobody is dropped with the review list saying N not matched - add those by hand. It also fetches no resume, so somebody in the system without documents links fine and then cannot be ranked. WHERE THAT LEAVES IT, measured: of 497 live candidates, 461 (93%) already have readable document text and 431 (87%) have metrics, so the gap is people who are not in the system AT ALL rather than people in it without a resume. The two paths that actually bring both a person and their documents are the Adobe Sign pilot application (265 of the 497) and resume intake (119) - together 77% of everyone live. THE PREVIEW IS ALREADY THE ANSWER TO HIS QUESTION and costs nothing: paste a real requisition list, and it reports matched versus unmatched and flags who has no readable documents, without writing a thing. If the unmatched share turns out to be large on a real Paycom list, the next step is letting the review step CREATE a candidate from an unmatched line - which is a different feature and needs deciding, since a candidate created from a name and an email has no resume and so cannot be scored either.
- [x] RECORDED, where candidates and resumes actually come from (Sep04) — worth having written down because the batch add's usefulness depends on it and nothing else states it plainly. Live candidates by origin: Pilot application via Adobe Sign 265, Resume intake 119, Employee backfill from the org chart 50, one XL captain CSV 22, then Indeed 7, Career Page 5, Manual entry 4, Offered-not-hired 4, JazzHR 4, Referral 3, Climbto350 3, LinkedIn 3. So the resume arrives WITH the person on the two big paths, and the small paths are where a candidate can exist without documents.
- [x] Add many candidates to a job in one pass (Sep04, from him) — paste a requisition's applicant list, review what it matched (email then phone then name, each needing exactly one live candidate, merged tombstones excluded from matching entirely so a merged person resolves to the KEEPER instead of reading as ambiguous), link the confirmed ones, then scan only that batch. Before this it was one search per person, and the scan afterwards could only be scoped by req, which sweeps up everyone already on it - 21 extra people on the AMT req. Preview writes NOTHING, the same dry-run-then-apply shape the scripts use and for the same reason: dev and production share one database. Apply deliberately takes IDS rather than the text again, so what was confirmed on screen is what gets written and a re-resolve cannot land on a different person. The three link rules moved into lib/candidates/link-to-job.ts and BOTH routes call it, rather than the batch path growing a second copy of guards that had already been written against two different columns in three places. Verified at commit time: both guards survive the extraction with the merged check still first and still going through the shared helper, the batch route requires candidates:write AND applies candidateScopeWhere so per-user candidate scoping holds, and zero applications carry source Batch add against a control of 35 carrying Manual entry - so their apply test really was cleaned up. NOT exercised: the reactivation, employed-hire and merged branches end to end, and the modal has never been clicked in a browser; only the API was tested.
- [x] SIZED, the one behaviour change, because the handoff flagged it without a number (Sep04) — the single-link endpoint used to return early on an already-linked application and never consider reactivation; it now does, so re-adding an archived candidate who is already on the job brings them back. Live data: 3,119 archived candidates are already on a job, which is the entire set the change can reach. The two guards hold - 2 merged-away tombstones and 2 employed hires (Michael Salvagnini, Stephanie Hernandez) correctly blocked. That leaves 3,115 who WOULD be reactivated, 85% of the candidate table, but ONE AT A TIME and only when somebody deliberately re-adds that person to a job they are already on. Before, that click did nothing at all. Judged safe to ship on that basis rather than on the code reading alone.
- [ ] The single Add candidate button does not warn that adding an archived person reactivates them (found Sep04) — the BATCH path handles this properly and is the one that could do it at scale: an archived match is tagged archived - adding reactivates in the review list, already-linked rows are NOT pre-selected so they must be ticked on purpose, and the result reports how many came back. The single button, used from both a candidate's page and a job's page, has none of that - it silently reactivates. Small, and the fix is a line of copy plus the same tag, but worth doing while the reason is fresh, because the reactivation is now reachable on a re-click where before it was not.
- [x] RECORDED, three things about batch add (Sep02 and Sep04, from him) — it should be a REAL FEATURE and not a script, because he does this often. The target that drove it was the Challenger 350 SIC req, the Challenger 350 First Officer job, which had 1 application on it as of Sep 2. He has NO PREFERENCE between saved-view and explicit-ids scoping for the scan; the goal he stated is simply to scan his active candidates and see which are qualified. And on Sep 4, holding the work for two days: he was overwhelmed and could not get to it, wants it added, and wants to be sure it does not break anything else and that he will know how to use it - which is why the behaviour change above was sized against live data before it went out rather than described.
- [x] The Paycom notice classifier has a runnable guard now (Aug 16) — scripts/paycom-notice-tests.ts, 20 cases against verbatim Paycom bodies, read-only with dryRun true throughout, exiting non-zero on failure and following the scripts/pilot-app-dry-run.ts pattern because this repo has no test runner. WHAT IT GUARDS: Paycom sends four offer notices about the same person from the same address, all carrying the wording offer for NAME (id) to fill Requisition N, and only Offer Accepted means the candidate said yes. Pending Offer Approved is our own approver signing off days earlier - for Dayten Schureman that was Aug 10, 13 and 14 against an acceptance on Aug 15 - so a subject pattern loosened to match the bare word offer would tick Candidate signed offer letter on somebody who can still decline, and nothing would fail visibly. It asserts on CLASSIFICATION rather than on a tick, so it fails for parser reasons and not for roster churn
- [x] The guard was mutation-tested, not just run green (Aug 16) — loosening the subject match from the full Offer Accepted form to a bare offer match turns 5 of the 20 cases red, including a reply quoting an old notice being acted on as a fresh acceptance, so it demonstrably catches the failure it exists for. lib/paycom/notices.ts was restored afterwards and confirmed clean. Re-run independently at commit time: 20 of 20 pass, exit 0, and the run wrote nothing - Paycom entries in the activity log stayed at 12 and Dayten Schureman's completedAt was unchanged to the millisecond
- [x] The Offer Accepted path fired for real in production (Aug 16) — Dayten Schureman's Candidate signed offer letter ticked at 5:33 PM Mountain from the routed Paycom notice, with a matching OFFER_ACCEPTED activity-log entry. Verified whole-scope rather than by sampling: of the 12 Paycom ticks ever written to the activity log this is the ONLY OFFER_ACCEPTED one, the other 11 being background-check notices across seven people from Jul 21 onward, so this was that path's first real firing rather than a repeat

- [x] Front tag names live in one registry now, and the Aug 16 rename had killed two of them (Aug 16) — the user renamed the tags into a numbered SkyShare Journey group, and a name that does not exist resolves to null while the sweep carries on untagged, so nothing errored and nothing was tagged. Each sweep carried its own private copy of the names - lib/paycom/scan.ts, lib/pilotapp/scan.ts, lib/travel/from-email.ts - plus a FOURTH nobody had counted, in app/api/front/webhook/route.ts, whose travel list guessed at travel booking and travel confirmation; an audit of the live account found neither exists or ever did, so it worked only because the real name happened to be first. All four now read lib/front/tags.ts. HONEST COUNT, because the handoff said four lookups across two sweeps and that overstates it: four individual spellings were dead, but resolveTagIdByNames tries each spelling in order and a surviving alternate saved two, so exactly TWO tags stopped being applied and both were in the Paycom sweep - 2.1 submitted background check and 2.2 background check complete, which are the ones the team actually watches. The pilot-app and travel sweeps were never broken. Audited read-only against the live account: 59 tags exist, 12 in the SkyShare Journey group, and all 13 names the code asks for resolve. RECORDED, from the user: matching is case-insensitive, but the N.N number prefixes are part of the tag name, and a wrong name silently tags nothing rather than erroring - which is the whole argument for one registry over three constants
- [x] Paycom's Offer Accepted notice now ticks the checklist, and it comes through FRONT (Aug 16) — Paycom addresses that notice to one person and copies no shared inbox, so it lands in a PRIVATE Front inbox that a company API token cannot read at all: Front answers 403 to that endpoint by design, with no setting that changes it. The user's own Front rule solves it by creating a copy in the shared HR Onboarding inbox, and the sweep's queries are written not to trust that copy's headers - bare address terms rather than from: clauses, because a rule-created copy may keep Paycom as sender or be rewritten, and Front searches bodies so a bare term is a strict superset. PROVEN TWICE the same day: a dry run found Dayten Schureman's acceptance with the position read correctly, then a live run ticked it, with candidate_signed moving 198 to 199 and a matching timestamp. The classifier guards the collision trap that makes this risky - three sibling subjects (Pending Offer Approval, Pending Offer Approved, Pending Offer Expires Soon) share the same body wording, and matching loosely would tick a signed offer days early on someone who can still decline
- [x] The Gmail reader for that notice was built and REMOVED the same day (Aug 16, the user's decision) — worth recording as a decision rather than deleting quietly, because the reasoning is what stops it being rebuilt. A second door reading aschaedig@'s mailbox directly shipped in the morning on the belief that Front could never see the notice. It could: the forwarding rule above already carried it, so both doors read the same mail. The cost of the second one was a WHOLE-MAILBOX Google grant, since gmail.readonly is the narrowest scope that can read a message BODY and Google has no per-sender scope. Removed: lib/paycom/gmail-scan.ts, lib/google/user-gmail.ts, the /api/gmail/scan-offer-accepted route, the Gmail branch of the nightly cron, and the offer-mailbox section of the scan dialog; auth.ts no longer requests the scope. THE GRANT WAS REAL AND HAS SINCE BEEN CLEARED, which is a different thing from never having happened and is worth the distinction. A stored token keeps the scopes it was issued with, so removing the scope from auth.ts revoked nothing by itself - and a token WAS issued carrying it, because Aimee signed in during the hour it sat there. Two independent records of that: the scope audit read 1 of 5 accounts holding gmail.readonly at the time, and Google answered SERVICE_DISABLED to a Gmail call, which it only reaches AFTER the scope check passes (with no scope it answers ACCESS_TOKEN_SCOPE_INSUFFICIENT, which is what it answers now). She then signed out and back in once more after the removal shipped, which cleared it - exactly as predicted. Re-verified Aug 17: 0 of 5 accounts carry any gmail scope, with four of five carrying calendar.events as the positive control, so there is nothing outstanding to revoke. Recorded this way on purpose - a present-tense query cannot establish a past absence, and a whole-mailbox grant on a named person's account being real for an hour is the honest version. The Gmail API itself was never enabled in the Google Cloud project, so there is nothing to switch off there; Calendar is a separate API, is enabled, and is untouched - verified with a live events.list returning 200 on the calendar.events scope the app actually requests. IF FRONT EVER STOPS CARRYING THE NOTICE, this is the trade to reconsider rather than a gap to quietly re-add
- [x] STANDING RULE from the user (Aug 16) — anything the app does off the back of an email should carry a Front tag saying why or how it happened, so the work is searchable in Front rather than visible only as an internal comment. Where a thread exists this now holds throughout: offer accepted, both background-check steps, a file filed, a candidate created, travel imported, or needs review, each alongside [automated]. The ONE place it structurally cannot hold is the Gmail door above - that notice has no Front thread to tag, which is the entire reason it is read from Gmail; if a forwarded copy does reach Front, the Front sweep tags it there
- [ ] Three Front tags exist but nothing sets them (Aug 16) — 1.1 offer sent to candidate, because no notice the app reads announces the send and Paycom's Pending Offer Approved only grants permission to send, which is a different event and ticking it off that would repeat the approved-versus-accepted mistake; 3.1 and 3.2 drug screen, because emailing docs to ITS is a manual step today, though mail from bgc-netjets@integritytesting.net does arrive in the account and may carry results, and nobody has read one closely enough to write a parser; and [tracking], which exists in the SkyShare Journey group but whose intended purpose the user has not stated and which the app does not set. Left unwired ON PURPOSE - a tag applied on a guess is worse than one never applied, because it looks like evidence
- [x] Dry-run pre-flight for bulk resume intake (Aug 6) — reports per file what an upload WOULD do before anything is written: create, reuse, duplicate, un-archive, already-on-job, file-already-attached, no-text, no-name. Built because bulk-uploading 137 Paycom resumes onto one job had TWO SILENT FAILURE MODES: extractFileText returns empty for .doc, .docx and scanned PDFs, so those people are invisible to keyword search forever and nothing says so; and resume-intake dedupes on email and phone only, never name, so a returning applicant with no parseable contact detail becomes a second profile. THE PARSING IS SHARED, NOT COPIED: name, email and phone extraction moved out of the route into lib/candidates/resume-fields.ts and both the route and the script import it, so the dry run cannot drift from the real run — verified, and the diff is a near-balanced 179 lines out of the route against 171 into the new module. Pre-flight over the 137 files, nothing written: 73 create, 63 reuse, 1 would-duplicate, 11 would-unarchive, 4 already-on-job, 12 file-already-attached, 2 no-text, 1 no-name, 35 the same person twice within the batch. Also measured while there: of 6,306 candidate files, 4,489 carry extracted text — 1,817 have none and are unsearchable
- [ ] resume-intake has no already-filed guard (found Aug 6) — re-uploading a resume already attached to a candidate creates a duplicate candidateFile row. The document-intake script checks originalFilename plus source before writing; the route does not. The pre-flight can see the condition and reports it as file-already-attached, which is how the 12 in this batch were spotted, but the route itself would still write them
- [x] THE AMT BULK UPLOAD RAN, and the pre-flight predicted it (Aug 6) — the user uploaded the 137 resumes the same session the pre-flight was built. Verified read-only afterwards, with the totals rather than a single number: candidateFile 6,306 to 6,429, AMT applications 143 to 246, and files containing Elevate MRO 0 to 8. EVERY NEW FILE EXTRACTED CLEANLY — withExtractedText rose by exactly the same 123 as the total, so none of the new resumes landed unsearchable, which is better than the pre-flight's two no-text predictions and means the two it flagged were among the reused rather than the created. Searching bare Elevate now matches 54 candidates of whom about 6 actually worked for an Elevate company, which is the concrete case the multi-select shortlist was built for
- [ ] THE AMT BULK UPLOAD, and the traps in it (Aug 6, from the user) — the target job is Aircraft Maintenance Technician, id cmqvr1znm001kxcrm0q0t0hri, reqId AMT.2, OPEN, department Maintenance. That was pinned by listing all 14 jobs matching Maintenance, Technician or Mechanic: exactly one is OPEN under that name, and the similarly-named Weekend Shift, G450 and Maintenance Technician jobs are all RETIRED, so picking by title alone would hit a dead job. THE FOLDER HAS A TRAP: the 137 AMT resumes are in Downloads/(16856)-ResumeDocuments created 2026-08-06 14:21, and the SAME folder holds 15 unrelated resumes from May 21 that must NOT go onto the AMT job — which is why the pre-flight takes a --since filter. SEARCH TERMS FOR THE FORMER EMPLOYER: Elevate is written Elevate MRO in the maintenance resumes, and Elevate Aviation appears to be the same company under its older name, with one resume carrying both — so search Elevate MRO and Elevate Aviation and never bare Elevate. The user wants any Elevate-named AVIATION company and not the non-aviation ones, Elevate Surgical and the mortgage and real-estate Elevate Team. Keyword counts today, with the non-zeros as the positive control proving the query works: Elevate MRO 0 files, Elevate Aviation 3, Elevate Jet 5, Elevate Charter 1, Elevate Holdings 1 — the zero is expected, since those 137 resumes are not uploaded yet
- [ ] Define the process for adding candidates we may hire (Jul 16) — before more automation, write down how a prospective hire actually enters the system: who adds them, from where (Paycom, a resume, a referral, a board), what the minimum record looks like, and which path they take through the app. The answer feeds the intake automation below and the unified candidate-to-employee lifecycle in the backlog.
- [x] Paycom background-check notice reads the checklist forward (Jul 21) — Paycom's "Background Check Requested Information Completed" mail in Front now ticks bg_check_start automatically. Polls Front (POST /api/front/scan-paycom), DRY RUN unless ?apply=1, leaves a note on the thread when it ticks, never unticks, and skips former staff. Two real gotchas fixed: Front puts the external sender in recipients[role=from] (author is empty on inbound, which silently matched nothing), and Paycom sends TWO body wordings under one subject. Name matching is exact, plus a guarded nickname tier (same surname + first three letters, must be unique) because Paycom uses legal names — Nicholas/Nick Lembo, Russell/Russ Herman, Zachery/Zach Davis. Verified over the whole Front history: 141/141 notices parsed, 107 agreed with what the team had already ticked by hand, 0 wrong matches, 0 outstanding. CAVEAT: the subject says "completed" but it means the check STARTED. Triggered two ways (Jul 21): a nightly Vercel cron at 7am MT that applies, and a "Check Paycom mail" button on the New hires page for recruiters+admins that runs it on demand and reports what it ticked. The cron FAILS CLOSED — no CRON_SECRET means it refuses rather than running unauthenticated, which is how the older calendar-sync cron behaves and should be fixed there too. CRON_SECRET is already set in Vercel scoped to Production (checked Jul 21 via vercel env ls — it has been there since the calendar-sync work), so the nightly run is live with no setup needed. It is absent from Preview and from local env, which is correct: Vercel only runs crons on Production, and everywhere else this route simply refuses. UPDATE Jul 21 — the background check is THREE steps, not two, and the checklist only modelled two. Step 1 is the user clicking Run Background Check in Paycom, which nothing reports, so it stays a manual tick. Step 2 is Paycom's "Requested Information Completed" mail, meaning the candidate filled in their details. Step 3 is Paycom's "Background check is completed" mail, meaning they are clear to hire. The handler had been ticking step 1 from step 2's email, conflating the two. Added a middle checklist line (bg_check_info, "Candidate submitted background check info"); step 2's email now ticks THAT and bg_check_start goes back to being a manual click. Backfilled the new line onto the 197 existing hires that have a checklist — DONE where their check had already come back, NA for archived and post-onboard so a new task never reads as outstanding work on someone finished, TODO for the 5 still pending; undo at scripts/bg-check-step/backfill.ts --undo. SUBJECT COLLISION worth knowing: the two subjects are "Background Check Requested Information Completed" and "Background check is completed" — both contain "background check" and "completed", so a loose pattern matches BOTH and would mark people clear-to-hire the moment they merely submitted their info. The two matchers key off "requested information" vs "is completed" and there is an explicit test for that collision. UNBLOCKED Jul 21 (user changed the Paycom notification settings so the completion mail now CCs hrotasks@): the completion mail (step 3) previously was NOT sent to hrotasks@skyshare.com — it goes only to the individual recipient (aschaedig@), so it lands in a personal Front inbox the API token cannot search. Confirmed by scanning the full history: 142 notices found, every one of them step 2, and an explicit Front search for the completion subject returns zero threads. The step-3 code is written and tested against the real email text and will start working the moment that notice is also sent to hrotasks@. NOT YET SEEN IN THE WILD: the CC only applies to checks completing from now on, so no historical sample exists to test against — the first real one is the proof. To make sure that first one cannot fail quietly, the Check Paycom mail panel now shows an amber warning listing any Paycom email it could not read (unrecognised subject, or a name it could not parse) instead of dropping it silently, which is how the first version of this lost 33 notices. WEBHOOK BUILT Jul 21, the Front rule was created Jul 30 (see that entry below) but is not yet PROVEN for Paycom specifically, since no real Paycom notice has come through it since: POST /api/front/webhook verifies a Front-signed payload against FRONT_WEBHOOK_SECRET and hands the conversation to the same handler the scan uses, so a webhook, the button and the cron all behave identically. FAILS CLOSED — no secret configured means it refuses everything. The token was confirmed to already carry the Application Triggers feature, so NO new token is needed; but it lacks rules:read, so we can neither create nor inspect the rule and a test delivery is the only proof it is wired right. Target inbox is HR Onboarding (inb_cru56), and the rule fires on ALL inbound mail there rather than one sender — the user's call, and it means the same pipe can later carry travel confirmations and sourcing mail. Because most deliveries are then ordinary HR mail, the endpoint reads the sender out of the payload and answers without spending a Front API request when it is not Paycom; a sender it cannot identify falls through and is fetched properly, so an unrecognised payload shape costs a request rather than a missed notice. ONE UNCERTAINTY: Front's exact signature header and digest encoding are not documented anywhere we hold, so the verifier accepts base64 or hex, with or without a sha256= prefix, across three plausible header names, and a REJECTED call logs the header names it arrived with (never the secret) so the first real delivery lets us narrow it to the one true scheme. Verified against the running app with a disposable secret: right signature accepted, wrong secret rejected, signature-for-a-different-body rejected, missing header rejected, and a real Paycom thread driven end to end (fetched, recognised, nothing re-ticked). Keep the nightly cron regardless as the missed-webhook safety net
- [ ] Paycom new-applicant email — configure Paycom to auto-send a new-applicant notification to a dedicated address on every application (confirm whether the resume can ride along as an attachment)
- [ ] Recruiting intake Google account + Drive folder — stand up a dedicated account and intake folder with least-privilege access (only what the script needs)
- [ ] Daily intake script — scan the intake Drive folder, create the candidate (with resume) in the system, then move the processed file to an archived / uploaded state so it is not re-imported

## Candidate Sourcing (paid job boards)
Surface matches from the boards we already pay for: ClimbTo350 + BizJetJobs (pilots) and JSFirm (maintenance).
- [ ] Job-board sourcing integration — pull resume-database candidates from the paid board accounts that match our open pilot requirements, dedupe against existing candidates, and queue them for recruiter review inside the Matcher. Decide the connection per board before building: official API / resume-database access (preferred) vs. authorized bulk export vs. assisted login — each has different ToS, reliability, and cost. Document which boards even offer an employer resume-search / API tier.
- [ ] Sourcing cost & matching model — scope cost (per board, per month, plus any per-search or compute cost) and the cheapest viable path (periodic export + import vs. live automated scan), and how a sourced profile is scored against a requirement before it reaches the recruiter.

## Events & Outreach
Career fairs, school visits, and community events — and the swag cupboard behind them.
- [x] Events & Outreach module (Jul 16) — new Recruiting section (sidebar: Events + Supplies) tracking the whole thing in one place. EVENTS: add an event (type, dates, venue/city/state, owner, website) with Planned / Confirmed / Complete / Canceled; Upcoming and Past tabs; each card shows going-vs-invited, supply count, and open to-dos. PER EVENT: "Who is going" (add anyone from the current roster incl. contractors, give them a role at the event like Booth lead, and track Pending / Confirmed / Declined), "What we are bringing" (pull items from the stock room with a quantity, or type a one-off like a borrowed banner; tick each as packed), and "What we need" (a to-do list with an owner and a due date, overdue flagged). SUPPLIES: the stock room — on-hand count, reorder-at threshold, vendor, and a reorder link per item, editable inline. THE REORDER MATHS is the point: projected = on hand MINUS what upcoming events have claimed but not yet packed, so the flag trips before a fair cleans you out rather than after; click a committed count to see which events are holding it. Packing a line releases the claim; a canceled or completed event gives its stock back. New Event / EventAttendee / SupplyItem / EventSupply / EventTask tables, events:read + events:write permissions, and its own Settings > Module Access row (admin Full, recruiter Full, hiring manager View, viewer Hidden). Verified end-to-end against the real roster.
- [x] Events from the inbox, a calendar, the aircraft question, and inviting employees (Jul 27) — FROM AN EMAIL: events arrive as mail from the organizer, so there are now two ways to file one. "Check the inbox" sweeps the recruiting mailbox for invitations to career fairs, school visits, conferences and airshows; "Give me one" takes a Front link or a pasted email. Either way Claude reads the email into a draft — name, type, dates, venue, city/state, event page, the organizer's own name/email/phone, the ship-materials-ahead address, and whether a static display is mentioned — and you review and edit every field before anything is written. Imported events land as PENDING DECISION, a new status meaning an invitation arrived and nobody has said yes yet; pending events deliberately do NOT reserve stock, so three unanswered conference emails cannot trip a swag reorder. The original email stays one click away on the event. Anything you pass on goes on a skip list so a re-scan stays quiet, and that list is reversible. CALENDAR: a month view in the Events section, coloured by decision (gold = still to decide, navy = going, green = confirmed), multi-day events drawn across every day they cover, with a small plane on any event taking an aircraft. AIRCRAFT: its own field on every event — Not decided / Not bringing one / Requested / Confirmed, plus tail number and ramp/FBO notes — tracked separately from the event's own status because it has the longest lead time (organizer notice, ramp space, air-stairs, GPU, crew), so a confirmed event with an undecided aircraft is now visible instead of invisible. WHO YOU INVITE ARE EMPLOYEES: the attendee picker is the whole current roster, filtered by department (pills with counts, splitting compound departments so picking FlightOps also catches "FlightOps | Managed Client") and searchable by name or job title. That picker was previously showing 27 of 189 current employees — the same stage-filter bug as the supervisor picker — and now shows all 189. Verified against the real mailbox: the sweep read 11 threads and correctly rejected a PayPal receipt and an invoice reminder that both mention "career fair", surfaced the USU 12th Annual Aviation Career Fair (Sep 24, Logan-Cache Airport) and Girls in Aviation Day (Sep 19, Delta hangar SLC), and flagged the latter as a possible duplicate of the one already on the calendar. HONEST CAVEATS: the Front scan is manual (a button, not a nightly job); duplicate detection warns but never blocks; and the whole review flow is click-driven so it could not be verified in a browser here, only over HTTP and against the real Front and Neon data. On the key: ANTHROPIC_API_KEY is set and the Claude path is what actually runs — verified on the live app, which returned source "ai" on the real USU email. This is the SAME confusion already corrected on Jul 20 in the Paycom-scan entry above: the key lives in .env.local, Next.js loads it at runtime, and only standalone tsx scripts (which load .env alone) cannot see it. The pattern-matching floor in parse-event-email.ts is defensive cover for an API error, a refusal or a rate limit — NOT a signal that anything is unconfigured, and the UI now says so in those terms.
- [ ] Events: automatic inbox sweep — the mailbox scan is a button today. Make it a nightly job that surfaces new invitations without anyone remembering to look, reusing the same skip list. Needs a decision on where it notifies (Command Center badge, email, or Front comment).
- [ ] Events: cost + ROI — spend per event (booth fee, travel, swag) and which events actually produced hires. The budget field and the travel module are both already there; needs the candidate-to-event link below.
- [ ] Events: link candidates met at an event — tag a candidate as sourced from a specific event so the pipeline (and later the ROI report) can trace back to it.
- [ ] Events: auto-deduct stock when an event completes — today on-hand is maintained by hand and completing an event just releases the claim. Deducting automatically is the obvious next step but needs a decision on the escape hatch (what happens when you take 8 and bring 3 back).
- [ ] Events: company-wide volunteer call-out email (added Jul 28) — when an event is happening, send ONE email to the whole company asking who wants to join, and collect the sign-ups back onto the event. The user's four rules, which the email AND the sign-up must both state plainly: (1) VOLUNTEER ONLY — it is not an assignment; (2) WE DO NOT PAY TRAVEL to get you here — so it is really for people already near the event, and the email should say so up front; (3) YOUR ACTUAL JOB COMES FIRST — attending is on top of the day job, never instead of it; (4) SIGN UP IF INTERESTED — a one-click way to raise a hand, no back-and-forth. HOW IT FITS WHAT EXISTS: the event already has a "Who is going" roster with Pending / Confirmed / Declined, so a volunteer who signs up should land THERE as Pending (interest is not a commitment — the organizer still confirms them), keeping one attendee list rather than a second parallel one. THE SEND rides the SAME Front email path as orientation and the welcome email (fetch the template from Front, personalize, preview, send from an HR mailbox, record it) rather than a new mail mechanism — it is the next thing that pipe would carry. DECISIONS FOR WHEN IT IS BUILT: who the blast goes to (truly everyone, or filterable by LOCATION since travel is unpaid, or by department), how the sign-up comes back (a link to a lightweight in-app "I'm interested" page keyed to the event vs a reply that Front parses), whether sign-ups can be capped or closed, and whether the organizer approves each volunteer before they count as going. Not now — captured per the user.

## Historical Candidate Archive (Jazz import)
Fold years of legacy JazzHR recruiting history into the app so old candidates appear as ordinary candidates — one profile per person, fully relational, origin (Paycom/Jazz/manual) invisible to recruiters. Source: full relational Jazz export (~3,282 candidates, 3,817 applications, 18k interviews, ~3,800 resumes, 162 jobs).

- [x] The archive can be paged properly (Aug 20, from feedback Jul 20) — 3,162 rows at 100 a page with only Previous and Next was the complaint. There are now 100 / 250 / 500 page sizes, mirroring the candidate list's numbers and reasoning rather than inventing a second scheme, and numbered page links with first, last and a window around the current page, so jumping from page 2 to 15 is one click. Real Links, not router.push, so they are ctrl-clickable. Changing the size returns to page 1 deliberately, since page 15 of 100s is not page 15 of 500s
- [ ] Nothing dedupes resumes, and 308 candidates have more than one (found Aug 20, answers a feedback question open since Jun 18) — of 1,627 candidates with an active resume, 1,319 have exactly one and 308 have several, the largest holding 12. Exact duplicates are rare, 3 groups by filename and size, but 162 groups are the same byte size on the same candidate, which is the same file re-uploaded under a different name. The system keeps and shows all of them: no constraint, no merge, no warning. Whether that is wrong is a DECISION, not a bug. The feedback item stays open because the app has nowhere to record an answer to a question, and closing it would bury this

- [x] Interview write-ups are visible from the Notes section at last (Aug 16) — a recruiter opening Notes saw an empty page for somebody who had been interviewed and written up at length, because the write-up lives on the Interview record rather than as a note. The user expected them in Notes because that is where they lived in JazzHR. Write-ups now appear in the SAME chronological feed as the notes rather than in a separate section, since splitting them is what let one go unseen: read-only, badged as an interview write-up, carrying the interview title and outcome, and pointing at the Interviews tab for editing. Write-ups with no text are dropped, because an interview record exists for every scheduled slot and empty ones would bury the notes under blank cards. THE HANDOFF SAID 41 IMPORTED CANDIDATES AND THE REAL FIGURE IS 45, corrected before this was written. Measured whole scope: 309 interviews, 289 with a non-empty write-up, 276 distinct candidates with one, and of those 45 had ZERO candidate notes, so their Notes tab was blank. By origin that is JAZZ 42, PAYCOM 2, MANUAL 1 — scoping it to imported candidates undercounted it, in exactly the way the note-author finding was first scoped to Jazz alone. Three definitions of non-empty were tested and all three return 45, so it is not an artefact of how the question was asked. Verified over HTTP on a real candidate: renders as Interview write-up, Recruiter screen, PASS
- [x] Test-data cleanup (Jun 26) — removed 4 TEST-flagged candidates so the import dedupes against a clean roster (42 → 38); reusable read-only audit at scripts/find-test-candidates.ts
- [x] Phase 0: schema + sync (Jun 26) — origin discriminator on Candidate + CandidateApplication, plus HistoricalSource, TimelineEvent, Tag/CandidateTag, CandidateAiSummary; pushed to Neon, existing 38 candidates auto-tagged PAYCOM
- [x] Phase 1: profile surfaces (Jun 26) — Historical Match banner (live facts: previous candidate, # applications, interviewers, notes, hired/declined, resume archived), unified Timeline tab, AI summary tab (display + empty state); shown on any candidate carrying Jazz history
- [x] Phase 2: import engine (Jun 26) — csv parser, weighted dedupe, dry-run + commit + undo (prisma/import-jazz.ts), bounded-concurrency writes. FULL IMPORT DONE: 3,159 Jazz candidates created + 3 merged into Paycom profiles, 56 jobs, 3,816 applications, 282 interviews, 5,710 files, 109 hour-metrics, 4,098 timeline events, 0 errors. System now 3,197 candidates total
- [x] Archive: browse all + pagination (Jul 16) — the archive now lists every historical candidate (3,162) with no search terms entered; before, it only ran a query once a filter was set, so the landing page showed nothing. And results page at 100 per page with Previous / Next + a "Page X of Y" indicator instead of silently stopping at the first 100 with no way forward; filters carry across pages and an out-of-range page clamps to the last one. Sort is still by name — most-recent-application-first may be the better default (open).
- [x] Historical candidates searchable (Jun 26) — candidate search now spans archived/historical records (default list stays active-only); Jazz candidates findable by name/email/phone/resume text
- [x] Phase 3: historical search (Jun 26) — /archive page (nav: Data → Historical Archive) with advanced filters (keyword, recruiter, interviewer, disposition, job title, candidate #, application #, date range); duplicate-review handled by existing /duplicate-review queue
- [x] Phase 4: AI summary (Jun 26) — Claude generation wired (@anthropic-ai/sdk, lib/archive/ai-summary.ts, /api/candidates/[id]/ai-summary, Generate/Regenerate button); on-demand, inputHash-gated, Haiku by default (ARCHIVE_SUMMARY_MODEL); graceful "not configured" until ANTHROPIC_API_KEY is set
- [x] Phase 5: reporting (Jun 26) — /archive/reports: applications by year, repeat applicants, offer acceptance rate, rejection reasons, recruiter + interviewer activity
- [x] Communication History (Jun 26) — imported the Jazz email archive (11,577 messages linked to candidates; the "283k" was inflated line-count, real records ~11.6k) into CandidateCommunication; new "Communication" tab on the profile with readable (HTML-stripped) threads + direction. Reversible via import-jazz-comms.ts --undo
- [x] Tag normalization (Jun 26) — backfilled 38 Tags + 1,661 Jazz category links (candidate_categories.csv: pipeline stages, assessments) into Tag/CandidateTag; profile + candidate list now read the union of legacy tagsJson and normalized CandidateTag (prisma/backfill-tags.ts, idempotent)
- [x] Resumes and documents actually attached (Jul 16) — the import had recorded 5,710 documents as REFERENCES only (a filename plus a path on the export drive), so a historical profile listed a document name you could not open. They are now real: 5,707 uploaded to S3 and viewable in the profile document viewer — resumes plus the cover letters, certificates and licences from the export's document folder. 0 lost, 0 errors; 33 skipped (30 belong to no candidate, 3 are non-previewable types such as .odt). Display names were cleaned up in the same pass: 5,704 renamed from opaque strings like resume_65e3652f23fbe.pdf to "Firstname Lastname Resume.pdf", built from the candidate's own name plus the document type and numbered when one person has several. Both steps reverse (scripts/attach-jazz-files.ts --undo, scripts/rename-jazz-display.ts --undo). Sampling first proved every PDF has a real text layer, so no OCR was needed. Companion: scripts/resume-archive-linkmap.ts maps every resume file on disk to its person — 76 percent join through the export's document table, and the remaining 1,543 loose files were named by reading the top of the PDF with Claude Haiku.
- [x] Link a live candidate to their historical profile, with merge (Jul 16) — when someone in the current pipeline also exists in the archive as a separate profile, their page now says so and offers to merge the old record into the new one (its applications, interviews, notes and documents move across; the old profile is archived, not deleted, so it stays recoverable) or to mark it not a match. Built on the duplicate scan and merge that already existed rather than new logic. Note from a rescan: the archive is already fully deduped — every historical-to-live overlap had been merged in earlier cleanup — so this waits on the next new candidate who matches an old application rather than showing a queue today.
- [x] Audited whether the Jazz notes imported correctly — they did, in full (Aug 6) — feedback is 1,357 of 1,357 rows imported as JAZZ_FEEDBACK, and the interview answers are complete at 282 of 282 interviews carrying the full question-and-answer block, across 270 candidates. THE SPARSENESS IS IN THE SOURCE, not the import: candidate_notes_and_comments.csv holds SEVEN rows for the whole company, and 2,474 of the 3,159 Jazz candidates have no written record in Jazz at all. Re-verified against the live database and the export CSVs before being written here, listing EVERY CandidateNote source value and every Interview source value rather than only the ones being claimed
- [ ] Jazz interview import silently drops undated sessions (found Aug 6) — prisma/import-jazz.ts line 312 skips an entire session when interviewed_at will not parse, and 1,792 of the 6,615 rows in candidate_interviews.csv have a blank date. 50 of the 321 prospects have EVERY row blank, so they got no Interview record at all: 750 rows dropped. Confirmed on five named candidates each showing zero interviews, against a positive control of candidates who do carry Jazz interviews. SMALLER THAN IT LOOKS, and the number that matters is 8 — only 8 of those 50 have any text in the dropped rows (33 rows with a response, 20 with interview notes, 42 in total). The other 42 people lost an empty shell. Fixing it means deciding what date an undated session should get
- [ ] Interview write-ups are invisible from the Notes section (found Aug 6) — 41 Jazz candidates have their only written record on the Interview record, and 227 more have it in both places, so a recruiter looking at Notes sees nothing. THE USER EXPECTED THESE IN NOTES because that is where they lived in JazzHR; for the imported data the opposite is true. Surfacing interview notes in the candidate Notes view is the fix
- [ ] Jazz scorecards and evaluations were never imported (found Aug 6) — there is no importer for candidate_scorecards.csv or candidate_evaluations.csv. Small, but it is real recruiter text. TWO FIGURES CORRECTED ON VERIFICATION: the scorecards are 33 rows of which 19 carry a criteria_comment, not 33 with comments. And the evaluations are 14 rows in which evaluation_note is EMPTY IN EVERY ONE — the actual text sits in rating_comment, 11 of the 14, lines like only about 4 years of direct HR experience. An importer written against evaluation_note would load nothing and conclude there was nothing there, which is the whole reason this is written down. The evaluations cover 4 prospects and one of those is named TEST, so it is 3 real people
- [ ] Prompt caching will NOT reduce the AI-summary cost, and adding it would fail SILENTLY (found Aug 7) — lib/archive/ai-summary.ts runs claude-haiku-4-5 with a shared system prompt measured at 97 tokens, against that model's 4,096-token minimum cacheable prefix, so a cache_control marker would return cache_creation_input_tokens 0, charge full price and raise no error at all. CHECKED AGAINST THE CURRENT ANTHROPIC REFERENCE rather than from memory, and that check closes the door completely: the SMALLEST minimum on any current model is 512 tokens, so moving to a different model cannot rescue a 97-token prefix either. TWO FURTHER REASONS it would not pay even above the minimum — caching discounts only the SHARED prefix while the bulk of every request is that candidate's own unique history, and generate-all-summaries.ts runs at concurrency 4, where an entry only becomes readable once the first response begins streaming, so four identical-prefix requests fire together and none can read what the others are still writing. THE RIGHT LEVER IS THE BATCH API — a flat 50 per cent off input and output, no prefix requirement and no minimum — but it needs a submit-poll-write restructure, and the total spend at Haiku rates may not justify that. Per-candidate context size was never measured, so the bill is still unknown
- [x] The Jazz export directory is still where the import read it (confirmed by the user Aug 6) — G:\Shared drives\Human Resources Admin\Jazz Stuff\skyshare_export_20250616\skyshare_export_20250616\data_records. Reachable from this machine, with all 16 export CSVs present, so the three gaps above can be re-run from source without asking JazzHR for another export. CONFIRMED AUG 6 to be the COMPLETE set of source files — there are no other spreadsheets or tabs holding Jazz data, which closes the open question about additional workbooks
- [x] Audited whether the Jazz identifier fields made it into the app — they all did, so no import is needed (Aug 6) — jazzCandidateNumber 3,159 of 3,159, jazzApplicationNumber 3,816 of 3,816, jobId linked 3,816 of 3,816, application status 3,816 of 3,816, and the applied job title reachable through the job link. historicalJobTitle is set on ZERO records because the real job link exists instead, which is the better outcome. THE STATUS IS NOT ALREADY REACHABLE BY ACCIDENT either: the main search does cover Candidate.stage, but stage is null on 3,146 of the 3,159 Jazz candidates and the hire status lives on the application. The vocabulary is preserved intact — Not Selected - Prescreen Disqualification 1,214, Not Selected - Future Consideration 610, Hired - Full Time 65. THIS CORRECTS AN EARLIER CLAIM MADE IN THE SAME SESSION that Jazz applications often carry historicalJobTitle rather than a job link: that was false, the count is zero, and the department-visibility concern raised on top of it does not stand
- [x] Every Jazz identifier is findable from the ONE main search box (Aug 6) — THIS IS ROCK 5 WORK, per the user: the historical candidate lookup rock is what this section serves, and it is tracked in Q3 Rocks as well as here. Prospect id, application id, job id, applied job title and hire status all resolve from the same box, and a job id returns EVERY candidate who applied to that req, which the user chose explicitly over returning only the job record. Before this, FIVE of the five were unreachable from that box rather than four as the audit first reported: the prospect id was findable only in the SEPARATE archive search, and the application id there only through its dedicated field. VERIFIED by calling the shipped functions against the live database rather than by clicking a page — a prospect id and an application id each return exactly one candidate, Trevor Harward, and a name plus that application id together still return him. The largest reqs carry 457 (Pilatus PC-12 Captain), 386 (Flight Coordinator) and 289 (Corporate Cabin Attendant) applications, so result sets are large but paginate
- [x] Multi-term search — SHIPPED, and it was a requirement rather than a nicety (Aug 6) — the box now splits on whitespace and requires EVERY term to match. It used to run ONE contains against the whole typed string, so any two-term query returned zero rows. THE USER'S REASON, which is why it ranked as a requirement: people commonly search two things at once, and a query that returns nothing is indistinguishable from the data being absent, which is how hiring managers learn to distrust the app. VERIFIED live — Pilatus returns 556, Pilatus Hired narrows to 36, and a query carrying one nonsense term returns 0. IT ALSO CAUGHT A LATENT BUG on the way: the tag filter ASSIGNED a top-level AND, which would have silently discarded the entire search query now that the query lives there. It appends instead, matching the department filter
- [x] Jazz ids are matched by prefix rather than substring (Aug 6) — the formats collide: projob_ contains job_, so a partial search for job_2023 matched all 3,816 application ids at once. Anchoring removes it, since projob_ does not START with job_. VERIFIED WITH BOTH CONTROLS, which is the whole point — POSITIVE: job_2023 reaches 725 candidates through 25 real 2023 requisitions. NEGATIVE: 0 through application ids, against 349 false hits under the old substring behaviour. ONE FIGURE CORRECTED from the handoff's 704, which counted Jazz-origin candidates only; the search spans every origin, so what a user actually gets back is 725 — 704 Jazz plus 21 Paycom people who applied to the same reqs. Substring matching is kept for the human-readable fields, where a fragment is what somebody means. NOTE: the 87 source job ids are not all Jazz — 76 are Jazz-shaped and 11 came from the website job-postings PDF import, so this matches those 11 too
- [ ] Search latency after the identifier work (found Aug 6) — queries now take roughly 1.5 to 4 seconds, because every typed term joins out through applications and jobs. Measured by the existing perf instrumentation during verification: 3,635ms for a prospect id, 1,525ms for an application id, 1,635ms for a two-term query. Worth an index pass. No schema change was made here, and NONE of the four indexes applied earlier the same day covers this join path — they are on Candidate, Interview and NewHire, not on CandidateApplication or Job
- [ ] Row-count gaps between the Jazz export and the database, and there are THREE (Aug 6) — candidates.csv has 3,282 prospect rows against 3,159 JAZZ candidates, and candidate_applications.csv has 3,817 rows against 3,816 applications. A THIRD surfaced while verifying the other two: jobs.csv holds 56 rows, but 76 Jazz-shaped job ids sit in the database, and only 48 distinct jobs are actually referenced by Jazz applications against 55 distinct job ids in the applications CSV. Merges and duplicates are the likely explanation for all three, and several jobs do carry status MERGED, but NONE of it is confirmed. Worth reconciling the exact list before anyone treats the archive as complete

## Candidates & Matcher
Working a candidate's record and acting on matcher results.

- [x] The candidates page is rebuilt around six derived segments (Sep05, from him) — Active, Offered, Talent pool, Hired, Not selected and Historical, as the page's top row of counts. The ladder lives in lib/candidates/buckets.ts and is shared by the list query, the tiles and later the Paycom importer, because two copies that drift are how somebody lands in Not selected on one screen and Talent pool on another. Order is load-bearing: hired for one job and rejected for three others is Hired, evergreen is a KEEP not a rejection, and no applications at all is not a rejection either. RE-VERIFIED at commit time against live data and independently of the handoff: the six partition EXACTLY - 3,646 candidates minus 31 merged is 3,615; Active 454 + Offered 0 + Pool 8 + Hired 19 + Not selected 17 is 498, which is precisely the live non-merged count; plus Historical 3,117, which is precisely the archived non-merged count. Every segment and both filters reproduce over HTTP with each list matching its own tile, an unknown bucket falls back to all 498, and the cross-axis intersections reconcile - 105 within Historical plus 3 within Active is the 108 Failed interview total.
- [x] THE SEGMENTS REPLACED THE STATISTICS STRIP rather than being added below it (Sep05, from him) — the page carried two rows counting the same people, which was most of why the top felt crowded; the numbers you were reading anyway are now the thing you click. Three tiles are gone by agreement: With files, With applications and Scheduled interviews. The archive count survives as the Historical segment, Recent interviews as a link. The editable grid went with the strip - he does not want Edit layout on this page at all, and the saved layout held no widgets.
- [x] THREE COUNTING BUGS found by measuring against live data rather than by reading code (Sep05) — Not selected was 322, of which 305 had NO APPLICATIONS AT ALL and every one was an ACTIVE record: the ladder fell through to a rejection whenever there was nothing to judge, so the segment was 95% wrong and read as these people were rejected. It is 17 now, and no applications means Active. Historical was counted over a population that had already excluded archived records, so it sat permanently at 0 beside five segments that worked. And Failed interview said 108 but opened a list of 3, because the count spanned the archive and the list did not. All three the same shape - a count that did not match what clicking it showed.
- [x] Applications expand as real table rows, and expanding is no longer slow (Sep05) — they were a nested grid inside one merged cell, which is why they could never line up with the columns above. Checked by cell index rather than by eye: header index 2 is Last applied to and the expanded job title is also index 2, index 3 is Status and the reason is index 3, seven cells against seven headers. Sorted most-recent-first once in the data layer so the collapsed row and the expanded list cannot disagree. And the row is its own memoised component with useCallback handlers - every row owns a stage select with a dozen options, so with all hundred inlined, opening one re-rendered all of them. Measured with a longtask observer on real clicks: six expands, nineteen rows added, worst main-thread block 51ms.
- [x] The table drops min-w-[1000px] and sheds columns instead of scrolling sideways (Sep05) — fixed layout on percentage widths 5/19/20/16/11/20/9, shedding Types and Activity at 1320px, Tags at 1020px and Status at 760px; the two that go first are the ones repeated inside the expanded row. The old min-width forced a horizontal scrollbar under 1000px, which the layout rule forbids outright. Measured at 1440, 1100 and 900px: zero truncated names, zero clipped headers, zero cell spills, no horizontal scrollbar, each breakpoint totalling 100. Status is a fixed 116px sized by measuring every option in the page's own font - Prescreen Complete is 108px and every other stage is 83px or less, so one label was charging all twelve rows 25px.
- [x] New page at /candidates/manage for the vocabulary behind the list (Sep05) — pipeline stages (add, rename, reorder, retire) and disposition reasons were constants needing a developer and a deploy; both are now stored settings that fall back to the constants, so nothing is migrated and a stage no longer offered still shows under Current on the person sitting on it. Tags gain recolour, rename, merge and delete. Recategorising a reason stores this wording means that group and beats the pattern matcher, reversibly; rewording rewrites imported text on every row carrying it, so it asks the server for the exact count first. 39 wordings fold into 12 groups today.
- [ ] Dark mode has NOT been looked at on any of the candidates rebuild (Sep05) — the classes were audited and the segment tiles are fully covered, with the only gaps the avatar background and the checkbox border, both pre-existing, but the page has never been opened with the toggle on. Deferred to the next deep-dive audit by agreement.
- [ ] No Employees segment, held deliberately (Sep05, from him) — the only candidate-to-employee link is NewHire.candidateId and only 169 of 461 rows carry it, so the segment would show 169 and silently omit around 292 real employees. Held until that backfill, on the grounds that a wrong count is worse than a missing one.
- [ ] Three smaller things left open on the candidates page (Sep05) — type ratings show for only 116 of 3,646 candidates so the Types column is empty for almost everyone until more resumes are extracted, and one stored value is the string 945, a bad extraction worth a pass over the type_ratings metric for other numeric junk. A bucket cannot be filtered in SQL because it is derived, so selecting a segment resolves the whole population to an id list first - exact at any page size, but one extra full-population read per load, which storing the derived bucket would remove once the Paycom import lands. And departments still render as a single label: the Department | Sub-department split is built and renders the moment the data carries a pipe, but no department has one until the import applies the 61 role moves.
- [x] RECORDED, the layout decisions behind the candidates page (Sep05, from him) — segments go ACROSS THE TOP, not down the side; a left rail was built first and rejected on sight. Of five layouts offered he picked C, count-forward tiles that REPLACE the statistics strip, and he confirmed he does not want the With files, With applications or Scheduled interviews tiles back. Edit layout is not wanted on this page at all - the editable grid was removed entirely, not hidden. Tags and Reasons management belongs behind a button on the candidates page, not its own nav item and not a segment. Employees as a segment is HELD until the NewHire.candidateId backfill rather than shipped with a partial count. Dark mode is deferred to the next deep-dive audit.
- [x] A role can now be screened in one sweep instead of one card at a time (Sep04, from him) — a keyword filter over the cards on a role's list, a checkbox per card, Select all, and one reason plus one note applied to the whole selection, with a real undo. Asked for directly: filter to jet 0, select all, mark Not a fit for this position, note not a fit no jet time. The per-card dropdown is unchanged for one-off calls. The bulk bar is also the first place a NOTE can be recorded on a position skip at all - until now every set-aside stored which reason but never why. Scale of the problem it removes, measured on the live G200 First Officer row: 114 of its 128 entries are aschaedig@skyshare.com working down the list one card at a time TODAY, each with its own separate timestamp.
- [x] The filter matches terms on ONE LINE of a card, and a number matches a WHOLE token (Sep04) — both rules were forced by measurement rather than chosen. Whole-card matching returned 26 cards for jet 0 on the live Challenger 350 First Officer scan and SIX had 3,190 / 4,110 / 12,055 / 13,204 and 23,000 jet hours: the word jet off their Jet time line, the 0 off a recency line. Bulk-skipping that as no jet time would have set aside a 23,000-hour pilot. And anchoring a number to a word START was not enough, because a thousands separator IS a word boundary - 0 matched the 000 in 4,000 and 200 matched 3,200. A both-sides boundary against a comma-stripped copy fixes both and makes 1500 find 1,500. HE ASKED SPECIFICALLY whether 50 jet hours would be swept up by jet 0 since 50 contains a zero. It is not, and that was re-checked at commit time rather than argued from the regex: 50, 500, 1,000, 4,000, 10,000, 23,000, 3,200, 1,050 and 8,650 all correctly excluded while 0 hrs matches, jet 50 does find the 50-hour pilot, and on a two-line card jet 0 correctly finds nothing while jet 23000 and recency 0 both hit. The cost is that terms from different lines no longer combine, so morales jet finds nobody - the right trade, since a query that quietly means something other than it looks is worse than one that finds nothing.
- [x] The bulk write is a single read-modify-write of the requirement's one skips row, returning every prior value so undo restores earlier decisions rather than clearing them (Sep04) — a loop over the existing single-candidate writer would have been forty reads and forty upserts of the same row, each starting from a snapshot taken before the last one landed. TWO CORRECTIONS TO THE HANDOFF, both from checking the live database. FIRST: it reports that the Apply button was never clicked against an ACTIVE role. It was. The G200 First Officer row carries 14 entries sharing one timestamp with note no jet time - the signature of one bulk write, and the same 14 the handoff describes selecting in the browser. So 14 real candidates on a live role are set aside. THE DECISIONS ARE CORRECT and are being left alone: all 14 have a stored jet metric of exactly 0, checked one by one against a control of five one-at-a-time skips on the same role returning 652 / 1,634 / 670 / 1,300 / 1,058, so 0 is a measured value and not a failed lookup. SECOND: the inert empty row it says it left behind on an inactive requirement does not exist - three rows exist in that scope and none is the id named, nor is there a requirement with that id.
- [ ] A bulk decision made from local dev records no author (found Sep04) — the 14 rows above carry by=null, while the 114 one-at-a-time skips beside them carry aschaedig@skyshare.com. Local dev bypasses auth, so there is no user to attribute it to. In production it would record the real one, so this is a dev-only artefact rather than a bug in the writer - but those 14 rows will permanently show no name against a decision about 14 real people, and anyone auditing them later should know why.
- [ ] A jet_time gate with a minimum of 0 would make jet 0 match EVERY candidate on that role (found Sep04) — the same-line rule stops a 0 from another line combining with jet, but a standalone 0 ON the jet line still counts, and min 0 puts one there. Confirmed by test: 1,200 hrs min 0 matches jet 0, as does 900 hrs 0 in type. HYPOTHETICAL TODAY, and that was checked rather than assumed - all 7 jet_time gates on ACTIVE requirements carry minimums between 200 and 1,500, and of the 40 enabled numeric gates only 3 have a null minimum and none of those is jet_time. So the shipped safety measurement stands. It rests on the data rather than on the rule, which is worth knowing before somebody sets a gate minimum to 0.
- [ ] Select all only reaches cards actually rendered (Sep04) — it respects the tier tab and skips collapsed groups, on purpose, because a bulk control must never act on people the recruiter cannot see. Worth watching whether it surprises anyone: on the Challenger 350 First Officer list, jet 0 matches 20 but Select all offers 14, because the other 6 sit in the collapsed Set aside group. Related and separate: the filter searches the cards ON THE LIST, which is the top 50 current plus top 50 archived rather than the whole scored pool, and says so plainly under the search box. Bulk triage across the FULL pool is a different job - the scan already scores everyone in memory, so it is a bigger display slice rather than more queries.
- [x] RECORDED, what he asked for and the question he asked before it shipped (Sep04, from him) — screening the matchboard one candidate at a time was taking too long; the ask was to filter a role's suggested candidates by keyword, select them all, and mark them not a fit with a shared note rather than repeating the same call per card. His example was Not a fit for this position as the reason and not a fit, no jet time as the note - that reason is now the bulk bar's default and that phrasing is the note field's placeholder. And before it went out he asked whether someone with 50 jet hours would be swept up by jet 0, because 50 contains a zero. They will not, and that answer is now checked against live data rather than argued from the regex.
- [x] The duplicate page now says which pairs it found, and no longer offers a merge that cannot happen (Aug 31) — the count and the queue were measuring different things: the banner counts every pair detected while the queue and all four tiles filter to OPEN, so one page showed three numbers that disagreed - banner 2, tiles 0, panel 44. The banner now separates detected from new from already-reviewed and links to the panel. THE SHARPER HALF IS THE BUTTONS: 42 of the 44 Reopen controls could never have led to a merge, because the scan auto-resolves any open item whose side is MERGED and the merge helper refuses a merged row, so reopening them puts them back only until the next scan silently recloses them. Eligibility is computed server-side against BOTH rules, and an ineligible pair shows a reason chip naming the surviving record rather than a button - not a disabled button, because a greyed button still reads as maybe later. Measured against the full closed set: 44 items, 2 actionable, 42 already merged, re-confirmed at commit time as RESOLVED 42 plus DISMISSED 2 with no OPEN row anywhere. THE PAIR HE ACTUALLY REPORTED IS ONE OF THE 42, since repairing the resurrection correctly set the shell to MERGED, and the page now says so. Open profile also used to point at the tombstone half the time and now targets the surviving side.
- [x] The last route that could resurrect a merged-away candidate is closed, and the guard is one helper instead of three opinions (Aug 31) — app/api/candidates/route.ts was the unguarded sibling: it tested employedHire but neither merge marker, then wrote archivedAt null and status ACTIVE onto a row matched by email or phone with no status filter, so posting a duplicate contact with a job id would have brought a tombstone back exactly as before. It now redirects the application onto the surviving keeper, or refuses with a 409 naming it, so the work lands on the real person rather than nowhere. THE PATTERN WAS THE REAL DEFECT: the same question was asked in three places against TWO DIFFERENT COLUMNS - one tested status MERGED, one tested mergeHistoryJson, one tested neither - and all three now call a single helper checking the union. Re-measured at commit time: status MERGED 31, mergeHistoryJson set 31, MERGED-without-history 0, history-without-MERGED 0. Two columns agreeing perfectly is exactly the state in which a later divergence ships unnoticed, which is why consolidating now rather than when they disagree is the point.
- [x] One row per candidate: merged tombstones are no longer listed (Aug 31, from him) — he searched Matt Smith, saw two, and asked the right question - is that one candidate or two. It is ONE. The second row is a tombstone: a merge moves the files, notes and applications onto the keeper and leaves the empty shell behind so the history survives. Search deliberately spans archived records so legacy Jazz candidates stay findable, so the shell kept coming back and a RESOLVED duplicate read as an unfixed one. His words: I just want one line for each candidate, and two people with the same name but different contact details are different people. A merged row is now excluded from the list entirely, searched or not; the keeper carries everything and is what search returns. 32 rows affected, all already archived, so only search changed. An archived badge stays on search results, since that answers a different question - why a row is not in the live pool.
- [x] Eight applicants a merge had buried are live again (Aug 31, applied to the live database) — eight merges folded a CURRENT Paycom applicant INTO that person's archived Jazz record, so the survivor stayed ARCHIVED and the person was out of the live list and the scan pool despite having applied. Not empty records either: Michael Klotz carries 15 files and 6 applications, Joseph Frascone 15 and 8. Also Evelyn Hsieh, Chris Kang, Martin Sharpe, Robert O'Brien, Kimbol Poulsen and Matt Smith. Only archivedAt and status moved, only on the keeper, and the tombstones were untouched. The set is DERIVED rather than a list of ids - every merged row whose own origin is not Jazz whose keeper is archived - so a recurrence is covered. Accounting reconciles across all 32 tombstones: 8 activated, 23 correctly left archived because Jazz folded into Jazz is archive housekeeping, 1 skipped as a cycle. Reversible via scripts/activate-buried-keepers.ts --undo.
- [x] Merges now default to keeping the live record, not whichever row was primary (Aug 31, his rule) — his words: always merge old into new. The duplicate queue kept whichever row happened to be primary, which is arbitrary with respect to which record is real, and that is what buried the eight above. The default now takes the first difference: a LIVE record beats an archived one; a current-system record beats the JAZZ import; otherwise the NEWER one. Rule one alone prevents the burial. A tie still keeps primary so nothing changes when the two are indistinguishable, and the picker still overrides - this moves the default onto the side that is almost always right rather than removing the choice. Replayed against the real failure cases including order-swapped, five for five.
- [x] A circular merge had left Chari Kroeplin with NO live record since Jul 16 (Aug 31, found by him) — her two rows were merged into EACH OTHER. The Paycom row went into the Jazz row on Jun 27, pointing the wrong way like the eight above; on Jul 16 that was corrected by merging the Jazz row back, which moved the data to the right place but left BOTH flagged MERGED. Nothing surfaced it, because she still appeared in the list as a tombstone and that looked normal - she only vanished once tombstones stopped being listed, which is how he found her. THE HOLE THAT ALLOWED IT: mergeCandidates refused a DROP that was already merged but never checked the KEEP, so merging into a tombstone was allowed, and doing that twice in opposite directions closes a loop. The keep side is now checked and REFUSES rather than silently following the chain, because a merged keeper means somebody is working from a stale view of which record is real. Survivor chosen by evidence - the row holding the data, 15 records against 0 - with its merge history cleared since it is no longer merged into anything. Verified across the whole table afterwards: 0 chains that never reach a live row, 0 cycles remaining.
- [x] The duplicate page now names the pairs a scan counts, and a closed one can be reopened (Aug 31) — the queue was never broken - it was right. The scan card counts EVERY pair it detects while the queue and all four stat tiles filter to OPEN, and all 44 items were RESOLVED or DISMISSED, so the banner was the only number on the page counting something nothing rendered. getDuplicateReviewData now returns the closed items themselves rather than just a count, and an Already reviewed panel lists each pair with both names, why it was flagged and when it closed. REOPENING MOVES THE REVIEW ITEM AND NOTHING ELSE: it merges nobody and un-merges nobody, and a pair that was genuinely merged stays merged. It exists because both merge entry points require an OPEN item while mergeCandidates itself would accept the pair, so this was a missing UI path rather than a limit of the merge engine - the route says so in its own header, since a Reopen button beside a merge queue is exactly the thing somebody will later assume un-merges people. The panel renders nothing when there are no closed pairs. Also corrected the empty-state copy, which said resolved and dismissed pairs stay cleared - true when written, now the opposite of what the page does. NOT VERIFIED: the click path, which needs an authenticated session.
- [x] FIXED THE SAME DAY IT WAS FOUND, guard first and then the row (Aug 31) — app/api/candidate-applications/route.ts now refuses to reactivate a candidate carrying mergeHistoryJson, alongside the exclusion it already had for employed hires. The guard shipped BEFORE the repair on purpose: re-archiving the row while the route could still resurrect it fixes nothing. Candidate cmqjupt5b is back to status MERGED with archivedAt set to the mergedAt in its own merge history, and only those two fields moved - stage, contacts and applications are untouched. THE APPLICATION WAS DELIBERATELY LEFT ALONE, which is the judgement call in this fix: the resurrection created one on the shell, but the keeper already holds an application to the SAME job from Jun 19, so moving it would duplicate and deleting it would be destructive - archiving the shell takes it out of view with nothing destroyed. Verified read-only after applying, and not from the script's own output: 32 candidates carry merge history, 0 are live, all 32 read MERGED, and the keeper is untouched at 3 files and 21 metrics. Reversible via scripts/fix-resurrected-candidate.ts --undo with the record committed under scripts/resurrected-candidate-fix/. THE DRY RUN CAUGHT A BUG IN THE FIX ITSELF: mergeHistoryJson is a String column rather than Json, so it arrives as raw text, and the first version found the row then silently skipped it while reporting nothing to do. That would have shipped as already-fine without a dry run. The duplicate-scan BANNER is a separate item and is still open - the page still counts pairs it cannot show anyone.
- [x] A MERGED-AWAY CANDIDATE CAN BE RESURRECTED BY LINKING IT TO A JOB, and one is live right now (found Aug31, from feedback cmthjmp2y)  — she reported the duplicate scan saying 3 possible pairs while showing her nobody and offering no merge. That symptom is a display fault; underneath it is a live data fault. app/api/candidate-applications/route.ts sets archivedAt null and status ACTIVE on ANY archived candidate attached to a job, with no check for mergeHistoryJson, so linking a job to a record that was merged away brings it back into the live scan pool as a hollow shell. RE-VERIFIED AT COMMIT TIME AND IT IS WORSE THAN A DUPLICATE - THE LIVE ROW IS THE EMPTY ONE. Candidate cmqjupt5b carries mergeHistoryJson naming cmqvr4z3r0fknxcrmnt8p5a4q as its keeper with a mergedAt of 2026-06-27, yet sits status ACTIVE with archivedAt null and holds 0 files and 0 metrics, while the keeper is ARCHIVED and holds 3 files and 21 metrics. Both are Matthew Smith on one email and one normalized phone 7757416288, so the Matt versus Matthew she saw is one pair under two spellings, not two people. POSITIVE CONTROL: all 32 candidates carrying merge history were listed and 31 are archived; this is the only live one. All 25 rows matching Smith were listed and there is no third Matthew Smith. FIX ORDER MATTERS - stop the reactivation first, repair the row second, because repairing it while the route can still resurrect it fixes nothing. WATCH THE IDS: eight candidate ids share the prefix cmqvr4z3r, and a prefix match returns the wrong person.
- [x] The duplicate scan banner counts something the page cannot show (found Aug31, same report)  — lib/duplicates/candidate-scan.ts returns candidatePairsFound as every pair detected this run and the card prints that as 3 possible pairs, while lib/data/duplicate-review.ts filters the queue AND all four stat tiles to status OPEN. So the banner is the only number on that page counting non-OPEN items and nothing renders their names. Verified against the full table rather than a lookup, and re-verified at commit time: all 44 review items are RESOLVED 42 and DISMISSED 2, with no OPEN row anywhere, which is the positive control for the empty queue being correct rather than broken. The three pairs are Matt Smith versus Matt Smith on an exact email match with a RESOLVED item from Jul 16, plus Brandon Edwards and Matthew Johnson, both DISMISSED Jul 28. SHE CANNOT MERGE ANY OF THEM TODAY: both merge entry points require an OPEN item while mergeCandidates itself would accept the pair, so this is a missing UI path rather than a merge engine limit. Fix is to name the pairs under the banner and add a Reopen action that flips a resolved or dismissed item back to OPEN.  *** REOPENED Aug 31: the banner was never touched. *** components/duplicate-review/CandidateDuplicateScanCard.tsx still prints candidatePairsFound as N possible pairs, the exact wording she complained about. What shipped was the closed-pairs panel, which is real and keeps its own entry - but it did not fix this. The page can now show three disagreeing numbers at once: the banner counting detected pairs, the queue and tiles counting OPEN ones, and the panel listing closed ones.
- [x] Raised the candidate match cap from 12 to 50 per half, at his request, and made the label honest (Aug 31) — 12 was too few to be a shortlist: every scan hit the ceiling, so the Screening tab permanently read All 12 while 3,541 people had been scored, 422 live and 3,119 archived, and PC-12 Captain alone carries 476 applications. Checked before changing it that this is a display slice only - the matcher already fetches and scores the entire pool in memory, so a bigger cap costs rendering and nothing else: no extra queries, no API calls, no slower scan. The tab now reads Top when a list comes back at the limit and All only when it does not, and the archive header reads top 50 of 3,119 when capped. The single place to change this is CURRENT_MATCH_LIMIT and ARCHIVE_MATCH_LIMIT in lib/candidates/scan-pool.ts, and he confirmed 50 is fine for now. NOT visually verified: the rendered label, because the panel appears only after a click and this app's content is unreadable in the Browser pane. The new logic is confirmed in the compiled client bundle and the page serves 200 with no error boundary. See also the correction under Fleet Positions and Roles - the MATCHBOARD applied count was never a display cap, and the entry claiming it was is mine and is now marked wrong.
- [ ] ONE BLOCKER LEFT ON THE VISIT GATE, AND THIS IS WHICH ONE (Aug 28) — the entry above already records that one of the two went; this names the one that did not, because the gate's own entry further down still opens TWO BLOCKERS TO CLEAR FIRST and will send whoever picks it up to re-do work that is already done. The travel_complete half is fixed: the tick is filtered to ORIENTATION, INDOC and TRAINING, so a visit trip carrying a newHireId can no longer falsely mark orientation travel arranged. What remains is the real one and it is unchanged - RECRUITING_VISIT trips attach to the CANDIDATE while the onboarding checklist hangs off the NewHire, and nothing joins them. The gate needs that join before it can be built.
- [ ] ONE OF THE PRE-OFFER VISIT GATE'S TWO BLOCKERS IS GONE (Aug 28) — the entry below names travel_complete ticking on any trip as a blocker, and that bug was closed on Aug 28 when Crew travel forced a purpose filter onto it. The gate can now link a visit trip to a newHireId without falsely marking orientation travel arranged. Still not built and still needs the rest of its design, so the entry below stays open, but that half of the reason it was blocked no longer applies
- [ ] SCORING IS PER AIRCRAFT AND SEAT, NOT PER JOB, and he needs it per job (raised by him Aug26) — his words: some jobs require a type rating and some do not, so that has to be able to be a different score per job. Today it cannot be. The scoring setup keys each profile on aircraftTypesJson[0] plus pilotSeat, so two genuinely different jobs on the same aircraft and seat necessarily share one setting and one cannot be changed without changing the other. HIS OWN CASE IS THE EXAMPLE: G450 and GV Captain in Salt Lake City and Gulfstream G450 Captain in Henderson both key on Gulfstream G450 plus PIC, so they are one profile and always have been. This is a design limit rather than a defect, and closing it means keying the profile on the requirement or the job instead, plus a migration for anything already saved under an aircraft-and-seat key. Worth deciding before more profiles are customised, because every override saved today is saved against the shared key
- [ ] HIS POSITION IS IN THE SCORING LIST, UNDER A NAME HE WOULD NOT RECOGNISE (found Aug26) — he could not find G450 and GV Captain on the scoring page and reasonably concluded it was absent. It is there, listed as Gulfstream G450 - PIC, because the list is named after aircraftTypesJson[0] and the seat rather than after the job. That is a findability problem in its own right: the page he reaches from a job does not name the job anywhere. Showing which requirements each profile covers, or naming the profile after the roles it governs, would answer the question he actually had. The CUSTOM and DEFAULTS badges already answer his other question - which ones have been changed - since CUSTOM means a saved override exists for that key
- [ ] AIRCRAFT ORDER IS WRONG ON SIX REQUIREMENTS, AND SCORING KEYS OFF IT (found Aug26) — the scoring profile takes aircraftTypesJson[0], and on 6 of the 28 requirements feeding that page element zero is not the aircraft in the title. Gulfstream G200 Captain carries Gulfstream G450, Gulfstream G200, Pilatus PC-12 and therefore scores under the G450 profile; PC-12 First Officer carries GV, Pilatus PC-12 and scores under GV; Citation 560XL First Officer carries GV, Citation 560XL, Pilatus PC-12 and also scores under GV; plus Evergreen PDP, Citation CE-525 First Officer and an INACTIVE Citation 560XL Captain. SO THE SHARED-PROFILE PROBLEM IS WORSE THAN THE DESIGN ALONE MAKES IT: the Gulfstream G450 - PIC profile currently governs five requirements, two of which are not G450s at all, so tuning his G450 scoring silently retunes a G200 Captain. This half is a DATA fix rather than a build - put the titled aircraft first - and it is worth doing whether or not per-job scoring is ever built
- [x] Every role on the matchboard read 0 applied, and always had (found and fixed Aug26, he spotted it) — he had attached candidates to the G450 and GV Captain job himself and the chip still said zero. CandidateApplication carries BOTH jobId and pilotRequirementId, both nullable; attaching a candidate to a job writes jobId, nothing has ever written pilotRequirementId, and the counter read pilotRequirementId. Measured: 4,045 application rows, 4,022 with a jobId, ZERO with a pilotRequirementId, and 0 of 61 requirements holding any. THE POSITIVE CONTROL IS WHY THIS WAS A BUG RATHER THAN A DATA QUESTION and is presumably how it survived so long - checking one role returns 0 and reads as a role nobody applied to, which is entirely plausible. Listing all 61 against 4,045 applications that plainly exist is what makes it a broken counter. The join already existed, since every requirement carries a populated sourceJobRecordId, so the count now reads off the linked job. Verified against live data before committing: G450 and GV Captain now shows 31, matching that job's 31 applications exactly, and 9 of 15 active roles show a non-zero count where none did. NOT VERIFIED: the rendered chip, only the query behind it
- [x] CORRECTED SAME DAY - I got this wrong first (Aug26) — I told him the scoring setup at pilot-requirements/scoring was GLOBAL and it is NOT. It has a Default (all positions) base plus PER-PROFILE overrides, and he corrected me from the screen. My mistake was stopping at scoring-config.server.ts, seeing a single WorkspaceSetting row and concluding one row means one config: the per-profile overrides live INSIDE that row as doc.profiles[key], and resolveProfileConfig and profileKey were sitting in imports I had already read. THE REAL SHAPE, which answers what he actually asked: It is a single WorkspaceSetting row, scope candidate-scoring and key config, and the page takes no role parameter at all. So his instinct was right: editing it changes the weights for EVERY role at once, and the reason his G450 and GV Captain job does not appear on that page is that no job does. What IS per-role is the pilot requirement itself, meaning the hours and the gates, plus per-requirement tier overrides and position skips. If per-job weights are ever wanted, that is a build rather than a setting
- [ ] An AMT role is occupying the G450 Captain slot on the matchboard (found Aug26) — requirements are collapsed onto canonical fleet positions and only the FIRST requirement id in each group is kept as the one to scan and edit. Two ACTIVE requirements resolve to slug g450-captain: G450 Aircraft Maintenance Technician, based Henderson NV, which is kept, and Gulfstream G450 Captain, which is hidden behind it. So opening G450 Captain on the board scans and edits the MAINTENANCE TECHNICIAN requirement. One other pair collapses the same way, onto no position at all: Citation CE525 Captain keeps the slot and Evergreen PDP is hidden. NOT the same thing as his G450 and GV Captain, which is fine - that resolves to its own slug g450-and-gv-captain, is Active in the fleet registry and is based in Salt Lake City, and is a genuinely different position from the Henderson G450 Captain
- [x] The candidate search follows you onto a profile, and a search no longer wipes your filters (Aug 16) — asked for by the user, and the round trip was worse than he described: the profile's own Back to candidates link is a bare href with no query string, so the button the page offers DISCARDS the search that got you there and drops you on the unfiltered list. Browser-back keeps it; the in-app control does not. The search form is now ONE component mounted in two places - the navy header on the list exactly as before, and a compact version on the profile's back-link row, held to a single row because that page already spends a lot of height on chrome before any of the actual record. Its action is explicit rather than inherited, which it has to be: a bare form posts to whatever route it is rendered on, so from a profile it would have submitted to /candidates/<id> and searched nothing. A SECOND, SEPARATE BUG FIXED IN THE SAME CHANGE - the old inline form carried a single q input, so running a search silently dropped ?tags=, ?depts= and ?size=. Filter to a tag, search a name, and the tag filter vanished with no sign it had. buildCandidatesHref exists precisely to stop a control clobbering parameters it does not own, and the search form had never adopted it; the component now carries them as hidden inputs, mirroring that builder exactly. DELIBERATELY NOT A TYPEAHEAD, though one was designed and rejected: the quick-search API matches displayName, normalizedEmail and normalizedName only, while this search ORs about twelve predicates including the text inside resumes and pilot applications, so a dropdown would have quietly found LESS than the box it replaced. Verified over HTTP: hidden tags, depts and size all present on a filtered URL, and zero hidden inputs on an unfiltered one as the control
- [ ] The candidates navy header is copy-pasted across five pages (found Aug 16) — CandidatesWorkspace, compare, departments, views and views/[id] each render their own copy of the gradient band, and CandidateViewTabs is mounted six separate times. A shared app/candidates/layout.tsx would fix the duplication and was fully designed, but it would touch seven routes, visibly move the tab row from below the band to above it on four pages nobody asked about, and give /recent-interviews tabs it has never had - and the band is not genuinely shared anyway, since four of its five copies carry a different h1 - so it is not one component used five times, it is five headers that resemble each other, and a layout would have to take the title as a parameter from each page anyway. PARKED BY THE USER (Aug 16), not dropped: his words were to pin the idea for now, maybe some other time. The full brief is written up and does not need re-deriving - before and after route trees, a mockup of the tab row moving above the band, and the itemised cost - at https://claude.ai/code/artifact/b0927e05-f7d3-4b4e-badc-e85bb2369dcb . The moment it becomes worth paying for is when the header needs a REAL change (a new tab, a different action button, a wording fix), because making that edit five times is the cost that justifies doing this once
- [ ] The candidate search API does not narrow by the viewer's departments (found Aug 16) — GET /api/candidates is gated on the candidates:read permission but runs a plain findMany with no viewer scoping, while lib/data/candidates.ts applies a department restriction for a restricted HIRING_MANAGER. So the endpoint returns people the main list would hide from that same user. NOT NEW and not created by any current work - it is reachable today through the Add-candidate-to-job picker - but it would land on every profile page view if a candidate typeahead were ever added there, so scope the endpoint before building one
- [ ] From a job with an open position, link to the HOT LEADS attached to it (Aug 12, asked for by the user) — a quick view of who the RECRUITING team rated well for that job, including people the hiring manager did not choose, plus how many progressed against how many the hiring team rejected. SCOPE IS THE HOT LEAD TAG ONLY, decided by the user: do not include 4.6 Potential Future Hire or any other tag, and do not widen it without asking. VISIBILITY — build it with NO new role gate and let it inherit the job page's module. That is not the same as everyone: the job cards live under the recruiting-jobs and jobs modules, both of which are ADMIN and RECRUITER only today with HIRING_MANAGER and VIEWER HIDDEN, so the link is already invisible to hiring managers and viewers without any extra work. DO NOT add an isAdmin code gate — an earlier note in that session said to, and it was wrong. Visibility here is a Settings-level toggle, the module-access WorkspaceSetting enforced by requireModulePageAccess, so tightening it later is a settings change rather than a deploy. A RELATED ASSUMPTION CORRECTED, checked against the live policy: restricting this view would NOT stop people seeing disposition reasons. Those are already rendered on /archive as a per-row badge with a disposition filter and charted on /archive/reports, both guarded by the ARCHIVE module, which today grants RECRUITER FULL_ACCESS and HIRING_MANAGER VIEW_ONLY. If the goal is that rejection reasons are not widely readable, the lever is the archive module's access, not this new page. GROUNDING: Hot Lead is an existing tag the recruiting team already uses, carrying 3 candidates out of 43 tags, and the user has confirmed a nearly-empty view at launch is expected because the tagging process is new. Tags hang off the CANDIDATE and not the application, so the list is candidates carrying the tag who also have a CandidateApplication for that job. The counts need no new fields — CandidateApplication already has status, stage, disposition, rejectionReason, decidedAt and offerStatus. Live data already holds the exact case described: Cayden Christensen is tagged Hot Lead, was rejected by the hiring team on Pilatus PC-12 NG Lead Captain, and is still an active applicant on Pilatus PC-12 Captain
- [x] Imported notes show the real author now, and it was every importer rather than Jazz alone (found Aug 7, fixed Aug 16) — all 1,357 Jazz feedback notes have authorId null, because import-jazz-feedback.ts appends the author's name into the note BODY as a trailing em-dash line rather than linking a User, and CandidateNotes.tsx renders note.author falling back to note.source. So every one displays the literal string JAZZ_FEEDBACK as its author while the real name — Aimee Schaedig on the note sampled — sits at the bottom of the text. WIDER THAN REPORTED, and the fix should cover all of it: every other importer has the same defect, with 8 resume-intake, 6 system, 6 JAZZ and 2 document-intake notes equally authorless, 1,379 in total. POSITIVE CONTROL that the column itself works: the 4 Manual and 3 Front reply notes all carry an authorId, so this is the importers not setting it rather than the field being unused. Cosmetic, small, and it affects the single largest block of historical notes in the system. FIXED Aug 16, and the finding above was re-verified read-only at commit time rather than taken on trust: 1,386 notes, 1,379 authorless, every per-source count reproducing exactly and summing to the independently counted totals. A new lib/notes/attribution.ts recovers the name from the trailing signature line, strips it from the body so it does not render twice, and maps every source to a human label so no raw enum can appear as a person again. Fixed at the pattern rather than for Jazz alone — one helper, one render site, all 1,379 covered. DISPLAY ONLY on purpose: backfilling authorId would mean matching 1,357 free-text names to User rows and writing to the shared live database to fix something only ever rendered. Verified over HTTP on a real Jazz candidate, the author line renders Aimee Schaedig and the body no longer carries the signature
- [ ] Pre-offer visit gate — APPROVED with the user, designed, NOT built (Aug 4) — non-local candidates are not hired without flying to Utah first, but the checklist opens on Verbal offer extended to candidate with nothing saying why it is stalled, and the only travel item it knows, travel_complete, is in the ORIENTATION group and means post-offer travel to orientation. Two different trips, one word. BUILD: a new group Before the offer, ordered ahead of OFFER, with one item, In-person visit complete, defaulting TODO and offering NA for local candidates; carried on BOTH the candidate application as a shared step key and the onboarding checklist, the same pattern the six offer steps already use, so a visit can be tracked without moving anyone to onboarding; ticks DONE when a RECRUITING_VISIT trip reaches COMPLETED, not BOOKED, because booking a flight is not a visit; and the trip's outstanding travel-checklist items render nested under that one line, so why has he not come out yet is answerable without leaving the page. DELIBERATELY EXCLUDED from deriveOfferStatus — any ticked offer step currently means Offer started, and booking flights must not make an offer look under way. Offer status stays NONE, or PLANNED if intent needs signalling, which is what Start an offer already sets. TWO BLOCKERS TO CLEAR FIRST: RECRUITING_VISIT trips attach to the candidate while the checklist hangs off the NewHire and nothing joins them; and the latent travel_complete bug above fires the moment that join exists
- [ ] Pre-offer visit chip on /offers and the New hires list (Aug 4) — somebody parked at the visit gate should read as their own status, taken from the trip date, rather than as a hire with almost nothing done. Approved alongside the gate above
- [x] Interview notes stopped drifting, and pasted formatting survives (Aug 4) — reported as bold that could not be corrected on one write-up, and as Gmail formatting being lost when pasted back. Measured on the stored HTML of that exact write-up: 5 invalid ul-inside-p nestings, 7 empty paragraphs, 6 zero-width spaces, and 4 bold tags wrapping nothing but a zero-width space or a line break. THE EMPTY BOLD TAGS WERE THE UNCORRECTABLE BOLD — no visible text to select, and the caret entering one turns bold on for the next thing typed. ROOT CAUSE was that the sanitizer is a string filter with no document model, so it stored markup the browser re-parses into a different tree; the editor then reads that different tree back and saves it, drifting a little on every open. New lib/richtext/normalize.ts parses a real DOM, is idempotent, and runs on load, blur and paste, so opening an existing write-up repairs it. The sanitizer now closes an open paragraph before a block tag the way a parser does, strips zero-width characters, and unwraps empty inline tags. Colours and sizes SNAP to the nearest brand token rather than being dropped for not matching exactly, which is what made pasting from Gmail feel like it did nothing; the allowlist stays closed because output is only ever built from the token file. Tables are flattened instead of deleted, so pasting from Word, Outlook or Paycom no longer loses the content outright. styleWithCSS pinned off, toolbar buttons no longer steal the selection, and a Clear formatting button added. VERIFIED against the live row: 5 to 0 invalid nestings, 7 to 0 empty paragraphs, 6 to 0 zero-width, bold tags 9 to 5 which is exactly the 4 empty wrappers, all 27 list items preserved, sanitize twice equals sanitize once, and all six XSS probes still clean. NOT verified: the client-side normalizer on a real paste and the load and blur repair paths, since there is no jsdom in the tree and the Browser pane cannot hydrate this app
- [ ] Existing stored write-ups are only repaired when somebody opens them for edit (Aug 4) — the normalizer runs client-side, so the roughly 300 rows already in the database keep their current markup until touched. A one-off script over Interview.notesHtml and CandidateNote.bodyHtml would fix them in place, and has NOT been written
- [x] lib/richtext/sanitize.ts no longer contains literal control characters (found Aug 4, fixed Aug 16) — the href guard rejected control characters with a class typed as RAW BYTES rather than escapes, so the file read as BINARY to the tooling: git and grep both answered "Binary file ... matches" instead of showing the line, and a raw NUL survives in source only until something reformats it. Rewritten as /[\x00-\x1f]/, which covers exactly the same range. VERIFIED BY BYTE AUDIT rather than by eye: the committed version held exactly two control characters, a NUL immediately followed by a 0x1F, and the new one holds none. The fix demonstrates itself — git grep now prints line 103 out of the stored blob, which is precisely what it refused to do before. The original note follows. (Aug 4) — its href test is a control-character range written with the raw characters rather than escapes. The test is CORRECT and was rightly left alone, but the file now reads as binary to grep and diff tooling, it showed as Bin in the commit stat, and reading it renders the regex misleadingly as a short two-character range. That misreading already produced one wrong claim in the session that fixed it, caught and self-corrected. Rewriting the same range as escapes would be behaviour-identical and would stop the file being opaque to tooling. Small, and worth doing before somebody else reads it wrong
- [x] Saved views got their own tab, index and a working list (Aug 6) — three faults in the view shipped hours earlier, ALL found by actually using it: the index only rendered on Compare when NO view was open, so opening a shortlist was a one-way trip and the view read as lost; a name was a link, so checking somebody meant losing the list; and a view could not be edited once saved. Now a name opens the person in a SIDE PANE — a button rather than a link, correctly, because it swaps a detail pane on the same page rather than navigating — showing contact, department, applications and documents. Ticked people can be removed, or kept while the rest are dropped. A view can never be emptied by accident: the API refuses an empty member list and tells you to delete the view instead, which was exercised and returns 400
- [x] Candidate department, DERIVED from the job applied to rather than stored (Aug 6) — chips on the records list and inside a view, a multi-select filter, and 100/250/500 page sizes for bulk work. Deliberately not a column: 3,167 of 3,598 candidates already reach a job carrying a department, so storing it would mean a 3,598-row write against the shared live database to duplicate what is already there, and the copy would go stale the moment an application moved. The recruiter vocabulary — Maintenance, Flight Ops, Sky Ops, Accounting, FBO — is flatter than the org taxonomy the calendar uses, so one mapping module holds the translation and the two cannot drift. THE FILTER IS OR, not AND like tags, because a candidate has ONE department and ANDing would always return nobody; the menu says so. Checked rather than assumed: maintenance 110 and fbo 2 together return 110, not 112, because Tavin Newell and Hobum Lee each applied in both departments — confirmed by direct query and by listing their applications
- [x] The Elevate saved view was never lost (Aug 6) — 37 candidates, saved by the user, present the whole time and only unreachable because the index would not render while a view was open. Re-listed after the membership editing was tested on a THROWAWAY view and still intact. It breaks down 20 Flight Ops, 9 Maintenance, 6 Other (five HR, one sales), 1 FBO, 1 Accounting — which is also the evidence for the department work being useful, since that spread is invisible on a plain list. RE-COUNTED after the Operations decision below: its 6 Sky Ops were the cabin attendants and are now Flight Ops, so Flight Ops went 14 to 20 and Sky Ops to none
- [x] DECIDED — Operations was never a category, it was one job, and it is Flight Ops (Aug 6) — THE USER'S RULE: pilots AND cabin attendants are both Flight Ops. The word Operations appeared on exactly one job, Corporate Cabin Attendant (RETIRED, 284 applicants), and the mapping module already read that job's TITLE as flight-ops while forcing its DEPARTMENT to sky-ops, so it disagreed with itself. FIXED AT SOURCE by setting that job's department to Flight Operations, a value 4 other jobs already use: 284 people become correct in one row, no ambiguous string is left in the job table, and the special case was DELETED rather than re-pointed. The comment says not to restore it — a future bare Operations should land in Other and be looked at, not be coloured by a mapping that was wrong once. CONSEQUENCE WORTH KNOWING: no job department maps to Sky Ops any more, so that filter chip is reachable only by a hand-set override until a req is filed under a Sky Ops department. That is the decision working, not a regression
- [x] Place the unassigned — a review screen that proposes a department from evidence (Aug 6) — 305 live candidates had no department, and the obvious fix does not work: 285 of them have NO APPLICATION AT ALL, so there is no job to read. Their department is in structured fields instead, and three cover 300 of the 305 — a Pilot Application document (268), the source they arrived through (12), and their current title (20). The screen proposes one per person and SHOWS WHICH FIELD IT CAME FROM so it can be overruled per row, and nothing is written until Apply. A KEYWORD CLASSIFIER OVER RESUME TEXT WAS BUILT, MEASURED AND THROWN AWAY, and the reason is recorded in the code so nobody rebuilds it: it scored 269 of 271 as flight ops with zero unmatched, which was it matching the blank form labels of the shared Adobe Sign pilot application template rather than anything a candidate wrote — the right answer for no reason, and it would have called a mechanic a pilot the moment one filled in that form. The document type gives the same answer as a fact. APPLIED AUG 6 after the user reviewed the breakdown and approved every proposal as-is — see the entry below
- [x] Candidate.departmentOverride added to the LIVE database (Aug 6) — a hand-set department that wins over the derived one, in BOTH directions in the filter: it puts somebody in its department and equally keeps them out of the one their application implied, or a corrected person would appear under both. Clearing it returns them to derived, so every write here is reversible. THE COLUMN IS ALREADY APPLIED and that was verified before shipping the code that reads it, by listing all 37 Candidate columns so a missing one would be visible. It was applied by hand as a single ALTER TABLE rather than by db push, because db push would also have applied four CREATE INDEX statements belonging to another session's un-shipped work — see the Platform entry. The write path was exercised on two candidates and REVERTED, with the unassigned count returning to exactly 305
- [x] APPLIED — every live candidate now has a department (Aug 6) — 305 overrides written, 297 Flight Ops and 8 Maintenance, and live candidates with no department went 305 to ZERO. The user reviewed the full breakdown first and approved every proposal as-is. Run as a committed script rather than by hand: dry run by default, and it REFUSES to write if a hand-set name matches other than exactly one row, if any candidate already carries a different department, or if the Operations job is not in the state it expects. Applied at a limit of 10 and verified before the remaining 295. Idempotent, so the second half of a batched run can finish — the first attempt correctly refused to continue because the job it had already fixed no longer matched, which is the guard working. VERIFIED, NOT ASSUMED: overrides in the database match the plan file exactly with 0 disagreeing, all 284 cabin attendant applicants resolve to Flight Ops, and every remaining Job.department value was listed as a positive control rather than checking that one string was gone. UNDO is one flag against the plan file, which lists every id written; all 305 were empty beforehand, so clearing them restores the previous state exactly. The plan file holds 305 candidate names and stays OUT of the repo, since scripts/out is gitignored — it rebuilds identically from the dry run
- [x] Joel Hansen's misspelled title is corrected by the user (Aug 6) — it reads Maintenance Control now and he resolves to Maintenance automatically, exactly as predicted. A data fix, not a classification problem, and the prediction held
- [x] The last unplaceable candidates are settled by the user (Aug 6) — it was three by the time it was applied, not five: Tara Ward turned out to be placeable after all, since her job title Lead Corporate Cabin Attendant reads as Flight Ops even though her current title is empty, and the earlier count read only the current title. THE USER'S CALLS: Fred Saadat to Flight Ops as a pilot, and Rich Paden (Dual Base Manager) and Don George (Aircraft Compliance Manager) both to Maintenance
- [ ] The records table filters by department but does not SORT by it (Aug 6, asked for by the user)
- [x] Multi-select on the candidate list, saved as a named view, shareable by link (Aug 6) — built for one concrete workflow: search a former employer, select all the hits, untick the few the word matched by accident, save the rest as a named shortlist, send that link to a hiring manager. Searching Elevate returns 54 people and only about 6 actually worked for an Elevate company, so THE UNTICKING IS THE POINT rather than an afterthought. A view stores candidate IDS ONLY, so opening it tomorrow shows today's stages and notes instead of a snapshot, and archived candidates are included when a view is opened because a shortlist that silently dropped somebody would show the manager a different list than the recruiter picked; ids that no longer resolve are counted and reported on screen rather than vanishing. Views live in a WorkspaceSetting, so no migration against the shared live database, and the shape to migrate to is documented if they ever grow relations. Selection is page-local on purpose — it is a shortlist being assembled, not a filter. The Compare tab gained a saved-views index with copy-link and delete
- [x] THE SHARED LINK IS NOT PUBLIC, and who can actually open it is now known (Aug 6) — it passes requireModulePageAccess for candidates, so a recipient needs a Journey account. A public token link was considered and REJECTED because it would expose candidate PII on the open web. The working session flagged that it could not test the recipient path and that a manager's link might 404; checked here against the stored policy, reading all 20 modules so a missing entry would be visible as missing. HIRING_MANAGER on candidates is showInSidebar true, accessLevel EDIT — their links work. THE ACTUAL RISK IS ELSEWHERE: VIEWER on candidates is HIDDEN, and VIEWER is the default role every new account is created with, so a link sent to somebody whose role has never been set will 404 for them. Set the role before sending, or expect a confused reply
- [ ] Compare columns are pilot-shaped and mostly empty for maintenance candidates (found Aug 6) — flight time, type ratings and certificates, so an AMT view shows little beyond name, title and stage. Maintenance columns — A and P, IA, avionics, airframe — are not built, which matters now that 137 AMT resumes are in
- [ ] Select-all covers only the 100 rows the list shows (found Aug 6) — a search matching more than 100 cannot be shortlisted in one pass. Same 100-row cap the tag filter had to be moved server-side to respect
- [x] The records table sorts by department now, not just filters by it (Aug 16) — asked for by the user Aug 6. The Department header is a three-state sort button, off then A to Z then Z to A, matching the affordance already on the comparison table. A candidate in more than one department sorts on the alphabetically first of their labels so the order is stable, and Unassigned sinks to the bottom in BOTH directions because it is the absence of a department rather than a value at one end of the alphabet — the null test runs before the direction flip, so that holds whichever way it is sorted. ONE REAL LIMIT, stated rather than glossed: it sorts client-side over the 100 rows the list returns, not the whole result set, which is the same cap the select-all item above already flags. Server-side ordering needs department pushed into the query, and department is DERIVED from the jobs applied to rather than stored on the candidate, so there is no column to order by yet
- [x] Candidate stage is a picked list, and editable from the main list (Aug 1, from the user) — stage was a free-text field carrying whatever the JazzHR import wrote plus whatever anyone typed into the profile, which is how a pipeline ends up with one-off stages that nothing can filter or report on. New lib/candidates/stages holds the real values counted from the live database, in pipeline order and grouped Open (New, Applied, Screening, Prescreen Complete, Interviewing, Offer, Hired) and Closed (Saved For Later, Withdrew, Rejected, Knocked Out, Archived). The dropdown now sits on EVERY ROW of the main candidates page, so moving somebody along no longer costs a trip into the profile, into edit mode and back — which is how stages went stale. It writes through the same permission-gated PATCH the profile uses, updates optimistically and PUTS THE OLD VALUE BACK if the save fails, so a row can never display a stage the database does not have. The profile's free-text box is the same dropdown. TWO DELIBERATE CALLS: nothing is migrated — the list only controls what the pickers OFFER, and a candidate sitting on an unrecognised value gets it shown under a Current group so opening the dropdown can never silently rewrite an imported stage; and Screening plus Interviewing are included though absent from the data, because the list UI has always colour-coded both, so the pipeline was plainly meant to have them. Confirm or trim that list. VERIFIED: 100 stage dropdowns render on the live page with their groups; the save click path is NOT tested, same preview-pane limit
- [x] Candidate notes & history (Jun 18) — the candidate profile Notes tab now adds/removes notes (attributed to the author) and a new Activity tab shows a per-candidate history timeline (edits, note add/remove, dedupe, interview changes) built from the activity log
- [x] Candidate pages resizable + document polish (Jun 19) — the Candidates list and the individual profile now have the Edit-layout resizable board (like Jobs/Calendar) with documents rendered inline; fixed the resume frame to fill its panel as you resize (was a fixed height), the inner cards to fill in edit mode, and the Currency panel to show an empty-state instead of a blank box
- [x] Matcher bulk export (Jun 20) — multi-select matched candidates (per pilot requirement) with select-all, then export the selected contacts (name, email, phone, current title, matched job) to CSV for Front / Paycom; recruiter/admin only. (Also listed under Candidate Evaluation.)
- [ ] Future-fit / talent-pool disposition — a candidate-level lane separate from per-role readiness AND the active shortlist: Shortlisted (pursuing now) vs. Talent pool = future fit (good, but not now — revisit later), visually separated so future-fit people never blend into the active shortlist. Complements the shipped Pros & cons + the scan-exclusion ignore states; keep it behavioral/job-related for compliance. (Part 2 of the pros-cons/future-fit plan; part 1 shipped Jun 18.)
- [x] Duplicate review: merges that failed silently (Jul 16, reported by Aimee) — the queue said "10 failed" and would not say why, so there was nothing to act on. Two separate faults. The reason was never missing: the API returns one for every failure ("That candidate is already merged.") and the queue threw it away, keeping only a pass/fail boolean — a single merge said "That action couldn't be completed" and a bulk run said "N failed". Both now name the pair and quote the server's reason, one line each. The failures themselves were all the same stale pair: every one of the 10 had a candidate that had ALREADY been merged away (7 on the drop side, 3 where the survivor itself was gone), so mergeCandidates rejected them and always would have. They were left behind by the standalone merge scripts, which — unlike mergeCandidates — do not resolve the review items they invalidate. The scan now closes any open pair whose keep or drop candidate is already merged or deleted, since it can never succeed, and reports how many it closed. Running it cleared all 10; the queue has 0 un-actionable pairs left.
- [ ] Multiple & duplicate resumes per candidate — define how the system handles several resumes on one candidate and duplicate resumes (extends the candidate duplicate merge work). Current behaviour, confirmed Jul 16 while attaching the Jazz archive: several resumes on one person already works by accident — they all attach and list, auto-numbered (one person has six) — but nothing marks which is current, and duplicates are not detected at all, so the same resume uploaded twice is simply two files. Still needs a decision, not code.
- [ ] Seed one complete sample application — upload a real Paycom candidate application and fill in every field (candidate details, application linked to a job, documents, flight metrics) so the system shows one fully complete, polished application end to end; doubles as a check that an application record looks good and complete

## Interview & Evaluation
Streamline the end-to-end interview experience.
- [x] SIM Eval is its own interview type, split from Technical (Sep04, from him) — TECHNICAL was labelled Technical / Sim and had to stand for both, though they are different events run by different people on different days, and a pilot pipeline lives or dies on the sim. SIM_EVAL added (pink in app, Flamingo in Google Calendar) and TECHNICAL relabelled to plain Technical. Rows already stored as TECHNICAL are untouched and still valid. Verified: 8 types with 8 distinct Google colour ids and no collisions, and the calendar legend renders SIM Eval with zero remaining occurrences of Technical / Sim.
- [x] FIXED a bug that made Technical interviews unrecordable from a profile (Sep04) — the interview type list existed in THREE hand-maintained copies, and the write-up one in lib/interviews/constants.ts had silently fallen two entries behind, holding 5 where the source held 7 and missing TECHNICAL and OFFER entirely. So an interview scheduled as Technical on the calendar could not be saved as Technical from the candidate profile: the PATCH route validated against the short list and dropped the value without erroring. All three now derive from lib/calendar/interview-types.ts, so adding a stage is one line. The two-behind claim was checked against the previous file rather than taken on trust.
- [x] RECORDED, SIM Eval and Technical are SEPARATE (Sep04, from him) — asked for directly. They are not two names for one event and must not be collapsed back into a single type on the grounds that the labels look similar.
- [x] An interviewer no longer has to be an app user (Aug 31, from him) — he had hiring managers running interviews who are not on the site, and no safe way to add them: the write-up dropdown listed every User row and nothing else, hand-creating a User row is the documented trap that locks somebody out of Google sign-in with OAuthAccountNotLinked, and an invite does not help because the User row is not created until their FIRST sign-in. That is why Kevin appeared before he had ever logged in - somebody hand-made his row. The list now unions Users with BookingHosts, which already exist for these people, carry a real address, and can be added from Scheduling admin without granting anybody access. isActive is ignored on purpose: it governs the booking calendar, and a host not accepting bookings is still a perfectly good interviewer. THE @-MENTION LIST IS DELIBERATELY NOT WIDENED - one function fed both pickers, and widening it would have put people with no account into mentions, sending notifications nowhere. Two lists now, with a comment on each. A host without an email is skipped rather than given a placeholder, because interviewerEmail is the identity the recent-interviews filter matches on; today that skips SkyShare Recruiting, a shared inbox. Measured: mentions stay at 5 Users, interviewers is 8, gaining Alex Andrade, David Ricks and Devon Carter. Confirmed working by him.
- [ ] ANSWERED, where the interviewer list comes from, and there are TWO of them (Aug31, from feedback cmsz0159w, asked Aug18)  — her question was where the list comes from and how to get more hiring managers onto it. The candidate profile Interviews tab lists EVERY person with an app account, with no role filter at all (lib/data/team.ts, a findMany on User where email is not null). The Calendar scheduling and edit-interview picker is a DIFFERENT list, the BookingHost table filtered to isActive. Live roster re-listed in full at commit time as the positive control: 5 User rows - Aimee Schaedig and Hannah Byers ADMIN, Jonathan Schaedig and Rich Paden HIRING_MANAGER, Kevin Sherman RECRUITER - and 0 users with a null email. 8 BookingHost rows - Aimee Schaedig, Hannah Byers and SkyShare Recruiting active as RECRUITER, and Alex Andrade, David Ricks, Devon Carter, Jonathan Schaedig and Rich Paden all INACTIVE as HIRING_MANAGER. So both hiring managers with accounts DO appear on the Interviews tab, and ZERO hiring managers appear on the calendar picker. ADDING PEOPLE IS A SETTINGS CHANGE PLUS AN INVITE, NOT A BUILD: tick Accepting bookings in Scheduling admin for the calendar picker, and for the Interviews tab send a Settings invite with role HIRING_MANAGER - do NOT hand-create a User row, which bounces them with OAuthAccountNotLinked and locks them out of Google sign-in. Zero invites are outstanding. THREE DEFECTS FOUND ALONGSIDE: the two pickers cannot agree by construction, since one reads Users and the other BookingHosts, and SkyShare Recruiting and Kevin Sherman each exist in only one; isActive is overloaded, labelled Accepting bookings in the admin UI while also controlling interviewer eligibility, which is the likely reason all five hiring managers vanished from the calendar picker; and User has no disabled flag, so a departed employee stays in the write-up picker permanently.
- [x] An interview's TYPE can be corrected after it is logged (Aug 20, from feedback) — a call recorded as a recruiter screen could never be changed to a hiring manager interview, because the type was set at creation and the edit form had no control for it. The edit form now carries it and the title follows unless it has been hand-edited
- [x] More than one interviewer can be recorded (Aug 20, from feedback) — a panel is normal and only one name could be stored. Co-interviewers are a new nullable Interview.coInterviewersJson, deliberately SEPARATE from interviewer and interviewerEmail rather than widening those into a list: interviewerEmail decides who is allowed to edit a write-up, so making it a list would quietly change who can edit what. One person still owns the write-up; everyone else who sat in is recorded beside them and shown on the row. Capped at ten and de-duplicated by email so a client cannot write an unbounded blob
- [ ] The interviewer dropdown was never missing anybody, and turning the missing people on has a CONSEQUENCE (found Aug 20, from feedback Aug 18) — it lists BookingHost rows filtered to isActive. All five HIRING_MANAGER rows - Alex Andrade, David Ricks, Devon Carter, Jonathan Schaedig and Rich Paden - exist but are switched off, leaving only the three RECRUITER rows. THE PART THAT MAKES THIS A DECISION RATHER THAN A FIX: the same flag gates the PUBLIC booking page at /book/slug, so switching somebody on both adds them to the dropdown AND makes their public booking link live. ANSWERED Aug 20, and the answer is NOT YET: he was told the consequence - that the same isActive flag both adds them to the dropdown and publishes their public booking page - and chose to leave all five switched off. So the dropdown showing only the three recruiters is now deliberate rather than a gap, and nobody should be switched on without asking him again. Kept open rather than closed because the five hiring managers are still absent from the dropdown and somebody will notice and re-report it; this entry is the answer to give them
- [ ] The SCHEDULE marker button is more rigid than the convention it copies (found Aug 17, re-verified against the live recruiting calendar Aug 18) — the button emits exactly SCHEDULE then name then dash then role, with no note, no suffix and no attendee, because it was modelled on a single worked example. His own six markers created Aug 17 show the real convention is wider, and every part of this was read off the live calendar rather than reported: a MAYBE SCHEDULE prefix for someone not yet confirmed (Gabriel Wishingrad), a parenthetical mode suffix - (video) on three, (see notes) on one - a free-text line ABOVE the Paycom link carrying the actual instruction (Goes on rotation Tuesday the 25th; Need to see if CPO wants to move forward first; Missed the planned time for interview, please reach out for other options), an internal attendee so somebody is actually notified (hbyers on four of the six, absent on two), and the candidate's fuller name than the interview title used - Kenneth Figg rather than Ken Figg. One has NO DASH AT ALL (Michael Fobert G200 SIC). The MAYBE one also carries no colorId where the other five are red 11, so the not-yet-confirmed variant may drop the colour too. So the generated event is a starting point he then edits by hand rather than the finished article. Most valuable to offer first are probably the free-text note and the optional internal attendee, since those carry the actual next step and the person who must act on it
- [x] AFTER 5PM IS THE RULE AND IT STAYS (Aug 18, settled by the user) — his words: he wants them to land after 5:00pm and he will move them out as he chooses, and he does not want anybody worrying about where they end up after he has moved them. So the shipped behaviour is correct as built and needs no change. RECORDED BECAUSE IT CORRECTS AN OVER-READING OF MY OWN, and the lesson generalises: all six of his Aug 17 markers were observed sitting the following morning at 03:00 through 08:00, one per hour, and that observation was accurate - but inferring from it that the button should CREATE them there was wrong. Where he drags an event afterwards is his workflow, not a specification. Post-move positions are not a design signal, and six of them are not more of a signal than one. The original RECORD had this right the first time: 45 minutes from 5pm on the interview's own day, and he moves it himself
- [x] Debrief queue - the interviews that happened and never got written up (Aug 17) — new page at /interviews/debrief, nav under Interviews and Scheduling. The gap was never a missing form but a missing PROMPT: POST /api/candidates/[id]/interviews has existed for a while and 7 real write-ups have gone through it, while nothing told anybody which interviews were still outstanding. The app could not know, and this is the crux: an Interview row is created BY the write-up, so the table holds 309 rows and ZERO upcoming. Re-verified whole-scope at commit time rather than taken from the handoff - total 309, past 309, upcoming 0, and by source JAZZ 282 / seed-demo 18 / manual-writeup 7 / local-calendar 2, summing exactly. The recruiter's own Google Calendar is therefore the only thing that knows an interview happened. DETECTION DELIBERATELY IGNORES THE TITLE, and the live calendar is the reason: of 154 events in a three-week window, 46 contain the word interview and NOT ONE is an interview - every one is Calendly buffer padding - while all 23 real interviews never contain it. A keyword rule would score 46 false positives and 23 misses. The rule shipped is the invite SHAPE: timed event, organised by the owner, exactly 2 human attendees, exactly 1 outside skyshare.com. Measured 23 of 23 detected, 0 false positives, with the 5-person Dinner with Robert and Rachelle correctly rejected. Matching is by attendee EMAIL, never name - 21 of 23 matched a candidate. At build time the queue held 16 real items from the prior three weeks, oldest first, with the 5 already written up correctly excluded. Also fixed a bug the edge cases caught rather than the happy path: a first-dash title split turned Jon-Micheal McKoy-PC12 PIC into the name Jon, so the rule is now the first dash whose left side has two or more words. Ages count by Mountain calendar day so a late-afternoon interview never reads as yesterday
- [x] Clearing a row as not needed, one at a time or in bulk (Aug 17, from the user, and the reason matters) — his words were that some of these people are not going to get scheduled for a second interview, and if they are old they have already been done, so he wanted to check them off without an invite being created. Every row carries a checkbox, a sticky bar clears the whole selection, and a Select them all shortcut picks everything older than seven days, which is the clear out all the old ones case. CLEARING TOUCHES NO CALENDAR AT ALL and the UI says so on the bar itself; only the explicit Needs scheduling button creates an event, and that event has no attendees, so no candidate is ever emailed by any part of this. Fully reversible - cleared rows collapse under a Cleared count with a Put back button, each keeping the reason it went. THE BULK REQUEST ALSO FIXED A REAL BUG rather than only adding a feature: all dismissals live in ONE WorkspaceSetting row as a JSON array, so each save is a read-modify-write of the whole list, and clearing ten rows as ten concurrent requests would have had every one read the same starting state with the last writer winning - silently losing nine. It is now one request and one write, and dismissEvent is a thin wrapper over the batch. The live store was left clean, verified at commit time: 39 WorkspaceSetting rows across 17 scopes and ZERO in any interview scope, so the real dismiss-and-restore exercise left no trace
- [x] One-click SCHEDULE marker from the debrief queue (Aug 17) — a Needs scheduling button on each row creates the to-do the user was already making by hand on the shared SkyShare Recruiting calendar, copied from a real example of his rather than invented: title SCHEDULE then name then dash then role, colorId 11 red against the interview's own colorId 3, no attendees at all, a fresh Google Meet link, and the candidate's Paycom application URL in the description. TIMING IS HIS OWN CALL AND CHANGED TWICE - a timed 45 minute block from 5pm Mountain on the SAME DAY as the interview, marked busy, which he then drags where it belongs. His first hand-made example sat at 05:00 the following morning but he confirmed that slot was incidental, and an all-day version was built and then rejected in favour of a real block. Times are sent as local wall clock plus an explicit America/Denver zone rather than a hard-coded offset, so they cannot drift an hour at each daylight-saving change. THE DESCRIPTION IS THE ONE PART THAT CANNOT BE AUTOMATED and the code never guesses it: the Paycom URL is not on the interview event, it is pasted from Paycom by hand, and it has no derivable formula - most stored links carry a per-application hash while the worked example carries none, so a constructed URL could silently point at the WRONG APPLICANT. Re-measured at commit time: of 3,622 candidates only 13 have paycomLink set, and of the 16 in the queue only 3 do. Where none exists the event is still created with an empty description and the UI says so
- [x] RECORDED from the debrief work (Aug 17, all from the user) — PAYCOM BLOCKS EVERYTHING and no integration of any kind is possible, so interview notes are copied across by hand permanently: do not propose a Paycom integration, now or later. His own words on the problem: interviews run back to back, the notes go into Paycom, and at the end of the day or the next day he has to find them and do the next step; he does not remember who is still pending, and the interview is complete without him going back and clicking on the person. Given four options he chose the in-app queue over a scheduled nudge, then chose to build the SCHEDULE-marker button on top of it. Not every interviewed candidate moves forward - some will not get a second interview and old ones are already done - so clearing a row must NEVER create a calendar event. THE QUEUE IS FOR HIS CALENDAR ONLY FOR NOW, aschaedig at skyshare.com; other Google Calendars are expected later, so keep that in mind but build only for his at this time. The candidate is usually or always an invited attendee on the interview event, which is what makes email an exact matching key. SCHEDULE markers go on the shared SkyShare Recruiting calendar, which this session already had OWNER access to - no new access and no new Google permission was required for any of this work. Marker timing is settled at 45 minutes from 5pm Mountain on the interview's own day, and he moves it himself afterwards. He framed the whole thing as a RIGHT-NOW option, not necessarily a forever one
- [ ] The same-day marker rule puts BACKLOG markers in the past (Aug 17) — 5pm on the interview's own day is right for an interview that happened today, but running it over the existing 16-item backlog would place the oldest marker at 5pm on Jul 31, three weeks ago, where it is useless as a reminder. Not a bug in the rule, a consequence of it: the rule was chosen for the going-forward case. Needs a decision before any bulk run - either land backlog markers on an upcoming evening instead, or only ever use the button on recent interviews
- [ ] Interviews are booked through CALENDLY, which shapes the data (Aug 17) — the interview event's description is a Calendly block naming a 30 minute phone interview with cancel and reschedule links, and its location is the recruiter's phone number. That is also what generates the buffer events that make title-based detection useless, and it means the interview record originates OUTSIDE both Paycom and this app
- [ ] The queue cannot be seen on a local machine, and the cause is Google OAuth credentials rather than anything in this feature (Aug 17) — the first wall was that local dev bypasses auth so there is no signed-in identity, and that IS fixed: resolveCalendarOwner falls back to DEBRIEF_DEV_CALENDAR_EMAIL, gated on isAuthRequired being false, which auth-config only allows when NODE_ENV is development AND neither VERCEL nor VERCEL_ENV is set, so it cannot engage on a hosted deploy. The second wall is not fixed and needs the user: .env.local carries no AUTH_GOOGLE_ID or AUTH_GOOGLE_SECRET, so no OAuth client can be constructed. Verified read-only against the Account table, whole scope: 5 google accounts, of which hbyers, jonathan, rpaden and aschaedig all hold a refresh token WITH calendar.events scope while ksherman holds neither. So the tokens are there and only the client credentials are missing - the same cause as the existing local dev parity item. Fill those two values in .env.local and the queue renders locally
- [ ] Two calendar interviews have no candidate record to file notes against (Aug 17) — both were interviewed but neither invite email matches anyone in the app. They render in their own section with a search link rather than being silently dropped or guessed at by name. Either they exist under a different address or they were never added
- [ ] The debrief queue has no nag, by scope rather than by oversight (Aug 17) — the user was offered a scheduled afternoon nudge and chose the in-app queue instead, so nothing chases him and he has to open the page. A sidebar count badge, or the nudge as a follow-on, is cheap now the queue exists
- [ ] What is still unverified on the debrief queue, and it is all one cause (Aug 17) — everything that needs a live Google session: the calendar read itself, populated rows rendering, and the Needs scheduling button reaching Google. What WAS proven: the detector and matcher as pure functions against a real 154-event dump of the live calendar, all four API paths exercised over HTTP with correct status codes and never a 500, the dismiss and restore round-trip exercised for real with disposable ids and then removed leaving no trace, and the SCHEDULE event shape proven by creating a real one on the live recruiting calendar. What nobody has watched is the app itself fetching from Google and drawing the rows
- [x] Independent interviewer scoring (Jun 18) — dedicated /interviews/[id] page where each interviewer fills their own scorecard (multiple per interview), reachable from the calendar interview modal; an aggregate shows every recommendation side by side
- [x] Per-question interview rubric (Jun 18) — each scorecard rates questions on the 4-point scale (Exceeds / Meets / Can Develop / Does Not Meet); questions pre-fill from the bank for the interview's department and are editable per scorecard
- [x] Rubric to numeric mapping (Jun 18) — Exceeds=4 / Meets=3 / Can Develop=2 / Does Not Meet=1; shows the labels but computes a per-scorecard average and a combined interview score (each interviewer weighted equally) for ranking
- [x] Interview question bank (Jun 18) — Recruiting > Question Bank: a reusable, searchable library of interview questions tagged by category, core value, and department, with full create / edit / delete and active toggle; the source the guide generator will draw from
- [x] Interview guide generator (Jun 18) — Question Bank > Build guide: pick a department, the core values to cover, and a length, and it assembles a balanced set of active questions from the bank (grouped by value, with category badges and interviewer guidance), with regenerate and copy-to-clipboard

## Calendar
- [x] Calendar department filter + color-coding (Jun 18, actually made to work Jul 16) — canonical departments (Crew, Maintenance, FBO, Support, each with sub-groups) drive a drill-down filter and color every interview on all calendar views by department; a Dept/Stage toggle switches the coloring (defaults to department); jobs map onto the taxonomy via an editable resolver. The honest history: this shipped in June and never worked against real data. It read the department off the interview's linked job, but Interview.jobId is nullable and is null for ALL 200 interviews we have — nothing sets it when an interview is booked from the scheduling form or arrives from the Google sync. So every event resolved to Unassigned: pick any department and the calendar went empty, and the color-coding was a single flat grey. Aimee reported it as a missing feature (Jul 16 feedback, "we want to be able to select the department or select all") because that is exactly how it behaved. Fixed by falling back to the departments of the candidate's job-linked applications when the interview has no job of its own, which recovers 199 of 200; the filter options are now derived from the interviews that actually exist, with counts, instead of the fixed taxonomy. Verified against the real loader: All 200, Crew 164, Support 21, FBO 10, Maintenance 4, Unassigned 1. The fallback is a heuristic — 9 candidates have applications across several departments, so their interview picks one deterministically rather than correctly. The durable fix is to populate Interview.jobId at scheduling time, which would make the fallback stop mattering; see the item below.
- [ ] Set the job on an interview when it is booked (Jul 16) — Interview.jobId is never populated: not by the scheduling form, not by the Google sync. Everything that wants to know what an interview is FOR (the calendar department filter and colours, the detail page chip, department-scoped question-bank filtering, the department prefix on synced Google event titles) has to guess from the candidate's applications instead, which is ambiguous for anyone who applied to more than one department. Fix it at the source. Note lib/google/interview-sync.ts also reads interview.job?.department directly and so silently omits the department prefix on every synced event — it needs the same treatment.
- [x] Editable department colors (Jun 18) — admins recolor each department (Crew/Maintenance/FBO/Support) from an on-brand palette via a Colors button on the calendar; saved per workspace, no migration
- [x] Interviewer roster + multi-department assignment (Jun 18) — team members (Scheduling) can each be assigned to one or more departments; the calendar interviewer field is now a roster picker that surfaces interviewers matching the selected job's department first (with their department tags), free text still allowed — foundation for per-interviewer scoring
- [x] Rename hiring manager to hiring team (Jun 20, finished Jul 16) — renamed the user-facing label to "Hiring Team" across the interview-type roles, scheduling host roles, and the Job Builder field; stored enum values (HIRING_MANAGER) kept for data integrity. Missed in June: two live BookingType rows still read "Hiring manager interview" on the booking and scheduling pages, because the June change fixed the code and the seed script but not the rows already in the database — the seed only runs for new data. Renamed both on Jul 16 (display text only; ids and booking links untouched), and the seed script now matches so a fresh environment agrees. Zero remain.
- [ ] Auto-import interview events from Google calendars — read interview events off my personal calendar AND the shared recruiting calendar and pull them into the app automatically, so they show on the in-app calendar and feed Reports without manual entry (extends the two-way Google Calendar sync)

## Design & Consistency
Make the whole app feel like one professionally designed product.
- [x] The Button primitive can finally express the locked hover rule (Aug 3) — it carried no gold glow on any variant, which is WHY 596 buttons stayed hand-rolled and 112 applied the glow by hand across 62 files: to comply with the rule you had to bypass the primitive. Added to primary, secondary, ghost, gold and toolbar, left off danger because a friendly gold bloom is the wrong signal for a destructive action, plus a new link variant. Call sites NOT migrated — that sweep is still open and is the larger job
- [x] Two dark tokens added for a surface and a text colour that had re-accumulated as raw hex literals (Aug 3) — 97 and 92 uses, 33 of them alongside brand-panel so two unnamed dark surfaces rendered side by side. That is the same drift the team already tokenized away once. Rendered output is byte-identical, checked against the served CSS rather than inferred. Only the definitions and the one primitive that baked in the literal were converted; roughly 189 call sites across 45 files remain as a mechanical sweep. Verified the config change adds colour tokens only — the locked 4px radius scale and existing brand tokens are untouched
- [x] Raw database enums are off the screen (Aug 3) — all three Calendar places, two dropdowns and the status badge, share one label map now, so CANCELLED and the Events module's Canceled no longer appear at once. Stored values are unchanged, confirmed in the rendered HTML
- [x] Dark-mode variants added to eight components that had none (Aug 3), including the Command Center roadmap status text. The orientation prep-task tick hardcoded its colours as SVG presentation attributes, which no dark class can reach; those became Tailwind utilities emitting the identical values
- [ ] Still open from the design review and not touched (Aug 3) — no PageHeader component exists, so 42 files copy the same header idiom into five competing treatments and five h1 sizes; clicking from the handbook into a chapter shrinks the title two steps
- [x] Match corner radii on the Interview guide & Candidates pages (Jun 18) — swapped the rounded-2xl heroes/cards on the Candidates (records + compare) and interview pages (question bank, guide, interview detail) to the site-standard rounded-xl (public booking + the color-editor modal left for the broader review)
- [ ] Full-site design review — go through every page from a professional graphic designer's perspective and make the design elements consistent (corner radius, spacing, typography, color usage, buttons, cards, headers); document the standard and apply it site-wide
- [~] Shared UI primitives — components/ui has Button / Input / Textarea / Badge / Modal / EmptyState. People module fully migrated; ~40 more clean-match buttons/textarea/modal migrated across candidates, jobs, pilot-requirements, interviews, orientation, scheduling (Jun 30, zero visual change, build+preview verified). Remaining is a JUDGMENT call, not mechanical: the rest of the buttons vary from the standard (font-bold vs semibold, px-3 vs px-4, disabled:opacity-60 vs 50) or are intentionally different (gold CTAs, dark-header toolbar buttons, the compliments value-color system, local style-maps, /15-border fields). Two options for a follow-up: (a) a "convergence pass" that accepts tiny deliberate shifts to standardize the ~150 near-matches, and/or (b) add real variants for the recurring exceptions (gold CTA, toolbar-on-dark). Decide per appetite. (Surfaced by the Jun 30 design audit.)
- [x] Typography upgrade (Jun 30) — replaced the dated Verdana body font with Inter (self-hosted via next/font, no runtime Google dependency) + system fallback. Instantly lifts perceived quality app-wide; verified in light + dark.
- [x] Accessibility pass (WCAG 2.1 A/AA) (Jul 9) — (1) one consistent gold :focus-visible keyboard ring for every interactive element (there were zero before); (2) icon-only buttons — audited every button in the app; it was already well-labeled, added the 7 missing aria-labels. Status pills already carry text labels (so color-only / 1.4.1 is satisfied). (3) Contrast: the brand-grey body-text token was darkened from #76787b (4.0:1) to #63666a (~5:1) so small secondary text passes AA (approved Jul 9). Optional future polish: a handful of text-brand-grey/70 opacity-reduced greys used for hints/icons are below AA if treated as body text. (Surfaced by the Jun 30 design audit.)
- [x] Dark-mode token + gaps (Jun 30) — the hardcoded dark card color (dark:bg-[#10243a], ~440 literals) is now the single brand-panel token (identical hex). And ~90 light-only status banners / alert boxes / status pills across ~50 files got their missing dark: variants (dark:bg-*-500/15, dark:text-*-300, dark:border-*-500/30, matching the Badge primitive), incl. lifting deep text-*-900 so alert text stays readable on the dark tint. Add-only (light mode unchanged); verified in dark mode. (Surfaced by the Jun 30 design audit.)
- [x] Mobile/responsive audit + fix (Jun 30) — audited the whole app at phone width. It was already largely responsive (sidebar → mobile drawer, most tables in horizontal-scroll wrappers, fixed widths gated behind xl:, modals on max-w). The one real bug: the Archived people table had an overflow-hidden wrapper that CLIPPED its right-hand columns (Start/Termination/State/Restore) off-screen on mobile — data unreachable. Fixed with a horizontal-scroll wrapper (verified at 390px: all columns now reachable). Optional future polish: card-reflow (stacked rows) on the heaviest list tables for a nicer phone layout, vs. today's horizontal scroll.
- [x] Design system v1 (Jun 18) — locked tokens applied site-wide: 4px corners, cool-mist #eaf0f7 page background, strong gold-glow hover, navy+gold selected state; oval pills squared
- [x] Dark mode v1 — opt-in (Jun 19) — class-based dark theme behind a sidebar toggle (persists to localStorage, with a no-flash script); page background flips to #0b1622, white cards to #10243a, body text to slate-100; dark: variants swept across the app incl. the newer Managed-aircraft panel and scoring-setup UI. Light mode is unaffected (dark: variants are inert in light)
- [x] Dark mode wired + polished (Jun 19) — fixed the real issue: the toggle component existed but was never rendered, so dark mode wasn't reachable; added it to the sidebar rail + mobile drawer. Also found dark: utilities were compiling class-based correctly in production (a stale local .next cache had made them look media-only). QA pass on key pages: strengthened the selected-state fill in dark (sweet tint), fixed the environment banner that stayed white, confirmed value chips read fine. Cards/text convert cleanly; contrast scan came back clean
- [x] Site-wide code & visual audit + cleanup (Jun 20) — parallel read-only audits across the whole codebase, then fixed everything safe: dark-mode class conflicts from the sweep (transparent panels, washed-out tabs, invisible body text — 61 files), the viewport-breakpoints-inside-resizable-panels bug on several pages (auto-fit grids), real text-overflow clipping (min-w-0), corner-radius source drift normalized to the 4px system (zero visual change), and dead-code trim to 0 lint warnings (removed unused imports/vars + a dead BlockLibrary retire feature, consolidated parseStringArray ×12 into one util). Types + production build verified clean

## Backlog (added Jul 14)
Captured from the Jul 14 braindump — not yet scoped.
- [~] Training start AND end dates, driving the move from "in training" to "on line" — when training info lands in the fleet section, capture a training START date and a training END date per person, not one date. Once the end date passes, someone sitting at "in training" can move to "on line" so the staffing board counts them as actually flying rather than training. DECIDE: automatic on the date, or a prompt someone confirms — training slips, gets rescheduled, or is failed, so an unattended auto-flip could quietly mark a pilot line-ready when they are not; a "training ended, move X on line?" nudge is probably safer. ALSO RECONCILE: the crew org chart currently counts training as FILLED, while the command-center mockup shows "in training" and "on line" as separate states — the two views need to agree on what a training seat means before this lands. Pairs with the Fleet / Recruiting Command Center item. Added Jul 21. HALF SHIPPED Aug 1 AND THE DECIDE IS ANSWERED (see Fleet Positions & Roles) — start and end dates are captured per pilot in the crew chart's Training tab, and automatic-versus-prompt is settled as a PROMPT: when an end date passes the chart offers to move that pilot to the line and never moves anyone on its own, exactly because training slips, gets rescheduled and is sometimes failed. STILL OPEN, unchanged: the crew chart counts training as FILLED while the command-center mockup shows in-training and on-line as separate states, so the two views still do not agree on what a training seat means.
- [ ] A&P Maintenance factory training tracking (added Jul 28) — the user's request: track factory / manufacturer training for the A&P maintenance technicians — who has BEEN, WHEN they went, who still NEEDS training, and ON WHAT (which airframe / engine / system the course is for). A per-tech, per-course record: technician, the training (aircraft type or system), status (needed / scheduled / completed), the date attended, and where (the factory / training vendor). The payoff mirrors the pilot side — a clear who-still-needs-what list plus a dated history — so the maintenance team sees gaps at a glance. HOW IT FITS: the maintenance org chart already lists the technicians and the fleet registry has the airframes, so training records hang off the existing people; it is the maintenance counterpart to the pilot training-dates item above and the Fleet Command Center's training-schedule view. SCOPE TO CONFIRM FIRST: whether ongoing maintenance training is recruiting / people-ops's to track or the maintenance department's own — the Fleet Command Center note already draws that line for pilots (new-hire training is recruiting-owned, recurrent training stays with the department), and the crew-compliance-out-of-scope decision flagged the same boundary; A&P factory training is closer to department-owned recurrent training, so confirm the owner before building. DECISIONS FOR LATER: whether "needs training" is derived from a required-courses-per-role matrix (this airframe needs these courses) or entered by hand; whether a completed course expires / recurs (factory training often runs on a recurrent cycle); and where it surfaces (the tech's profile, a maintenance training board, or both). Not now — captured per the user.
- [x] Org charts: edit per card, not a whole-chart edit mode (Jul 22) — added Jul 21 as an ADOPTION fix: the team's instinct is to click the aircraft they want to change, and both charts only offered one Edit button at the top that flipped the ENTIRE chart into edit mode. Now clicking a card and pressing Edit this aircraft (crew) / Edit this location (maintenance) edits just that card, with its own Save and Cancel on the card. Shipped on BOTH charts per the standing org-chart rule. The old whole-chart mode was NOT deleted — it is demoted to Edit all in the header for a bulk session, which was the user's call over removing it outright, so nobody's existing habit breaks. HONEST SCOPE NOTE: per-card is a UX boundary, not a storage one. The roster is one JSON blob, so saving a card still writes the whole array — two admins editing different cards at the same time is still last-write-wins, exactly as before, and this pass did not fix that. Nor is Save strictly one card: a MOVE deliberately touches two (the pilot leaves one aircraft and lands on another), so Cancel restores a snapshot taken when the editor opened rather than reverting one group, and the UI says so. Unsaved work is guarded at all four exits — closing the card, Escape, clicking a different card, and switching to Edit all. Removing an aircraft while card-editing saves immediately, because removing the card also closes the only editor that could have saved it. Verified: both charts compile and lint clean, both pages render over HTTP, the header now reads Edit all and the read-mode hint tells admins they can edit from the card. NOT VERIFIED, and this one was actually tested rather than assumed — the click path. Driving the chart from the preview pane was attempted: React is hydrated on the page but the chart's own card elements carry no React props there, so a synthetic click does nothing and the modal never opens. The pane cannot exercise this UI, there is no test tooling installed in the repo to do it headlessly, and so the interaction itself rests on compile plus review. FIRST REAL CLICK IS THE TEST — if per-card edit is broken, it is broken in a way only a person can find.
- [x] Org charts: Edit all no longer loses your work (Jul 22, REPORTED DATA LOSS) — the user ran an Edit all pass, edited card after card, left without pressing Save at the top, and lost the lot. Nothing had asked. Four unguarded exits, all now closed on BOTH charts: (1) the edit bar's button reads Cancel once you have changes and called exitEdit with NO confirmation — one click, everything gone; it now confirms, as does the header Editing all toggle, which discards through the same path. (2) Reload or closing the tab had no beforeunload guard. (3) ANY link click navigated away and took the edits with it — the sidebar, or a pilot's name linking to their candidate profile, which is a link sitting right inside the chart you are editing. (4) The Save button lived only in the header bar, off-screen while you are scrolled down inside a card, which is precisely the trap: the work happens in the modal and the Save is somewhere you cannot see. Save all changes is now repeated in every card footer with a loud amber Unsaved warning, so it is in front of you where the editing happens. New shared hook components/fleet/orgchart/useUnsavedGuard.ts covers the exits React cannot see (beforeunload plus a capture-phase link interceptor); the deliberate in-app discards confirm at their own call sites so they can name what is about to be lost. Modified clicks and target=_blank links are left alone, since opening a new tab does not lose the page. NOT VERIFIED interactively — the guards only arm once the chart is dirty, and this app's React state cannot be driven from the preview pane, so this rests on compile, lint, render and review. The failure it fixes was found by a person using it, which is the same way the next one will be. EDIT ALL WAS RECONSIDERED AND KEPT (Jul 22): with per-card editing now covering the real workflow, the obvious question was whether to just delete the mode that caused the loss. Asked, and the answer was keep it for now — so the two edit models coexist on purpose. Do not remove Edit all as a tidy-up; if it is ever dropped, that is a decision to take deliberately.
- [x] Crew chart: duplicate an aircraft into a new tail (Jul 22, requested) — there was NO way to add an aircraft to a crew chart from the UI at all: edit mode only ever touched people and seats, and the groups array was never appended to. Adding a managed tail meant a developer. Now, in edit mode, opening a card offers Duplicate this aircraft (type, tail number, owner) and Remove this aircraft. THE TRAP THIS AVOIDS, worth writing down: editing the crew-data.ts seed would NOT have worked either — once anyone edits the chart, the whole roster is stored as a WorkspaceSetting override (fleet/crew-roster) that REPLACES the seed, and this workspace has had one since Jul 21, so a seed edit would have compiled, looked correct, and changed nothing on screen. DESIGN CALL: a copy carries the seat LAYOUT but not the crew — three captains becomes three OPEN captain seats, and pilots, tags, departures, notes and the photo are all left behind, since a new tail does not inherit the old tail's people. An aircraft with no first-officer seat keeps that as no seat rather than an empty one. Remove exists so a duplicate made by mistake is not permanent; both are local until Save changes, and Cancel still discards. Verified against the LIVE stored roster read-only (13 assertions): duplicates of a dedicated-crew tail, a shared-pool tail and a cabin-equipped aircraft all survive the save normalizer with seat counts carried, no people copied, pool and shared-pool flags preserved and tags/notes/photo dropped. NOT verified: the click-through, same browser-pane limit as every other chart change — the form was built against the existing edit-mode patterns and checked by compile plus a real-data round-trip. CREW ONLY, AND THAT IS A DECISION — NOT AN OVERSIGHT (asked and answered Jul 22): the standing org-chart rule says every chart ships the same capabilities, so the obvious next step was to give the maintenance chart the same duplicate action. The user was asked and said to leave it: it is not needed yet, and a new maintenance LOCATION is rare in a way that a new managed TAIL is not. Recorded here on purpose so that six months from now this reads as a deliberate call rather than an unfinished job. If maintenance ever does need a new location, build it then — do not build it just to make the two charts match. Note that per-card editing, the other half of this work, DID ship on both charts, so this is the only place they intentionally differ.
- [ ] Fleet / Recruiting Command Center — pull the mockup's views into the app. Mockup artifact: https://claude.ai/code/artifact/b78da930-223e-4b21-821b-186e451fdcdd — a five-view "Recruiting Command Center" that stands in for roughly 32 workbook tabs. Four things in it the app does NOT have today. (1) TRAINING SCHEDULE — per new hire: position, start date, orientation date, training dates, location (FSI / CAE / EFT), and confirmed vs tentative; it feeds an "in training" seat count. Scope note from the mockup: recruiting-owned NEW-HIRE training only, ongoing crew recurrent (.293 / .297) stays with the department. (2) BONUSES — referral (naming who was referred), sign-on, and relocation payouts, each with its trigger (6 months from completion, one-year anniversary, 30 days after start, a named paycheck, 135.293 completion), the amount, and paid vs due status, with an automatic reminder when one comes due. (3) PILOT MINIMUMS — one authoritative table per aircraft and seat (total time, PIC, multi, jet/turbine, instrument) feeding both the job posts and the screening gates; the mockup caught a real conflict worth checking, G200 Captain minimum listed as 4,000 on one tab and 4,500 on another. (4) FLEET STAFFING BOARD — per aircraft the count, base, and captain/FO pay, and per seat target vs filled / in training / open / on hold, with open computed as target minus who is actually flying the line, plus a per-seat REASON line (which is the staffing-history item under Fleet Positions & Roles). It also has a candidate-pipeline kanban with each card linked to the seat it fills. DECIDE: does this become a Fleet-side command center, and is the existing Command Center still worth keeping, or should it be merged into / replaced by this? Added Jul 21.
- [ ] Recruiter daily to-do list — somewhere in the app for each recruiter to track their day-to-day tasks, instead of a notebook or a separate app. Decisions to make before building: personal/private per recruiter vs shared and visible to the team; free-text tasks only, or does it also auto-pull real work from the pipeline (candidates to call back, offers awaiting signature, interviews to schedule, onboarding checklist items coming due); due dates and whether unfinished items roll over to the next day; and where it lives (a Command Center home panel vs its own nav item under Recruiting). Added Jul 20.
- [x] Org chart heights + edit clipping, take two (Jul 17) — the first attempt didn't fully land. HEIGHTS: the grid was set to align-items:start, so cards took their natural height and a JS pass tried (and raced the web font) to equalize them — hence "same after refresh, random on load." Replaced it with pure CSS: the grid stretches each row (align-items:stretch) and the card fills its cell (height:100%), so cards in a row are the same height live, on first paint, no JS and no font race. The JS equalize pass was deleted from both charts. CLIPPING: the edit modal still cut off the bottom of each section box — added a flex spacer at the very bottom of the scrolling modal (reliable where padding-bottom on a scroll container gets dropped) and more bottom padding on each section, so nothing sits flush against the edge. Confirmed the grid now computes align-items:stretch and the card height:100% with no leftover inline min-height; both charts render clean. NOT verified: the pixel result — the preview pane still reports card heights as 0, so the equal-height and spacing were confirmed by computed styles + the CSS mechanism, not measured.
- [x] Org chart polish: offered restyle, equal card heights, edit-modal clipping (Jul 17) — three fixes to both charts from using them. (1) OFFERED now reads as a YELLOW box with RED marching ants around it — the in-training yellow plus the open-seat red ants combined, since an offered person is neither open nor in training. The gold glow the user disliked is gone (person rows, seat squares, and the edit list all updated). (2) The cards used to load at random heights and only line up after a manual refresh — the equal-height pass measured before the web font loaded and reflowed the text. Replaced the timed re-measures with a ResizeObserver (guarded against looping on its own writes) that re-equalizes whenever a card's real size changes, so they are level on first paint without a refresh; confirmed no ResizeObserver loop warnings. (3) The Edit modal clipped its controls — a two-column grid was too narrow once each row carries name + status + move + link + remove. Edit mode now gives each section the full modal width, rows wrap instead of clipping, and long names break. NOT verified visually: this app's layout can't be measured or seen from the preview pane (cards report height 0 there), so these were checked by compile + render + no-console-error and by reasoning about the CSS, not by eye — the user will see the actual result.
- [x] Org charts: an "Offered" status with a gold glow (Jul 17) — the status dropdowns (both the add and the move-arrival pickers) had On staff / In training / Candidate / Candidate-internal, but nothing for a candidate we have actually EXTENDED AN OFFER to — a distinct, further-along state than a tentative candidate. Added "Offered" as a new seat bucket across the shared model (Seat.offered, counted as pending/open-until-they-start like a tentative candidate) so it shows in both charts' add and move dropdowns and in future department charts for free. Offered people render in gold with a gold glow on hover — the brand's offered signal — in the person rows, the little seat squares, and the edit list, distinct from the marching-ants open seat. Verified: it counts correctly (pending, not filled), survives normalization and a real save/load round-trip on BOTH rosters (live rosters restored byte-for-byte), typecheck + lint clean, both charts render. NOT verified: the click-through / the glow itself, same browser-pane limit.
- [x] Org charts: Edit all can no longer silently lose your work (Jul 22) — shipped by a concurrent session (commit 3ee283f) after a REAL reported loss: someone edited many cards in an Edit all pass, left the page without saving, and lost the lot with nothing asking first. Four unguarded exits closed on BOTH charts: (1) the edit bar's Cancel discarded everything in one click and now confirms, as does the header "Editing all" toggle; (2) reload and tab-close had no beforeunload guard; (3) any link click navigated away and took the edits — including a pilot's name inside the chart you were editing, which links to their profile; (4) the root cause — Save lived only in the header bar, off-screen once you scroll into a card — so "Save all changes" now repeats in every card footer with an amber unsaved warning. New shared hook components/fleet/orgchart/useUnsavedGuard.ts. Build, typecheck and lint clean; NOT verified interactively (the guards only arm once a chart is dirty and this app's React state cannot be driven from the preview pane). NOTE: this entry was written by the SOP session that committed the roadmap; the session that did the work held its own fuller note back to avoid clobbering in-flight edits — expand or replace this rather than adding a second entry. The Org Charts SOP (docs/sops/04-org-charts.html) was re-checked against this commit and updated for the new Save all changes button, the Cancel confirm, and the leave-page warning.
- [x] Crew chart move now offers to record the matching role change (Jul 17) — the first real sync between the org chart and the employee role journey. Moving a pilot on the crew chart who is linked to a candidate that has a current employee record pops a prompt: record this as a role change on their journey? — with an editable role title (derived from the destination aircraft + seat, e.g. Citation 560XL First Officer), an effective date, and a transition type (Transfer / Lateral / Promotion / Upgrade), plus Skip. Confirming closes their current role on that date and opens the new one, syncing their headline position — never automatic, one confirmation. Chain: chart name → the stored candidate link → GET /api/candidates/[id]/employee finds the current NewHire → POST /api/new-hires/[id]/roles. Only fires for a LINKED person who is actually a current employee, so it stays quiet otherwise. Crew only — a maintenance location/shift move is not a role change. Came from the user realizing (with Matt Dahle, PC-12 Captain moving to 560XL FO) that a chart move didn't touch the journey. Verified end to end on a throwaway employee: the lookup finds the linked employee (and returns null when there is none), and the role change closes the old role, opens the new one with the seat detected, and syncs the position — Matt's real record untouched. NOT verified: the prompt's click-through, which cannot be driven from the preview pane. STILL OPEN: full auto-sync (the move and the role change are still separate writes; recording one does not do the other), and the maintenance/crew charts do not move people automatically off role changes.
- [x] Crew (pilot) org chart: link people to candidate profiles (Jul 17) — the crew chart already had the cross-aircraft move; added the same per-person "link to candidate" picker the maintenance chart got, so a pilot's name links to their candidate profile in both the read and edit views, stored on the crew roster (name → candidateId) and surviving a move. The picker was extracted to a shared component (components/fleet/orgchart/LinkPicker.tsx) and both charts now use it, so any future department org chart gets linking by reusing one component. STANDING RULE recorded (memory: org-chart-standards): every org chart — present and future — must ship BOTH the cross-location move AND candidate-profile linking. Verified crew end to end against the real API with capture-and-restore (live roster left byte-for-byte as found): the roster now stores { groups, links }, a link persists, groups are preserved, junk links are dropped; both charts render and the maintenance chart still works after the shared extraction. NOT verified: the click-through, same browser-pane limit as before. ENHANCED Jul 20 (from real use — couldn't add returning employee Nick Lembo): the link picker now searches BOTH candidates AND current employees, not just candidates — an employee with no candidate record yet (hired before the app, a direct hire, a rehire) is now findable directly. Picking an unlinked employee resolves through a shared lib/candidates/ensure-candidate-for-hire.ts that creates a minimal linked candidate on the server (origin MANUAL, scan-excluded HIRED) and returns its id, so the chart link stays a candidate id and nothing downstream changed. New GET /api/fleet/people-search + POST /api/fleet/link-employee. Reactivating/marking-onboarded never made an employee findable here (the link never read the new-hire queue) — fixed at the source. Verified: search surfaces unlinked employees, link-employee idempotent for an already-linked hire, charts compile.
- [x] Maintenance org chart: move people between locations + link them to candidate profiles (Jul 17) — two gaps the user hit trying to move Augie from Flex Pool to Ogden afternoons. FIRST: the maintenance chart could add, remove, change status within a group, and open/close positions, but had NO way to move a person to a different group/shift — the pilot chart has always had that. Added the cross-location move (mirrors the pilot chart): pick the destination group and shift plus arrival status; it reopens the seat they left and fills a seat at the destination. SECOND: the org charts stored people as plain name strings with no tie to their candidate record — the "third representation" that has never been linked. And it can't be auto-matched: the chart says "Augustin Quintero", his candidate profile says "Auggie Quintero". So added an explicit per-person "link to candidate" picker (searches candidates, you pick once); the link is stored on the roster (name → candidateId), survives a move, and renders the person's name as a link to their profile in both the read and edit views. Additive storage — the mx-roster WorkspaceSetting now holds { groups, links } and still reads the older bare-array saves. Verified end to end against the real API with capture-and-restore so the live chart was left exactly as found: the move round-trips (Augustin lands in Ogden afternoons, Flex Pool reopens), the link persists, and junk links are dropped. NOT verified: the move/link click-through in the browser — this app's React handlers can't be driven from the pane, so the panels were built by mirroring the working pilot-chart move and the candidate-search picker, and checked by compile + real-data round-trip rather than clicking.
- [~] Unified candidate → profile → employee lifecycle (BIG — needs brainstorming, must work reliably) — when a candidate is added, or a new hire is added to pre-onboarding, auto-create one person profile that carries all the way through and moves onto the Employees list once they're hired. Goal: a single record per person across Candidate → New Hire (pre-onboarding) → Employee, so status/dates/documents stay in sync and nothing is re-entered as they progress. Context/why it's big: today Candidate, NewHire, and the fleet org-chart roster names are three separate, unlinked representations of the same person — this project is about unifying (or reliably linking) them, which also unlocks cross-module automation (e.g. checking "signed offer letter" advancing their fleet-chart status). Added Jul 15. STARTED Jul 16 — the Candidate-to-NewHire half of the link now exists, though the auto-create goal does not. A hire can finally carry the candidate it came from (NewHire.candidateId existed in the schema but nothing ever set it, so all 448 hires were unlinked); a candidate's page has a "Move to pre-onboarding" action that creates the hire prefilled from the job they were hired into, suggested once an application says hired; and a backfill matched existing hires to candidates by email first, then by unique name — 100 of 448 now linked (52 email, 47 name, 0 ambiguous, plus 1 by hand), 94 of them reaching back to a Jazz profile so an employee opens their old resume and interviews. The other 348 hires have no candidate record at all (hired outside the pipeline) and were left alone. Worth knowing: the move action deliberately detects an existing hire with the same name and offers to LINK instead of creating a second record — 5 supposedly-new candidates turned out to be current employees already, so this is the common case, not the edge case. Still open: nothing auto-creates a profile. LINK BETWEEN THE THIRD REPRESENTATION AND A CANDIDATE SHIPPED Jul 17 on BOTH org charts (maintenance + crew) — see the item just below; only auto-linking (matching without a manual pick) is still to do. AUDITED Jul 16 — the create path was read hard against real rows rather than executed, and it does NOT throw; it succeeds and writes a record that is wrong in three ways. Of the 31 candidates it can fire for today: 15 would land a position reading "FILLED - Citation 560XLS+ Captain" (that prefix means the REQ was filled, so it is a lie stamped on someone just starting, and no existing hire carries it), 15 would get no position at all, and all 31 would get NO START DATE — which quietly matters most, because a hire with no start date is filtered out of nearly every panel on the New hires dashboard (starting soon, upcoming starts, ready for their start, the chart) and never escalates to due or overdue. You would move someone in, see their record, and then never see them again outside the grid. The auto-create of their role journey also silently does nothing, because it requires a start date. FIXED Jul 16: the FILLED prefix is now stripped before it can reach a hire (verified against all 29 real values; the cleaned "Citation 560XL Captain" matches a real job title exactly), and the offer now captures the start date. THE STRUCTURAL FIX SHIPPED Jul 16 — see the offer-steps item below. The disconnected records were never a discipline problem: the checklist only existed once you created a new hire, and its first six steps are OFFER steps, so the only way to track someone you MIGHT hire was to create the record that orphans them. Now the offer steps live on the candidate, and signing creates the hire already linked. The create path was also EXERCISED end to end for the first time (on a throwaway candidate, since we will not mint a fake employee in the live database) and it works: linked, start date carried, offer group already complete, and a second create for the same candidate now refused rather than silently duplicating. STILL OPEN: the move action should ask for position, department and start date rather than assuming them (a candidate with no offer still lands with no start date, which hides them from the New hires dashboard); and the four people already in pre-onboarding are still not linked to candidates. Reversible: scripts/link-hires-to-candidates.ts --undo. DIRECTION SET Jul 20: user chose MODEL A — reliable links (keep the tables but guarantee every hire links to a candidate) over introducing a single Person entity (too big a migration on the live DB). Jul 20: the 4 current pre-onboarding hires are now ALL linked (0 orphans) — Bryan Weber was already linked; Gavin Craner, Grace Ward and Luke Webb were true orphans (added via the quick-add/import side doors with NO candidate to link to), so a minimal candidate was backfilled for each (origin MANUAL, stage Hired, scanExcludedReason HIRED so they never surface in the matcher/recruiting scans) then linked; reversible via scratchpad/orphan-backfill-undo.json. STILL OPEN for reliable-links: (a) STOP NEW orphans — the quick-add + import intake paths should auto-create-or-link a candidate so no future hire is orphaned; (b) auto-match roster↔candidate without a manual pick; (c) harden the orphan-hire path server-side — its admin/recruiter gate is UI-only (POST /api/new-hires only checks candidates:write).
- [x] Offer ↔ onboarding: one checklist, edit from either side (Jul 20) — the 6 offer steps and the onboarding checklist's OFFER group are the SAME 6 keys but lived in two stores that diverged after a candidate was moved in (tick "signed" on one, the other never heard). Decided with the user: TWO-WAY SYNC (not a hard cutover) + MANUAL EARLY MOVE (not auto-create on offer-start), and moving a candidate to onboarding should happen BEFORE the offer is signed so offer-stage people are trackable on the board. PHASE 1 SHIPPED + verified (throwaway end-to-end, 7/7, clean teardown): ticking a step on the candidate Offers tab OR the onboarding checklist now updates BOTH, routed through the audited recordOfferStep/recordOfferStatus so dates/timeline/declined are preserved — lib/offers/onboarding-sync.ts, plus the write-back on /api/onboarding-tasks/[id]. PHASE 2 SHIPPED (Jul 20): the same OfferControl stepper now renders on the onboarding record page (app/people/[id]), fed by the hire's linked offer and editable there, driving the sync. PHASE 3 SHIPPED (Jul 20): the move-to-onboarding panel gives the prominent gold "move them in now" treatment the moment an offer is underway (STARTED/SENT), not only when signed, so offer-stage people land on the board pre-signed. PHASE 4 APPLIED (Jul 20): scripts/reconcile-offer-onboarding.mts run with --apply — 60 "offer behind" steps healed (offer side backfilled from the checklists via the audited recordOfferStep), 12 steps left BLOCKED (2 direct-hire orphans with no application to hold an offer — expected, they never had an offer). A fresh dry-run confirms 0 fixable divergences remain. All four phases done: the offer and the onboarding checklist are now one source of truth, editable from either side, with early move-in the encouraged flow. The Offer SOP documenting the early-move flow shipped as docs/sops/02-offers.html and was rewritten in the Jul 22 SOP audit.
- [x] Add a job by hand (Jul 17) — the missing first step. Jobs could only ever arrive through a CSV or PDF import or a seed script, so a role that exists in real life but was never imported could not be hired against AT ALL: an offer lives on an application, an application needs a job, and there was no way to make one. That is a dead end for the exact case it matters for — a requisition you are hiring on right now. Jobs now has a New job button: title, department, city, state, status and an optional requisition id. Whether it is a pilot or a support role, the seat, and the aircraft are all worked out from the title by the SAME functions the importer uses, so a hand-made job behaves identically to an imported one everywhere downstream — the matcher, the filters, the requirements. It also checks for an existing job with that title first and offers it, because this database already carries four separate Gulfstream G200 First Officer rows and picking the wrong one puts an offer on a requisition that was filled in 2025; you can still create anyway, deliberately, in one click. Verified against the real routes: a maintenance title correctly comes out as SUPPORT with no pilot seat, a pilot title still gets the right seat and aircraft, the duplicate is refused and offered, force creates, and a blank title is rejected. Test jobs deleted. NOT verified: the rendered form, which cannot be exercised in this app.
- [x] Link an ARCHIVED candidate to a job (Jul 17) — the user couldn't find Matt Dahle to link him to the XL SIC (560XL First Officer) position: the "Add candidate" search on a job called /api/candidates, which filtered archivedAt:null, and Matt is archived — so he simply never appeared. Archiving a candidate should not stop you reconsidering them for a new role. The search now takes an includeArchived flag (off by default, so the everyday picker stays active-only), returns whether each result is archived, and orders active candidates first; the "Add candidate to job" dialog and the org-chart link picker both pass it and badge archived results. Linking an archived candidate to a job REACTIVATES them (archivedAt cleared, status ACTIVE) — otherwise they'd have a live application but stay hidden from the candidate list. EXCEPTION (added same day): NOT if the candidate is archived because they were already HIRED and are a current employee (a linked NewHire in ACTIVE/POST_ONBOARD, not terminated). Those records are archived precisely because the person graduated to an employee — reactivating would wrongly push an employee back into the candidate pipeline. This surfaced with Matt Dahle, whose candidate record is archived because he is an active PC-12 Captain; his linked NewHire is why the guard skips him. Verified: linking Matt leaves him archived, while a stale archived candidate with no employee record still reactivates. Verified against real data: Matt is excluded from the default search and surfaces (badged) with the flag; reactivation confirmed on a throwaway archived candidate + job, then cleaned up. SEPARATE thing the user should know: the canonical "Citation 560XL First Officer" job is currently RETIRED after their duplicate-merge (the survivor is the retired Jazz req with 209 applications) — it is still linkable, but they may want to toggle it Active.
- [x] Active vs inactive jobs, active first (Jul 17) — the Jobs list interleaved roles you are hiring for with long-closed ones, so the ones you need got buried. Active vs inactive now maps onto the existing status (Active = OPEN, Inactive = RETIRED) rather than a new field — the near-dead activeStatus column was left alone. Active jobs sort to the top of each list (a stable sort, so the pilot/support grouping and title order are preserved within each), inactive cards are muted with an Active/Inactive badge, there is a per-job Active | Inactive toggle on the job detail, and a Hide inactive (N) checkbox to collapse to just what you are working. Also cleaned up the classification bulk-fix from earlier today (20 jobs corrected). The toggle routes through the recruiting-jobs PATCH and, like the Paycom req, returns early so a status change cannot trip the classification logic and flip a support job to pilot — verified precisely that a retired pilot job flips active/inactive with its seat and pilot flag untouched, a bad status is refused, and classification-only edits still work. Confirmed rendering over HTTP (active-first order, the toggle, the filter) with no error boundary.
- [x] Moving someone in now asks for what is missing, and fills in their company email (Jul 16) — the move to onboarding used to be a browser confirm box that assumed position, department and start date, and left whatever it could not guess empty. It is now a small form gathering all four at the one moment someone is actually thinking about that person: position, department, start date, and their company email. This closes a silent hole — a hire with NO START DATE is filtered out of nearly every panel on the New hires dashboard (starting soon, upcoming starts, ready for their start, the chart) and never becomes due or overdue, so assuming it meant losing them; the form now says so plainly if you leave it blank, rather than failing quietly. COMPANY EMAIL is filled in by the house rule — first initial plus last name at skyshare.com, so Hankyu Park is hpark@skyshare.com — because it is what the business cards need and nobody should type it. Multi-word surnames keep the whole surname (Don St George is dstgeorge, not dgeorge). Checked against the real roster: the rule reproduces 158 of the 175 existing addresses, and the 17 it does not are early employees on first-name addresses, which is legacy rather than a competing rule. IMPORTANT — the rule COLLIDES, so it suggests and checks rather than assigning: seven real pairs on the roster generate the same address (Kevin and Kieran Sherman both make ksherman, which is exactly why Kieran is kieran@), so a clash is shown for a person to resolve and is never auto-assigned — two people sharing a mailbox is worse than a blank field. Applied in the API too, not only the form, so every path that creates a hire gets it. Verified: the rule against all 175 real addresses, plus auto-fill, the collision case (second person correctly gets nothing rather than someone else's address), and an explicit address always winning.
- [x] Work an offer as STEPS, on the candidate (Jul 16) — the flow fix. The six offer steps your team already ticks (verbal offer, draft, supervisor signs, president signs, letter sent, candidate signed) now live on the candidate's application, so you can work an offer for someone you only MIGHT hire. Ticking them drives the offer status by itself — letter sent means sent, candidate signed means signed — so there is no separate status to remember, and signing asks for the start date. Moving them into onboarding then creates the hire already linked to the candidate, with those six steps already green, their dates intact, and the start date filled in. Same six task keys and labels as the onboarding checklist, deliberately: nothing is re-ticked and no second checklist exists. WHY THIS WAS THE REAL PROBLEM: the 19-step checklist only came into being once a new hire record existed, and its first six steps are offer steps — so the only way to track a possible hire was to create the very record that ends up disconnected from its candidate. That is what produced 448 orphans, and it was still happening (all four people in pre-onboarding were added Jul 8 and Jul 14 with no candidate link). Declining stays an explicit action, not a derived one — a decline is something THEY do, and tidying a checkbox afterwards must not silently undo it. Verified end to end through the real routes on a throwaway candidate, then deleted: all six ticks derive the right status, unticking walks it back, a bogus step is refused, the start date and all six completed steps carry onto the hire, and a second create for the same candidate is now refused with a clear message instead of silently duplicating. NOT verified: the rendered UI, which cannot be read in this app.
- [x] Offer "started" state + an Offers tab on the candidate (Jul 17) — the offer ladder was Planned / Sent / Signed, which lumped "we have decided to offer but not begun" together with "verbal done, drafted, signatures in progress." Now there is a distinct STARTED between PLANNED and SENT: Planned = the intent, before any step (set by the "Start an offer" action); Started = any prep step ticked (verbal, draft, supervisor or president signs); Sent = the letter is out to the candidate; Signed = they signed. Clearing every step on an offer already opened floors it back to Planned rather than vanishing to "no offer" (that is the explicit "No offer" choice). Applied everywhere the ladder is enumerated — the derive, the chip colours, the Offers list filters/counts/order, and the rank used when carrying an offer onto a hire. One existing offer (Auggie Quintero) was upgraded PLANNED -> STARTED to match reality, routed through the single transition function so his timeline stays correct. SECOND part: the candidate profile now has its own OFFERS tab (Documents / Applied to / Offers / Notes …) so the offer has an obvious home instead of hiding inside the application list; "Applications" was renamed "Applied to" (it is the jobs they applied to), and each application there links to its offer with a "manage in Offers" jump. Verified: the full ladder derives correctly end to end on a throwaway (planned→started→sent→signed, and the floor back to planned), typecheck + lint clean, tabs and counts render (Documents/Applied to/Offers/Notes). NOT verified: the Offers-tab body and tab-switching — this app's React handlers cannot be driven from the browser pane, so the tab could not be clicked to inspect; it reuses the same OfferControl already confirmed working.
- [x] Offer section + reorganize the hiring stages (Jul 16, reconciled done Jul 20) — the offer TRACKING, the Offers VIEW, and the NAV REORG all shipped; this line's lead long read "still to do" while its own body said "shipped too", now reconciled per the Rock #1 review. The one remaining thread — a canonical application-stage ENUM — is split out as its own future item below (net-new, never scoped, not "finishing" this). Decided with the user: an offer is the LAST ACT OF RECRUITING, not the first act of onboarding (the recruiter makes it; signed is the handoff), so it lives on the APPLICATION rather than as a parallel record, because you always offer someone a specific job. Shipped: offer state on each application — planned, sent, signed, declined, with the sent/signed/declined dates, a decline reason, and the start date named on the offer. NO PAY AMOUNT, on purpose: Paycom owns comp, we own the workflow. "Verbal" was deliberately dropped — an offer can be sent AND agreed verbally at the same moment, so as a state it fights "sent"; record that as a note. Signing now drives the move-into-onboarding prompt and prefills the start date from the offer, so it is not re-typed. CORRECTION Jul 16 — that prefill was DEAD ON ARRIVAL and is now fixed: nothing in the app could ever set the start date. The function and the API both accepted it; the control simply never sent it, so the prefill described a path that did not exist. Marking an offer signed now asks for the start date, with a "not known yet" escape so the signature is never blocked on it, and an offer already marked signed grows an "add their start date" link (which is the state Bryan Weber is actually in). Fixing that exposed a second bug: the transition function is idempotent, so on an already-signed offer it returned before the start date was applied — the date would have been silently swallowed and reported as saved. The guard now lets a late-arriving start date through on its own, without re-firing any of the downstream work the guard exists to protect. Built so the later automation is not a rewrite: ONE function (lib/offers/record-offer-status.ts) owns every transition and stamps who said so — MANUAL today, FRONT/PAYCOM later — and everything downstream reacts to the state, never to the click. Re-confirming an offer is a no-op, so an inbound notification cannot double-fire. Also added Candidate.paycomPersonId and Job.paycomReqId, because Paycom's "Offer Accepted" email quotes a person id and a requisition number and those are the only exact keys we get (matching on name alone is what created duplicate people). BOTH WERE DEAD FIELDS UNTIL Jul 17 — the columns existed and nothing in the app could write to them, so the automation had nothing real to match on and the numbers could not be captured even by hand. Now: a Paycom ID box on the candidate profile (next to Owner and Source), and a Paycom requisition on the job detail that you can add or change in place, plus a box on the new-job form. Both are digits-only, since that is what Paycom issues, and both can be cleared. Kept separate from our own requisition id on purpose — that holds the Jazz codes (AMA.1) which will never match a Paycom number. Worth knowing: the job route treats a requisition number as plain metadata and returns early, because everything else in that endpoint is classification and would otherwise flip the job to a pilot role as a side effect of recording a number — verified that a maintenance job with a Paycom req stays support with no seat. THE NAV REORG SHIPPED TOO (Jul 16): the sidebar now reads Home / Recruiting / Onboarding / People / Fleet / Data / Admin. Recruiting is people we MIGHT hire (candidates, jobs, pilot requirements, matchboard, offers); Onboarding is people who are JOINING (new hires, orientation, travel, business cards); People is people who WORK HERE (employees, compliments). One question decides where anyone clicks: have they signed? have they started? The old People section held both someone starting in six weeks and someone who had worked here six years, which was the fog. "Pre-onboarding" is retired as a term — the nav, the page heading and the back-links all say "New hires" now. Deliberate calls: routes did NOT change (/people still serves New hires) because saved page layouts are keyed by page id and renaming would break them, and access did NOT change — both groups keep the people module id, so Settings > Module Access still has one row governing both. If Employees ever needs to be visible to someone who should not see New hires, that is a follow-up. Travel sits under Onboarding because that is its main use, though some trips are recruiting visits, so it is not a perfect fit.
- [ ] Canonical application-stage pipeline (future decision, split out Jul 20) — today Candidate.stage and CandidateApplication.stage are free-text strings with no shared list, so they drift. A fixed enum (e.g. Applied → Screened → Interviewing → Offer → Hired) would make the pipeline consistent and reportable. Net-new scope, NOT part of the shipped nav/offers reorg above — decide whether it is wanted before building.
The Offers list shipped too (Jul 16): Recruiting > Offers shows every offer with filters for out / signed / planned / declined. It leads with the thing that actually falls through the cracks — a SIGNED offer that nobody has moved into onboarding yet gets called out with a count at the top and a "move them in" button on the row, rather than blending into a list. It rides on the candidates module's access rather than adding a permission, since it is a view of candidate applications. STILL TO DO: the nav reorg to Recruiting (might hire) / Onboarding (signed, not started) / People (works here) — the test being "have they signed? have they started?". Note: "pre-onboarding" as a term is itself part of the fog and should retire into that middle section. The nav change touches every page, so it wants its own run.
- [ ] Kevin's rolling turnover doc
- [ ] Review the training end dates in the Recruiting Status Tracking tab to update pilots' Aircraft Service Date — plus add an info box that explains what the Aircraft Service Date is
- [x] Fleet > Managed Aircraft: move the N477KR card to show before N515RP and N739S — and model N477KR as Jack's dedicated aircraft AND as backup coverage flown by the SkyShare pool pilots
- [x] Org chart: fix the colors (too much red and a lot of dark blue) to match the site palette better — gold header rule, navy KPI numbers, gold eyebrows, lighter cool-blue division band
- [x] Org chart: make sure the MX crew is also accurate — counting/render logic verified consistent with the crew rules (training=filled, 2+ open alert); name-level data re-diff still needs the source cleanup CSV (not in repo)
- [x] Org chart: add the ability to edit the chart by openings and move pilots around — admin "Edit roster" mode on the crew chart (add/remove pilots, move between aircraft, open/close seats); persists to a WorkspaceSetting override (scope fleet/crew-roster) over the crew-data.ts seed, with reset-to-source
- [x] Org chart: when a pilot is moved on the chart, reflect that move on their employee-journey profile (Jul 17) — done for the crew chart: moving a linked pilot offers to record the matching role change on their employee journey (editable title/date/transition-type, skippable, never automatic). See the "Crew chart move now offers to record the matching role change" item above for detail. Still a confirmation-based offer, not full auto-sync.
- [ ] Employee recognition: add the info and start building (still waiting on final approval, but can begin now)
- [ ] Help section: training guides for every page anyone might need to access
- [ ] SOP viewer: a two-pane view beside your work, plus a whole-handbook option (added Jul 28) — the user's request: when you open an SOP from a page, show it in a SIDE panel so you can still see the item you are working on AND the SOP at the same time, instead of the SOP taking over the screen. Today the in-app Handbook (Data → Handbook) opens a chapter in a full iframe viewer, so following a procedure means flipping back and forth between the SOP and the actual screen you are meant to be doing it on — exactly the friction this removes. TWO PARTS: (1) a two-pane / side-drawer mode so the SOP docks beside the working page (the SOP is READ-ONLY reference, so a panel that does not steal the whole viewport is the right shape), and (2) an explicit option to open the WHOLE handbook (all chapters), not just the single chapter, so you can browse across SOPs from the same place. DECISIONS FOR LATER: whether the panel is global (a Help/handbook button on any page that deep-links to the relevant chapter) or lives only on specific pages; how it behaves on a phone (a drawer, not a split, at that width); and whether the pane remembers which chapter you had open (the Handbook already has a route-aware bookmark to build on). Pairs with the Help section item above. Not now — captured per the user.
- [x] Bug: with the left sidebar minimized, hovering the Recruiting tab renders the flyout BEHIND the candidate page (z-index) so you cannot see all of the dropdown items (fixed Jul 16, reported by Hannah) — the flyout already asked for z-50 and it made no difference, because position:sticky ALWAYS creates a stacking context: the sidebar shell is sticky, so the flyout's z-50 only ranked it against its own siblings inside that shell. The shell itself had no z-index, so it sat at the default level and, being before the main content in the markup, anything painted in the page won over it — on the Candidates page that was the loading skeleton. Fixed by giving the sticky shell z-30 so the whole sidebar (flyout included) sits above the page content, still below the mobile drawer and modals at z-50. Raising the flyout's own z-index, the obvious first guess, could never have worked.

## Q3 Rocks — due Oct 1 (the only priority until they ship)
HARD DEADLINE Oct 1, 2026. "Done" means all three gates, not just built: (1) built, (2) tested and bugs worked through, (3) IN USE by the team. Most are built — the remaining work is certifying and driving adoption. Finish-by dates below leave a one-week buffer (Sep 24) before Oct 1. Live tracker (recomputes against today's date): https://claude.ai/code/artifact/894084ef-3de3-451e-b771-8cb4ea7f7bc9 — plus a Monday-morning scheduled agent that reports where each rock stands against its finish-by date.
- [x] ROCK 4 GOT ITS FIRST OUTSIDE SIGN-OFF ON THE PRODUCT ITSELF (Aug 28, from the user) — he sent the Aug 28 travel work to Hannah, she tested it, and his words were that she is happy with it so far and we can consider this done. That matters because of what this rock's own line says the gate is: adoption stopped being the blocker back in August, and what has been holding it is his read that what exists does not feel good and needs a lot more. This is the first time somebody outside has looked at a round of that work and said it is good. WHAT SHE SIGNED OFF ON: item times naming Mountain (Utah) time and saying arrives-versus-departs, a Crew travel purpose, trips that have passed folding themselves away, not-needed field groups for the roles that have no indoc, a traveler tab strip, the hub linking out to real profile pages instead of rendering a profile at its own foot, and the date box no longer saving half-typed years.
- [~] ROCK 4 IS NOT CLOSED BY THAT, and the honest reasons are worth writing down rather than leaving to be rediscovered (Aug 28) — her words were happy so far, which is one sitting with one round of changes, not the rock's second gate of tested and bugs worked through. Three things are named elsewhere in this file and all three were re-checked when this was written: the rock line itself still names both ends of the reimbursement confirmation loop as missing, how accounting tells us a payment went and how we tell the employee it arrived, and neither moved. The reimbursement SOP is written and serves in the app but has still never been published as an artifact. And training travel content is still a locked slot on the trip checklist, waiting on Hannah and the CPO for what a training traveller is actually told. Finish-by is Sep 12.
- [ ] ADOPTION SCOREBOARD Aug 24 (from the check-in, supersedes the Aug 17 entry below) — 38 days to Oct 1, NOTHING OVERDUE, FOUR ROCKS DONE, and the verdict is AHEAD at 4 done against 3 due. It moved twice in one morning, which is the honest shape of the day: it opened at ON TRACK, because rock 3 Business cards passed its Aug 22 date two days earlier leaving 3 of 6 due against 3 delivered and no cushion, then went back to AHEAD when the user declared rock 5 Historical interview history done. TWO ROCKS REMAIN. Rock 4 Travel for Orientation / Indoc at Sep 12 is the only one anyone is working and the only one left with a gate that code can close; its adoption gate is already MET, his words being that she did use the travel, so what remains is modifications he could not yet name plus the two reimbursement confirmation loop ends open since Aug 3. UNNAMED WORK IS THE RISK ON IT, because unnamed work cannot be scoped, cut or finished: the action is getting Hannah's list written down, and all three outcomes close the rock, since small items get done, big items are improvements rather than gates, and an empty list means it is finished already. Rock 6 Crew chart at Sep 24 is deliberately sequenced last by his own words, probably their final rock, and stays HELD from the team by his choice, which is a decision and not neglect - but it is now the ONLY rock whose gate is pure adoption, and adoption takes weeks after a build stops, so the date the hold lifts was asked for rather than the rollout pushed
- [ ] ROCKS 4 AND 5 TRADED FINISH-BY DATES, and it cost nothing (Aug 24, from the user) — his request in the Monday check-in, made in the same breath as declaring rock 5 done. Rock 4 Travel for Orientation / Indoc moves Sep 5 to Sep 12; rock 5 Historical interview history moves Sep 12 to Sep 5. WHY IT IS FREE RATHER THAN SHUFFLING: rock 5 closed the same day, so handing it the earlier slot costs nothing at all, while rock 4 gains a real week at precisely the moment its remaining work is still unnamed. Oct 1 is untouched, rock 6 Crew chart keeps Sep 24, and the one-week buffer before Oct 1 survives. Applied the same day to the scheduled check-in's own instructions so next Monday reports against the new dates and does not reopen rock 5. Arithmetic re-checked at commit time rather than copied: Aug 24 to Oct 1 is 38 days, three finish-by dates have passed (Aug 1, Aug 15, Aug 22), four rocks are done (1, 2, 3, 5), and nothing is late
- [x] SETTLED THE SAME DAY: rock 6 is Crew chart (asked and answered Aug 24) — this file named it Pilot recruiting status tracking, in its own rock line and in the Aug 17 scoreboard, while the check-in handoff and the project memory both call it Crew chart. Both describe real work and the entries under it mention the crew chart by name, so this is one rock with two names rather than a mix-up that has already happened. It is recorded because his standing format rule is to give a rock's NAME alongside its number EVERY time so they cannot be confused, and a rock with two names defeats that rule from the inside. He picked Crew chart. Applied to the rock line and to the two Aug 24 entries above, and the project memory now matches. STILL OUTSTANDING and not fixable from here: the scheduled check-in keeps its own SKILL.md outside this tree, so next Monday still reports the old name unless that file is edited too
- [x] The live Q3 tracker is updated in place, same URL, no new link (Aug 24) — rock 5 Historical interview history closed on it, the rock 4 and rock 5 dates swapped, and the verdict now computes to AHEAD at 4 done against 3 due with 38 days left. Three alert panels whose framing the declaration invalidated were CORRECTED IN PLACE RATHER THAN DELETED, matching that page's own standing rule that a board which retires its own stale calls cannot be audited: the Aug 6 the-gate-has-not-moved panel now records that it was right on the day and has since been overtaken, and the search-latency panel keeps the finding while dropping the spent argument that it threatened rock 5's adoption. Baseline stamped 2026-08-24a so a stale tick saved in anybody's browser is discarded rather than merged, and the footer says plainly that NOTHING was re-queried, because a declaration and a date change are decisions rather than measurements - the Aug 7 live verification still stands behind rocks 1, 2, 3 and 6. Done by the check-in rather than handed off, because the tracker is a published artifact and not a file in this tree, so there was nothing for the commit agent to stage. NOT VERIFIED and stated as such by the session that did it: the rendered page was never inspected, since the Browser pane will not execute JS against a local file snapshot, so the arithmetic was checked by running the page's own formulas rather than by reading the board. The artifact's favicon may also have changed, because the previous one is not readable from the page source and the redeploy passed a rock emoji
- [x] A latent bug in the tracker's burn-down rail, found and fixed by the date swap (Aug 24) — the rail decided whether to hide a pin's date label by comparing each pin to the PREVIOUS ROCK IN THE ARRAY, which silently assumed the finish-by dates run in rock-number order. They stopped doing so the moment rocks 4 and 5 swapped, and rock 5's Sep 5 label would have rendered blank on a pin with clear space either side. Neighbour proximity is now judged by POSITION on the rail rather than by array index, with ties broken by rock number, because a rock's number is its identity and must never be reordered. Verified by running the page's own formulas: all six labels show, smallest gap 9.6 per cent against a 6 per cent threshold. A GOOD ADVERTISEMENT for changing data you think is inert - the swap was a two-date edit and it exposed an ordering assumption nobody had written down
- [ ] ADOPTION SCOREBOARD Aug 17 (from the check-in, supersedes the Aug 3 entry below) — 45 days to Oct 1, NOTHING OVERDUE, THREE ROCKS DONE, and the verdict is AHEAD. Two finish-by dates have now passed, rock 1 Pre-Onboarding on Aug 1 and rock 2 Orientation scheduling on Aug 15, and both were finished early; rock 3 Business cards was declared done Aug 6, sixteen days before its Aug 22 date. So the calendar has asked for two and three are delivered. THE THREE OPEN ROCKS HAVE THREE DIFFERENT SHAPES, which is the useful thing to record: rock 4 Travel for Orientation / Indoc is the only one being actively built and its blocker is the product rather than adoption; rock 5 Historical interview history gained real capability on Aug 6 but its gate still waits on a genuine lookup nobody can manufacture; rock 6 Pilot recruiting status tracking is deliberately held from the team. Only one of the three can be moved by working on it. Its own blocker, the search latency, is already tracked in Candidates and Matcher rather than restated here - one gap, one entry
- [x] THE MONDAY CHECK-IN'S SILENT FAILURE IS FIXED, and the fix is a permission SHAPE rather than a retry (Aug 17, applied with the user's go-ahead after the commit agent was paused) — eight shape rules now cover read-only git only: log, status, show and diff, in both the PowerShell and the Bash form. DELIBERATELY NOT A BARE GIT WILDCARD, so add, commit, push, reset, rebase and clean all still prompt, which is the right answer for a task whose only job is to read and report. The task's own instructions gained a rule naming those four verbs as the only git it may use, carrying the Aug 10 and Aug 17 history so a later session does not quietly widen it, plus the instruction that was actually missing: if a git call comes back empty, SAY SO AT THE TOP OF THE REPORT rather than carrying on without it, because a check-in built on a git log that never arrived is the exact failure being fixed. VERIFIED INDEPENDENTLY at commit time rather than taken from the handoff: the settings file parses as valid JSON with 90 allow rules, every git rule was listed with write verbs flagged, no bare git wildcard exists and neither reset, rebase nor clean is permitted, the four read-only verbs are present in both forms, and the exact command that hung this morning was re-run and returned four commits normally. Rule G is in the task file. HONEST LIMIT, and it is the whole caveat: all of that was verified from an INTERACTIVE session, which proves the rule matches and not that an unattended fire completes. The real test is Mon Aug 24 at 08:35 with nobody watching. Treat this as fixed-but-unproven until then, and remember lastRunAt will look clean either way
- [ ] BROAD GIT WRITE PERMISSIONS ALREADY EXIST IN THE ALLOWLIST AND NOBODY APPEARS TO HAVE DECIDED THEM (found Aug 17 while validating the fix, PRE-EXISTING and not introduced by it) — git add, git push and git rm are each approved by bare wildcard, so any session in this tree can stage, push or remove files without a prompt. Confirmed by listing every git rule in the file rather than probing for one. That sits badly against the working agreements, where only the commit-and-push agent touches git history and a subagent must never run git at all - a rule that exists because a previous session clobbered a sibling's uncommitted work. THE PERMISSIONS ARE THE ONE PLACE THAT RULE IS NOT ENFORCED, ONLY ASKED FOR: git add by wildcard means even git add -A, the single command the working agreements forbid most firmly, would not raise a prompt. Left alone deliberately rather than narrowed, because tightening it mid-flight could break the commit agent in the middle of a batch. It wants a deliberate decision rather than being inherited
- [ ] THE MONDAY CHECK-IN HANGS ON A PERMISSION PROMPT, and has probably never run unattended (diagnosed Aug 17, after two silent Mondays; FIXED the same day, see above) — the Aug 17 session started at 14:36:25Z, printed its opening line, issued three tool calls and got exactly ONE result back, the Get-Date at 14:36:33Z, then stopped dead with isRunning still true nearly two hours later. The call it never got back was a git log with a computed --since date. THE CAUSE IS THAT NO ALLOW RULE CAN EVER MATCH IT TWICE: every git permission for this repo is a frozen literal, and the two that are this command are frozen at --since=2026-07-21, the day they were approved. The task computes that date from the last check-in, so the string changes weekly and never matches again, which raises a prompt nobody is present to answer, while the scheduler records a successful lastRunAt for merely having LAUNCHED the process. VERIFIED INDEPENDENTLY at commit time: the scheduled task reports enabled true with lastRunAt 2026-08-17T14:36:25.473Z, and both frozen rules are still in the settings file, one spelling --pretty and one --format, so the live command misses on the date AND on the flag. Session list is the positive control - runs exist for Jul 21, Jul 27, Aug 3 and Aug 17, none at all for Aug 10, and every completed run has its activity at a time somebody was at the machine. A status report that silently does not fire is worse than none, because its silence reads as nothing to report, and this is the accountability instrument for six rocks against a hard date. FIX PROPOSED, NOT APPLIED: replace the frozen literals with shape rules for read-only git only - log, status, show, diff - and never a bare git wildcard, which would cover reset and push; separately give the task a fixed window so its command string stops varying. Not applied because it changes permissions and that file carries another session's uncommitted edits
- [ ] THE ROCK LINES ARE THE LEAST CURRENT THING IN THIS SECTION, and that is now three check-ins running (Aug 17) — the rock line is the first thing anyone reads and it keeps being the misleading part. Rock 6 Pilot recruiting status tracking still opens with ADOPTION Jul 27, UNCHANGED at ~30 per cent, three weeks stale. Rock 5 Historical interview history still opens with ADOPTION Jul 29 although the Aug 6 Jazz entry is the current state. ONE HALF OF THIS IS FIXED IN THE SAME COMMIT: the Rock 6 training-import entry that still claimed the paste was outstanding is closed, verified against the live database at 107 records. What remains is the pattern rather than the instance - SUGGESTED FIX, not applied because it rewrites lines the user reads weekly: give each rock line its CURRENT reading first and keep the older text below it as history, so the top of a rock line is never the oldest thing on it
- [x] RECORDED from the Aug 17 check-in (from the user) — rock 3 Business cards was declared done Aug 6, sixteen days early; his Aug 17 words, that 1, 2 and 3 are all finished, restated that rather than declaring it fresh. The Monday Aug 10 check-in did not run at all and he was away from the project and did not chase it. The scheduled check-in is ONLY weekly rock tracking - not a general status report, and not a place to pick up other threads. STANDING FORMAT RULE, his instruction: always give a rock's NAME alongside its number, every single time, so they cannot be mixed up. HIS OPERATING MODEL, stated the same day: the commit-and-push agent is the one that makes changes, commits, pushes and updates the roadmap; other sessions can act, but changes are SEQUENCED through that agent, and work pauses on request so the two do not collide - which is how the permission fix above was applied, with his explicit go-ahead while the commit agent was paused
- [ ] Loose ends around the check-in, none of them blocking (Aug 17) — two approvals still point at skyshare-talent.vercel.app, the pre-rename domain retired Aug 3, so they can never match again; a git -C rule still names an old OneDrive path that is not this working tree; and the Aug 17 check-in session is still marked isRunning after hanging at eight seconds, so it can be cleared. Cleanup rather than a broken instrument, and kept separate from the write-permission entry above, which is a decision rather than tidying
- [x] Indoc travel content is filled in, and it was already sitting in the mailbox (Aug 16) — the checklist had held a locked slot since Jul 31 saying we were waiting on the Chief Pilot's Office for what an indoc traveller is told. Nobody needed chasing: Devon Carter's INDOC Welcome and Upcoming Details, sent to Aimee and Hannah on Mar 26 2026, had all of it, and that email is now the source of record for indoc logistics. The slot carries the Ogden address at 3715 Airport Rd, Ogden, UT 84405, on site and ready at 0800 on the first day, the arrival route through the west side main entrance and up the stairs to the classroom behind the half walls, the requirement that all assigned online training is COMPLETED BEFORE THEY ARRIVE with completion times written down, notebook and pen provided, lunch provided daily, phones away during class, airline and hotel booking covered during indoc itself, and the contacts to hand over — cpo@skyshare.com for anything Chief Pilot's Office including days off, crewinfo@skyshare.com for ALL hotel and airline communication, skyops@skyshare.com or 801 516 9189 for flight scheduling, and the on-duty manager on 435 220 4924 for flight-related admin only. The March dates were left out on purpose since they change per session. CAVEAT, and it is why this is not fully closed: there are ZERO INDOC trips in the system, so this text has never been rendered on screen. Whole-scope positive control on the trip table, re-run at commit time and reconciling to a total of 6 — RECRUITING_VISIT 4, ORIENTATION 1, TRAINING 1, INDOC 0, INTERVIEW 0, OTHER 0
- [x] The reimbursement SOP exists, and the fillable invoice is dead (Aug 16, from the user) — his words were that we are not going in that direction and to do away with the entire Google Doc invoice part. It assumed the TRAVELLER submitted their own claim, which was never true: HR submits, and the traveller's whole job is handing over receipts. DO NOT REBUILD IT, or any traveller-facing expense form — the claim is an email from HR to payables@skyshare.com with the receipts attached, carrying traveller name, dates, purpose and the expenses. Worth recording why nothing was deleted: the invoice could not be found, and this roadmap already explains that — the Jul 22 entry near the bottom of this section records the user deleting the personal-domain copies of three rock documents, the fillable reimbursement invoice among them. So the absence is the expected consequence of a deletion already made rather than an unexplained gap. The work was therefore writing the document that was missing: docs/sops/06-travel-reimbursement.html, a sixth handbook chapter registered against /travel. It states plainly that HR submits, records the invoice as dropped so nobody rebuilds it, and marks the two genuinely open ends as unfilled rather than guessed — how accounting confirms back to us that a payment went out, and the approval and payment timeline, neither of which accounting has given us. The planned Arranging travel chapter keeps the booking half
- [ ] Training travel content is the last locked slot on the checklist (Aug 16) — the indoc half is now filled from the CPO's own welcome email, but nothing was found covering what a TRAINING traveller is told, so that slot still reads as waiting on Hannah and the CPO. Honest scale, because the handoff implied something live: there is exactly one TRAINING trip in the system and its status is COMPLETED, so the blocked item sits on a finished trip rather than in anybody's way today. It bites on the next training trip, not this one. Whole-scope positive control, reconciling to a total of 6 — RECRUITING_VISIT 4, ORIENTATION 1, TRAINING 1, INDOC 0, INTERVIEW 0, OTHER 0. BEFORE ASKING ANYONE TO WRITE THIS FROM SCRATCH, check whether a training equivalent was already sent to somebody: the indoc half turned out to be sitting in Devon Carter's Mar 26 welcome email all along, so the same may well be true here
- [ ] The new reimbursement SOP has not been published yet (Aug 16) — docs/sops/06-travel-reimbursement.html is written and registered in the handbook, so it serves in-app at Data then Handbook, but unlike the other five chapters it has no published artifact URL and the README lists it as not published. Two things also went unverified and should be closed before anyone relies on the document: the new indoc checklist text has never been rendered, because no INDOC trip exists to render it against, and the SOP itself was never opened in the in-app viewer
- [x] THE FIRST UNATTENDED SEND WORKED (Aug 3) — the orientation reminder cron ran on its own and emailed all six Aug 4 attendees, which was the last open item on rock 2 and the first time anything in this app has emailed a real person with nobody watching. Verified read-only against the live database, whole-scope rather than a single lookup, because this is the same claim that was reported wrong the week before. All four WorkspaceSetting rows in scope orientation were listed, and all UPCOMING sessions with them. The reminder-runs row holds five runs: Jul 30, Jul 31, Aug 1 and Aug 2 all outcome nothing-due with zero sessions checked, then Aug 3 at 15:21:55Z outcome sent, one session checked, six sent, zero failed, zero skipped — Gavin Craner, Luke Webb, Axel Leimer, Serene Cummings, Bryan Weber and Tara Ward, each to their own skyshare.com address. All six attendee rows on session cmrwrtm8z000064rmn5trtizi now read invite, supervisors, reminder. So the cron has been firing daily since Jul 30 and correctly did nothing on the four days nothing was due, which is the entire reason the nothing-due record exists
- [x] TWO CLAIMS IN THE MONDAY CHECK-IN WERE WRONG AND ARE CORRECTED HERE (Aug 3) — both understated how far along a rock was, and both were caught the same morning. ONE: the check-in said rock 2's only remaining gate was somebody other than the user running a session. Hannah had already done it, so the gate was already met and the rock was reportable as done. TWO: the check-in called rock 5 a true zero running three weeks. That contradicted this very file — the Historical interview history line has read OFF ZERO AT LAST since Jul 29, recording the first real lookup — and the error came from trusting a stale summary instead of reading the roadmap line that was already open on screen. Same root cause as the Aug 2 false alarm: reporting from memory rather than from the record. The status instrument is worth nothing if it does not read the record first, and that is now two consecutive Mondays it failed to
- [ ] The Sep 1 orientation must NOT be sent yet (Aug 3, from the user) — Hankyu Park and Grace Ward are on it and Hannah will work on it, but too much changes between now and then to send anything. The session is armed, which is correct and harmless, because arming only takes effect on the calendar day before and nothing can go out early. Verified: both attendees currently read sentTemplateKeys empty, so nothing has gone out. Do not treat that empty send state as an outstanding task, and do not arm anything further on it
- [x] Rock 4's two remaining reimbursement loops, named (Aug 3, from the user) — the process itself was confirmed Aug 1, HR emails payables@skyshare.com with the receipts, and there is now an email thread with accounting, but two ends of the loop are still unfine-tuned and neither is a code problem. ONE: how accounting confirms back to US that a reimbursement has actually been sent. TWO: how we then confirm with the EMPLOYEE that they received it. Until both exist a traveller can be reimbursed and nobody in HR can prove it without asking. That, alongside finishing the SOP, is what is left of the rock
- [x] Rock 5's gate is real reasons to use it, not manufactured tests (Aug 3, from the user) — Hannah searched a candidate, pulled one up, found the one she needed and got the information she needed out of it. That is a complete real-world use, not a demonstration, and it is the second confirmed lookup after the Jul 29 one. The user rates the rock fairly good and 50 per cent or more and does not yet know of anything that needs changing on it. HIS CONSTRAINT IN HIS OWN WORDS: he cannot just make up reasons to test it. So this rock moves when genuine lookups happen, and the useful action is making sure the team reaches for it when they have a real one rather than asking anybody to stage a search
- [x] Rock 6 is deliberately withheld from the team until it is ready (Aug 3, from the user) — the training sheet was added recently and the chart has the bones, but nobody else is using it yet and that is a choice, not a stall. He does not want it introduced to the team before things are ready, and it probably needs a lot more work first. Do not push a team rollout of the crew chart until he says it is ready: the adoption pressure that is right for the other rocks is the wrong instrument for this one, and treating a deliberate hold as neglect is how a status board loses its credibility
- [x] STANDING PRINCIPLE, DONE DOES NOT MEAN PERFECT (Aug 3, from the user) — his words: a project can be done and then improved later, and we need to make sure the project is usable and working rather than holding off on doing it until it is perfect. This governs how the three gates get judged from here. A rock with known future improvements is still DONE if the team can use it and it works, polish items do not hold a rock open, and a rock the user has declared complete does not get re-litigated. Applied immediately to orientation, and it is the reason rock 3 reads 90 to 95 per cent done rather than blocked
- [x] The live Q3 tracker now shows the user's own ratings rather than only computed ones (Aug 3) — updated in place at the same URL, no new link. Orientation is marked met twelve days early and its items rewritten to what actually happened. Where the user has given a figure it is now the headline number and the card says whose call it is, so the board reads 90 to 95 per cent for cards and 50 per cent or more for historical instead of arguing with him through arithmetic; the checklists underneath became the open work rather than the score. Rock 5's items carry Hannah's lookup. Rock 6 gets a new state, held by choice, coloured apart from idle so a deliberate hold never reads as neglect. The verdict line can now say AHEAD when more rocks are finished than the calendar has asked for. The Aug 2 false-alarm notice is replaced by a verified block recording that the send did land and a corrected block recording the two wrong claims, and neither self-retires. Baseline bumped so stale local ticks are discarded rather than merged. VERIFIED BY RUNNING THE PAGE'S OWN SCRIPT against a stub DOM, not by reading it: verdict Ahead 2 done 1 due, 59 days, 2 of 6, 0 overdue, and the six cards reading 100, 100, 90 to 95, 67, 50 or more, and 50. The preview pane could only load it as a static snapshot, so the checkbox and localStorage paths were NOT exercised and are unchanged from the version already in use
- [x] ROCK 3's LOOP CLOSED — the business-card queue is empty (Aug 5) — the arrive, hand-out and confirm half had never run, and Aug 4 was its test case. The user marked the cards received the next day and it went further than the cohort: verified read-only across ALL active staff, RECEIVED went 101 to 111 and the count still sitting at ORDERED is now ZERO, so the ten people who were waiting were all worked through rather than just the two at orientation. In the cohort itself Tara Ward and Grace Ward are RECEIVED and the other five were NOT_NEEDED. Every item on the tracker card now ticks from live data, so it computes to 100 per cent seventeen days before its Aug 22 date. AWAITING THE USER'S WORD to record it as declared done the way rocks 1 and 2 were — his Aug 3 rating of 90 to 95 per cent predated the hand-out running, and declaring a rock finished is his call rather than an arithmetic result. Focus moved to travel, which is next by date at Sep 5. NOTE FOR EVERYONE: the tracker is a STATIC published page, not a live view — marking something in the app does not update it, and it has to be edited
- [x] Q3 tracker re-verified against the live database and corrected (Aug 5) — the board was carrying an Aug 3 baseline and two of its facts had moved. ROCK 6's training import IS DONE and the tracker still showed it as not: the stored crew roster holds 105 training records, ALL 105 carrying the real tracking sheet's own columns, 98 of them archived history, and both the crew and maintenance rosters were edited again on Aug 5, so the user is actively working in it. It stays HELD from the team by his decision, which is not the same as stalled. ORIENTATION IS FULLY CLOSED OUT: the Aug 4 session ran and is marked COMPLETE with 7 attendees, one more than the six the reminder emailed, because Grace Ward was moved in on the morning and handled by hand. AND ONE REAL OPEN QUESTION SURFACED ON THE FOCUS ROCK: Aug 4 was meant to be the business-card test case for the arrive, hand-out and confirm half, and it has now happened — but of the 7 attendees, 5 are NOT_NEEDED and the two who needed cards, Tara Ward and Grace Ward, are BOTH STILL ORDERED rather than RECEIVED. The positive control is that 101 active staff are marked RECEIVED, so that status is one the team genuinely uses and this is not a reporting artefact. Either the cards did not arrive in time for orientation or nobody updated the two records, and which of those it is IS the rock. Baseline stamp bumped to 2026-08-05a so stale local ticks clear rather than masking the corrections
- [x] ALL OF THE AUG 6 JAZZ WORK BELONGS TO ROCK 5 (Aug 6, from the user) — his correction, and it changes how the day reads: the historical candidate lookup rock is what every piece of the Jazz archive work serves, rather than being general backlog. SHIPPED FOR IT TODAY — all five Jazz identifiers (prospect id, application id, job id, applied job title, hire status) are now findable from the ONE main search box, where four of them previously returned nothing anywhere and the fifth only worked on a different page; multi-term queries now AND properly instead of returning zero rows for anything with a space in it; and ids are prefix-matched, so a partial job id no longer drags in all 3,816 applications. ALSO AUDITED AND CLEARED — the Jazz notes imported completely, 1,357 of 1,357 feedback rows and 282 of 282 interviews, so the sparseness recruiters see is in the source rather than the import. Full detail in the Historical Candidate Archive section. THE GATE IS UNCHANGED AND IT IS WORTH BEING STRAIGHT ABOUT THAT: rock 5 moves on REAL lookups, per his own constraint that he cannot manufacture reasons to test it, and today added capability rather than a use. What it does change is the odds on the next genuine one — pasting an id straight out of Jazz used to return nothing at all. THE RISK TO WATCH IS LATENCY: searches now take 1.5 to 4 seconds, and this is the one rock whose gate is somebody CHOOSING to reach for the tool, so slowness costs more here than anywhere else in the app
- [ ] ADOPTION SCOREBOARD Aug 3 (SUPERSEDED by the Aug 17 entry at the top of this section, kept for the trend) — 59 days to Oct 1, NOTHING OVERDUE, TWO ROCKS DONE, and the verdict is AHEAD rather than on track: rock 1 finished five days early and rock 2 twelve days early, and rock 1 is still the only rock whose finish-by date has actually passed. 1 Pre-Onboarding DONE | 2 Orientation DONE | 3 Business cards 90 to 95 per cent, only a couple of fine details left at the end and they are not what is keeping it from being done, with more known this week as the cards arrive and the hand-out half finally runs | 4 Travel better but NOT done, parts genuinely in use, reimbursement still being tested, an email thread with accounting open, and probably still needs a bit of work | 5 Historical interview history HAS A COMPLETE REAL USE and the user rates it fairly good, 50 per cent or more | 6 Crew chart has the bones and probably needs a lot more work. THE PATTERN HAS CHANGED AND IT IS WORTH RECORDING: the two rocks that closed are the two the user drove personally to a real cohort, and the four still open are all waiting on real-world events rather than on code — cards physically arriving, an accounting loop being exercised, recruiters having genuine reasons to search, and a team being ready to be introduced to a tool
- [ ] ADOPTION SCOREBOARD Aug 2 (SUPERSEDED by the Aug 3 entry above, kept for the trend) — 60 days to Oct 1 and NOTHING IS OVERDUE: rock 1 is the only one whose finish-by date has passed and it is DONE, five days early, so the burn-down says ON TRACK. 1 Pre-Onboarding DONE | 2 Orientation supervisors email and internal session summary both SENT for Aug 4, leaving only the reminder, which is armed and fires Aug 3 | 3 Business cards DOWNGRADED from effectively-done: the user says it still has to be tested, and the arrive / hand-out / confirm half of the loop has never run — the Aug 4 cohort is the test case | 4 Travel: the reimbursement process was CONFIRMED Aug 1 (HR emails payables@ with receipts), so the external blocker named for weeks is cleared; what is left is finishing the SOP and certifying the manual flow | 5 Historical interview history NOTHING NEW, a true zero for a fortnight and still the cheapest rock on the board | 6 Crew chart: no adoption, but it produced its first real usage feedback Aug 1-2 — six issues from working in it, all fixed. The user asked explicitly that 4, 5 and 6 be left alone this week. PATTERN WORTH RECORDING: every rock that has moved recently is one the user drove personally, and the stalled ones need somebody ELSE to act, which is the harder problem and the reason they should not be left to September
- [x] Q3 rocks tracker rebuilt (Aug 2) — the live tracker at https://claude.ai/code/artifact/894084ef-3de3-451e-b771-8cb4ea7f7bc9 was the Jul 18 version, showing three coarse gate buttons per rock and a baseline claiming every rock was still built-only. Replaced in place, same URL, with per-rock open ITEMS: each rock scores built as given plus tested and in-use earned from its real remaining actions, so completeness moves on real events rather than one manual switch. Countdown, burn-down rail, verdict and overdue counts all compute from the live date, so the page corrects itself between check-ins. Baseline Aug 2: rock 1 100 percent, orientation 67, cards 63, travel 58, crew 50, historical 33, overall 62. Ticks persist per browser and are keyed to a baseline stamp, so a refreshed baseline clears stale ticks instead of letting one mask a corrected fact. NOT verified: the artifact could not be opened in the Browser pane, so its rendering is unconfirmed and the percentages are hand-computed from the same formula rather than read off the page
- [x] Q3 rocks tracker baseline corrected after the false alarm (Aug 2) — the tracker-rebuilt entry above cites percentages computed while the baseline still wrongly held the Aug 3 reminder as unarmed and travel's accounting step as unconfirmed. Both are fixed in the published page, so those figures are stale: orientation is 73 not 67, travel 67 not 58, overall 64 not 62; rocks 1, 3, 5 and 6 are unchanged at 100, 63, 33 and 50. The page also now carries a PERMANENT correction notice naming the false alarm rather than a dismissable alert, and it does not self-retire — a status board that deletes its own wrong calls cannot be audited. Baseline stamp bumped so any ticks saved against the wrong version are cleared rather than masking the fix. THE VERIFICATION IS THE POINT: this check was done with a positive control, listing all four WorkspaceSetting rows in scope orientation and every UPCOMING session rather than probing for one key, and the raw output was pasted. That is the habit whose absence produced the false alarm the day before. NOT verified: the artifact's rendering, since the preview pane timed out again, so the percentages are hand-computed from the same formula the page runs
- [x] FALSE ALARM CORRECTED — the Aug 3 reminder IS armed and the cron IS running (Aug 2) — the Monday check-in reported that the orientation / reminder-armed WorkspaceSetting row DOES NOT EXIST, that nothing had ever been armed, and that the Aug 3 run would therefore email nobody. That is wrong, and it was checked read-only before being written down. The row exists, was last written 2026-08-01T15:48Z, and holds BOTH upcoming sessions: the Aug 4 session (cmrwrtm8z000064rmn5trtizi, 6 attendees, UPCOMING) and the Sep 1 session (cms544132000304l5z7z244q0, 2 attendees). Two earlier sessions had already verified the same thing on Jul 30 and Aug 1, so three independent checks now agree and only the check-in disagreed. STRONGER EVIDENCE STILL, from the run log the instrumentation writes: orientation / reminder-runs shows the cron firing daily and reporting honestly — 2026-08-02T15:44Z nothing-due, 2026-08-01T15:05Z nothing-due, 2026-07-31T15:28Z nothing-due — which is correct, because the Aug 4 session's send day is Aug 3. So the cron is alive, it ran this morning, and tomorrow it should find the session due. THE LESSON IS ABOUT THE ALARM, NOT THE FEATURE: a false negative on a safety check is expensive, because it sends somebody to fix a thing that is not broken and teaches them to distrust the instrument. Anything claiming a scheduled send will not happen should be verified against the live row before it is written down
- [ ] ADOPTION SCOREBOARD Jul 28 (SUPERSEDED by the Aug 2 entry above, kept for the history) — 65 days to Oct 1 and NOTHING IS OVERDUE: zero rocks are past their finish-by date, and rock 1 finished four days early, so the burn-down says ON TRACK. 1 Pre-Onboarding DONE | 2 Orientation supervisors email and session summary both SENT for Aug 4; only the reminder is left, and it is ARMED since Jul 29 and re-confirmed against the live database Jul 30, so it fires Aug 3 on its own (see the rock) | 3 Business cards downgraded from "effectively done" — the user says it still has to be tested, and the arrival-and-hand-out half of the loop has never run | 4 Travel NOTHING NEW, accounting still unconfirmed | 5 Historical interview history NOTHING NEW — now a true zero for 8 straight days and still the cheapest rock on the board, one recruiter doing one real lookup | 6 Crew chart NOTHING NEW, still no real usage since per-card editing removed the stated excuse on Jul 22. The user explicitly said to leave 4, 5 and 6 as they are this week. So the pattern to watch is that every rock which has moved recently is one the user drove personally, and the three stalled ones all need somebody ELSE to do something — which is the harder problem and the reason they should not be left to September. ADOPTION SCOREBOARD Jul 27 (superseded) — 1 Pre-Onboarding IN USE, spreadsheet abandoned | 2 Orientation OFF ZERO SAME DAY — supervisor data imported, then the Aug 4 session run for real from the app: calendar event created, 15 guests added, 6 invitations sent, 4 already accepted | 3 Business cards IN USE, real order placed and printer preview received | 4 Travel MOVED OFF ZERO, trips booked in the app, accounting still unconfirmed | 5 Historical interview history NO CHANGE, still nobody confirmed a real lookup | 6 Crew chart NO ADOPTION CHANGE, last week's edits were build work not usage. So two rocks are effectively at the line and three have not moved. The two that have not moved with the nearest dates are Orientation (Aug 15) and Historical interview history (Sep 12); rock 6 is the one nobody has touched at all as a user.
- [x] ORIENTATION SUPERVISOR GAP CLOSED (Jul 27) — was the single blocker on the Aug 4 session: all 7 attendees had NO supervisor, and 452 of 453 hire records had neither a linked supervisor nor a typed supervisor email, so the Front template could not address a single send. HR supplied a 152-person roster with primary and dotted-line supervisors and it is now imported: 152 employees updated, primary supervisors LINKED to the supervisor's own record (so the address is read live and cannot go stale) plus 103 secondary supervisors. ALL SEVEN Aug 4 attendees now resolve to a real linked supervisor — Axel Leimer, Gavin Craner, Bryan Weber, Grace Ward, Luke Webb, Serene Cummings and Tara Ward. Tara was the last one and is not in the roster at all (she starts Aug 3, after it was cut); the user confirmed Harry Mitchel. Whole-database no-supervisor count fell from 452 of 453 to 299 of 453, and among CURRENT employees only 35 remain, all of them people the roster omits. NOTE the three cabin attendants stay pointed at Harry Mitchel for now rather than at Tara: the user wants them moved under her later, once she is fully ready to supervise. The orientation send is no longer blocked on data.
- [x] Orientation calendar event — IN REAL USE (Jul 27). The user created the Aug 4 event FROM THE APP, added all 6 attendees and all 6 supervisors as guests, and the invitations went out. Verified against Google, not taken on trust: event d8t6kf27926n1gsfb4iee752is on aschaedig@skyshare.com carries 15 guests — 6 new hires, 6 supervisors (Jonathan Schaedig, Rich Paden, Harry Mitchel, Kevin Sherman, Chantil Hughes, Jared Esselman) and 3 internal (Hannah Byers, Morgan Langholf, Ricky Lee) — with the right title, description, street address, Meet link and the Orientation colour, and FOUR OF THE SIX NEW HIRES HAVE ALREADY ACCEPTED (Axel Leimer, Luke Webb, Serene Cummings, Tara Ward; Bryan Weber and Gavin Craner still pending). The event Claude created out-of-band earlier was deleted by the user and replaced with an app-created one, so the app owns the record and there is no duplicate. Original context below. — the invitation template TELLS the new hire a calendar invite went to their company email, and until Jul 27 nothing created one. Surfaced by the user mid-send on the Aug 4 run: he got as far as the invitations and realised the event did not exist. STOPGAP DONE (Jul 27): the Aug 4 event now exists on his own Google Calendar (Tue Aug 4, 9:30am-3:00pm pinned to America/Denver, 180 2400 W Salt Lake City) created deliberately EMPTY, no guests, notifications off, so he can eyeball it before a single invitation reaches a new hire. Adding the seven guests is a separate explicit step because it sends real invites. THE FINDING THAT MATTERS FOR THE BUILD: the app's own Google Calendar integration has NEVER invited anybody. attendeeEmails is declared in GoogleEventInput and mapped in toEventResource, but neither interview-sync nor booking-sync ever sets it, so every event the app has created in its life has zero attendees; sendUpdates is never passed either, and the Google API will not email invitations without it; and the integration authenticates as a SERVICE ACCOUNT, which cannot invite attendees at all without domain-wide delegation. So wiring orientation to the existing path as-is would produce an event on a shared team calendar that lands on nobody's personal calendar — not the thing anyone wants. None of it is testable locally either: the three GOOGLE_SERVICE_ACCOUNT / GOOGLE_SHARED_CALENDAR_ID vars are in neither .env nor .env.local, so this can only be exercised in production. DECIDE FIRST, then build: either grant the service account domain-wide delegation and pass sendUpdates, or drive it from a real user's Google account (the personal-account route, which is where an orientation invite normally originates anyway and has none of these limits). The user picked "wire it into the app" as the durable answer, but it will NOT be ready for Aug 4 and should not be on that critical path. ALSO NOTE a data nit worth fixing while in here: the session record says location "SkyShare HQ, Salt Lake City" while the Front template body carries the real street address 180 2400 W — the calendar event should use the street address so it maps. IN-APP BUTTONS SHIPPED (Jul 27), everything except the Google leg: a "Google Calendar invite" panel now sits on the session page ABOVE the email panel, deliberately, because the invitation email claims a calendar invite already went out. Two separate buttons, never one — Create calendar invite (silent, sendUpdates none, NO guests) and Add N attendees to the invite (the irreversible one, sendUpdates all, named-confirm listing exactly who gets emailed). The panel previews the real title, date, address, guest count and the rendered description before anything is created. New lib/orientation/calendar-event.ts is PURE (no Prisma, no Google, no env) and builds title, description and location from one session in one pass, which is how the user's propagate-a-change rule stops depending on human memory: the date lives in the title AND the description AND the location field, so all three move together. It also implements flag-anything-off-normal — a session that is not 9:30-3:00 at 180 2400 W is allowed but produces a warning saying "this is different than normal, check the invitation email matches". Title format is the user's own, including the ordinal (Tuesday, August 4th) that Intl does not produce; the day is always resolved in Mountain so a late-evening UTC instant cannot roll the date. Storage is a WorkspaceSetting map, not a new column, following the orientation-sends precedent — no migration against the shared live database. lib/google/calendar.ts gained an invite path (createInviteEvent, addInviteAttendees, getInviteEvent) that acts as a real user rather than the service account (originally by impersonation, replaced the same day by per-user OAuth — see below), passes sendUpdates explicitly rather than defaulting it, creates a Meet link with a deterministic requestId so a retry cannot mint a second room, and MERGES attendees on add — events.patch replaces the array, so patching in only the new people would have silently uninvited everyone already there. VERIFIED: typecheck and lint clean; the builder run against the real Aug 4 session produces exactly "New Hire Orientation in SLC - Tuesday, August 4th", 9:30-3:00 MT, colour 5, all seven attendees resolving to company addresses; ordinal edge cases 11th/12th/13th/21st correct; the live GET endpoint returns the full preview. SWITCHED TO PER-USER OAUTH (Jul 27) — the domain-wide-delegation plan is DEAD and no Workspace admin is needed. The user pointed out he already signs into the app with Google, which was the right instinct: the app can ask for calendar scope at that same consent screen and act as the signed-in person, so the service-account and GOOGLE_IMPERSONATE_SUBJECT path was deleted rather than configured. Proved from the live database first — all 5 google accounts had scope = userinfo.email/profile/openid ONLY and refresh_token = null on every one, so the existing OAuth genuinely could not have worked as it stood. Two changes made it real: auth.ts now requests calendar.events with access_type offline and prompt consent (both are required, or Google returns a one-hour access token and no way to renew it, which is exactly why every stored refresh_token was null), and a new events.signIn handler writes the tokens back on EVERY sign-in. That handler is the non-obvious part: PrismaAdapter only persists tokens via linkAccount, which fires the FIRST time an account is linked, so every pre-existing user would have kept their day-one scopeless token forever and the button would never have worked for them. refresh_token is only overwritten when Google actually sends one, since it omits it on repeat consents. New lib/google/user-calendar.ts loads the user's token, refreshes it a minute early, writes the fresh one back, and returns a plain nullable-client shape rather than a discriminated union — this project compiles with strict false, and without strictNullChecks TypeScript will NOT narrow a boolean discriminant, which cost three compile errors before the shape changed. The invite helpers in lib/google/calendar.ts now take a client instead of building one, keeping that module free of any opinion about which identity is calling. COST OF THIS CHOICE, stated because it shows in the product: the event belongs to WHOEVER CLICKS, so two people using the button means two organizers. ORIENTATION_CALENDAR_ID can force one shared target, but everyone using it then needs edit rights on that calendar. TWO THINGS LEFT, neither of them code: (1) everyone signs out and back in ONCE to hit the new consent screen — until they do, the panel tells them exactly that; (2) if the Workspace restricts sensitive scopes via app access control, an admin may still have to allow calendar.events, which cannot be checked from here. CANNOT BE TESTED LOCALLY AT ALL: local dev bypasses auth, so there is no signed-in Google identity and the preview correctly reports "nobody is signed in" — this only works on the real site. Verified as far as is possible: typecheck and lint clean, and the live preview endpoint returns the right title, all 7 attendees resolving to company addresses, and the honest blocker. The Aug 4 event itself was created out-of-band through Claude's own Google connector and already has the final title, description, address, Meet link and Orientation colour — so Aug 4 is not waiting on any of this. GUEST PICKER SHIPPED (Jul 27) — the user confirmed the OAuth flow works on the live site, then asked for supervisors and others to be addable. An "Add other guests" section now appears once the event exists. Supervisors get their own one-click row because they are the common case and the app already knows them: it reuses resolveSupervisors from the EMAIL path rather than re-deriving it, so "who is X's supervisor" can never mean one thing on the invite and another on the email, and a linked record still beats a typed address. They are deduped across the session and show how many attendees each covers — on the real Aug 4 session that resolves to 6 supervisors across the 7 attendees, with Jonathan Schaedig covering 4 and Rich Paden 3, plus a Select all. Everyone else comes from a search over the SAME endpoint the supervisor picker uses (new-hires/supervisor-search: active, not terminated, tenured people included) so "who counts as an employee" has one answer; already-invited people are shown greyed with "already invited" rather than hidden, and a free-typed email address is accepted too, because not everyone who belongs at orientation is on the roster and refusing them would just push the work back into Google by hand. Adding is the same irreversible send as the attendee button — named confirm listing exactly who, then sendUpdates all. Verified: typecheck and lint clean, the preview endpoint returns the 6 deduped supervisors with coverage counts, and the employee search returns real people. GAP CLOSED Jul 28 — the user asked whether maintenance supervisors would get four separate emails, which surfaced that grouping only happens on the BULK path: clicking Send on each attendee's Supervisors cell one at a time still sends per attendee, so Rich Paden would have collected three. The single-send dialog now detects that case and names it — "Rich Paden also supervises Bryan Weber and Luke Webb on this session" — and points at the bulk bar, without removing the ability to send just one when that is genuinely what you want. Confirmed what the grouped email actually says by building it against the live Front template: Rich gets ONE email opening "Hi Rich, Your new hires Bryan Weber, Gavin Craner and Luke Webb are attending New Hire Orientation", to him alone, nobody cc'd. NOT verifiable locally, same reason as the rest of the panel: no signed-in Google identity, so the picker correctly does not render at all.
- [x] Orientation email: send to everyone at once (Jul 27) — the invitation went out one dialog at a time, which for the Aug 4 session meant seven dialogs and is how a person gets missed. The grid now has a checkbox per attendee plus a select-all in the header; picking anyone reveals a bulk bar with the three templates, and the batch dialog previews the WHOLE run before a single email moves: every recipient row, everyone who will be SKIPPED and why, and one representative body (reading the same template seven times proves nothing). A batch is a LOOP, not one email with seven recipients — each is built and sent individually, because the templates open "Hi <first name>," and the supervisors template goes to a different person entirely, so one combined message would be wrong for everybody on it. Sends are sequential rather than parallel: Front is rate-limited and a burst that trips a 429 halfway would leave the batch in a state nobody can reason about. It reuses the single-send path wholesale, so the tick and the send record behave identically either way, and it continues past failures rather than aborting, reporting sent-vs-failed per person at the end. Anyone who already had that template is skipped by default with an opt-in tickbox to send again, so a second run over the same session cannot quietly email everyone twice.
- [x] Stop cc'ing the internal list on every single orientation email (Jul 27) — found by the user on the FIRST real send, which is exactly when it would show up. Measured from the send records rather than guessed: 6 invitations went out and the six-person standing list received SIX COPIES EACH, plus Jonathan Schaedig 4 and Rich Paden 3 as supervisors of multiple hires — roughly forty redundant internal emails for a six-person cohort. The cause is structural, not a bug: one personalised email per new hire means anyone on a fixed cc list gets one per hire. The user considered a single BCC'd email and chose better — that would have killed the "Hi <first name>," greeting and copied every supervisor on the whole cohort. Instead the standing list is now cc'd on NOTHING (not the invitation, not the reminder, not the supervisors email) and gets ONE summary per session naming everyone attending, with role and supervisor per person, and an explicit flag against anyone whose invitation has not gone out yet. That summary is genuinely more useful than the six duplicates it replaces: the invitation tells a new hire where to go, it never said in one place who is coming. Its body is written in the app rather than fetched from Front, deliberately and with the reason recorded — there is no Front template because the team never sent this email — and the note says to switch it to fetchTemplate if HR wants to own the wording. Recorded per session with the attendee count at send time, so adding someone afterwards marks the summary stale on screen instead of letting it look current. The cc editor was relabelled from "Cc'd on every orientation email" to "Who gets the internal summary", because leaving that wording would have described behaviour that no longer exists. RESOLVED SAME DAY, second round: the supervisor cc came off the attendee emails too. It was flagged as still-duplicating (Jonathan 4 copies, Rich 3) and the user's call was to remove it, reasoning that the supervisor already gets the supervisors email. Extended to the REMINDER as well as the invitation, since leaving it there would reproduce the identical complaint a week later — flagged to the user rather than done silently. NOBODY is now cc'd on any orientation email: each goes to exactly one person. Verified against the live Front templates for all 6 attendees across both attendee templates — every send resolves to a single recipient with an empty cc, and Jonathan and Rich appear zero times. THE RULE WORTH REMEMBERING, now written into the code: on a per-hire send, any recipient who is not the hire gets multiplied by the cohort size. Put them on a digest, not a cc. Verified against the real session: builds for all 6 attendees with correct roles and supervisors, no warnings.
- [x] Orientation communication history (Jul 27) — the user asked whether he would be able to see what was sent, when, to whom and who was CC'd. Answer was "partly, and one of those not at all", so it is now all four. FIRST, THE EXPECTATION: sends go out through FRONT from hrotasks@ (an HR automation mailbox, so automated mail stays out of the human hr@ inbox) with archive false, so each one sits as a conversation in the Front inbox the team watches. NOTHING lands in the sender's Gmail Sent folder, because Gmail is not the sending identity — worth knowing before anyone goes looking there. CC WAS NEVER RECORDED: it was computed at send time from the standing list plus the hire's supervisors and then thrown away, so "was Kevin copied on Axel's invitation?" was permanently unanswerable. Now stored, along with the subject as actually sent. Records written before today have no cc, and the history says "not recorded" for those rather than showing a blank that would read as "nobody". New collapsible history table on the session: when, who it is about, which email, to, cc, who sent it, and a link through to the Front conversation. Only APP sends appear — a hand-ticked box is somebody's recollection, not a record, and listing the two together would make the weaker one look like evidence. FRONT LINK FORMAT, corrected Jul 28: originally recorded as unverified guesswork, which was too pessimistic — lib/travel/from-email.ts and lib/events/front-event-scan.ts already build the same /open/<id> URL for inbound travel and event mail, so it is the established shape in this codebase, not a guess. Still worth one real click to confirm, and the raw conversation id is shown alongside so the record stays usable either way. ALSO FIXED: the claim that frontConversationUrl was the single place to change it was FALSE when written — the helper sat in a module that pulls in Prisma, so the client-side history table could not import it and hardcoded the URL inline instead, leaving two copies and a dead function. Moved to lib/front/links.ts, which has no imports at all and is therefore client-safe, and both sides now use it. The older travel and events call sites still inline their own copy; point them at the helper if you are touching them anyway. BOTH NOW VERIFIED BY THE USER (Jul 28): he clicked a real Front link and it worked, and the populated table rendered with live data — the two things that could only be closed by a real send, closed. Also better than reported: every send record on the Aug 4 session carries a cc, all ten of them, so the earlier warning that the first invitations would read "not recorded" forever was wrong; that history is complete.
- [x] Calendar invite survives Google rate limiting (Jul 27) — a real user hit "Rate Limit Exceeded" adding 8 guests, with no indication of what went wrong or whether anything had changed. DIAGNOSIS FIRST, because the instinct is to blame the guest count and that is wrong: adding N guests is always TWO calls, one events.get and one events.patch carrying the whole merged list, so 8 guests and 20 guests are identical work and a bigger cohort will NOT make this worse. What actually triggered it was the RATE of writes to one event — create, add attendees, add guests in quick succession, with the panel re-reading after each — and the fact that the Google path had NO retry at all while the Front client has had proper 429 backoff since it was written. Google returns 403 rateLimitExceeded as a TRANSIENT signal it expects callers to retry; we were treating it as fatal. Added withGoogleRetry around every invite call: exponential backoff 1/2/4/8s plus jitter (so two people clicking at once do not retry in lockstep and throttle each other again), retrying 429, 5xx, and 403 ONLY when Google names a rate-limit reason — a permission 403 must not be retried or it loops against a real access problem. Errors now carry Google's actual reason and HTTP code instead of a bare string, and the thrown error keeps its status so the "deleted in Google" 404 branch still works. The panel also reloads after a FAILED action, not just a successful one: a rate-limited add previously left the old guest count on screen, which reads as "it half worked" when nothing changed. Verified all four paths: transient throttle recovers silently, persistent throttle gives an actionable message, a permission 403 stops after one attempt, and a 404 preserves its code.
- [x] Orientation supervisors email: one per SUPERVISOR, not one per attendee (Jul 27) — caught while answering the user's question about bulk send, before anyone was emailed. The supervisors template is keyed off the NEW HIRE, so selecting all 7 Aug 4 attendees would have sent 7 emails in which Jonathan Schaedig got 4 near-identical copies and Rich Paden 3. A bulk supervisor send now groups by individual supervisor: 7 sends become 6, each to a different person, and each names the hires they cover ("Your new hires Axel Leimer, Gavin Craner, Bryan Weber and Luke Webb are attending New Hire Orientation"). The Front template body is still fetched untouched — what is added is a greeting and that one line, because the template cannot know the names and a supervisor reading "your new hire" with no name has to go and ask. Its own dialog rather than a branch in the attendee one, since a row here is a SUPERVISOR covering several hires and rendering that through an attendee-shaped table would misrepresent what a row is. Ticks the supervisors template for every attendee the email covered, since the fact recorded is "this hire's supervisor was told" and one email can satisfy that for four of them. Verified against the real roster: 6 groups, Jonathan 1 email covering 4, Rich 1 covering 3, Harry 1 covering 2.
- [x] Orientation reminder: scheduled one business day before (Jul 27) — the user schedules it a business day before and asked whether the app could. It can now, ARMED PER SESSION at his choice rather than globally. New Vercel cron at 15:00 UTC weekdays (9am MT) sends "3. Reminder" to every attendee of an armed session who has not had it. THIS IS THE FIRST CRON IN THE APP THAT EMAILS A REAL PERSON — every other one scans a mailbox and ticks internal state, which is recoverable. Two things keep it safe and neither should be removed: it only touches sessions somebody deliberately armed, so the default for every new session is no automatic mail; and it is idempotent, so a double run, a retry or a manual send earlier the same day cannot email anybody twice. Fails closed on a missing CRON_SECRET (verified: 503) rather than treating absent config as open, matching the paycom-scan precedent and NOT the older calendar-sync one. CHANGED Jul 28 TO THE PLAIN DAY BEFORE: it originally skipped back to the previous business day, and the user overruled that — the email says "REMINDER: orientation is tomorrow", and sent on a Friday for a Monday orientation that sentence is simply false. He would rather a reminder land on a Sunday than be wrong. So all weekday logic is gone, which also disposes of the public-holiday caveat, since nothing is skipped any more. THE CRON SCHEDULE HAD TO CHANGE WITH IT and this is load-bearing: it ran weekdays only (0 15 * * 1-5), so a Monday orientation needing a Sunday reminder would never have fired at all. Now daily. Verified: Tue Aug 4 to Mon Aug 3, Mon Aug 3 to SUNDAY Aug 2, Sun Aug 9 to SATURDAY Aug 8, the Jan 1 year boundary, the spring DST boundary, and a 03:00Z instant still reading as the previous Mountain day. Armed state is a WorkspaceSetting, no migration.
- [x] Orientation email: show what was actually sent (Jul 27) — the user asked what the tick meant and whether sending from the app would mark it, which was the right question: the grid rendered "the app sent this and Front confirmed" and "somebody ticked a box" as the SAME green tick, so it could not answer either. The app has recorded when, to whom and by whom for every send since the send path shipped and was showing none of it. Three states now instead of two: a SOLID tick plus the send date for an app send (hover gives the full timestamp, recipient and sender), an OUTLINED tick reading "by hand" when the key is ticked but no send record exists, and an empty circle for not sent. A legend under the grid says which is which, because otherwise the outlined tick just looks like a rendering glitch. The hand tick deliberately stays toggleable — an email sent from Front directly still needs recording — this only stops the two facts looking identical. The optimistic local update after a send now writes a provisional record too, not just the tick, or the mark would flash as "ticked by hand" for the moment before the refresh lands, which is the opposite of what just happened. Also confirmed for the user: sending from the app ticks the box automatically, server-side, only after Front confirms, so there is never a need to tick by hand for an app send.
- [x] Orientation dates read like a person wrote them (Jul 27) — the user asked for "4th" not "4". The calendar invite already did ordinals but the EMAIL did not, so the same session read "August 4th" on the invite and "August 4" in the email — precisely the drift the propagate-a-change rule exists to stop. Extracted lib/dates/ordinal.ts and pointed BOTH at it, so there is now one implementation rather than two that can diverge. Handles the 11th/12th/13th exceptions that a naive n%10 gets wrong (11st, 12nd, 13rd) and resolves the day in Mountain, verified with a 05:30Z instant that must read as the 22nd and not the 23rd.
- [x] Employee roster data-quality fixes surfaced by the supervisor import (Jul 27) — two independent audit passes over the HR roster, both confirmed with the user and now applied. FIXED: Nicholas Lembo's primary supervisor was set to Jonathan Schaedig (the roster had ksherman@ against Schaedig's name; the other 11 rows with that label use jonathan@) with Kevin Sherman kept as his dotted line; Hannah Leslie's primary was set to Chantil Hughes (the roster had jesselman@ against Hughes's name) with Jared Esselman kept as her dotted line — in both cases the dotted-line address had been pasted into the primary column. Britt Harmon and Benjamin Heinze had their Line Service Manager and Technician titles SWAPPED in the app; the roster came from a Paycom report and the user confirmed the swap, so Harmon is now Line Service Manager and Heinze Line Service Technician. Devon Carter was listed as his own dotted-line supervisor (all 69 pilot rows carry the same boilerplate Ricks/Carter pair and his row was not exempted) and stays blank. CHECKED AND CORRECT ALREADY, NO CHANGE NEEDED: Dayne Shafer, whose roster work-email is a personal gmail but whose app record already holds dshafer@skyshare.com with the gmail in personalEmail. CONFIRMED CORRECT BY THE USER, NOT ERRORS: the four addresses whose initials do not match their owner's legal name (btyler, tharvey, rstephani, bmilne); the five Base Support staff dotted-lined to the Director of HR, which is genuinely how that team is split (primary to the DoM, dotted to the DoHR); and the three pilots showing CJ2 in the app against PC-12 in the roster, who are mid-transition onto the CJ2. DELIBERATELY NOT IMPORTED: names, emails, departments and positions — only supervisors were written, because the roster's name column would have overwritten 26 preferred display names with legal ones and its department column would have erased the Managed Client qualifier from 11 people. ORG TREE NOW CLOSED (Jul 27, round 2): the user filled in the 19 leaders and 9 contractors the Paycom report omitted, and those are imported too — 26 more records, matched by email where there was one and by name for the contractors who have none, with zero conflicts. One extra fix was needed to actually close it: Jonathan Schaedig's supervisor had been stored as plain TEXT (Tommy Aoki) with no link since before this work, so the entire 29-person maintenance branch terminated at him instead of reaching the top; it is now a real link to the same person. RESULT: 180 primary and 116 secondary supervisor links, zero reporting cycles, zero self-supervision, and 181 of 189 current employees trace all the way up to Tommy Aoki. The 8 that do not are Cory Bengtzen (Founder and CEO) and Tommy Aoki (President), who are the top, plus 7 contract pilots the user left off the list (Adam Evans, Callan Cooper, Chris Root, Mark Ompad, Pete Zaccagnino, Raymond Proud, Tommy Ishii) — none of whom has a work email on file either. OPEN QUESTION for the user: the chart currently has TWO roots, since Tommy Aoki (President) has no supervisor and neither does Cory Bengtzen (Founder and CEO) — if the President reports to the CEO, that one link makes it a single tree.
- [ ] THE ACTUAL Q3 WORK IS ADOPTION, NOT BUILDING (added Jul 22) — ITS TWO-ZEROES READING IS SUPERSEDED (Aug 3): this names Orientation and Travel as the two zeroes, and Orientation is now DONE twelve days early while Travel has parts genuinely in use. See the Aug 3 scoreboard above for current status. The thesis itself still holds and is why it stays. Original text follows. All six rocks are BUILT. Every one of them now fails only gate 3, in use by the team, so this is an adoption quarter and planning it as a build quarter is how Oct 1 gets missed. The two zeroes are the whole risk: Orientation is 0% with the nearest deadline (Aug 15) and Travel is 0% (Sep 5) — nobody has switched to either. Adoption does not move on its own and it does not respond to a late sprint, so the next real push belongs here rather than on any of the ~25 good ideas captured in the Backlog. Concretely: pick a named first user per zero rock and sit with one real run (Hannah for pre-onboarding, an actual orientation session, one real trip), because the friction that stops adoption only shows up in a live run — every blocker fixed so far was found that way, not by review. Note that one rock's adoption gate had a KNOWN cause sitting in the Backlog: per-card org chart editing (the team clicks the card they want to change; the chart only offered a whole-chart edit mode), which gates Pilot recruiting status tracking at ~30%. THAT ONE SHIPPED Jul 22 on both charts — so the excuse is gone and the question is now simply whether the team uses it; its click path could not be verified by anyone but a person, so a real run is the test. Treat feature requests as a deferral of this until the rocks are in use.
- [x] Pre-Onboarding tracking — finish by Aug 1. DONE Jul 27, ahead of its date: the user confirmed Hannah is NO LONGER USING THE SPREADSHEET and the team is fully running pre-onboarding in the app. All three gates met — built, worked through in real daily use, and the old process abandoned. One honest caveat: the Sheet is abandoned but not yet archived; the user is keeping it readable for about another week to be sure nothing is still needed from it, then archiving it. That is a formality, not an open gate, but the rock is only irreversible once it is gone. ADOPTION Jul 20 was ~80%. Built (mature); Jul 18 removed the top adoption-blocking friction (orphan-hire buttons admin-gated + demoted, silent dedup now surfaced, start-date foot-gun closed with a dashboard bucket, candidate-side "Link to a job"). Left: end-to-end test pass + confirm the team is using it daily. Step-by-step runbook: the Google Doc was TRASHED by the user on Jul 22 and its link is dead — do not restore it. The canonical step-by-step is now the Pre-Onboarding SOP at docs/sops/01-pre-onboarding.html (repo = source of truth), published to https://claude.ai/code/artifact/248592bd-8a62-4087-be52-5ea13eb50053. That is the document Hannah works from when the Sheet is retired.
- [x] Orientation scheduling and tracking — finish by Aug 15. DONE Aug 3, TWELVE DAYS EARLY, declared by the user. Three things closed it. FIRST, the automated reminder sent itself to all six Aug 4 attendees with nobody watching and nothing to fix afterwards — verified read-only, whole-scope: reminder-runs holds five runs, nothing-due on Jul 30, Jul 31, Aug 1 and Aug 2, then Aug 3 at 15:21:55Z outcome sent, one session checked, six sent, zero failed, zero skipped, to Gavin Craner, Luke Webb, Axel Leimer, Serene Cummings, Bryan Weber and Tara Ward at their own skyshare.com addresses, and all six attendee rows now read invite, supervisors, reminder. SECOND, everything for orientation now happens inside the app: invitations, supervisors email, internal session summary, calendar event with all fifteen guests, and the scheduled reminder have all run for a real cohort. THIRD, HANNAH HAS ALREADY RUN IT — the gate reported that morning as still open, somebody other than the user driving a session, had already been met before it was written down. The user ran it himself as well and says he is confident the system works. Improvements can follow and do not reopen the rock. EARLIER HISTORY BELOW. ADOPTION Jul 28: the supervisors email AND the internal session summary have both gone out for Aug 4, confirmed by the user — so two of the three items left open on Jul 27 are closed and only the reminder remains. THE REMINDER WAS AT RISK AND IS NOW ARMED — this paragraph used to say it would silently send to nobody, which was true when written on Jul 28 and stopped being true a few hours later. RE-CONFIRMED READ-ONLY AGAINST THE LIVE DATABASE Jul 30: the WorkspaceSetting row orientation/reminder-armed exists and holds exactly {cmrwrtm8z000064rmn5trtizi: true}, written 2026-07-29T01:14Z, so the Aug 4 session is armed and the Aug 3 cron will find it. The Vercel cron is registered daily at 15:00 UTC. Nothing is outstanding here; the remaining risk is simply that this is the FIRST unattended email the app has ever sent, so Aug 3 is worth watching rather than assuming. THAT WATCHING IS NOW INSTRUMENTED (Jul 31): every run records itself including a quiet one, so a cron that never fires is visible as a missing record; a health check reports in plain English whether it will go out and whether it did; and a dry run shows the exact emails for Aug 3 without sending them. None of it has been exercised against a real send, so it makes Aug 3 checkable rather than certain. Session now carries 5 attendees, not the 6 quoted below — Gavin Craner moved to the Sep 1 session, which is also why the already-sent internal summary reads stale. ADOPTION Jul 27 (LATE): NO LONGER ZERO — the rock moved from 0% to a real forward run in a single evening. The Aug 4 session was driven end to end from the app by the user: calendar event created from the session, 6 attendees plus 6 supervisors plus 3 internal watchers added as guests (15 total), and the invitation sent to all 6 new hires. Confirmed against Google and the app's own send records rather than reported: every attendee has an invite recorded, and 4 of 6 have already accepted the calendar invite. COMPLETED Jul 28: the supervisors email went to every supervisor (one each, grouped) and the session summary went to all six internal watchers. The reminder is now ARMED and verified armed against the live setting — it fires Monday August 3, the day before, which will be the first unattended send this app has ever made. Gavin Craner, who came off the session after his invitation went out, has also been removed from the calendar invite. AUG 4 IS THEREFORE DRIVEN END TO END FROM THE APP: event created, guests added, invitations sent, supervisors emailed one each, internal summary sent, reminder scheduled. THREE FLAGGED DECISIONS ALL CLOSED as accept-as-is, so nobody reopens them: supervisors deliberately get NO day-before reminder (they have the supervisors email and the calendar invite); the internal summary wording stays the app's rather than moving to a Front template; and the Aug 4 session keeps no stored address, falling back to the constant with its warning showing. NOTE Gavin Craner was removed from the session after the invitations went out — 6 attendees became 5 — which correctly leaves the already-sent summary marked stale on screen, since it went covering 6. ADOPTION Jul 27 (EARLIER THAT DAY) read STILL 0%, and the single highest-risk rock on the board by a distance. The user is taking it on this week and named the blocker himself: supervisors and their emails have to be updated first. Measured the same day and it is worse than it sounds — every one of the 7 real Aug 4 attendees has no supervisor recorded, and 452 of 453 hire records have none, so the send path built Jul 22-24 cannot address a single email. See the ORIENTATION IS BLOCKED item above for the names. Fill in those 7 and the Aug 4 run is unblocked. ADOPTION Jul 20 was 0% — nobody has switched. Built (mature). Left: test pass + team adoption. LIVE RUN STARTED Jul 22 on the real Aug 4 session (9:30am-3:00pm MT) — the first FORWARD use of the module; the 3 sessions already in the database were all logged after the fact, which is exactly what 0% meant. Two gaps surfaced in the first ten minutes and were fixed before the run rather than worked around, because the user was explicit that a brand-new process should be changed to fit the work: (1) A SESSION HAD NO END TIME AT ALL — the schema and the form held one instant, so "9:30 to 3:00" could not be recorded. Added a nullable endsAt (one additive ALTER TABLE, previewed first, existing sessions unaffected), wired through create, reschedule, the card and the detail header, with both API routes refusing an end that is not after the start, and defaulted the form to the real standard 9:30-3:00 so the common case is zero typing. (2) THE ATTENDEE PICKER COULD NOT SHOW THE ONE FACT THE DECISION TURNS ON — it suggested every active hire who had not attended, with NO start-date filter, and the dropdown showed only name and role, so choosing who actually comes meant working from memory. Suggested now means active + not attended + starts on or before the session day, options read "Name - Role - starts Aug 3", and the list is grouped Suggested / Starts after this session / Everyone else with NOBODY hidden — the user's rule is suggest the obvious ones but keep full freedom to add, hold, remove and move, so nothing is ever blocked, only sorted. Verified read-only against the live roster before creating anything: the new logic returns EXACTLY the 7 attendees the user confirmed by name, puts Hankyu Park (starts 8/24) and Auggie Quintero (10/1) under "starts after", and leaves Brandon Edwards un-suggested but addable — he has no signed offer so no start date, which is the correct signal rather than a bug. Also asserted the two-kinds-of-date pairing holds: the session instant reads its day in Mountain, a start date reads its day in UTC. Build, typecheck, lint clean. ROUND 2 (Jul 22) — user ran the new flow on localhost, confirmed it works, and reported three more things, all now fixed. (a) CHANGING "DAYS BEFORE" DID NOT REORDER THE CHECKLIST, which is what anyone would expect it to do: the list sorted by a hidden order value fixed at creation and days-before was display-only, not even editable. It now sorts by days-before, furthest out first, with the old order only breaking ties — so that one number controls both meaning and position — and days-before plus owner are editable in place. Verified on a real session: 14,14,10,7,7,5,3,3,3,2,2,1, with a task the user added during testing (no days set) correctly sorting last. (b) ADD AND REMOVE WERE TECHNICALLY THERE BUT WEAK — add took only a label, so anything added had no owner and no timing and sank to the bottom, and remove was an X at zero opacity until you happened to hover the row. Add now takes label, owner and days; remove is always visible. (c) THE ONE THAT ACTUALLY MATTERED: per-session edits died with the session. The twelve tasks were hardcoded and copied in at creation, so curating one session changed nothing for the next and every future orientation would regenerate the aspirational items the team does not actually do yet. The standing checklist now lives in a WorkspaceSetting with the hardcoded list as a fallback, and a "Save as default for new sessions" button promotes the session you just curated — you fix a real list where you can see what you really do, then make it stick. Existing sessions are deliberately never rewritten; their checklist is a record of that day. EMAIL DIRECTION SET: the team keeps its ~50 templates in FRONT and edits them there, so the app must not hold a second copy — the user wants a SEND BUTTON per attendee that sends the Front template, exactly like the welcome email. Good news is that pattern already exists and is already right: lib/front/onboarding-email.ts fetches the template body from Front AT SEND TIME precisely so the app cannot drift, adds only a personalized greeting, previews the exact email, sends from hrotasks@ on confirm, then records the conversation and ticks the task. Orientation should reuse it, with one improvement — list Front's templates and let the user MAP each orientation email to one in the app, instead of hardcoding a template id the way the onboarding email does. BLOCKED ON A DECISION: FRONT_API_TOKEN is not in the local .env (production has it — the welcome email really sent), so the template list cannot be enumerated and no send can be tested from a dev machine. URGENT: "send invitations" is due 14 days before, which for Aug 4 was Jul 21 — already yesterday — so the send path is the critical path now, not a nice-to-have. FRONT TEMPLATES READ (Jul 22, token was there all along in .env.local — an earlier claim that it was missing was wrong, the check only loaded .env): the real orientation sequence in Front is 1. New Hire Orientation (rsp_qnije), 2. (Supervisors) New Hire Orientation (rsp_qnimy), 3. REMINDER: New Hire Orientation (rsp_qnioq), plus 4. Exec Team Questions. THE APP'S OWN FIVE EMAIL SLOTS ARE FICTION — invitation / confirm_request / confirmed / directions / reminder were invented and match nothing the team sends; they should be replaced by a mapping to the real Front templates. THE TEMPLATE ITSELF SPECIFIES THE AUTOMATION: its body opens with a red block telling the sender to delete that block, set to = each new employee's company email, cc = the supervisor plus a fixed list (hrotasks, Morgan, Ricky, Hannah, Kevin, Aimee), and "Update Day, Date 3 times" — which reconciles exactly, 1 placeholder in the subject and 2 in the body. All of that is what the button should do. A time discrepancy surfaced on the way (template said 9:30-2:00, the real day is 9:30-3:00); the user corrected the Front template and the fix was verified by re-reading it. SUPERVISOR FIELD SHIPPED as the prerequisite: NewHire had no supervisor at all, so the cc could never be filled — added nullable supervisorName + supervisorEmail (previewed SQL, two additive columns), threaded through the row type, selector, mapper and the PATCH whitelist, and exposed as two fields on the hire detail page. Verified: build, typecheck, lint clean and the fields render on a real hire. STILL TO BUILD: the send itself — map each orientation email to a Front template, strip the red instruction block, substitute Day/Date, fill to and cc, preview the exact email, send from hrotasks on confirm, then record the conversation and tick the emailed box, reusing lib/front/onboarding-email.ts wholesale.
- [x] Business card process — finish by Aug 22. DONE Aug 6, SIXTEEN DAYS EARLY, declared by the user. The arrive, hand-out and confirm half had never run in the life of this rock; the Aug 4 orientation was its test case and it ran. Verified read-only across ALL active staff with the whole status distribution as a positive control: RECEIVED went 101 to 111, NOT_NEEDED 61 to 62, and the count still sitting at ORDERED is ZERO — the ten people waiting were all worked through, not just the two at orientation. In the cohort itself Tara Ward and Grace Ward are RECEIVED and the other five were NOT_NEEDED. His Aug 3 rating of 90 to 95 per cent predated the hand-out running. EARLIER HISTORY BELOW. ADOPTION Jul 28, CORRECTING JUL 27: gate 2 is NOT met, so "effectively done" was too generous and is withdrawn. The user's own words are that the process is well built but still has to be TESTED. What is left is the back half of the loop, which no order has ever been through: the cards arriving, being handed out, and somebody confirming that whole hand-off actually got handled. The order that is out is the first half only. So this rock is built and part-used, not finished — the Aug 4 cohort is the natural test case, since those cards should arrive for real people with a real deadline. ADOPTION Jul 27: IN USE — the user placed a real order through the process and has received the proof/preview email back from the print company, so the whole loop has run end to end on live work. Only minor logistics details are left to settle. Effectively done pending those; do not spend build time here. ADOPTION Jul 20 was ~80%. Built (essentially complete). Jul 19 reviewed + hardened: auto-built cards, crew-number rules, variants, orientation-deadline reminders, missing-email catch, printer-ready copy all in place — plus fixed the all-active-staff roster gate (was hiding 147 tenured employees), reworked the printer copy to the stacked no-hyperlink format per the user, unified the first label to "skyops", and added per-status tabs. Left: team actually running real orders through it (the copy-to-Front-then-printer flow — user validates that himself; direct app-to-printer email is a deferred next-quarter item riding on the parked Front integration).
- [~] Travel for Orientation / Indoc — finish by Sep 12 (DATE SWAPPED with rock 5 on Aug 24 at the user's request, from Sep 5). IN ACTIVE BUILD as of Aug 6, and the shape of what is left changed: ADOPTION IS NO LONGER THE BLOCKER. Hannah booked travel with it this week, so it has a second real user beyond the user himself. What is holding it is the thing itself — his own read is that what exists does not feel good and needs a lot more, and he and Hannah are building it together now. That makes this rock different from the other two open ones, where the gate is somebody else acting: here the gate is the product getting better, and it is the one open rock actively being worked. Still separately missing, both ends of the reimbursement confirmation loop: how accounting tells us a payment went, and how we tell the employee it arrived. EARLIER HISTORY BELOW. ADOPTION Jul 27: OFF ZERO — real trips have now been booked through the app, which is the first genuine usage this rock has had. REIMBURSEMENT PROCESS CONFIRMED Aug 1 — this was the whole gate and it is now answered. Hannah confirmed with accounting: HR sends an email to payables@skyshare.com with the receipts attached and the ordinary details of the trip — traveller name, dates, and the purpose of the trip and the expenses. No form, no portal, no approval step named. IT IS SIMPLER THAN THE DRAFTS ASSUMED, AND THE SUBMITTER CHANGED: HR does the submitting, not the traveller, so the candidate-facing step may be no more than send your receipts to HR — worth settling before the SOP and the fillable invoice are finished on the old assumption that the traveller submits. Left on this rock: fold these steps into the draft SOP, decide whether the sample invoice is still needed at all, correct the in-app text that still says accounting has not confirmed, and certify the manual trip flow with the team using it. ADOPTION Jul 20 was 0%. Manual flow built (auto-fill from confirmations is an IMPROVEMENT, not required for the rock). Left: certify the manual trip flow + team using it. NEW REQUIREMENT FOR DONE (Jul 19): when a candidate or employee books their own travel, they must have a dead-simple way to get reimbursed — (a) nail down the exact reimbursement process accounting needs (candidate vs employee may differ), (b) an easy-to-read step-by-step SOP we can send them, and (c) a free, editable (Google Doc) sample invoice/expense form prefilled with the basics so they only add their own info. Draft SOP + sample invoice created as Google Docs on the work domain (Jul 19) — fillable invoice: https://docs.google.com/document/d/1jCcDpdMj8kkVq9RLegOybJkbzhiFb3KNPQvpy30UEbQ/edit ; how-to-get-reimbursed SOP (DRAFT): https://docs.google.com/document/d/1DpO5zbsBtjsPtWnAM2Dna3egKo-To4PohiEPLHJeBDs/edit — the accounting-specific steps were confirmed Aug 1 (email payables@skyshare.com with receipts plus name, dates and purpose), so both documents can now be finished and sent. STILL UNCONFIRMED, and nobody should invent answers for these: approval, payment method and timeline, and whether a candidate is handled any differently from an employee. Hannah's answer covers how to submit, not how long it takes to come back.
- [x] Rock 4's reimbursement gate is answered (Aug 1) — the travel checklist put the blocker on screen, and it was chased and closed the same day. Hannah confirmed with accounting: HR emails payables@skyshare.com with the receipts and the ordinary trip details (traveller name, dates, purpose of the trip and the expenses). No form and no portal, and HR submits rather than the traveller. Approval, payment timeline and any candidate-versus-employee difference remain unasked
- [x] The travel checklist names the real reimbursement process (Aug 1) — it had shipped that morning saying accounting had not confirmed the process, which stopped being true the same afternoon; the note now states what to actually do. HR emails payables@skyshare.com with the receipts attached and the trip details (traveler name, dates, purpose of the trip and expenses), and it says plainly that HR sends it rather than the traveler, since that is the part most likely to be assumed the other way round. Approval and payment timeline are still described as unspecified rather than glossed, so the stages after Sent to accounting are honestly labelled as tracked by hand
- [x] Finish the reimbursement SOP and decide the sample invoice's fate (Aug 1, closed Aug 16) — the draft SOP and the fillable Google Doc invoice were both built assuming the TRAVELLER submits to accounting. HR submits, so the traveller-facing document may collapse to send your receipts to HR, and the invoice may not be needed at all. Settle that before finishing either, rather than filling accounting's steps into a document aimed at the wrong reader. CLOSED Aug 16 in both halves: the invoice is dropped in full on the user's instruction, and the SOP is written for the right reader. See the two entries at the top of this section
- [x] The Jul 31 indoc-and-training item is closed, because only one half of it survives (Jul 31, closed Aug 16) — it was annotated SUPERSEDED on Aug 16 and pointed at the newer entries, but left OPEN, so a single remaining gap was being carried by two open items in the same section and would have been counted twice by anyone reading the board. The indoc half is DONE and shipped, filled from the CPO's own welcome email. The training half is tracked in its own entry at the top of this section, with the honest scale on it: one TRAINING trip exists and its status is COMPLETED, so it bites on the next trip rather than today. Closed so the training-content gap has ONE open item rather than two
- [x] Interview write-ups, formatted notes, and a Recent interviews list (Jul 29) — interviews are still run in PAYCOM, so this is where the write-up goes afterwards and how you find who you are working on. FOUR PARTS. (1) A rich-text editor on both the Interviews and Notes tabs — bold, italic, underline, strike, bullets, numbers, headings, quotes, links, five text colours, three highlights, four sizes, and @-mentions of teammates. (2) An interview write-up form: paste the notes, set the DATE THE INTERVIEW HAPPENED (not today — that is what the windows count from), pick the interviewer, and optionally a Pass/Maybe/No outcome and a 1-5 star rating. (3) A Recent interviews page at /candidates/recent-interviews with 30/60/90-day windows, filtered by interviewer and defaulting to whoever is signed in, one row per candidate showing their most recent interview. (4) A Recent interviews link in the Candidates header. SECURITY IS THE REAL WORK HERE: pasted HTML from Paycom, Word and Gmail is stored and rendered back into a page full of candidate PII, which is a stored-XSS surface. lib/richtext/sanitize.ts is an EMIT-ALLOWLIST, not a blocklist — it walks the input and writes out only tags and attributes it recognises, escaping all text, so a payload it has never seen still cannot survive. The style attribute is allowed only when it exactly matches one of the strings the editor itself produces, which keeps colours on the design system and makes CSS injection impossible without needing a CSS parser. 18 attack cases pass, and an end-to-end test through the real route with script tags, img onerror, onclick and a javascript: href stored ZERO dangerous fragments while keeping all six formatting features, the mention and the hardened external link. IDENTITY IS THE EMAIL, not the typed name — two people spelling their own name differently would silently break the my-interviews filter. Schema is additive and nullable (Interview.notesHtml/outcome/rating/interviewerEmail/mentionsJson, CandidateNote.bodyHtml/mentionsJson) and the existing plain-text notes and body columns are still written, so search and every current reader carry on unchanged; 302 interviews and 1,370 notes were untouched. BOTH ITEMS ONCE LISTED HERE AS STILL OPEN ARE NOW CLOSED, by the two entries directly below: editing and deleting a saved write-up shipped the same day, and a mention now sends a real email and accepts a reply back onto the profile.
- [x] Interview write-ups: formatted, editable, and now notify mentions by email (Jul 29) — three follow-ups from the same-day feedback on yesterday's ship. RECENT INTERVIEWS BUTTON moved out of the header and into the stat-tile row itself, styled as a navy call-to-action tile (icon, label, an arrow instead of a count) rather than a passive white number card, per a screenshot showing exactly where it should sit. EDIT AND DELETE now exist for both interview write-ups and notes, which were add-only. A hover-revealed pencil/trash pair; delete is a two-click arm-then-confirm on the button itself rather than a browser confirm() dialog, matching the pattern used elsewhere. New PATCH/DELETE at /api/candidates/[id]/interviews/[interviewId] and PATCH at .../notes/[noteId], deliberately separate from the SCHEDULING route (/api/interviews/[id], which pushes to Google Calendar and needs calendar:write) since a write-up has neither a calendar event nor that permission's blast radius. Ownership is checked against the CANDIDATE ID in the URL, not just the row id — verified live: deleting through the wrong candidate's URL 404s and the row survives, so one candidate's interview can't be reached or deleted through another candidate's page. An edit that would empty the write-up out entirely is refused (400) with a message pointing at delete instead, rather than silently saving a blank note. MENTIONS NOW SEND AN EMAIL. lib/notifications/mentions.ts, sent from hrotasks@ (the existing automation mailbox) so a reply-all does not appear to come from a colleague. This is treated as safe to auto-send, unlike the rest of the app's Front integration which defaults to drafting for a human to review, because every recipient is a mentioned TEAMMATE's own @skyshare.com address — the same internal-only shape as the nightly hrotasks@ digest, never a candidate or new hire. Never throws: a notification failing must not undo the note or interview it rode in on. Only NEWLY added mentions notify on an edit, so re-saving a write-up does not re-email someone already told. VERIFIED LIVE Jul 29-30, which the original entry could not claim: a real mention to Jonathan Schaedig produced a real email he received and replied to twice, and both replies were read back correctly. The original caveat — that testing would email a real teammate as a side effect — was resolved by the user choosing to run it against a real person rather than by finding a way around it. Everything else re-verified end to end: create, edit (date/outcome/rating/notes all move), delete, and the note equivalents, including that a script tag and an onerror handler still cannot survive the edit path.
- [x] Contractors are out of recognition for good, and the code says why (Aug 2, from the user) — asked directly and answered plainly: employmentStatus CONTRACT, 17 people, get no compliment, no birthday and no work anniversary. Their words were that they do not want contractors included in the compliments, so do not re-propose it. NOTHING BEHAVIOURAL CHANGED, because CURRENT_EMPLOYEE_WHERE was already ACTIVE-only and the code has always matched this answer; what was missing was the answer being written where the next person would read it rather than only in the roadmap. CURRENT_OR_CONTRACT_WHERE is deleted — it was defined while the question was still open and wired to nothing, and an unused export with an inviting name is precisely how a settled decision gets reversed by somebody tidying up. The reasoning survives in the comment, which names the decision, its date, and an explicit do-not-add-without-asking; a future feature that genuinely needs contractors should ask first and add the filter then. Verified that nothing in lib, app or components still referenced the removed constant
- [x] Recognition's Cancel button could not be ctrl-clicked (Aug 1) — it was a router.push to /compliments, so a click that changes the whole screen could not be opened in a new tab, which is the standing site-wide rule. Now a real Link. Audited the other seven router.push calls in the same sweep and left them alone deliberately: every one is a post-submit redirect (after a delete, after creating a trip or an event), which is the case the rule does not cover
- [x] FIXED Aug 3, see the Platform entry — The Settings production-readiness badge goes green on the wrong value (found Aug 1) — the Production environment label tile reads READY whenever NEXT_PUBLIC_APP_ENV is set to anything other than the literal string local development, so a local machine labelled development shows a green READY beside the word development. Found while asking the user to use that very panel to confirm the live site reports production, which makes it worse than cosmetic: it is the panel someone checks before trusting a deployment, and the light can be green while the value is wrong. The displayed VALUE is accurate; only the badge lies. Two-word fix in lib/data/settings.ts — treat anything that is not exactly production (or staging) as todo
- [x] Compliments celebrations could never show a birthday (Aug 1, from the user) — reported as add current employees anniversaries and birthdays, and the feature already existed, which made it worth measuring rather than adding. The query required stage IN (ACTIVE, POST_ONBOARD) alongside employmentStatus ACTIVE. That reads as currently employed and is not: stage tracks ONBOARDING progress and flips to ARCHIVED when someone completes it, so the filter kept only the newest arrivals — 28 of 173 current employees, and exactly the people with the SHORTEST service, which is the opposite of who has an anniversary worth celebrating. Counted against the live database, ZERO of those 28 had a birthday on file while 63 of the wider group did, so the Birthdays half of the page could not have shown a single person, ever. Fixed by keying on employmentStatus ACTIVE, the field the Employees directory already treats as current staff. Live result went from 0 birthdays to 6 birthdays and 18 anniversaries inside the 45-day window, two of them falling today. Every celebration's avatar and name now link to /people/[id] as the user asked, and the empty state points at the employee record, since a missing birthday there is a missing celebration here. Verified by running the real query against live data, not by reasoning about the filter
- [x] The onboarding-stage-versus-employment bug, fixed in all eight places this time (Aug 1, found by the user) — the celebrations fix earlier the same day was correct and left the SAME bug in seven other call sites, four of them in the file being edited and one 57 lines below a comment explaining why the filter was wrong. NewHire.stage tracks ONBOARDING and goes to ARCHIVED once onboarding is filed away, so stage IN (ACTIVE, POST_ONBOARD) means joined recently and not works here — and because it returns a plausible subset rather than an error, nothing looks broken. Measured on the live database: 28 of 173 current employees matched, so 145 people could not be given a compliment at all, the send action REJECTED any compliment addressed to them, and every percentage on the page divided by 28. Now fixed at all eight sites behind ONE named constant, lib/data/current-employee.ts CURRENT_EMPLOYEE_WHERE (employmentStatus ACTIVE plus canceled false — both halves needed, since employmentStatus alone would keep a cancelled offer on the roster), so the definition is greppable and the next change is a single edit; ROSTER_STAGES is gone. MEASURED BEFORE AND AFTER by running the real functions, not by re-deriving the filter: roster 28 to 173 (stage breakdown ARCHIVED 145, ACTIVE 12, POST_ONBOARD 16, exactly the excluded group), dashboard and budget roster size 28 to 173, candidate-to-employee lookup 21 to 114, nobody dropped, and outstanding plus per-person liability 0 dollars before and after — the programme has no recognitions and no points yet, so this cost nothing and was the cheapest possible moment for it. INTENT WAS CONFIRMED WITH THE USER FIRST, since adding 145 people to a recognition programme is not a pure bug fix. THE EIGHTH SITE IS NOT COSMETIC: the candidate-to-employee lookup feeds the org charts' role-change prompt shipped this week, which was therefore silently absent for 93 of 114 linked employees. CONTRACTORS ARE EXCLUDED, DECIDED BY THE USER Aug 2: employmentStatus CONTRACT, 17 people, get no compliment, no birthday and no anniversary, and that is the intended behaviour rather than an oversight. Nothing needed changing to honour it, since the code already excluded them. CORRECTED LATER THE SAME DAY: this entry said CURRENT_OR_CONTRACT_WHERE would stay defined and wired to nothing so the option was documented. It has since been DELETED — an unused export with an inviting name is how a settled decision gets reversed by somebody tidying up, and this same file had already been through one round of that this week. The reasoning now lives in a comment naming the decision and saying not to add contractors without asking. ALSO STILL OPEN, logged not changed: the same stage filter appears in app/api/candidates/route.ts:171, app/api/candidate-applications/route.ts:59 and app/api/new-hires/route.ts:91, all candidate-to-hire linking and duplicate detection, where it may or may not be correct. lib/data/events.ts:180 is deliberately different and was left alone. NOT verified: no compliment has actually been SENT to one of the newly-included 145, so the send action accepting them is proven by reading the query rather than by exercising it
- [x] Fleet card dialog: bottom sections ran to the edges, and Edit blended into the header (Aug 1, from the user with a screenshot) — two things from the previous day's ship. The sections below the seat columns (transitioning out, the edit hints, every action row) are direct children of the scrolling dialog, which carries no padding of its own — the header and the seat columns each bring their own — so their text sat flush against both edges and read as cut off. A new m-sect class gives all seven of them the header's 20px gutter across both charts, and the modal's bottom spacer went from 16 to 22px. Separately, Edit Crew had been moved to the card header the day before and was navy on a navy bar, so it disappeared into it; it is now gold on navy with heavier weight and a small shadow, the brand's own emphasis pairing, and Edit Team on maintenance matches. NOT VISUALLY VERIFIED: these live inside the aircraft dialog, which only exists once a card is opened, and this app's client components do not attach handlers in the preview pane — so this was checked by compile, lint and build only. The user sees the real result first
- [x] Candidates page felt broken on click, plus five more fixes (Jul 29, completed Jul 31) — reported as "Back to candidates loads slowly, I don't know if it worked." THE LINK WAS NEVER THE PROBLEM — Back to candidates and the sidebar Candidates item were both already real Links. What was missing was any sign a click had registered: with zero visual feedback, a few hundred ms of real backend latency reads as broken, and the natural response — click something else — races the original navigation, which is what actually looked broken. Two changes: (1) getCandidateListData was switched from Promise.all over 7 independent queries to prisma.$transaction over the same 7 — THIS WAS REVERTED THE SAME DAY in commit 73bec66 and the code is back on Promise.all, so ignore it: the theory was never proven, the local measurements behind it were noise (694ms / 1503ms / 791ms across runs), an array-form $transaction actually runs the queries SEQUENTIALLY on one connection which is worse against a remote database, and it added a transaction timeout as a brand-new failure mode. The real cause of the slowness turned out to be Link prefetching — see the two entries below. (2) a new LinkPendingIndicator component (Next 15's useLinkStatus) on both links shows a spinner the INSTANT a click registers, before the destination has fetched or rendered anything, so slow or fast the click always visibly does something. AMPERSAND BUG, found while testing: pasted text containing an ampersand rendered back as the literal text "&amp;" on screen. Root cause was in lib/richtext/sanitize.ts — the text handed to the sanitizer is always already-HTML-escaped (it comes from element.innerHTML, which the browser ALWAYS returns pre-escaped), and the old code escaped it a second time, turning "&amp;" into "&amp;amp;"; one round of browser decoding on render left the entity itself sitting on screen. Fixed by decoding fully before re-encoding once, which makes the whole pass idempotent regardless of how many encoding passes the input already had. 25 sanitizer tests pass, including the exact double-escape case and a numeric-entity case. TAB NOW INDENTS A BULLET (and Shift+Tab outdents) in the rich-text editor — previously Tab either typed a literal tab or jumped focus out of the editor entirely, since a bare contentEditable does nothing useful with it. Only intercepted while the caret is actually inside a list item, so tabbing out of the editor elsewhere still works. NEXT STEP FIELD on interview write-ups ("Schedule with hiring manager", "Waiting on references", ...) — its own column on the Recent interviews page alongside candidate, date, interviewer and outcome, editable after the fact like everything else on a write-up. UNDO A JOB LINKED BY MISTAKE — a small x on each application in the Applied to tab, DELETE /api/candidate-applications/[id]. Refuses (409) if the application already carries real offer progress (offerStatus not NONE), since deleting the row would silently throw that progress away along with the mistaken link — a link made in error and undone seconds later has none of that. Ownership checked against the candidateId in the URL, verified live: a mismatched candidateId 404s and the row survives. PAYCOM LINK — a small green badge (profile header, and a tiny square icon next to the name on the candidates list) that opens this person's own Paycom record in a new tab. Pasted in by hand rather than constructed from paycomPersonId, because there is no reliable public URL formula from that id alone and a guessed link risks silently pointing at the wrong person. Additive nullable Candidate.paycomLink column, applied to the live database ahead of this commit (3,513 rows untouched). Colour is a close approximation of Paycom's brand green — their exact hex lives in an internal brand-standards PDF these tools could not open. Verified live end to end on a real candidate's real profile (a throwaway test link set, confirmed rendering on both the profile and the list, then cleared back to null) rather than a synthetic one, and the interview/mention fixes were checked against a real leftover interview the user had already logged through the shipped feature, confirming the recent-interviews dedup (one row per candidate, most recent wins) behaves correctly rather than being a bug. STILL OPEN, and the reason this entry is not ticked: the original report asked for THREE things and only two are done. Speed is fixed (the prefetch storm, two entries below) and the click now gives instant feedback (LinkPendingIndicator), but "make the hover area more noticeable, then if you click anywhere in the hover area it takes you back" was never done — Back to candidates is still a bare text link with no padding and no hover background, so the clickable area is exactly the width of the words. It is a small CSS change on one element in CandidateProfileWorkspace.tsx, deliberately left rather than done silently, since it is a visual-design call about how prominent that control should be. NOW DONE (Jul 31) — Back to candidates is a padded, bordered box with the standard gold-glow hover, measured on a real profile at 110x14 = 1,545px before and 129x26 = 3,378px after, so 2.19x the click area with the height nearly doubled. Height is the dimension that causes near-misses on a one-line link, which is why that number matters more than the area. All three parts of the original report are now shipped.
- [x] Mention emails now have real content, and a reply gets added to the profile (Jul 29) — the first mention email shipped yesterday was minimal (a flat "you were mentioned" line). Now: greets the recipient by FIRST NAME, names the mentioner by first name too (resolved from the User table, not their raw email), names the role they applied for when one exists (the candidate's most recently touched job application — write-ups don't collect their own job, so this is the honest best answer to "what are we talking about"), and carries TWO links — straight to the interview/notes tab (candidates/[id]?tab=interviews, a new query-param the profile page now honors on load) and to the general profile. REPLYING NOW ADDS THE FEEDBACK TO THE CANDIDATE'S PROFILE, as the email itself promises. Each mention email creates a MentionNotification row keyed on its Front conversationId, so a reply on that exact thread is traceable back to the right candidate with no guessing; the webhook checks this before Travel or Paycom, since a reply's sender is a real teammate and would otherwise be silently discarded by the Paycom-sender filter, the same bug class Travel had to avoid. THE HARD PART WAS SEPARATING THE REPLY FROM THE QUOTED ORIGINAL. A stable marker is embedded at the very TOP of the sent email (both the HTML and the plain-text body) — an early version put it at the BOTTOM and a test caught it immediately: with the marker at the tail, the greeting line ("Hello Jonathan,") that precedes it sat ABOVE the cut point and leaked into every note. Fixed by moving the marker first, so everything below it is unambiguously the quoted original. A second test caught a related bug: searching for the bare marker TEXT left its own "<!--" comment-opener dangling in the kept content; fixed to search for the opening syntax too. Falls back to recognising gmail_quote, blockquote, "Original Message", "wrote:" and From:/Sent: headers for clients that don't preserve the marker. 6 stripping test cases pass, including Gmail-style, Outlook-style, no-recognisable-markup, formatting-preserved, and reply-with-nothing-new (writes no note rather than an empty one). The reply lands as a CandidateNote clearly sourced "Front reply" (sourceRef = the conversation id), sanitized through the same allowlist as every other rich-text field, attributed to a real User when the sender's address matches one, and a confirmation comment is left on the Front thread. Verified: the DB half end-to-end against the real schema (create the tracking row, look it up, create the note, read it back correctly attributed, clean up) — VERIFIED WITH TWO REAL REPLIES on Jul 29-30 — Jonathan replied "received" and then a full sentence asking for a video-call interview with Rich Paden, and both landed on Robert Patrick's profile as correctly-attributed "Front reply" notes. Getting there exposed two real bugs the synthetic fixtures could never have caught (message ordering, and Front-native replies carrying no quoted original); see the entries below.
- [x] The real cause of the site-wide slowness: the sidebar was prefetching every page, on every page (Jul 29) — after two rounds of guessing at the candidates page specifically (a bad $transaction theory, reverted; then real timing added, which showed a perfectly normal 680ms), production logs showed the actual pattern: dozens of unrelated routes (/offers, /events, /matching, /employees, /pilot-requirements, /recruiting-jobs, ...) all completing within the same instant, repeating every few seconds, on every page load. Per Next.js's own Link documentation, prefetch fires the moment a Link ENTERS THE VIEWPORT — not on hover, not on click. The sidebar with its roughly 15-20 nav items renders on every single page in this app, and none of those Links opted out, so every page load was quietly firing a REAL serverless invocation and a REAL database auth check for every nav item at once, whether or not anyone was ever going to click them. That is real, repeated load on the one shared database competing with whatever page a person actually asked for — which is a far better explanation for it feeling worse than any single page's own query time, and for it not feeling like it would ever finish: every reload restarted the same storm. Fixed with prefetch={false} on all seven Link elements in components/layout/Sidebar.tsx (the icon rail, the collapsed-mode flyout, the mobile drawer, and the account link) — none of these destinations are static content that benefits from warming; every one is fully dynamic and auth-gated, so prefetching bought nothing but the extra load. The click-feedback spinner shipped earlier the same day (LinkPendingIndicator) is what actually answers "did my click register" now, which was the real point of prefetching a slow page in the first place. CONFIRMED against real production logs the same day rather than assumed: a fresh log pull after the fix showed the cross-page burst (/offers, /events, /matching, /employees ... all completing in the same instant) genuinely gone. That same pull is what surfaced the row-level instance of the identical bug — see the entry directly below.
- [~] The same prefetch storm was one level deeper too: every row in a list (Jul 29) — pulled fresh production logs to CONFIRM the sidebar fix rather than assume it, and it worked: the cross-page burst (/offers, /events, /matching, ... all at once) is gone. But the SAME log pull showed a second instance of the identical bug, one level down: the instant /candidates loaded, roughly 15 simultaneous /candidates/[id] requests fired — one per row visible in the table, each prefetching a FULL candidate profile (interviews, notes, files, applications, tags, travel) well before anyone clicked anything. Orientation's session list has the same pattern, and its calendar view is the worst case — up to ~31 session links can be on screen in a single month grid, all prefetching at once. Fixed with prefetch={false} on the candidates table's row link and all three of Orientation's session links (card, calendar cell, cohort's Open session button). LIKELY NOT THE LAST ONE: any page listing many records that each link to a heavier detail view has the same shape — /people, /jobs, /fleet and others were not audited this pass and probably need the same fix; flagging rather than sweeping the whole app blind. Verified: clean typecheck, lint, and production build. Confirming this one against real logs needs the next page load, the same way the sidebar fix was actually confirmed rather than assumed.
- [x] Mention-reply, tested against a REAL reply — two ordering bugs found and fixed, and the auto-webhook gap confirmed (Jul 29) — the user added a mention to Jonathan, he replied "received" in Front, and no note appeared on the candidate. Checked the SEND half first: it worked, a MentionNotification row existed pointing at the right candidate and write-up. Checked whether the RECEIVE half had ever run: Vercel logs showed zero webhook calls for this reply at all, meaning the Front rule that is supposed to route replies to our endpoint has never actually fired — the same standing gap already tracked here for Travel and Paycom automation since Jul 21, not a new bug, but now confirmed to block this feature too. Fetched the real conversation directly to test the processing code against Jonathan's actual reply rather than only synthetic fixtures, and that surfaced two real bugs. FRONT RETURNS MESSAGES NEWEST-FIRST, not oldest-first as both lib/notifications/process-mention-reply.ts and lib/travel/from-email.ts assumed: mention-reply was reading the LAST array entry expecting the newest message and instead got back my own original outbound email; travel's importer had the identical assumption backwards, reading messages[0] as "the booking" when index 0 is actually the newest message in the thread, not the oldest. Both fixed — travel's has been silently latent rather than manifested, since every travel thread processed so far has had only one message. Separately, a reply typed NATIVELY IN FRONT carries no quoted original at all: Jonathan's "received" was followed only by Front's own auto-appended contact-card signature block, which none of the existing gmail_quote/blockquote/Outlook boundary patterns had any reason to catch. Added a front-signature boundary pattern and a 7th stripping test case reproducing this exact shape; all 7 pass. A first recovery attempt, run before the ordering fix, produced a WRONG note pulling my own sent email instead of Jonathan's reply — caught immediately, deleted before anyone reading the profile would see it, and re-run correctly once the fix was in: Robert Patrick's profile now correctly carries a "Front reply" note reading "received". Verified: typecheck, lint (0 errors), a real production build, and the corrected note read back from the live database. RESOLVED Jul 30 — the Front rule was created and this no longer needs manual recovery; see the entry below for exactly how and where it lives.
- [x] Pasting into the rich-text editor stopped silently re-formatting itself on save (Jul 30, from the user) — reported as text changing boldness, spacing and bullets between typing it and saving it, and pasted formatting not surviving at all. ROOT CAUSE was that the editor had NO paste handling whatsoever, so the browser inserted Word/Gmail/Google-Docs markup verbatim. Google Docs specifically wraps every copy in a b tag carrying font-weight normal — an artifact it uses to CANCEL inherited bold, not to apply it. The editor rendered that correctly at paste time because the inline style cancels the tag, but sanitizeRichText on save keeps the b tag outright while dropping any style that is not on its 5-colour/4-size allowlist, so the cancelling style vanished and previously-plain text came back BOLD on the next load. Foreign colours, fonts and sizes had the same fate in miniature: none of them can survive the allowlist, so carrying them into the editor only to have them stripped later was never going to work. Paste now runs through a walker that resolves actual emphasis — reading both the tag and any inline style that overrides it, so font-weight normal cancels a b ancestor and font-weight 700 applies bold with no tag at all — and re-emits ONLY the tags this editor itself produces. What you see the instant you paste is therefore exactly what a save keeps. Headings collapse to h3 (the only heading the toolbar makes), lists and blockquotes are preserved, http/https/mailto links survive and a javascript: link degrades to plain text. VERIFIED against the real docs-internal-guid shape plus Word, Google Docs bullets, style-only bold and a malicious link, and separately confirmed the cleaned output is byte-stable through the real server-side sanitizer — it round-trips unchanged except for the sanitizer adding its own target and rel to links, which is a hardening step and not reformatting. NOT verified by a real human paste in a browser: a concurrent session's dev server was thrashing the shared .next directory at the time, so the logic was proven directly instead of through the running app.
- [x] Sidebar section headings no longer drift off the left edge when they wrap (Jul 30, from the user) — a screenshot showed INTERVIEWS and SCHEDULING sitting at different left edges from the headings around them. Cause is a browser default nobody wrote: a button element carries text-align center in the user-agent stylesheet, and the collapsible section header in Sidebar.tsx is a button with no text-align of its own. A short label looks fine because there is nothing to centre against, but a label long enough to WRAP gets each line centred inside the button's own width, so the short line visibly drifts. The non-collapsible variant of the same header is a div and never had the bug, which is why it only affected some headings. Fixed with one text-left class. Confirmed by reading computed styles in a live render both before (centre, three wrapped lines at three different left offsets) and after (left, all three flush at the same offset).
- [~] The Front rule that Mention-reply, Travel and Paycom were all waiting on now exists (Jul 30) — three separate entries above (this one, Travel's clause (b), and Paycom's "waiting on the Front rule") shared one standing gap: nobody had ever created the Front rule that calls POST /api/front/webhook, and our API token lacks rules:read so it can neither create nor inspect one itself. Fixed by hand in Front's own UI, worth recording exactly how since it does not match the rule-webhooks dev docs and cost real time to find: Settings (the gear icon) → the workspace-scope icon (leftmost of three at the top of the settings sidebar, labelled by initial — "H" for this org) → Automation → Rules and macros. NOT the "Rules" page reachable from the personal "Me" scope — that one only lists rules owned by the signed-in user and has no way to create a rule that fires for the whole team, and it looks enough like the right page to cost time confirming it wasn't. Built via Create rule → Create from scratch → Create linear rule (the templated "Send to a Webhook" card exists but its hover-triggered "Create rule" button did not reliably respond to a scripted click, so this was built by hand instead). Trigger: Inbound message is received — the general form, not the "(new conversation)" variant, which would miss every reply and is the entire point. Condition: Conversation is in HR Onboarding, confirmed via the Front API that this — not the similarly-named "HR" inbox — is where hrotasks@ actually lives (inb_cru56). Action: Send to webhook, target https://skyshare-talent.vercel.app/api/front/webhook. CORRECTED Aug26, and the original is left standing because it was true when written: the Aug 3 rename moved the target, and the rule now points at skyshare-journey.vercel.app/api/front/webhook. Confirmed by the user reading the rule's own edit screen, which is the only thing that can confirm it - the endpoint answers identically on BOTH hostnames because they are one deployment, so no amount of curl can tell you which host Front actually calls. The rule is still NAMED Talent-Ops webhook; that is cosmetic and renaming it is optional. THE TARGET WEBHOOK FIELD IS DECEPTIVE: it looks like a picker of pre-configured connectors and on some clicks opens an autocomplete against unrelated Front tags, but clicking directly into the field itself (not its adjacent "+" icon) reveals it is a real free-text field. FRONT_WEBHOOK_SECRET in Vercel Production was independently confirmed already correct by the user, so no rotation was needed. VERIFIED for mention-reply only: both of Jonathan's real replies landed correctly once this rule existed. NOT YET VERIFIED for Travel or Paycom specifically — same rule, same endpoint, but no real travel or Paycom email has come through it yet to prove those two paths actually fire end to end.
- [x] Historical interview history — finish by Sep 5 (DATE SWAPPED with rock 4 on Aug 24 at the user's request, from Sep 12). DONE Aug 24, TWELVE DAYS EARLY, declared by the user. He was asked in the Monday check-in to declare it or name what was missing, and he declared. HIS EVIDENCE, in his own words: the historical interview data has been accessed MULTIPLE TIMES, he feels it is pretty good, and he has not come across anything that prevents him from accessing the data. Measured against the three gates that is built, tested across repeated real use with no blocker surfacing, and in use by the team - with no named gap left, which is the DONE DOES NOT MEAN PERFECT principle applied exactly as he wrote it on Aug 3. THIS ROCK IS THE CLEAREST CASE OF ITS OWN RULE WORKING: it sat at a true zero for three weeks and the standing instruction was never to ask him to stage a search, since he cannot manufacture reasons to test it, so it could only ever move on genuine lookups - one on Jul 29, a second with Hannah on Aug 3, then enough of them by Aug 24 that he stopped noticing. The 1.5 to 4 second search latency stays open as an ordinary performance item under Candidates and Matcher and does NOT reopen the rock. EARLIER HISTORY BELOW, kept in full rather than trimmed. ADOPTION Jul 29: OFF ZERO AT LAST. The user and Hannah looked up a real historical candidate together today and the record held up — his words were that it was pretty good. That is the FIRST confirmed real lookup this rock has ever had, after sitting at a true zero since Jul 20. A second, unprompted piece of evidence the same day: the feedback asking to remove irrelevant tags was filed FROM a historical profile — Tommy Harvey, origin JAZZ, archived, carrying 4 documents, 2 notes and an interview — so someone was reading an archived record closely enough to be bothered by its leftover JazzHR stage tags, and that fix is now live. WHAT IS LEFT: pretty good is not certified. Find out what was missing on that lookup, because the gap between pretty good and right IS the rest of this rock, and on this evidence it is small. Two named people using it once is not yet a habit either — the gate is the team finding what they need, so a second or third real lookup is what turns this into done.  ADOPTION Jul 27: NO PROGRESS since Jul 20, confirmed by the user. Nobody has confirmed a single real lookup, so this is a true 0 and it has been a true 0 for a week. It is also the cheapest rock to move: one recruiter, one real search for a past candidate, done in ten minutes. It stays at zero purely because nobody has been asked. ADOPTION Jul 20: unknown — usage is not visible to anyone, which is itself the finding; treat as 0 until someone confirms a real lookup. Jazz archive imported + searchable. Left: confirm the team can find what they need + any bug fixes.
- [x] Rock 6's training import: the paste happened, and this entry sat stale for sixteen days (Aug 1, closed Aug 17) — the Training tab and its paste-import shipped Jul 31 against an idealised shape, and the user then supplied the real Recruiting Status Tracking Training Info export the same day. THE FILE DISPROVED THE MODEL WITHIN HOURS: records had been keyed one-per-person and the sheet holds six pilots twice over, so the columns and the whole record model were reworked to match the real thing (see Fleet Positions & Roles). The blocker named here is therefore cleared — the columns are no longer a guess. WHAT REMAINS IS ONE HUMAN STEP: the 104 records are still the user's to paste and save, deliberately, because that review-then-save is the only gate in front of a shared live database. Until that paste happens the tab still runs on three seeded pilots, so this is a working importer over placeholder data — closer to adoption than yesterday, but not adoption. The parse is already proven against the real CSV: 104 records, 98 archived, 14 blank or divider rows skipped, exactly 2 unreadable cells preserved as notes. CLOSED Aug 17, and re-verified against the live database rather than taken from the Aug 5 entry that first contradicted it: the stored crew-roster setting holds 107 training records, 102 of them archived, carrying the real sheet's own columns (hireType, pool, position, startDate, orientationDate, indocDate, start, end, vendor), and it was last written 2026-08-13. So the paste not only happened, the user has kept working in it a week afterwards - the counts have moved past the 105 and 98 the Aug 5 entry recorded. This line saying the paste was still outstanding was the single most misleading thing in the section, because it sat under rock 6 describing a human step that had already been taken
- [x] Rock 6 got its first real usage feedback (Aug 1, closed Aug 16) — the user reported six issues from actually working in the crew chart, which is the first time this rock has produced use-driven bug reports rather than build activity. All six are addressed (see Fleet Positions & Roles), including the one that cost real work: a pilot moved onto a managed PC-12 when a fractional seat was meant, because the position list could not tell six identical PC-12 rows apart. Adoption is still the gate, not the build — but the excuses named on Jul 27 are now a step shorter
- [~] Crew chart — finish by Sep 24. NAME SETTLED Aug 24, by him, asked and answered: this rock is Crew chart. It was titled Pilot recruiting status tracking here until then, and older dated entries still say that - they are records of what was written at the time and are deliberately left alone. ADOPTION Jul 27: UNCHANGED at ~30%. The user confirmed the only movement last week was the per-card editing work shipped Jul 22 — that is build activity, not usage, and per-card editing was the known adoption excuse, so the excuse is now gone and nothing else stands between the team and the chart. Nobody has yet tracked real pilot recruiting status on it. ADOPTION Jul 20: ~30%. This IS the crew org chart (fleet/crew) — the old pilot-recruiting-status spreadsheet built into the site (edit roster, move pilots, open/close seats, candidate links, journey sync all shipped). So it is BUILT, not the build-risk rock first assumed. The remaining gate is ADOPTION: the team actually tracking pilot recruiting status on the chart and retiring the spreadsheet. Left: certify end-to-end + drive the team off the xlsx onto the chart.
- [x] Decide which pre-onboarding document is THE one (Jul 22) — there were two describing the same flow: the runbook Google Doc (last corrected Jul 18) and docs/sops/01-pre-onboarding.html (fully audited and rewritten Jul 22 against the current code). RESOLVED same day: the user trashed the Google Doc. The SOP is canonical, and rock 1 above now points at it. One document, no drift.
- [x] Delete the personal-domain copies of the three rock documents (Jul 22) — the pre-onboarding runbook, the fillable reimbursement invoice and the how-to-get-reimbursed SOP had each been created on a personal Google account before being re-created on the work domain, leaving stale duplicates that could drift or be sent to a real new hire. DONE: the user deleted all three personal-drive files on Jul 22, and will remake them only once the processes are certain. The pre-onboarding runbook is retired entirely — docs/sops/01-pre-onboarding.html replaced it.

## Q4 Rocks — after Oct 1 (do not start until Q3 ships)
Real, but out of scope until every Q3 rock is done. Noted here so they are not lost.
- [ ] Interview Feedback and Tracking
- [ ] Dashboard and reporting
- [ ] Interview Scheduling
`;
